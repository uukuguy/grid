---
context: default
phase: milestone-boundary-v3.16
task: 0
total_tasks: 0
status: paused
last_updated: 2026-08-27T12:56:21Z
---

<current_state>
v3.16 is shipped and the CI/contract-matrix recovery is closed. There is no active phase or plan, no remaining recovery task, and no blocker. Resume at milestone scope selection for v3.17.
</current_state>

<completed_work>

- Restored all workflow-referenced Phase 2.5 and Phase 3 Make targets (`7c2f4884`).
- Closed runtime contract gaps: immediate DONE termination, compaction construction, post-finish OpenAI usage, workspace-relative paths, deterministic hook probes, and contract corrections (`b8980481`).
- Declared and bridged clean-runner Python dependencies and Pydantic-AI 2.x compatibility (`0136fb49`).
- Removed the stale Phase 3 Matrix blocker and persisted the verified final state (`59b0944c`).
- Verified generic CI run `33073147718` and Phase 3 Contract Matrix run `33072602355` as successful.
- Verified `cargo build -p grid-runtime --bin grid-runtime` locally.

</completed_work>

<remaining_work>

- Select the v3.17 milestone scope under ADR-V2-024's data/integration axis.
- Likely candidates are grid-server multi-user follow-on, web-platform quality improvement, or grid-desktop quality improvement.
- Do not begin multi-tenant OBSTACK, grid-eval OBSTACK projection, or ecosystem-health work until their producer contracts exist.

</remaining_work>

<decisions>

- Generic CI excludes `grid-desktop`; dedicated Desktop CI owns GTK/WebKit/glib provisioning and coverage.
- Missing runner targets, dependencies, system packages, and startup compatibility are immediate fixes, not deferred gaps.
- ADR-V2-025-permitted hook capability failures in sample/reference runtimes remain non-blocking; Grid and Claude primary jobs are blocking and green.

</decisions>

<critical_anti_patterns>

| Pattern | Consequence | Guardrail |
|---|---|---|
| Relying on globally installed Python packages | Clean runners fail despite local success | Validate runtime startup from declared metadata in an isolated environment |
| Waiting for another broadcast item after `DONE` | Streams hang indefinitely | Yield `DONE` and break immediately |
| Emitting OpenAI `MessageStop` at `finish_reason` | Usage frames arriving afterward are lost | Defer stop until the `[DONE]` sentinel |
| Installing desktop Linux packages in generic CI | Generic gates inherit unrelated GTK/WebKit/glib fragility | Keep desktop dependencies and tests in dedicated Desktop CI |
| Assuming runtime-only changes trigger the matrix | Dependency fixes can miss push-path filters | Use `workflow_dispatch` or expand workflow paths when appropriate |

</critical_anti_patterns>

<infrastructure>

- Branch: `main`; recovery state commit: `59b0944c`.
- Generic CI: https://github.com/uukuguy/grid/actions/runs/33073147718
- Phase 3 Contract Matrix: https://github.com/uukuguy/grid/actions/runs/33072602355
- No services, watchers, or background processes were left running.

</infrastructure>

<required_reading>

1. `.planning/STATE.md`
2. `.planning/PROJECT.md`
3. `docs/status/RESUME-NEXT-SESSION.md`
4. `docs/PROJECT_PRODUCT_OVERVIEW.md`
5. `docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md`

</required_reading>

<next_action>
Run `$gsd-resume-work`, confirm the milestone-boundary state, then select and bootstrap v3.17. Do not reopen the completed CI recovery unless new evidence shows a regression.
</next_action>
