---
context: research
phase: null
task: null
total_tasks: null
status: in_progress
last_updated: 2026-07-17T14:20:28+0800
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure THIS session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **CLAUDE.md 在本仓库是 symlink → AGENTS.md** — 不通过 `Edit` 工具改 CLAUDE.md(工具拒绝写 symlink)。直接 Edit/Write AGENTS.md 即可,内容自动同步给 CLAUDE.md。Symlink 不要 commit (git 不 track symlink,working tree 中 CLAUDE.md untracked 是预期状态)。

- [ ] CONSTRAINT: **`tools/eaasp-*/` 的精确定性** — 不是 "shadow / separate upstream" (那是 ADR-V2-023 起草时点的过时措辞),也不是 "production 完整 EAASP"。准确表述: **EAASP v2.0 平台尚未完整实现前,按平台契约做的"模拟器"级参考实现**。EAASP 平台设计文档权威源在 `docs/design/EAASP/` (`EAASP-Design-Specification-v2.0.docx` 为规范)。

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| **沿用过时措辞** | 直接抄 CLAUDE.md/ADR-V2-023 里的 "shadow / separate upstream" 措辞 | blocking | 写 EAASP 相关描述前,先读 `docs/PROJECT_PRODUCT_OVERVIEW.md` §2.2 + §5 拿到准确框架 |
| **漏掉 EAASP 设计文档索引** | 谈 EAASP 但不引用 `docs/design/EAASP/` | blocking | CLAUDE.md § "EAASP v2 Architecture" 段应同时给设计文档索引表(per 2026-07-17 docs sync)|
| **声称有"上游 EAASP 项目"** | 不存在 | blocking | EAASP 平台全部在本仓同仓孵化,本团队自做 |
| **"shadow / separate upstream" 措辞沿用** | CLAUDE.md 旧措辞 | blocking | 改 CLAUDE.md → 必须引用 `docs/PROJECT_PRODUCT_OVERVIEW.md` 拿口径 |

## <current_state>

**Session 类型**: 用户 2026-07-17 反馈 CLAUDE.md / README / 项目现状叙事脱节 → 通过 docs sync PR 修复叙事。已 SHIPPED + pushed。

**当前 milestone / phase 状态**:
- 项目现 milestone: 全部 SHIPPED(v3.0/v3.1/v3.2/v3.3/v3.4/v3.5 + Grid 独立产品 Activation)。
- .planning/STATE.md: Grid 独立产品 Activation 8/8 SHIPPED 2026-06-17。
- 本次 docs sync 不属于任何 active phase — 是一次用户驱动的 documentation update。

**已完成 push**: `0461d97f docs: align Grid/EAASP narrative across CLAUDE, README, ADR-V2-024 note, MEMORY` (5 files changed, 331 insertions(+), 29 deletions(-))

**Working tree 状态**:
- `CLAUDE.md` — untracked symlink (git 不能 track symlink,设计如此)
- `AGENTS.md` — tracked 文件,内容带本次 sync 修改
- 工作目录干净(只有 CLAUDE.md symlink 一项 untracked)

## <completed_work>

**docs sync 全部完成**,涵盖:

1. 调研 ADR 现状(ADR-V2-023 → V2-024 → V2-029 链路),读 `EAASP_v2_0_EVOLUTION_PATH.md` §三 8 Phase 路线
2. 代码级验证 EAASP 边界(Cargo workspace members 含 `tools/eaasp-certifier/mcp-orchestrator/skill-registry`,`scripts/dev-eaasp.sh` 是真实 dev workflow)
3. 读双 README + .planning/PROJECT.md + ADR-V2-024 全 + ADR-V2-029 全
4. 写新文档 `docs/PROJECT_PRODUCT_OVERVIEW.md`(255 行)— 项目级产品情况 single source of truth
5. 修正 CLAUDE.md (= AGENTS.md,5 处):
   - line 9-13 (engine 接入面 / Grid 独立产品描述)
   - line 22-31 (Product legs at a glance 表 — 加 EAASP tools 关系列 + Status 从 Dormant 改 Active)
   - line 39-52 (EAASP v2 Architecture 段 — 加 NOTE 段 + 加 docs/design/EAASP/ 索引表)
   - line 95 (Grid 独立产品 status paragraph)
6. 修正 README.md + README.zh.md architecture 图标注 + 加 "This stack ships as one repository" 段
7. ADR-V2-024 Affected Modules 表下加 NOTE (2026-07-17) 澄清段(不动 frontmatter status)
8. MEMORY.md index 加 pointer 行 + 新建 `project_product_overview_2026_07_17_docs_sync.md` + `project_grid_two_leg_strategy.md` 加 Forward correction 段
9. `git push origin main` — commit `0461d97f` 已 pushed to remote
10. Smoke test: `cargo check -p grid-types` exit 0(纯文档改动,build 通过)

## <remaining_work>

**无 remaining work** — 本次 docs sync 是一个完整闭环任务,已经 ship + push + 验证。

后续战略选择(已在 `docs/PROJECT_PRODUCT_OVERVIEW.md` §4 列出,用户决策):
- A. 启动新 milestone — web-platform/ 9.0 push / grid-desktop feature / release 打包
- B. 三个薄弱环节修补(grid-desktop IPC + web-platform 测试 + web/ 测试覆盖)
- C. 推进 contract-v1.3 / certifier 升级
- D. 启动 EAASP 引擎层 v2.1 (L4 Event Room / A2A / Phase 3 OPA 后端 — per EVOLUTION_PATH §四)

## <decisions_made>

| 决策 | 理由 |
|------|------|
| **新建 `docs/PROJECT_PRODUCT_OVERVIEW.md`** 而不是 inline 修改 | 未来 session 提问"项目是什么"时,需要 single source of truth。Inline 修改分散在 4 个文件会让未来 session 重复读 |
| **ADR-V2-024 用文档层 NOTE 段,不新立 supersede ADR** | "shadow vs production" 是 ADR Accepted 时点的 stale wording,不影响 ADR Decision 物质内容。新立 ADR 风险大于收益 |
| **README 保留 "EAASP or your own platform" 替代性措辞,但加 "or 本仓库 tools/eaasp-*"** | 对外文档要保留 substitutable messaging(卖点);同时加上本仓即生产部署路径,避免误导 |
| **不 push 工作单独 commit 拆分 docs/AGENTS/README/ADR** | 跨 4 文件的叙事对齐是一个 logical unit,拆 commit 会让 git history 难以回溯 |
| **CLAUDE.md 作为 symlink 维持(由 AGENTS.md 维护内容)** | 用户已经设置 symlink,尊重它的设置;不改不破坏 |
| **MEMORY.md 不替换 `project_grid_two_leg_strategy.md`,而是加 Forward correction 段** | MEMORY 是历史档案,删/改有追溯意义。前向修正既保留历史又传达当前事实 |
| **Commit 不自动拆 WIP,直接 commit + push** | 本次任务完成度高(无 half-done state);CLAUDE.md commit policy "Push is autonomous" 适用 |

## <blockers>

无 active blockers。所有 docs sync 任务已完成 + smoke tested + pushed。

## Required Reading (in order)

未来 session 或 resume 时:

1. **`docs/PROJECT_PRODUCT_OVERVIEW.md`** — 项目级产品情况 single source of truth(2026-07-17 docs sync 产物)
2. **`docs/design/EAASP/EAASP_v2_0_EVOLUTION_PATH.md`** — EAASP v2.0 长期演化路径(5 层 + 3 管道 + 4 元范式 + 7 阶段,8 Phase 现状)
3. **`docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md`** — Phase 4 双轴 substance 决定 + NOTE (2026-07-17) 段
4. **`docs/design/EAASP/adrs/ADR-V2-029-engine-data-integration-boundary.md`** — 双轴 crate-level enforcement 落地
5. **`.planning/STATE.md`** — 当前里程碑状态(全部 SHIPPED,Grid 独立产品 Activation 2026-06-17 closed)
6. **`.planning/PROJECT.md`** §"What This Is" — 这段写得最准,2026-04-26 已经有的正确叙事,本次 docs sync 把这段作为 source of truth 推到 CLAUDE.md / README

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN**: 把 `tools/eaasp-*/` 称为 "shadow / local mock / separate upstream" → **structural mitigation**: 任何 EAASP 描述前先读 PROJECT_PRODUCT_OVERVIEW.md §2.2 + ADR-V2-024 NOTE 段 + EVOLUTION_PATH §三
- **ANTI-PATTERN**: 说 "EAASP 是另一个独立项目 / production EAASP 在另处" → **structural mitigation**: EAASP 平台全部在本仓,EAASP 设计文档权威源在 `docs/design/EAASP/`,没有"上游"项目
- **ANTI-PATTERN**: 把 ADR-V2-024 之前的 "Leg A primary / Leg B dormant" 当作现行策略 → **structural mitigation**: 双轴 substance(engine vs data/integration)取代 Leg A/B;Grid 独立产品 2026-06-17 已 SHIPPED,不再是 dormant
- **ANTI-PATTERN**: 用 `Edit` 工具改 CLAUDE.md — 它是 symlink → AGENTS.md,Edit 工具拒绝。改 AGENTS.md 即可
- **ANTI-PATTERN**: 在 MEMORY.md 老 topic 文件里直接修改历史事实 → **structural mitigation**: 用 Forward correction 段补正,保留历史档案可追溯

## Infrastructure State

- **Working tree 干净**: 只有 `CLAUDE.md` untracked symlink(设计如此,git 不 track symlink)
- **Remote**: `origin/main` 与 local HEAD 同步,均为 `0461d97f`
- **Worktrees**: 已清理(`docs-sync-eaasp-narrative-0717` 已 `ExitWorktree remove`)
- **Background processes**: 无(纯 docs 操作)
- **Memory sync**: `~/.claude/projects/-Users-sujiangwen-sandbox-LLM-speechless-ai-SGAI-grid-sandbox/memory/` 已同步(MEMORY.md index + 新建 `project_product_overview_2026_07_17_docs_sync.md` + `project_grid_two_leg_strategy.md` Forward correction)

## Pre-Execution Critique Required

N/A — 本次纯文档 synchronization,无 design / execution 二分点。

## <context>

本次 session 由用户 2026-07-17 反馈"CLAUDE.md / README / 项目现状还没有同步,需要修正"触发。CLAUDE.md 用了 ADR-V2-023 (Superseded 2026-04-28) 起草时点的过时措辞"local shadow / separate upstream",与 ADR-V2-024 双轴 substance + 实际项目状态不一致(项目本身正确:EAASP 全栈 + Grid 全栈同仓孵化,Grid 独立产品 8 phases 已 SHIPPED)。

按 chain-first diagnosis:先读 EVOLUTION_PATH §三 + ADR-V2-024 全 + ADR-V2-029 全 + ADR-V2-023 全 + 代码级验证(`scripts/dev-eaasp.sh` 是真实 dev workflow,Cargo workspace 包含 `tools/eaasp-certifier/mcp-orchestrator/skill-registry` 作为 first-class member) → 拿到准确框架 → 写 PROJECT_PRODUCT_OVERVIEW.md 作为 single source of truth → 同步修正 CLAUDE/AGENTS + README 双语 + ADR-V2-024 NOTE + MEMORY → commit + push。

关键校正 (用户 2026-07-17 中途反馈):"`tools/eaasp-*` 应该是 EAASP 平台还没有完整实现的情况下,先按平台契约做的'模拟器'级实现" — 完全修正了我原本的"去掉 shadow 措辞"草率做法。最终准确表述是 **"模拟器级参考实现"**(既承认按契约做的,也明确未完整)。

CLAUDE.md 是 symlink → AGENTS.md 是关键发现。Edit 工具出于安全拒绝写 symlink,Write 工具也类似。改 AGENTS.md 内容会通过 symlink 自动生效给 CLAUDE.md。

## <next_action>

无需 next action — 本次 docs sync 任务已闭环。下次 session resume 时:

1. 读 `docs/PROJECT_PRODUCT_OVERVIEW.md` 重建项目上下文
2. 若用户问"项目是什么 / 状态如何 / EAASP 怎么定位" → 直接给 PROJECT_PRODUCT_OVERVIEW 的摘要
3. 若用户启动新 milestone(产品矩阵 §4 列出 4 个方向)→ 走 `/gsd-new-milestone`

未来 session 触发"resume work"时,resume-work 会读 `.planning/HANDOFF.json`(本文件存在的话)+ `.planning/STATE.md`(仍是"all milestones SHIPPED")。

---

*此 handoff 由 `$gsd-pause-work` 流程 2026-07-17 写入。docs sync 全部 completed + pushed + verified,无 outstanding work,无 blockers。*
