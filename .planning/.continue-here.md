---
context: default
phase: v3.15.6-obstack-实战补完
task: null
total_tasks: 6
status: shipped
last_updated: 2026-08-12T05:20:00.000Z
---

# Session Handoff — v3.15.6 OBSTACK 实战补完 ✅ SHIPPED + tagged

> **Canonical handoff lives in `docs/status/`.** This file mirrors the GSD state machine view.

## Current state

**v3.15.6 SHIPPED,tag `v3.15.6` 已打(2026-08-12)**。5/6 阶段完成(6a 文档诚实化 / 6b 测试补完 / 6c 死代码激活 / 6g tag 前验证+修复 / 6h 补齐证据+demo 改造);**6d(web-platform Dashboard)+ 6e(CLI 全局接入)显式 deferred → v3.16**(D-53 / D-54),**不在本 tag 声称范围内**。

`OBSTACK_DESIGN` §0.1 = **20/23** —— L1 侧 6 条 series 全部真跑验证 + 负控;但 **6i 收尾复核查出 L2/L3/L4 三层 observability 零生产调用点**(同 6c 失败模式,第 2/3/4 次),三行由 ✅ 降 ⚠️,登记 `V316-L2L3L4-OBS-01` → v3.16。**tag 打早了**(对 L1 的声称仍成立),不撤,更正由 §0.1 + LEDGER 承载。

## 这轮为什么值得看

tag 前的验证推翻了 6c 声称的闭环,连锁查出 4 个问题:

| 问题 | 性质 |
|---|---|
| 6c.2/6c.3 emit 挂在 `on_tool_call/on_tool_result/on_stop` | Grid 是 Tier 1(`native_hooks: true`),L4 从不调用这三个 hook RPC → **死代码**;实测真实 tool-call turn 产出 `"scopeMetrics":[]` |
| `init_observability` 的 `drop(provider)` | `SdkMeterProviderInner::drop` 会 `shutdown()` → 导出管道**启动即死**(仅 1 个空批次) |
| `requests.*` 三个 helper | **自 OTel 模块落地起从未被调用过**;且 `TimeBlock::record_request` 双减 in_flight,首次真用即让 gauge 变负 |
| demo 脚本 4 个独立缺陷 | registry 空→handshake 静默降级 / LLM 步 30s 超时且失败不致命 / 5 事件手工 ingest / Observe 检查 grep 已不存在的文本且**从不失败** → **能在什么都没证明的情况下 exit 0** |

外加一个安全问题:`op` label 直取路径尾段 → 任意对端可无限造 time series(免认证内存耗尽),已改为 proto 21 RPC allowlist。

## 双向证据(这次不只验 PASS)

**正向**:真跑 exit 0 —— 560 chunk、真实 `memory_search` tool call、16-event timeline(含 PRE_TOOL_USE + POST_TOOL_USE + STOP,无合成事件)、4/4 required series、`in_flight` 归 0。

**负向**:喂 6g 之前的日志形态(单个空批次)→ **exit 1 并准确列出 4 条缺失 series**。即这套检查能抓住当初那个 bug。

## 门禁

- `cargo test -p grid-runtime --lib` — **100/100**
- `cargo check --workspace` — 0 errors(6g 顺带修好 `eaasp-goose-runtime` 缺 `business_key`,自 6b.2b 起 main 就编译不过)
- `make rbac-audit` — PASS(134 routes);`make v3.10-spec-audit` — PASS(38 rows)
- 全部改动限于 `grid-runtime` → **ADR-V2-023 P1(shared-core rule)保持,无需新 ADR**

## 遗留 / follow-up

1. **`V316-L2L3L4-OBS-01`(6i 已确认,不再是"待查")** — L2/L3/L4 三层 `observability.py` 定义齐全(L2 6 个 / L3 4 个 / L4 5 个 `record_*`)但 **`src/` 全域 0 处生产调用**,`main.py` 0 提及,meter 恒为 `_NoopMeter` → 指标恒 0。各层 4/4、12/12 测试**全部只测 helper 自身**,无一条断言生产路径会调用。修复判据沿用 L1 6g/6h:接入调用点 + 真跑看计数器动 + **配负控**;**不接受"模块已写 + 测了模块"作为闭环证据**。
2. **6d + 6e** 顺延 v3.16。
3. **CI 两条 workflow 长期 FAIL(既有缺口)**:`Phase 3 Contract Matrix` 缺 `v2-phase2_5-ci-setup` Makefile 目标;`CI` 缺 `glib-sys` 系统库。
4. **`.env` 影子变量陷阱**:shell 导出的旧 key 会盖住 `.env`(dotenvy 不覆盖已存在环境变量),表现为 401。已写进 demo 脚本头部注释。

## Required Reading

1. `docs/status/PRODUCTION_USABILITY_2026-08-11-obstack6g.md` — 6g + 6h 完整证据链(§1–§7 = 6g,§8 = 6h)
2. `docs/design/EAASP/OBSTACK_DESIGN.md` §0.1 — 当前 20/23,含数字七次变动的完整来龙去脉
3. `docs/superpowers/plans/2026-08-09-obstack-v3-15-6-completion.md` — 6 阶段任务清单 + 锁决策 D-47..D-54
4. `.planning/STATE.md` — GSD 状态机

## Next Action

```text
v3.16 scope 决策:
  - V316-L2L3L4-OBS-01       (6i 新发现;把 L1 的做法复制到 L2/L3/L4)
  - grid-server multi-user   (3.7.4 deferred;ADR-V2-024 Open Item #3 优先轴)
  - web-platform 7.5 → 9.0
  - grid-desktop 6.5 → 9.0
  - 6d + 6e 顺延
```

---

*Resume with `/gsd-resume-work`.*
