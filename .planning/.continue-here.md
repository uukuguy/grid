---
context: default
phase: v3.15.6-obstack-实战补完
task: 6f
total_tasks: 6
status: in_progress
last_updated: 2026-08-12T00:20:00.000Z
---

# Session Handoff — v3.15.6 OBSTACK 实战补完 (4/6 阶段完成,6g 拦下 6c 的假闭环)

> **Canonical handoff lives in `docs/status/`.** This file mirrors the GSD state machine view.

## Current state (one-paragraph summary)

**v3.15.6 — 4/6 阶段完成**。6a(文档诚实化)+ 6b(测试补完)+ 6c(死代码激活,**经 6g 修正**)+ **6g(tag 前验证与修复)** 完成;**6d + 6e deferred → v3.16**(D-53/D-54);**6f(tag v3.15.6)仍未做,且前置条件已扩大**。

6g 是本轮关键:准备 tag 前先验 6c 的 4 处 `⚠️→✅`,查出 **2 项不成立** —— (1) 6c.2/6c.3 把 OBSTACK emit 挂在 `on_tool_call/on_tool_result/on_stop`,而 Grid 是 Tier 1(`native_hooks: true`),L4 从不调用这三个 hook RPC,**实测真实 tool-call turn 产出 `"scopeMetrics":[]`**;(2) 更底层 `init_observability` 的 `drop(provider)` 触发 SDK `shutdown()`,导出管道启动即死(仅 1 个空批次)。两者均已修复,真实 deepseek turn 实测 **4/6 series 出数,批次 1 → 40+**。§0.1 由 6c.7 的 23/23 **诚实降为 21/23 (91%)**。

## 6g 产出

| commit | 内容 |
|---|---|
| `4defa334` | 修 `cargo check --workspace` —— `eaasp-goose-runtime` 缺 `business_key`,自 6b.2b 起 main 就编译不过 |
| `0318aca9` | provider 存入 `OnceCell` 绑定进程生命周期(原 `drop(provider)` → `shutdown()`);导出间隔加 `EAASP_OTEL_INTERVAL_SECS`(ADR-V2-028 默认回落 30s) |
| `d39db604` | emit 移到 `map_events_to_chunks` 的 `AgentEvent` 流;`in_flight` 改 Drop guard(断连记 `abandoned`、lag 记 `lagged`);三个 hook 方法恢复 no-op 且**不重复 emit**;`classify_event` 拆纯函数以便断言;10 新测试 |
| `75859214` | §0.1 降 21/23 + 6g 证据文档 + DEFERRED_LEDGER 两项更新 |

**约束保持**:全部改动在 `grid-runtime` 内,未动 `grid-engine` → **ADR-V2-023 P1 (shared-core rule) 保持,无需新 ADR**。95/95 tests PASS;dual-gate PASS(134 routes / 38 rows)。

## 实测证据(真实 LLM,非 demo ingest)

```
l1.runtime.llm.total    {model=deepseek-v4-flash, status=ok}  = 2
l1.runtime.flow.outcome {business_key=bc56d286-…, complete}   = 1
l1.runtime.flow.outcome {business_key=36ed4d39-…, complete}   = 1
l1.runtime.in_flight    {op=turn}                             = 0    ← Drop guard 收支平衡
失败 turn: flow.outcome{status=error}=1 + errors.total{agent_error}=1
```

## 6f 前置条件(已扩大,tag 前必须收敛)

1. **`tool.total` + `requests.*` 端到端实证** —— 6g 验证用例(算术提问)未触发 tool call,这两条 series 只有单测覆盖。按本次教训,**单测通过 ≠ 线上会动**。
2. **§0.1 剩余 2 项 ⚠️ 收敛**。
3. **demo 脚本改造** —— 5 个手工 `/v1/events/ingest` 未改;30s LLM 超时对 reasoning model 偏短;9b/9c 证据检查 grep 旧日志文本行,恒为 0 却不判失败(**证据检查本身失效**),应改为解析 `resourceMetrics` JSON 断言 series 非空。

## 其他阻塞 / 陷阱

- **`.env` 影子变量**:shell 导出的旧 `DEEPSEEK_API_KEY` 会盖住 `.env`(dotenvy 不覆盖已存在环境变量),表现为 401 且难以一眼看出。跑 live 前先 `unset` 或比对哈希。
- **L4 `/v1/sessions/{id}/message` 挂起** —— 240s 无响应,skill-registry 报 `threshold-calibration not found`。独立问题,与 OBSTACK 无关。
- **CI 两条 workflow 长期 FAIL(既有缺口,已复核非本次引入)**:`Phase 3 Contract Matrix` 缺 `v2-phase2_5-ci-setup` Makefile 目标;`CI` 缺 `glib-sys` 系统库。

## Required Reading (in priority order)

1. `docs/status/PRODUCTION_USABILITY_2026-08-11-obstack6g.md` — 6g 完整证据链 + 未决事项
2. `docs/superpowers/plans/2026-08-09-obstack-v3-15-6-completion.md` — 6 阶段 task 清单 + 锁决策 D-47..D-54
3. `docs/status/RETROSPECTIVE_2026-08-11-OBSTACK-V3-15-6.md` — 6a/6b/6c 复盘(注:其"已闭环"结论被 6g 部分推翻)
4. `docs/design/EAASP/OBSTACK_DESIGN.md` §0.1 — 当前 21/23
5. `.planning/STATE.md` — GSD 状态机

## Next Action

```text
# 6f 前置:补 tool.total 端到端实证
1. 找/建一个真实触发 tool call 的 skill
2. 启栈跑真实 turn,确认 l1.runtime.tool.total 出数
3. 收敛 §0.1 剩余 2 项 ⚠️
4. 改造 demo 脚本(真实驱动 + 超时放宽 + 证据检查改 JSON 断言)
5. 之后再 git tag v3.15.6
```

之后进入 **v3.16 scope 决策**(ADR-V2-024 data/integration 轴):`grid-server multi-user` / `web-platform 7.5 → 9.0` / `grid-desktop 6.5 → 9.0` / 6d + 6e 顺延。

---

*Resume with `/gsd-resume-work`.*
