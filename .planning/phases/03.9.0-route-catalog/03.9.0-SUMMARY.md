---
phase: 03.9.0
plan: 01
subsystem: grid-server-rbac
tags: [axum, rbac, route-catalog, authorization]
requires: [v3.8-rbac-middleware]
provides: [typed-route-catalog, explicit-public-allowlist]
affects: [03.9.1, 03.9.2]
key-files:
  created:
    - crates/grid-server/src/rbac/catalog.rs
    - crates/grid-server/src/rbac/mod.rs
    - crates/grid-server/tests/route_catalog.rs
  modified:
    - crates/grid-server/src/lib.rs
requirements-completed: [CAT-01, CAT-02, CAT-04]
completed: 2026-07-26
---

# Phase 03.9.0 Summary — Route catalog and public allowlist

A typed 139-entry HTTP route catalog now provides stable authorization metadata for the complete `grid-server` surface, with exactly three explicitly public routes.

## Commits

- `96200ba` — RED: failing catalog contract
- `ddd4397` — GREEN: public catalog and allowlist
- `831e2d4` — formatting isolated to phase files

## Verification

- `cargo test -p grid-server --test route_catalog -- --test-threads=1` — 3/3 PASS
- `cargo check -p grid-server` — PASS; 16 pre-existing warnings
- Catalog uniqueness and representative coverage verified by `cat_01_catalog_is_complete_and_unique`.
- Public allowlist symmetry verified by `cat_02_public_allowlist_is_exact_and_symmetric`.

## Decisions Made

- Catalog entries use canonical fully-prefixed paths, including `/api/v1` and nested `/scheduler` paths.
- Route metadata is plain public Rust data so Phase 03.9.1 can wire enforcement and Phase 03.9.2 can audit it without runtime introspection.
- Existing seven-action vocabulary is used as an initial semantic draft; Phase 03.9.1 owns coherent extension and matrix regeneration.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Isolated rustfmt from pre-existing workspace formatting drift**
- **Found during:** Task 3 verification
- **Issue:** Workspace-level `cargo fmt --check` reported unrelated pre-existing formatting differences and an attempted targeted invocation formatted the crate module graph.
- **Fix:** Restored all unrelated files individually and retained formatting only for files created in this phase.
- **Files modified:** `crates/grid-server/src/rbac/catalog.rs`, `crates/grid-server/tests/route_catalog.rs`
- **Commit:** `831e2d4`

## Known Stubs

None.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: authorization-catalog | crates/grid-server/src/rbac/catalog.rs | New trust-boundary metadata classifies every HTTP route; enforcement lands in 03.9.1. |

## Self-Check: PASSED

All created files exist and all listed commits are present in git history.
