# Phase 3.7.1: grid-cli 实战可用性补全 — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `3.7.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-07-19
**Phase:** 3.7.1-grid-cli-production
**Areas discussed:** 实战场景选择, Quickstart 0→first-success 路径, Error recovery UX, Plan structure 拆 audit/fix

---

## 实战场景选择

| Option | Description | Selected |
|--------|-------------|----------|
| 5 全选 (multi-step + memory + hook + stream + parallel) | All 5 candidate scenarios as acceptance baseline | ✓ |
| 3 核心场景 (multi-step + memory + hook) | Recommended default — covers document + control loop | |
| 3 表现型场景 (multi-step + streaming + parallel) | Skips memory/hook; user-visible only | |

**User's choice:** 5 全选 (multi-step + memory + hook + stream + parallel)
**Notes:** User wants full coverage of grid-cli's capability surface. Accepts longer validation cycle in exchange for comprehensive验收 baseline.

**Locked as:** D-01 (CONTEXT.md) — all 5 scenarios S1-S5 are IN scope.

---

## Quickstart 0→first-success 路径

| Option | Description | Selected |
|--------|-------------|----------|
| `grid quickstart` 子命令 | Single CLI command: pre-flight + run scenario | |
| 独立 quickstart script + doc | `scripts/quickstart.sh` + `docs/cli/QUICKSTART.md` | |
| 两者都要 (CLI 命令 + doc) | Both paths shipped together | ✓ |
| 你决定 | Agent picks | |

**User's choice:** 两者都要 (CLI 命令 + doc)
**Notes:** User wants both — `grid quickstart <scenario>` for hands-on contributors + `docs/cli/QUICKSTART.md` for CTO/lead review.

**Locked as:** D-03, D-04 (CONTEXT.md) — both paths ship; `grid quickstart` is a new subcommand; default scenario = S1.

---

## Error recovery UX

| Option | Description | Selected |
|--------|-------------|----------|
| Fail-fast + suggest `grid doctor --repair` | Plain-text error + fix hint; preserves observability of underlying cause | ✓ |
| Retry-with-backoff 自动重试 | Default 3-retry with exponential backoff; output during retries | |
| Interactive prompt (确认后再决策) | TTY prompt "retry? doctor? logs?" | |
| 你决定 | Agent picks | |

**User's choice:** Fail-fast + suggest `grid doctor --repair` (Recommended)
**Notes:** User prefers enterprise-observability — see the actual error, then decide. CI-safe (no implicit retry). Aligns with Activation-9.0 / production-quality framing.

**Locked as:** D-05, D-06, D-07 (CONTEXT.md) — fail-fast + text/JSON dual mode based on TTY.

---

## Plan structure 拆 audit/fix

| Option | Description | Selected |
|--------|-------------|----------|
| 1 plan (audit + fix together) | Fast but reviewer doesn't see gap table | |
| 2 plans (audit + fix separate) | Plan 01 = read-only audit → gap table; Plan 02 = REQ-IDs + fix + UAT | ✓ |
| 2 plans + 按需补充 plans | Audit + Fix + potential overflow plans | |
| 你决定 | Agent picks | |

**User's choice:** 2 plans (audit + fix separate) (Recommended)
**Notes:** User prefers lower execution risk — visible audit gap matrix before committing fix scope. Plan 02 may overflow into Plan 03 if audit reveals too many gaps; if so, defer.

**Locked as:** D-08 (CONTEXT.md) — Plan 3.7.1-01 audit + Plan 3.7.1-02 fix.

---

## the agent's Discretion

- Audit scope depth (spot-check vs exhaustive)
- Walkthrough doc template format
- Doctor --repair coverage (extending existing 10 checks)
- JSON error schema field names

## Deferred Ideas

- `grid studio` / TUI as quickstart entry point — out of scope (TUI is separate surface)
- `grid` shell completions test scenario (S6) — out of current 5
- `grid eval` integration as S6 — defer; surfaced as audit finding if eval still stubby
- `grid mcp` log streaming as S6 — defer; surfaced as audit finding if MCP still stubby
- `grid quickstart --tui` flag to launch Studio after scenario setup — out of scope

---

*Discussion closed: 2026-07-19. CONTEXT.md updated. Ready for /gsd-plan-phase 3.7.1.*