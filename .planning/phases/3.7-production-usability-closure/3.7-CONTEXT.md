---
type: context
phase: 3.7-production-usability-closure
milestone: v3.7
date: 2026-07-19
author: Claude (claude-opus-4-8) via Claude Code CLI
---

# v3.7 实战可用性补全 — Context (Decisions Locked)

## Trigger

On 2026-07-19 the user clarified the post-Activation Quality 9.0+ scoreboard declared in v3.6 docs sync **does NOT mean real-world usable**. Activation 9.0 measures internal code health (tests, coverage, ADR enforcement, lint); it does NOT measure 实战可用性 (real-world usability). The next milestone must close this gap.

## User direction (2026-07-19) — verbatim quotes

> "首要的核心工作应该是 grid-engine/grid-cli/grid-web/EAASP"

> "grid-cli 单人直接操作智能体工作；grid-web 方便网页dashboard等方式监控、跟踪智能体执行；底层核心是 grid-engine；EAASP 是本地 EAASP 平台仿真模拟，可以接近实战完成企业级智能体支撑平台功能执行与调试"

> "现在需要的是可以完整独立工作 ... 接下来应该是产品运行层面的可用性补全，涉及功能完整性、可用性、实战场景案例等"

> "grid-server 是下一步再讨论，目前先把单用户的 grid-cli/grid-web/EAASP仿真做好"

> "grid-web 单用户 UI 目前还是实战不可用状态"

> "grid-server 是下一步，多用户登录场景"

## Scope (locked 2026-07-19)

| Surface | Activation Score | Real-world Status | v3.7 Target |
|---------|------------------|-------------------|-------------|
| `crates/grid-cli/` | 9.0 | 完整独立工作 (单用户跑 agent) | 端到端实战 verification; 16 commands work in 真实场景 walkthrough |
| `web/` (grid-web single-user UI) | 9.0 | **实战不可用** (per user) | Dashboard for monitoring/tracking agents; close Activation-9.0 ↔ 实战-不可用 gap |
| `tools/eaasp-*/` (EAASP 本地仿真) | 5/7 audit | 接近实战企业平台 | Simulator-level → credible enterprise simulation; wire minimum Phase 3 governance gate hooks |
| `crates/grid-server/` | 9.0 | Deferred to v3.8 | Multi-user login scenario — user explicitly deferred |

## Out of scope (deferred to v3.8+)

- `grid-server` multi-user login scenario — RBAC + JWT tenant scoping + cross-user session isolation. User explicitly deferred until single-user stack is 实战可用.
- EAASP v2.0 platform-evolution gaps (Phase 3 production OPA / Phase 4 A2A / Phase 5 L5 Cowork UI / Phase 6 ecosystem) — only minimum credible hooks wired in 3.7.3; full implementations remain future work.
- `web-platform/` (Quality 7.5) and `grid-desktop` (Quality 6.5) — user did NOT include them in v3.7; stay in Activation-deferred backlog.

## 实战可用 acceptance standard

1. **grid-cli**: A non-developer can `grid` + start a realistic enterprise-style task (multi-step agent + tool use + memory + hooks) and observe meaningful output without CLI-flag tuning.
2. **grid-web** (`web/`): A non-developer can open the dashboard, observe a running agent's progress in real time, see its tool calls, see its memory writes, and stop/resume it without code intervention.
3. **EAASP 本地仿真**: A non-developer can `eaasp session run -s <skill> -r <runtime> "<prompt>"` against a realistic enterprise scenario and see L2 memory + L3 governance gate + L4 SSE streaming behave as a credible enterprise platform.

## Shared core rule (ADR-V2-023 P1 retained under ADR-V2-024)

Any change to `grid-engine` / `grid-runtime` / `grid-types` / `grid-sandbox` / `grid-hook-bridge` must work for both engine 接入面 and Grid independent product. v3.7 work predominantly touches `crates/grid-cli/` + `web/` + `tools/eaasp-*/` — none are shared core, but if a change crosses that boundary it must respect the rule.

## Sub-phase mapping (locked)

| Phase | Goal | Touches | Output |
|-------|------|---------|--------|
| **3.7.1 grid-cli 实战可用性补全** | Run any enterprise-style agent scenario from CLI without manual stitching | `crates/grid-cli/` | 3 实战 scenarios PASS end-to-end without code intervention |
| **3.7.2 web/ 实战可用性补全** (grid-web dashboard 实战化) | Dashboard a non-developer can use to monitor/track/stop/resume agents | `web/` | Walkthrough recording of dashboard end-to-end with real agent |
| **3.7.3 EAASP 本地仿真补全** (Phase 0–2.5 + Phase 3 governance hooks) | Simulator-level → credible enterprise simulation | `tools/eaasp-*/` | 1 实战 enterprise scenario with observable governance gate behavior |
| **3.7.4 SKIPPED** | grid-server multi-user deferred to v3.8 | (none) | Empty |

## Key files for next-session pickup

- `.planning/phases/3.7-production-usability-closure/3.7-CONTEXT.md` — this file (decisions locked)
- `.planning/ROADMAP.md` §v3.7 — milestone entry with sub-phase plans
- `.planning/PROJECT.md` §Current Phase + §Active — scope table
- `.planning/STATE.md` frontmatter — `milestone: v3.7, status: in_progress`
- `.planning/phases/3.6-post-activation-docs-sync/3.6-HANDOFF.md` — prior milestone closure notes
- `docs/PROJECT_PRODUCT_OVERVIEW.md` — canonical SSOT
- `docs/status/PRODUCT_STATUS_2026-07-17.md` — frozen audit snapshot

## How to resume in a new session

1. Claude Code session start: read `AGENTS.md` (via `CLAUDE.md` symlink). The Product status block surfaces the 5 canonical facts.
2. Run `/gsd-progress` to see current planning state.
3. Current focus: Phase 3.7.1 grid-cli 实战可用性补全. Start with `/gsd-discuss-phase 3.7.1` if context is needed, or `/gsd-plan-phase 3.7.1` if scope is already locked.
4. Sub-phase 3.7.4 is intentionally empty — do NOT plan work into it.