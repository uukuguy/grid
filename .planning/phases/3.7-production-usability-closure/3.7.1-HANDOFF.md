---
type: handoff
phase: 3.7.1-grid-cli-production
from_milestone: v3.7 实战可用性补全 (Production-Usability Closure)
to_phase: 3.7.2 (web/ dashboard 实战化) OR live-walkthrough rerun
date: 2026-07-19
author: Claude (claude-opus-4-8) via Claude Code CLI
---

# Phase 3.7.1 Handoff — grid-cli 实战可用性 → next

## Status: v3.7.1 SHIPPED ✅ (CLI surface); live walkthrough PARTIAL

- Plan 01 (audit): 3 tasks complete → `docs/audit/3.7.1-GAP-AUDIT.md` (271 lines, 9 REQ-AUDITs)
- Plan 02 (fix + docs): 7 tasks complete → 8 REQ-AUDITs closed (88.9%)
- 10 commits on `main`; working tree clean
- 14/14 hermetic scenario integration tests PASS
- 7/7 unit tests PASS (error UX)
- `grid doctor`: 12 checks, 9 PASS, 3 WARN, 0 FAIL (incl. new Hooks File + Eval Bridge)

## What was done

| Commit | Purpose |
|---|---|
| `28447abc` | docs(audit): Phase 3.7.1 gap audit — S1-S5 vs 17-command CLI surface |
| `062577d2` | docs(3.7.1-01): plan-01 summary — overflow verdict FITS |
| `a0ee775f` | feat(grid-cli): `grid quickstart <scenario>` (REQ-AUDIT-06, D-03/D-04) |
| `4ccc0f8d` | feat(grid-cli): error UX per D-05/D-06/D-07 (REQ-AUDIT-05) |
| `1027fbfa` | feat(grid-cli): `grid session resume` + `grid run --parallel` (REQ-AUDIT-01/-02) |
| `92215e24` | feat(grid-cli): doctor to 12 checks + agent typed-error parity (REQ-AUDIT-07/-08) |
| `6db922a9` | test(grid-cli): 14 hermetic scenario integration tests (S1-S5) |
| `4fe74ef0` | docs(cli): QUICKSTART.md + 5 scenario walkthroughs (D-02/D-03) |
| `f7f39091` | docs(3.7.1): PRODUCTION_USABILITY_2026-07-19 walkthrough + Plan 02 summary |

State machine commits (gsd-tools calls clobbering v3.7 frontmatter; restored):
- `15bf5f97` docs(state): restore v3.7 frontmatter after gsd-tools planned-phase call
- (no extra commit needed for `c0144a1c` from earlier — that was the v3.6 SHIPPED cleanup)

## What is on disk now

### Modified files (committed in this run)

- `crates/grid-cli/src/commands/quickstart.rs` (NEW, 210 lines) — `grid quickstart <scenario>` with 5 scenario runners
- `crates/grid-cli/src/error.rs` — `ErrorClass` + `classify()` + `is_retryable()` + `fix_hint()` + `to_json()` + `Serialize` derive; `AgentNotFound` variant + factories; 4 new tests
- `crates/grid-cli/src/commands/types.rs` — `SessionCommands::Resume { session_id }` variant
- `crates/grid-cli/src/commands/session.rs` — `resume_session()` function (REQ-AUDIT-01)
- `crates/grid-cli/src/commands/run.rs` — `RunOptions::parallel` + `run_parallel()` (REQ-AUDIT-02)
- `crates/grid-cli/src/commands/state.rs` — `#[derive(Clone)]` on `AppState` (so it can move into spawn closures)
- `crates/grid-cli/src/commands/doctor.rs` — `check_hooks_file()` (REQ-AUDIT-07) + `check_eval_bridge()` (REQ-AUDIT-03 observability); total 12 checks
- `crates/grid-cli/src/commands/agent.rs` — typed `GridError::agent_not_found` in 3 branches (REQ-AUDIT-08) + silent-show fix (REQ-AUDIT-09)
- `crates/grid-cli/src/commands/mod.rs` — `pub mod quickstart` + 3 re-exports
- `crates/grid-cli/src/lib.rs` — `pub retry: bool` global flag + `Quickstart` variant + `--parallel <N>` flag
- `crates/grid-cli/src/main.rs` — Quickstart dispatch arm + TTY→JSON error path + `cli.retry` thread

### New test files

- `crates/grid-cli/tests/common_scenarios.rs` (shared helpers)
- `crates/grid-cli/tests/S1_multi_step_tool_use.rs` (3 tests)
- `crates/grid-cli/tests/S2_memory_driven.rs` (3 tests)
- `crates/grid-cli/tests/S3_hook_governance.rs` (2 tests)
- `crates/grid-cli/tests/S4_streaming_stop_resume.rs` (3 tests)
- `crates/grid-cli/tests/S5_parallel_batch.rs` (3 tests)

### New docs

- `docs/audit/3.7.1-GAP-AUDIT.md` (271 lines) — Plan 01 audit output
- `docs/cli/QUICKSTART.md` (84 lines) — standalone CTO/lead doc
- `docs/cli/scenarios/S{1..5}-*.md` (5 files, ~60 lines each) — per-scenario walkthroughs
- `docs/status/PRODUCTION_USABILITY_2026-07-19.md` (200+ lines) — dated walkthrough record

### Planning artifacts

- `.planning/phases/03.7.1-grid-cli/3.7.1-CONTEXT.md` (locked decisions D-01..D-08)
- `.planning/phases/03.7.1-grid-cli/3.7.1-DISCUSSION-LOG.md` (audit trail)
- `.planning/phases/03.7.1-grid-cli/3.7.1-01-PLAN.md` + `-01-SUMMARY.md`
- `.planning/phases/03.7.1-grid-cli/3.7.1-02-PLAN.md` + `-02-SUMMARY.md`

### Untracked / scratch (none)

- No `.superpowers/`, no `docs/superpowers/`, no `.lwm/` artifacts.
- All GSD state lives under `.planning/`.

## What is NOT done (next milestone candidates)

### Candidate A — Re-run live walkthrough with LLM API key

The CLI surface is verified, but actual end-to-end LLM calls were not exercised
(no `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` in env when this ran). Re-run:

```bash
export OPENAI_API_KEY=sk-...
cargo run --bin grid -- quickstart S1   # full S1 walkthrough
cargo run --bin grid -- quickstart S2   # full S2 (memory add + ask)
export GRID_HOOKS_FILE=./hooks.yaml
cargo run --bin grid -- quickstart S3   # full S3 (hook decision prompt)
cargo run --bin grid -- quickstart S4   # create session, then `grid session resume <id>`
cargo run --bin grid -- quickstart S5   # launch 3 parallel agents

# Update PRODUCTION_USABILITY_2026-07-19.md with actual transcripts.
```

Source: `docs/status/PRODUCTION_USABILITY_2026-07-19.md` §"Known limitations".

### Candidate B — `web/` dashboard 实战可用性补全 (Phase 3.7.2)

Per v3.7 milestone scope, next sub-phase is `web/` (single-user UI).
Per CONTEXT.md D-01, user 2026-07-19 stated "grid-web 单用户 UI 目前还是实战不可用状态".

New sub-phase 3.7.2-web-production:
- Audit current `web/` 8 tabs against 实战可用 (Activation 9.0 may not match 实战)
- Wire `grid-server` WS streaming so dashboard reflects running agent in real-time
- Tool-call event ordering in UI; memory write visibility; stop/resume UX
- Plan + execute (similar pattern to 3.7.1)

### Candidate C — EAASP 本地仿真补全 (Phase 3.7.3)

Wire minimum credible Phase 3 governance gate hooks so EAASP local tools
behave like an enterprise platform (credibility for customer evaluation).
Currently SHIPPED 8 phases (Phase 0–2.5); Phase 3 OPA + Phase 4 A2A + Phase 5
L5 + Phase 6 ecosystem are future work.

### Candidate D — REQ-AUDIT-04 (`grid mcp logs` live streaming)

Per audit overflow verdict (Plan 01 Section 5), this REQ-AUDIT-NN is
L-sized and requires upstream `agent_runtime.mcp_manager` API change.
Should become a Plan 03 of v3.7.1 (or move to v3.7.4 REQ backlog).

### Priority guidance

Per user 2026-07-19 instruction "目前先把单用户的 grid-cli/grid-web/EAASP仿真做好",
the natural progression is **Candidate B (Phase 3.7.2 web/)** next. Candidate A
is a quick 30-min rerun that should happen before declaring v3.7.1 fully
green.

## Key files for next-session pickup

- `docs/PROJECT_PRODUCT_OVERVIEW.md` — canonical SSOT
- `docs/status/PRODUCTION_USABILITY_2026-07-19.md` — Phase 3.7.1 audit record (read first)
- `.planning/STATE.md` — `milestone: v3.7, status: in_progress` (gsd-tools clobbers v3.7 frontmatter on every state call; restore pattern at `15bf5f97`)
- `.planning/ROADMAP.md` §v3.7 — milestone entry with sub-phase mapping
- `.planning/phases/3.7-production-usability-closure/3.7-CONTEXT.md` — milestone context (D-01..D-08 for 3.7.1; 3.7.2/3.7.3 need discuss-phase)
- `.planning/phases/03.7.1-grid-cli/3.7.1-02-SUMMARY.md` — Plan 02 closure detail

## How to resume in a new session

1. Claude Code session start: read `AGENTS.md` (via `CLAUDE.md` symlink). The Product status block surfaces the 5 canonical facts.
2. Run `/gsd-progress` to see current planning state.
3. User decision: pick Candidate A (live walkthrough rerun) or Candidate B (Phase 3.7.2 web/) next.
4. If Candidate A: set `OPENAI_API_KEY` then run `cargo run --bin grid -- quickstart S1..S5` and update `PRODUCTION_USABILITY_2026-07-19.md` with real transcripts.
5. If Candidate B: `/gsd-discuss-phase 3.7.2` to lock decisions for web/ dashboard 实战化.

## Known caveats / handoff notes

- **gsd-tools state clobbering**: `state record-session` and `state planned-phase` overwrite v3.7 frontmatter with their own v3.6/8-phase template. Restored twice in this run (`c0144a1c`, `15bf5f97`). Pattern: gsd-tools writes the correct `status: executing` + `last_activity` + `progress.total_plans` values; restore v3.7-specific fields after each call.
- **Cargo integration test auto-discovery** requires `tests/*.rs` siblings, not subdirs. Plan spec used `tests/scenarios/` subdir; flattened to root with `common_scenarios.rs` shared helper.
- **Live walkthrough deferred**: no LLM API key in env when this ran. Hermetic CLI-surface tests are the verification record.
- **REQ-AUDIT-04 deferred** to next milestone (Plan 03 candidate) per audit overflow verdict.
- **AppState Clone derive** added to enable `tokio::spawn` parallel agent dispatch (REQ-AUDIT-02). Internal Arc<>'s are shared cheaply; PathBuf/OutputConfig/GridRoot are all Clone.

## Final state

```
$ git log --oneline -10
f7f39091 docs(3.7.1): PRODUCTION_USABILITY_2026-07-19 walkthrough + Plan 02 summary
4fe74ef0 docs(cli): QUICKSTART.md + 5 scenario walkthroughs (D-02, D-03)
6db922a9 test(grid-cli): add 14 hermetic scenario integration tests (S1-S5)
92215e24 feat(grid-cli): extend doctor to 12 checks + agent typed-error parity
1027fbfa feat(grid-cli): add `grid session resume` + `grid run --parallel`
4ccc0f8d feat(grid-cli): error UX per D-05/D-06/D-07 (REQ-AUDIT-05)
a0ee775f feat(grid-cli): add `grid quickstart <scenario>` subcommand
062577d2 docs(3.7.1-01): plan-01 summary — audit complete, overflow verdict FITS
28447abc docs(audit): Phase 3.7.1 gap audit — S1-S5 vs 17-command CLI surface
15bf5f97 docs(state): restore v3.7 frontmatter after gsd-tools planned-phase call

$ git status --short --branch
* main...origin/main
clean — nothing to commit
```

Local main and origin/main are in sync at `f7f39091`. Working tree clean.
v3.7.1 grid-cli 实战可用性 work is COMPLETE on the CLI surface. Ready for
Phase 3.7.2 (web/) or live walkthrough rerun.