---
phase: 08.3-l4-p3-hardening-mechanical-copy-paste-from-l3-patterns
plan: 02
subsystem: cli
tags: [httpx, pytest, typer, verify-v2-mvp, error-handling]

# Dependency graph
requires:
  - phase: 08.2-l4-foundation
    provides: "L4 SSE streaming, session CRUD endpoints, CLI client patterns"
provides:
  - "CLI test coverage for 5xx server errors (exit_code=4)"
  - "Cleaned pyproject.toml (removed unused respx dep)"
  - "session show --limit flag with [1, 500] clamping"
  - "Safe dict.get() guard in session run command"
  - "Dynamic version parsing in verify-v2-mvp.py"

affects: ["eaasp-cli-v2 tests", "verify-v2-mvp CI", "L4-09..L4-13 REQs"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "httpx.MockTransport for 5xx error simulation in CLI client tests"
    - "typer.Option with --limit/-n flag pattern for paginated commands"
    - "dict.get() with None guard for defensive JSON response parsing"

key-files:
  created: []
  modified:
    - tools/eaasp-cli-v2/tests/test_client.py
    - tools/eaasp-cli-v2/pyproject.toml
    - tools/eaasp-cli-v2/src/eaasp_cli_v2/cmd_session.py
    - scripts/verify-v2-mvp.py

key-decisions:
  - "Clamp --limit to [1, 500] for show command to match L4 API constraint"
  - "Keep TEST_SKILL_VERSION as fallback in verify-v2-mvp.py for backward-compat when stdout parsing fails"

patterns-established:
  - "5xx error test pattern: MockTransport + json_response + assert exit_code==4"
  - "Defensive response shape guard pattern: result.get(key) + not-check + graceful exit"

requirements-completed: [L4-09, L4-10, L4-11, L4-12, L4-13]

# Metrics
duration: 2min
completed: 2026-06-10
---

# Phase 08.3 Plan 02: CLI Hardening + Verify Script Fix Summary

**5 mechanical P3 items: 5xx test coverage, unused dep removal, --limit flag, response shape guards, and dynamic version parsing — ~129 LOC across 4 files.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-06-10T04:34:50Z
- **Completed:** 2026-06-10T04:37:02Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Added `test_call_500_raises_server_error` and `test_call_503_raises_server_error` tests (L4-09 / D42) — both assert `CliError.exit_code == 4`
- Removed unused `respx>=0.21` from CLI pyproject.toml dev dependencies (L4-10 / D43)
- Added `--limit`/`-n` CLI flag to `session show` command with [1, 500] clamping (L4-11 / D44)
- Replaced `result["session_id"]` with `result.get("session_id")` + missing-key guard in `session run` (L4-12 / D45)
- verify-v2-mvp.py now parses `version` from `skill submit` stdout JSON; falls back to hardcoded `TEST_SKILL_VERSION` (L4-13 / D61)

## Task Commits

Each task was committed atomically:

1. **Task 1: L4-09 5xx tests + L4-10 remove respx + L4-11 show --limit** - `cbf71505` (feat)
2. **Task 2: L4-12 response shape guard + L4-13 dynamic version parsing** - `c3c828a7` (feat)

## Files Modified
- `tools/eaasp-cli-v2/tests/test_client.py` — Added 2 new 5xx tests (test_call_500_raises_server_error, test_call_503_raises_server_error)
- `tools/eaasp-cli-v2/pyproject.toml` — Removed `respx>=0.21` from dev dependencies
- `tools/eaasp-cli-v2/src/eaasp_cli_v2/cmd_session.py` — Added `--limit` flag to `show` command with clamping; replaced `result["session_id"]` with `result.get("session_id")` in `run` command with graceful exit
- `scripts/verify-v2-mvp.py` — Assertion 1 now parses version from submit stdout JSON; assertion 2 uses parsed version from shared state

## Decisions Made
- `--limit` clamped to [1, 500] to match L4 API constraints — safe default of 100 remains
- verify-v2-mvp.py keeps `TEST_SKILL_VERSION = "0.1.0"` as fallback when stdout parsing fails (backward-compat for non-JSON CLI output)
- No other `result["key"]` accesses in cmd_session.py needed changes — `list_cmd`, `close`, `show`, `events_cmd`, and `send` all already use safe patterns

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
- Pre-existing test failure in `test_cmd_memory.py::test_search` (Rich table rendering issue) — unrelated to this plan's changes. All 13 relevant tests (5 test_client + 8 test_cmd_session) pass cleanly.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness
- All 5 REQ-IDs (L4-09 through L4-13) completed for plan 02
- Ready for plan 03 (if any) or phase completion
- LEDGER items D42, D43, D44, D45, D61 ready for ✅ CLOSED marking

---
*Phase: 08.3-l4-p3-hardening-mechanical-copy-paste-from-l3-patterns*
*Completed: 2026-06-10*
