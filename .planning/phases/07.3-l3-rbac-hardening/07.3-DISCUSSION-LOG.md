# Phase 7.3 Discussion Log

**Session:** 2026-06-07
**Status:** Context gathered, ready for `/gsd-plan-phase 7.3`

## Questions Asked

### 1. RBAC Strategy (D8/D46)
- **Q**: Where should `access_scope` enforcement live?
- **A**: "Both + skill-registry guard"
  - D8: FastAPI middleware/Depends for L3 runtime RBAC
  - D46: skill-registry namespace validator at registration time

### 2. Skill Usage Telemetry (D9)
- **Q**: How should `skill_usage` be implemented?
- **A**: "Both paths with fallback"
  - Primary: query L2 audit log via HTTP
  - Fallback: aggregate from L3's own telemetry_events table

### 3. Error Handling + Logging (D22/D23)
- **Q**: What error shape and logging setup?
- **A**: "Standard JSON error + loguru per ADR-V2-028"
  - JSON `{"error": "internal_error", "detail": str(e)}` for unhandled exceptions
  - loguru configured in create_app() lifespan with stderr sink

### 4. Test Timing Fix (D26)
- **Q**: How to replace asyncio.sleep(1.1)?
- **A**: "Monotonic tiebreaker column"
  - Add `_tiebreaker: int` to TelemetryEventIn
  - Tests inject explicit tiebreaker values

## Key Decisions Captured

See CONTEXT.md §decisions for full detail:
- D-01: RBAC = FastAPI Depends (D8) + skill-registry namespace guard (D46)
- D-02: Telemetry = L2-primary + L3-fallback (D9)
- D-03: Error = JSON envelope + loguru per ADR-V2-028 (D22/D23)
- D-04: Test timing = monotonic tiebreaker (D26)
- D-05: D17 = `.get("hook_id")` with typed HTTPException
- D-06: D18 = Path(min_length=1, pattern=r"^[a-zA-Z0-9_-]+$")
- D-07: Plan structure = single plan, 8 tasks, 4 waves
- D-08: Auth token = X-Session-Scope header, no signing (v3.3 lightweight)

## Next Step

`/gsd-plan-phase 7.3` — single plan, 8 tasks (L3-01..L3-08)
