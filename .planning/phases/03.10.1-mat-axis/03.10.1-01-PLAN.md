---
phase: 03.10.1
plan: 01
type: auto
autonomous: true
wave: 2
depends_on: [03.10.0]
requirements: [MAT-02, MAT-03, COMPAT-01, COMPAT-02, COMPAT-03, TRACE-01]
---

# Phase 03.10.1 Plan — MAT memory and skill-manifest alignment

## Objective

Measure and reconcile the existing L2 memory MCP tool catalog and skill manifest skeleton against the canonical v2.0 specification, limiting code changes to small compatibility corrections and deferring structural features.

## Tasks

### Task 1: verify the seven memory MCP tools

Compare current names, request schemas, response shapes, pagination, status transitions, and time-decay behavior to the spec. Add focused tests that lock the implemented seven-tool surface. Apply only small, backward-compatible corrections.

### Task 2: verify skill manifest fields and MCP declarations

Compare skill registry frontmatter with required v2.0 fields, including name/version, entrypoints/workflow, required tools, MCP server dependencies, permissions/access scope, hooks, and risk metadata. Document aliases and missing structural fields; do not migrate schema.

### Task 3: compatibility and traceability gate

Verify proto hashes/surface are unchanged, shared core is untouched, L1 substitutability checks remain green, and update the matrix/evidence. Register every unresolved structural gap in `DEFERRED_LEDGER.md`.

## Verification

- Focused L2 memory and skill-registry tests pass.
- `cargo test -p eaasp-skill-registry -- --test-threads=1` passes.
- Relevant L2 Python tests pass.
- `make v2-phase3-e2e-rust` passes.
- `cargo check --workspace` passes.

## Success criteria

- Seven tool shapes and manifest fields have honest aligned/drift/missing status.
- No schema migration or dependency addition occurs.
- MAT-02, MAT-03, COMPAT-01..03, TRACE-01 have evidence.
