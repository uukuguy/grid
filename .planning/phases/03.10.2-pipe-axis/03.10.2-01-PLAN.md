---
phase: 03.10.2
plan: 01
type: auto
autonomous: true
wave: 3
depends_on: [03.10.1]
requirements: [PIPE-02, PIPE-03, PIPE-04, COMPAT-01, COMPAT-02, COMPAT-03, TRACE-01]
---

# Phase 03.10.2 Plan — PIPE orchestration boundary alignment

## Objective

Measure the three platform pipelines against the real L4/L3/L2/L1 data flow, preserve the working gRPC and governance boundaries, and add focused regression guards rather than implementing deferred OPA, A2A, or Cowork capabilities.

## Tasks

### Task 1: trace and test orchestration stages

Map input/context/governance/dispatch/result/audit/output stages to concrete symbols. Verify L4 calls L1 over real gRPC and that L2 MCP definitions are resolved by L4 then sent through ConnectMCP, with no MCP config supplied through process environment.

### Task 2: reconcile SSE, governance, and lifecycle semantics

Compare event payloads and session lifecycle with spec. Preserve v3.7.3 risk defaults and append-only governance events. Preserve double-Terminate no-op contract; document any L4 API-level lifecycle mismatch instead of changing architecture.

### Task 3: record platform evolution gaps

Document production OPA and five-stage approval-chain gaps, A2A/Event Room, Cowork UI, and integration/marketplace gaps. Update DEFERRED_LEDGER and trace evidence, then run focused L3/L4 and compatibility gates.

## Verification

- Focused L4 gRPC/MCP/session/SSE tests pass.
- Focused L3 governance tests pass.
- `make v2-phase3-e2e-rust` passes.
- `cargo check --workspace` and `make rbac-audit` pass in that order before tests.

## Success criteria

- Platform boundary checks are backed by executable tests.
- OPA/five-stage approval and future phases remain explicit gaps.
- PIPE-02..04, COMPAT-01..03, TRACE-01 have evidence.
