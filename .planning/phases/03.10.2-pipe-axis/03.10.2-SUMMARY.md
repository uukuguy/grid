---
phase: 03.10.2
plan: 01
subsystem: eaasp-orchestration
tags: [grpc, mcp, governance, sse, session-lifecycle]
requires: [03.10.1]
provides: [payload-driven-mcp-guard, pipe-boundary-evidence]
affects: [03.10.3]
key-files:
  modified:
    - tools/eaasp-l4-orchestration/tests/test_session_orchestrator.py
requirements-completed: [PIPE-02, PIPE-03, PIPE-04, COMPAT-01, COMPAT-02, COMPAT-03]
completed: 2026-07-26
---

# Phase 03.10.2 Summary — PIPE platform boundary alignment

Executable tests now prove L4 uses real gRPC lifecycle calls and payload-driven L2 MCP resolution while the existing risk gate, append-only governance events, and deliberate OPA/five-stage gaps remain unchanged.

## Commits

- `dc19494` — prove L2 → L4 → ConnectMCP payload flow ignores discovery env

## Verification

- L4 MCP subset — 5/5 PASS
- L4 gRPC/session/SSE-governance subset — 32/32 PASS
- L3 gate/audit/policy/session validation subset — 49/49 PASS
- v3.9 `make rbac-audit` — PASS, 134 routes
- Rust compatibility gate — 22/22 PASS (3 aggregate + 15 compaction + 4 retry)

One broader L4 run surfaced a **pre-existing** stale assertion expecting policy version `"3"` although D39 production behavior hashes policy content. It is unrelated to v3.10 changes and was not modified.

## Decisions Made

- Treat MCP server `env` maps as explicit payload fields, not discovery env; reject legacy `EAASP_TOOL_FILTER` / `EAASP_MCP_SERVERS` influence in the regression test.
- Preserve L1 double-Terminate NO-OP while documenting L4 second-close `InvalidStateTransition` as a separate API-level drift.
- Keep the current risk-aware request/final-decision gate intact; do not relabel it as the five-stage approval chain.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Synced the existing locked L3/L4 dev environments**
- **Issue:** Generated grpcio code requires 1.80 while the ambient Python environment had 1.67.
- **Fix:** Used each tool's committed `uv.lock` and isolated `.venv`; no dependency file changed.
- **Verification:** 86 focused Python tests PASS.

## Known Stubs

None introduced. OPA, deterministic verifier, A2A, Event Room, and crash recovery are registered platform gaps.

## Threat Flags

| Flag | File | Description |
|---|---|---|
| threat_flag: configuration-boundary-test | tools/eaasp-l4-orchestration/tests/test_session_orchestrator.py | Test verifies untrusted ambient discovery env cannot replace skill-declared MCP dependencies. |

## Self-Check: PASSED

Modified test file exists and commit `dc19494` is present in history.
