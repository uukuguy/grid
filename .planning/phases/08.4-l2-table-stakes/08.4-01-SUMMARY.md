---
phase: 08.4-l2-table-stakes
plan: 01
type: execute
wave: 1
completed_date: 2026-06-10T16:27:38Z
duration: ~10 min
tasks_completed: 3
tasks_total: 3
files_modified: 6
files_created: 0
tests_total: 134
tests_passed: 134
tests_failed: 0
requirements:
  - L2-09
  - L2-12
  - L2-13
  - L2-14
  - L2-15
  - L2-16
key_decisions:
  - "_row_to_memory promoted to public API row_to_memory with deprecation shim"
  - "HNSW key parsing fixed to rsplit(':v', 1) for versioned IDs with colons"
  - "MCP numeric args now raise ToolError('invalid_arg') instead of ValueError/TypeError"
  - "HTTPException handler returns flat JSON {code, message} per L2 contract"
  - "MockEmbedding seed uses full 32-byte SHA-256 digest for test determinism"
subsystem: l2-memory-engine
tags: [l2, correctness, d-items, input-validation, error-handling]
---

# Phase 08.4 Plan 01: L2 Correctness Floor — D-Item Fixes Summary

**One-liner:** Six mechanical P3 correctness fixes (D14, D96, D97, D99, D101, D92) across 5 L2 source files, closing the L2 correctness floor with 134/134 tests passing.

## 1. Executed Tasks

### Task 1: Promote `row_to_memory` + fix HNSW key parsing + zero-weight warning (D14, D96, D97)

**Commit:** `20d3f44` — `feat(08.4-01): promote row_to_memory to public API + fix HNSW key parsing + zero-weight warning (D14, D96, D97)`

| D-Item | What | Files |
|--------|------|-------|
| D14 | Renamed `_row_to_memory → row_to_memory()` (public API). Added deprecation shim `_row_to_memory` with `DeprecationWarning`. Updated all callers in `index.py`. | `files.py`, `index.py` |
| D96 | Changed `hit.id.split(":v")` → `hit.id.rsplit(":v", 1)` for HNSW key parsing. Fixes versioned IDs like `doc:v1.2:v42` where version is the last segment. | `index.py` L340 |
| D97 | Added `logger.warning()` when `HybridIndex` initialized with `weights=(0,0)`. | `index.py` L155-160 |

### Task 2: MCP dispatcher int()/float() try/except + HTTPException response shape fix (D99, D101)

**Commit:** `635617b` — `feat(08.4-01): wrap MCP int()/float() conversions + flatten HTTPException response (D99, D101)`

| D-Item | What | Files |
|--------|------|-------|
| D99 | Wrapped `int()` conversions in `_memory_search` (top_k) and `_memory_list` (limit, offset) in try/except `(ValueError, TypeError)` → raises `ToolError("invalid_arg")` instead of native exception leakage. | `mcp_tools.py` |
| D101 | Added custom `@app.exception_handler(HTTPException)` in `create_app()` that returns flat `{"code": ..., "message": ...}` JSON without FastAPI's default `{"detail": ...}` wrapper. | `api.py` |

### Task 3: Widen MockEmbedding seed from 8 bytes to full 32-byte SHA-256 digest (D92)

**Commit:** `a0bd006` — `feat(08.4-01): widen MockEmbedding seed from 8 bytes to full 32-byte SHA-256 digest (D92)`

| D-Item | What | Files |
|--------|------|-------|
| D92 | Removed `[:8]` slice from `int.from_bytes(digest, "little", signed=False)` to use full 32-byte SHA-256 digest as random seed. Eliminates 2^64 seed collision risk for 1024-dimensional vectors in test suites. | `provider.py` |

## 2. Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] D101 flattened response broke two test assertions**

- **Found during:** Full test suite run after Task 2
- **Issue:** `test_s2t4_state_machine.py::test_api_returns_409_on_invalid_transition` and `::test_api_returns_404_on_missing_memory_id_confirm` accessed `resp.json()["detail"]["code"]` expecting old FastAPI nested format
- **Fix:** Updated both assertions to `resp.json()["code"]` (flat format). Updated docstring to reference D101 custom handler.
- **Files modified:** `tests/test_s2t4_state_machine.py`
- **Commit:** `90bace6` — `fix(08.4-01): update test assertions for flat HTTPException response format (D101)`

## 3. Verification Results

**Full L2 test suite:** 134 passed, 0 failed, 4 pre-existing warnings (test_mcp_server.py pytestmark-asyncio)

```
cd tools/eaasp-l2-memory-engine && .venv/bin/python -m pytest tests/ -x -q
134 passed in 2.57s
```

## 4. Key Files Modified

| File | Changes | Lines Changed |
|------|---------|---------------|
| `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/files.py` | D14: public `row_to_memory()` + deprecation shim | +15 |
| `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/index.py` | D14/D96/D97: import update, rsplit, zero-weight warning | +12 |
| `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/mcp_tools.py` | D99: try/except wrapping for int() conversions | +12 |
| `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/api.py` | D101: custom HTTPException handler + imports | +16 |
| `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/embedding/provider.py` | D92: full 32-byte seed | 1 line |
| `tools/eaasp-l2-memory-engine/tests/test_s2t4_state_machine.py` | Test update for flat response format | 2 assertions |

## 5. Commits

| # | Hash | Message |
|---|------|---------|
| 1 | `20d3f44` | feat(08.4-01): promote row_to_memory to public API + fix HNSW key parsing + zero-weight warning (D14, D96, D97) |
| 2 | `635617b` | feat(08.4-01): wrap MCP int()/float() conversions + flatten HTTPException response (D99, D101) |
| 3 | `a0bd006` | feat(08.4-01): widen MockEmbedding seed from 8 bytes to full 32-byte SHA-256 digest (D92) |
| 4 | `90bace6` | fix(08.4-01): update test assertions for flat HTTPException response format (D101) |

## 6. Threat Mitigations Verified

| Threat | Mitigation | Status |
|--------|-----------|--------|
| T-08.4-01 (Tampering - numeric arg leakage) | D99: try/except → ToolError for bad int()/float() args | ✅ |
| T-08.4-02 (Info Disclosure - schema leakage) | D101: flat JSON response, no `{"detail": ...}` wrapper | ✅ |
| T-08.4-03 (DoS - malformed IDs) | D96: rsplit(":v", 1) handles versioned IDs with colons | ✅ |
| T-08.4-04 (Repudiation - zero-weight misconfig) | D97: logger.warning creates audit trail | ✅ |
