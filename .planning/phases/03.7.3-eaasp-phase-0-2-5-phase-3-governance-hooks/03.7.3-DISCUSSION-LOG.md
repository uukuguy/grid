---
type: discussion-log
phase: 3.7.3
date: 2026-07-23
mode: auto (climb 持续自主推进)
---

# Phase 3.7.3 — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `03.7.3-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-07-23
**Phase:** 3.7.3 — EAASP 本地仿真补全 (Phase 0–2.5 + Phase 3 governance hooks)
**Gathered by:** Claude (claude-opus-4-8) via Claude Code CLI
**Mode:** `auto` (per user authorization "climb 持续自主推进 3.7.3" 2026-07-23; selected recommended option for all gray areas)

**Areas discussed:** 6 gray areas (auto-selected from analyze_phase step)

---

## Area 1: Risk classification taxonomy

| Option | Description | Selected |
|--------|-------------|----------|
| Tool-level risk metadata in skill manifest | Three levels (read / write_local / write_external); declared per skill; default `read`; L3 policy can override | ✓ |
| Regex / path-based classification | Match tool call args against allow/deny patterns at runtime | |
| Scope-based (resource + action) | Per-resource RBAC matrix | |

**Auto-selected:** Tool-level risk metadata in skill manifest.
**Rationale:** Reuses already-SHIPPED `tools/eaasp-skill-registry` from Phase 2; simplest credible hook; deterministic and reviewable; L3 policy can still override per-hook for fine control.

---

## Area 2: Hook enforcement point

| Option | Description | Selected |
|--------|-------------|----------|
| L3 policy-engine intercept | Single chokepoint; L1 stays protocol-pure | ✓ |
| L4 orchestration event-driven | Distributed across L4 event consumers | |
| L1 runtime hook_envelope PreToolUse | Per-L1 adapter; breaks L1 substitutability contract | |

**Auto-selected:** L3 policy-engine intercept.
**Rationale:** Single chokepoint; aligns with `docs/design/EAASP/PHASE_3_DESIGN.md` design intent; preserves L1 contract substitutability (ADR-V2-006 + `contract-v1.2.0`); L4 SSE forwarding is a side effect, not the gate.

---

## Area 3: Approval UX surface

| Option | Description | Selected |
|--------|-------------|----------|
| CLI synchronous prompt (primary) | `eaasp session run` blocks on user input | ✓ |
| Async event stream (SSE only) | External consumer must subscribe | |
| Both CLI + SSE | Same logic, two surfaces | |

**Auto-selected:** CLI synchronous prompt (primary) + L4 SSE emission (D-05 parallel).
**Rationale:** CLI is primary entrypoint per 3.7-CONTEXT.md §实战可用 acceptance standard #3; SSE emission makes the gate observable for future Phase 5 L5 UI without requiring consumers in 3.7.3.

---

## Area 4: Audit & approval record

| Option | Description | Selected |
|--------|-------------|----------|
| Extend L3 audit.py with `governance_decisions` table | Captures who/when/decision/rationale | ✓ |
| Just track outcome (allow/deny) | No rationale or approver | |
| No audit; rely on SSE event log only | Lose offline record | |

**Auto-selected:** Extend L3 audit.py.
**Rationale:** Per Phase 3 design principle ("审批链 + Verifier + OPA"); cheap to add; provides offline auditability required for enterprise evaluation.

---

## Area 5: Demo scenario coverage

| Option | Description | Selected |
|--------|-------------|----------|
| 1 end-to-end scenario | Matches ROADMAP.md §3.7.3 validate line exactly | ✓ |
| 2-3 scenarios | Cover more risk levels | |

**Auto-selected:** 1 scenario (S8 — Risk-classified external write via mock-scada).
**Rationale:** ROADMAP.md §3.7.3 Validate is explicit: "1 实战 enterprise scenario (e.g. agent writes to external system, governance gate triggers, user approves, action completes)". Adding more scenarios = scope creep.

---

## Area 6: Backward compat with Phase 0-2.5

| Option | Description | Selected |
|--------|-------------|----------|
| Default mode preserved (shadow); opt-in via skill risk_level | Zero regression on existing tests | ✓ |
| New default mode added (gate); existing behavior changes | Breaks existing tests | |
| Parallel enforcement (shadow + gate) | Complex, debugging nightmare | |

**Auto-selected:** Default preserved + opt-in via skill risk_level.
**Rationale:** All 12 L3 tests + 14 EAASP Python runtime tests + 5 cli-v2 tests must pass unchanged (per Phase 2.5 SHIPPED + Phase 3 sign-off 2026-04-18 baseline); new gate is purely additive.

---

## the agent's Discretion

- Internal naming of L3 audit table columns / sqlite indexes
- Exact CLI prompt wording (must follow `Approve? [y/N]` convention)
- How L4 SSE consumers subscribe to `governance.request` events (interface only)
- Test fixture data values

## Deferred Ideas

None — discussion stayed within Phase 3.7.3 scope. Out-of-scope items (full OPA backend, multi-party approval, sandbox tier wiring, L5 UI consumers, HTTP approval endpoints) documented in CONTEXT.md §"Out-of-scope (explicit, deferred to v3.8+)".