---
phase: 03.7.3-eaasp-phase-0-2-5-phase-3-governance-hooks
plan: 02
type: execute
wave: 2
depends_on:
  - 3.7.3-01
files_modified:
  - tools/eaasp-l3-governance/src/eaasp_l3_governance/managed_settings.py
  - tools/eaasp-l3-governance/src/eaasp_l3_governance/db.py
  - tools/eaasp-l3-governance/src/eaasp_l3_governance/audit.py
  - tools/eaasp-l3-governance/src/eaasp_l3_governance/policy_engine.py
  - tools/eaasp-l3-governance/tests/test_gate_engine.py
  - tools/eaasp-l3-governance/tests/test_audit_governance.py
  - tools/eaasp-skill-registry/src/models.rs
  - tools/eaasp-skill-registry/src/skill_parser.rs
  - tools/eaasp-l4-orchestration/src/eaasp_l4_orchestration/event_stream.py
  - tools/eaasp-l4-orchestration/tests/test_sse_governance_events.py
  - tools/eaasp-cli-v2/src/eaasp_cli_v2/cmd_session.py
  - tools/eaasp-cli-v2/tests/test_cli_approval.py
  - tools/mock-scada/src/mock_scada/server.py
  - tools/mock-scada/src/mock_scada/snapshots.py
  - tools/mock-scada/tests/test_server.py
  - examples/skills/scada-set-setpoint/SKILL.md
  - docs/cli/scenarios/S8-risk-classified-external-write.md
  - docs/status/PRODUCTION_USABILITY_<actual-date>.md
autonomous: true
requirements:
  - REQ-EAASP-01
  - REQ-EAASP-02
  - REQ-EAASP-03
  - REQ-EAASP-04
  - REQ-EAASP-05
  - REQ-EAASP-06
  - REQ-EAASP-07
  - REQ-EAASP-08
user_setup: []

must_haves:
  truths:
    - "Existing skills and hooks without risk metadata remain `read` and preserve current behavior; the existing shadow/enforce mode contract is unchanged."
    - "A `read` action is automatically allowed, while an enforced `write_local` or `write_external` action returns a gate request before the tool side effect executes."
    - "Shadow-mode write actions are allowed but still produce an append-only governance audit row."
    - "For a gate request, `eaasp session run --yes` approves, `--no` denies, and no flag prompts `Approve? [y/N]` with No as the default; a denial never invokes the external tool."
    - "L4 records `governance.request` and `governance.decision` through the existing session event stream; event write failure is best-effort and cannot change the gate decision."
    - "S8 updates the deterministic mock-SCADA setpoint only after approval, records request/final decisions, and exposes governance behavior to a non-developer observer."
    - "All new tests are hermetic, all existing targeted L3/CLI/mock-SCADA tests remain green, and no shared core, grid-server, proto, full OPA, sandbox-tier, multi-party, or L5 code is changed."
  artifacts:
    - path: "tools/eaasp-l3-governance/src/eaasp_l3_governance/policy_engine.py"
      provides: "Risk-aware PolicyEngine.evaluate_gate and GateDecision contract"
      contains: "evaluate_gate"
    - path: "tools/eaasp-l3-governance/src/eaasp_l3_governance/db.py"
      provides: "Append-only governance_decisions schema"
      contains: "CREATE TABLE IF NOT EXISTS governance_decisions"
    - path: "tools/eaasp-l3-governance/src/eaasp_l3_governance/audit.py"
      provides: "Transactional record_governance_decision persistence"
      contains: "record_governance_decision"
    - path: "tools/eaasp-skill-registry/src/skill_parser.rs"
      provides: "Backward-compatible skill-manifest RiskLevel metadata"
      contains: "RiskLevel"
    - path: "tools/eaasp-cli-v2/src/eaasp_cli_v2/cmd_session.py"
      provides: "Primary synchronous approval UX"
      contains: "--yes"
    - path: "tools/eaasp-l4-orchestration/src/eaasp_l4_orchestration/event_stream.py"
      provides: "Governance request/decision event helpers"
      contains: "governance.request"
    - path: "tools/mock-scada/src/mock_scada/server.py"
      provides: "Deterministic scada_set_setpoint external write"
      contains: "scada_set_setpoint"
    - path: "examples/skills/scada-set-setpoint/SKILL.md"
      provides: "Deployable S8 skill manifest declaring write_external"
      contains: "risk_level: write_external"
    - path: "docs/cli/scenarios/S8-risk-classified-external-write.md"
      provides: "Non-developer S8 walkthrough"
      min_lines: 170
      contains: "Approve? [y/N]"
    - path: "docs/status/PRODUCTION_USABILITY_<actual-date>.md"
      provides: "Dated S8 transcript and automated/manual evidence"
      contains: "S8"
  key_links:
    - from: "tools/eaasp-skill-registry/src/skill_parser.rs"
      to: "tools/eaasp-l3-governance/src/eaasp_l3_governance/policy_engine.py"
      via: "risk_level value carried from skill metadata into evaluate_gate"
      pattern: "risk_level"
    - from: "tools/eaasp-l3-governance/src/eaasp_l3_governance/policy_engine.py"
      to: "tools/eaasp-l3-governance/src/eaasp_l3_governance/audit.py"
      via: "every allow or gate_request persists a governance decision row"
      pattern: "record_governance_decision"
    - from: "tools/eaasp-cli-v2/src/eaasp_cli_v2/cmd_session.py"
      to: "tools/eaasp-l3-governance/src/eaasp_l3_governance/policy_engine.py"
      via: "gate request resolved synchronously before external tool dispatch"
      pattern: "gate_request|Approve\\?"
    - from: "tools/eaasp-cli-v2/src/eaasp_cli_v2/cmd_session.py"
      to: "tools/eaasp-l4-orchestration/src/eaasp_l4_orchestration/event_stream.py"
      via: "governance request and final decision appended to session events"
      pattern: "governance\\.(request|decision)"
    - from: "examples/skills/scada-set-setpoint/SKILL.md"
      to: "tools/mock-scada/src/mock_scada/server.py"
      via: "required tool scada_set_setpoint with write_external risk"
      pattern: "scada_set_setpoint"
---

<objective>
Implement the minimum credible Phase 3 governance gate across L2 skill metadata, L3 policy/audit, L4 events, the primary EAASP CLI approval UX, and one deterministic mock-SCADA S8 scenario.

Purpose: make a risk-classified external write visibly pause before its side effect, accept a human/flag decision, persist auditable evidence, emit observable governance events, and complete or deny the action without changing L1 or implementing the deferred production OPA chain.
Output: production gate wiring, hermetic targeted tests, deployable S8 skill, 170+ line walkthrough, dated transcript/evidence, and clean Cargo workspace check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.7.3-eaasp-phase-0-2-5-phase-3-governance-hooks/03.7.3-CONTEXT.md
@.planning/phases/03.7.3-eaasp-phase-0-2-5-phase-3-governance-hooks/03.7.3-01-SUMMARY.md
@docs/audit/3.7.3-GAP-AUDIT.md
@docs/design/EAASP/EAASP_v2_0_EVOLUTION_PATH.md
@docs/design/EAASP/PHASE_3_DESIGN.md
@docs/design/EAASP/adrs/ADR-V2-006-hook-envelope-contract.md
@tools/eaasp-l3-governance/src/eaasp_l3_governance/policy_engine.py
@tools/eaasp-l3-governance/src/eaasp_l3_governance/managed_settings.py
@tools/eaasp-l3-governance/src/eaasp_l3_governance/audit.py
@tools/eaasp-l3-governance/src/eaasp_l3_governance/db.py
@tools/eaasp-skill-registry/src/models.rs
@tools/eaasp-skill-registry/src/skill_parser.rs
@tools/eaasp-l4-orchestration/src/eaasp_l4_orchestration/event_stream.py
@tools/eaasp-cli-v2/src/eaasp_cli_v2/cmd_session.py
@tools/mock-scada/src/mock_scada/server.py
@docs/status/PRODUCTION_USABILITY_2026-07-19.md
@docs/status/WEB_PRODUCTION_USABILITY_2026-07-20.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add risk contracts, append-only audit persistence, and PolicyEngine gate logic</name>
  <read_first>
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/audit/3.7.3-GAP-AUDIT.md (REQ-EAASP-01..03 and exact API/DDL decision table from Plan 01)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.3-eaasp-phase-0-2-5-phase-3-governance-hooks/03.7.3-CONTEXT.md (D-01, D-02, D-03, D-07, D-08)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/design/EAASP/EAASP_v2_0_EVOLUTION_PATH.md (P4/P5/P7 and Phase 3 row)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/design/EAASP/adrs/ADR-V2-006-hook-envelope-contract.md (existing hook contract; run `/adr:trace` before editing if governance marks any target as affected)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l3-governance/src/eaasp_l3_governance/managed_settings.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l3-governance/src/eaasp_l3_governance/db.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l3-governance/src/eaasp_l3_governance/audit.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l3-governance/src/eaasp_l3_governance/policy_engine.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l3-governance/tests/conftest.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l3-governance/tests/test_policy_engine.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l3-governance/tests/test_audit.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-skill-registry/src/models.rs
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-skill-registry/src/skill_parser.rs
  </read_first>
  <files>
    - tools/eaasp-l3-governance/src/eaasp_l3_governance/managed_settings.py
    - tools/eaasp-l3-governance/src/eaasp_l3_governance/db.py
    - tools/eaasp-l3-governance/src/eaasp_l3_governance/audit.py
    - tools/eaasp-l3-governance/src/eaasp_l3_governance/policy_engine.py
    - tools/eaasp-l3-governance/tests/test_gate_engine.py
    - tools/eaasp-l3-governance/tests/test_audit_governance.py
    - tools/eaasp-skill-registry/src/models.rs
    - tools/eaasp-skill-registry/src/skill_parser.rs
  </files>
  <behavior>
    - "Legacy ManagedHook and V2Frontmatter payloads without risk_level deserialize to read."
    - "Risk parser accepts exactly read, write_local, write_external and rejects unknown values before persistence."
    - "evaluate_gate returns allow for read in enforce and shadow modes."
    - "evaluate_gate returns allow with rationale shadow mode for write_local/write_external when the effective hook mode is shadow."
    - "evaluate_gate returns gate_request for write_local/write_external when the effective hook mode is enforce."
    - "All allow and gate_request decisions are persisted, and transaction failure rolls back without a partial row."
    - "The eight primary gate cases cover read, write_local, write_external, and an invalid fourth input across effective enforce/shadow modes; edge cases cover empty IDs/action preview, unknown hook behavior, and mode override precedence."
  </behavior>
  <action>
    Follow RED→GREEN→REFACTOR. First create `test_gate_engine.py` and `test_audit_governance.py`, run only these files, and confirm failures refer to missing contracts. Then implement:

    1. In `managed_settings.py`, define `RiskLevel = Literal["read", "write_local", "write_external"]`, `_VALID_RISK_LEVELS`, add `risk_level: RiskLevel = "read"` to `ManagedHook`, and add `ensure_risk_level(risk_level: str) -> RiskLevel`. Do not change `mode: HookMode = "enforce"` or `ensure_mode` semantics (D-07).

    2. In Rust `skill_parser.rs`, add `#[derive(Debug, Clone, Default, PartialEq, Eq, Serialize, Deserialize)] #[serde(rename_all = "snake_case")] pub enum RiskLevel { #[default] Read, WriteLocal, WriteExternal }` and `#[serde(default)] pub risk_level: RiskLevel` on `V2Frontmatter`. If `models.rs` exposes skill metadata directly to the registry API, add the same serialized field there only where required by the Plan 01 audit; do not duplicate competing sources of truth. Add inline Rust tests proving missing→Read and all three serialized values. This implements D-01 despite the source artifact being Rust rather than Pydantic.

    3. In `db.py`, add the exact table and index from Plan 01:
    `governance_decisions(decision_id TEXT PRIMARY KEY, session_id TEXT NOT NULL, hook_id TEXT NOT NULL, tool_name TEXT NOT NULL, risk_level TEXT NOT NULL CHECK(...), decision TEXT NOT NULL CHECK(...), approver TEXT, rationale TEXT NOT NULL, ts TEXT NOT NULL DEFAULT (datetime('now')))` with decision values `allow|approve|deny|gate_request` and risk values `read|write_local|write_external`; index `(session_id, ts DESC)`.

    4. In `audit.py`, add typed `GovernanceDecisionOut` and exact public method `async def record_governance_decision(self, decision_id: str, session_id: str, hook_id: str, tool_name: str, risk_level: str, decision: str, approver: str | None, rationale: str) -> GovernanceDecisionOut`. Validate non-empty identifiers/tool/rationale and enum values at entry. Use `BEGIN IMMEDIATE`, parameterized `INSERT`, `SELECT` persisted row, commit, rollback on every exception, and close connection. Never update or replace existing rows.

    5. In `policy_engine.py`, add `GateDecisionValue = Literal["allow", "approve", "deny", "gate_request"]`, `GateDecision(BaseModel)` with exactly `decision_id`, `decision`, `rationale`, and exact method `async def evaluate_gate(self, session_id: str, hook_id: str, tool_name: str, risk_level: str, action_preview: str) -> GateDecision`. Resolve the hook from latest managed settings, apply `get_mode_override(hook_id)` above its declared mode, then use this table: `read→allow`; write risk + shadow→allow/rationale `shadow mode`; write risk + enforce→gate_request/rationale `approval required`. Generate `gd_<uuid hex>` IDs and persist every result via `AuditStore.record_governance_decision(..., approver=None, ...)`. Unknown hook raises `HookNotFoundError`; unknown risk/empty inputs raise `ValueError` before any DB write. Do not add REST/MCP endpoints (D-06).
  </action>
  <verify>
    <automated>cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l3-governance && uv run pytest tests/test_gate_engine.py tests/test_audit_governance.py -x --tb=short && cargo test --manifest-path /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-skill-registry/Cargo.toml skill_parser -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `ManagedHook.model_validate({hook_id, phase}).risk_level == "read"`; current `mode` default remains `enforce`.
    - Rust tests prove absent risk metadata defaults to `RiskLevel::Read` and snake_case round trips all three values.
    - `grep -A12 'CREATE TABLE IF NOT EXISTS governance_decisions' db.py` shows all nine columns and both CHECK constraints.
    - Gate tests assert all required risk/mode cases and edge cases, including zero audit rows after invalid input.
    - Audit rollback test injects a DB failure and proves no partial row remains.
    - No files under `api.py`, `mcp_server.py`, `proto/`, `crates/grid-*`, or `lang/` are changed.
  </acceptance_criteria>
  <done>REQ-EAASP-01..03 are implemented with backward-compatible risk metadata, append-only decisions, and tested L3 gate logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire governance events and synchronous CLI approval resolution</name>
  <read_first>
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/audit/3.7.3-GAP-AUDIT.md (REQ-EAASP-04/05 exact event and CLI contracts)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.3-eaasp-phase-0-2-5-phase-3-governance-hooks/03.7.3-CONTEXT.md (D-02, D-04, D-05, D-06, D-07, D-08)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l3-governance/src/eaasp_l3_governance/policy_engine.py (Task 1 GateDecision/evaluate_gate contract)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l3-governance/src/eaasp_l3_governance/audit.py (Task 1 append-only final decision API)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l4-orchestration/src/eaasp_l4_orchestration/event_stream.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l4-orchestration/src/eaasp_l4_orchestration/api.py (existing SSE consumers of session_events; no new endpoint)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l4-orchestration/tests/ (existing SessionEventStream fixtures and schema setup)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-cli-v2/src/eaasp_cli_v2/cmd_session.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-cli-v2/src/eaasp_cli_v2/client.py (existing `call` and `stream_sse` contracts)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-cli-v2/tests/conftest.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-cli-v2/tests/test_cmd_session.py
  </read_first>
  <files>
    - tools/eaasp-l4-orchestration/src/eaasp_l4_orchestration/event_stream.py
    - tools/eaasp-l4-orchestration/tests/test_sse_governance_events.py
    - tools/eaasp-cli-v2/src/eaasp_cli_v2/cmd_session.py
    - tools/eaasp-cli-v2/tests/test_cli_approval.py
  </files>
  <behavior>
    - "governance.request persists exactly decision_id, hook_id, tool_name, risk_level, action_preview under the existing event envelope."
    - "governance.decision persists exactly decision_id, decision, approver."
    - "A failing event append is logged and swallowed by the best-effort helper, while gate/CLI decision processing continues."
    - "--yes and --no are mutually exclusive and simultaneous use exits 2 before session creation."
    - "--yes resolves a gate_request to approve without prompting; --no resolves it to deny without prompting; neither flag prompts `Approve? [y/N]` and empty/No input denies."
    - "Denied actions return exit code 4 and no external tool dispatch is performed; approved actions continue once."
  </behavior>
  <action>
    Write `test_sse_governance_events.py` and `test_cli_approval.py` first and confirm RED. Then:

    1. In `event_stream.py`, preserve `append()` and add exact typed convenience methods `async def emit_governance_request(self, session_id: str, decision_id: str, hook_id: str, tool_name: str, risk_level: str, action_preview: str) -> int | None` and `async def emit_governance_decision(self, session_id: str, decision_id: str, decision: str, approver: str) -> int | None`. They call `append()` with exact strings `governance.request` and `governance.decision` and exact payload keys from D-05. Validate non-empty inputs and allowed values. Catch only persistence/runtime exceptions around `append`, log with Loguru, and return `None`; never convert a gate denial to approval or vice versa. Existing SSE endpoints already fan out `session_events`; do not add endpoints or a second broker.

    2. In `cmd_session.py`, extend exact command signature to `def run(message: str = typer.Argument(...), skill: str = typer.Option(..., "--skill", "-s"), runtime: str = typer.Option("grid-runtime", "--runtime", "-r"), no_stream: bool = typer.Option(False, "--no-stream"), yes: bool = typer.Option(False, "--yes", help="Approve governance gates non-interactively"), no: bool = typer.Option(False, "--no", help="Deny governance gates non-interactively")) -> None`. Reject `yes and no` with `typer.BadParameter("--yes and --no are mutually exclusive")`, producing exit code 2.

    3. Add small typed helpers rather than enlarging `run()` beyond 50 lines: `def _resolve_gate_request(request: dict[str, Any], *, yes: bool, no: bool) -> tuple[str, str]` and async orchestration helper(s). Render the exact block: `[governance] Risk-classified action requested:`, `tool:`, `risk:`, `effect:`, then `typer.confirm("Approve?", default=False)`. Map approval to `("approve", "cli:--yes"|"cli:interactive")` and denial to `("deny", "cli:--no"|"cli:interactive")`.

    4. Integrate with the actual gate signal identified by Plan 01 rather than inventing a new HTTP endpoint. When the stream/event payload contains `event == "governance.request"` (or the existing session-event envelope contains `event_type == "governance.request"`), pause before acknowledging/dispatching the external action, resolve the decision, record a new append-only final row ID `${request_decision_id}_final` with rationale `resolved request <request_decision_id>`, and emit `governance.decision`. If approved, continue the pending tool exactly once; if denied, do not call it and exit 4. If the current L4 stream cannot carry the pending action callback, keep the approval resolver as the CLI client callback used by S8's in-process harness; do not weaken the requirement by prompting after side effect. This is D-02's critical invariant.

    5. Preserve existing chunk rendering and all five existing cli-v2 session tests. Do not add a L3 REST endpoint, modify L1/proto, or make SSE delivery authoritative (D-04 primary CLI; D-05 parallel best-effort).
  </action>
  <verify>
    <automated>cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l4-orchestration && uv run pytest tests/test_sse_governance_events.py -x --tb=short && cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-cli-v2 && uv run pytest tests/test_cli_approval.py tests/test_cmd_session.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Event tests read back both exact event types from the real temp SQLite `session_events` table and assert all required payload keys.
    - Best-effort test forces `append()` to fail and proves the helper returns `None` without raising.
    - CLI help lists both `--yes` and `--no`; simultaneous flags exit 2.
    - CLI tests cover auto-approve, auto-deny, interactive yes, interactive default/no, and prove the tool callback count is 1 only for approval and 0 for denial.
    - Denial exit code is exactly 4 and the prompt includes `Approve? [y/N]`.
    - No new FastAPI route, MCP tool, L1 RPC, or proto field is introduced.
  </acceptance_criteria>
  <done>REQ-EAASP-04/05 are closed: approval is synchronous and pre-side-effect, while request/decision events remain observable and best-effort.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship deterministic S8 external-write fixture, walkthrough evidence, and targeted phase gates</name>
  <read_first>
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/audit/3.7.3-GAP-AUDIT.md (REQ-EAASP-06..08 and S8 contract)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.3-eaasp-phase-0-2-5-phase-3-governance-hooks/03.7.3-CONTEXT.md (D-08, D-09, D-10 and one-scenario limit)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/design/EAASP/EAASP_v2_0_EVOLUTION_PATH.md (P3 manual observable verification and Phase 3 approval visibility)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/mock-scada/src/mock_scada/server.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/mock-scada/src/mock_scada/snapshots.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/mock-scada/tests/test_server.py
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-skill-registry/src/skill_parser.rs (Task 1 risk_level manifest shape)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-cli-v2/src/eaasp_cli_v2/cmd_skill.py (actual `eaasp skill deploy/submit/promote` syntax)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/examples/skills/threshold-calibration/SKILL.md (existing deployable skill pattern)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/cli/scenarios/S7-web-dashboard.md (walkthrough structure)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/status/PRODUCTION_USABILITY_2026-07-19.md (dated evidence style and honest blocked transcript pattern)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/status/WEB_PRODUCTION_USABILITY_2026-07-20.md (append/update style)
  </read_first>
  <files>
    - tools/mock-scada/src/mock_scada/server.py
    - tools/mock-scada/src/mock_scada/snapshots.py
    - tools/mock-scada/tests/test_server.py
    - examples/skills/scada-set-setpoint/SKILL.md
    - docs/cli/scenarios/S8-risk-classified-external-write.md
    - docs/status/PRODUCTION_USABILITY_<actual-date>.md
  </files>
  <behavior>
    - "scada_set_setpoint requires non-empty device_id/setpoint_name and finite numeric value; invalid input leaves state unchanged."
    - "Before approval the deterministic setpoint remains at its original value; approval changes xfmr-042 temperature_limit_c from 65.0 to 70.0 exactly once; denial leaves 65.0 unchanged."
    - "The skill manifest deserializes with risk_level write_external and required tool scada_set_setpoint."
    - "S8 hermetic flow produces gate_request, governance.request, final approve/deny audit row, governance.decision, and approved external write in that order."
  </behavior>
  <action>
    Add S8 without creating another scenario.

    1. In `snapshots.py`, add an isolated deterministic in-memory setpoint store initialized with `{"xfmr-042": {"temperature_limit_c": 65.0}}`, plus typed `get_setpoint(device_id: str, setpoint_name: str) -> float` and `set_setpoint(device_id: str, setpoint_name: str, value: float) -> dict[str, Any]`. Validate non-empty strings, `isinstance(value, (int, float))`, and `math.isfinite(float(value))`; return `{device_id, setpoint_name, previous_value, value, status: "updated"}`. Provide a test-reset helper scoped/documented for hermetic fixtures without exposing a production network reset endpoint.

    2. In `server.py`, add MCP tool name exactly `scada_set_setpoint`, input schema requiring `device_id`, `setpoint_name`, `value`, description declaring it an external write that requires governance approval, and handler `_handle_scada_set_setpoint(args: dict[str, Any]) -> dict[str, Any]` delegating to `set_setpoint`. Keep legacy `scada_write` blocked and read behavior unchanged.

    3. Create deployable `examples/skills/scada-set-setpoint/SKILL.md` following the existing skill-registry frontmatter syntax. Required exact fields include `name: scada-set-setpoint`, `risk_level: write_external`, `workflow.required_tools: [l2:scada_set_setpoint]`, and a PreToolUse scoped hook targeting `scada_set_setpoint`. The instructions must tell the agent to propose device `xfmr-042`, setpoint `temperature_limit_c`, value `70.0`, wait for governance approval, and never claim success before the tool result.

    4. Extend `test_server.py` to verify valid update, invalid value/input non-mutation, legacy tool behavior, and the pre/post values. Add one hermetic cross-component S8 test in the most local existing test package (prefer L3 `test_gate_engine.py` or CLI `test_cli_approval.py`; do not create a separate top-level test harness) using common fixture helpers: deploy enforce hook, evaluate `write_external`, assert gate_request/audit, emit request, resolve approve, persist final row/emit decision, call `_handle_scada_set_setpoint`, and assert event/audit ordering and final 70.0. Add a denial case within the same S8 test module, not a second scenario.

    5. Write `docs/cli/scenarios/S8-risk-classified-external-write.md` at least 170 lines, non-developer-oriented, with prerequisites, exact `eaasp skill deploy` command confirmed from `cmd_skill.py`, exact `eaasp session run -s scada-set-setpoint -r grid-runtime "Update xfmr-042 temperature_limit_c to 70.0"`, expected governance block and `Approve? [y/N]`, `--yes` and `--no` automation paths, audit SQL/query evidence, L4 event evidence, failure indicators, and cleanup. Do not include real credentials or external IPs.

    6. Run `date +%Y-%m-%d` and create/update `docs/status/PRODUCTION_USABILITY_<actual-date>.md` with an S8 section. Record actual command outputs when available. If LLM credentials/services are absent, include a clearly labelled mock transcript backed by the hermetic S8 test, list exact missing prerequisites, and do not claim a live PASS. D-10 permits mock transcript when no API key; distinguish `HERMETIC PASS` from `LIVE BLOCKED`.

    7. Run targeted tests only: new/changed L3 files, L4 governance event file, CLI approval + existing CLI session tests, mock-SCADA tests, skill-registry parser tests, then the entire L3 package command explicitly required by the phase (`uv run pytest tests/ -x --tb=short`). Also run the existing EAASP Python runtime and cli-v2 regression groups identified by the audit only if they are package-targeted; do not run `cargo test --workspace` or `make test`. Run `cargo check --workspace` as the required non-test Rust gate.

    8. Stage only Phase 3.7.3 files, preserving the user's pre-existing `.planning/STATE.md` modification unstaged. Commit logical task units with required footers; the final closure commit subject must be `docs(3.7.3): S8 governance walkthrough and usability evidence`.
  </action>
  <verify>
    <automated>cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l3-governance && uv run pytest tests/ -x --tb=short && cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-l4-orchestration && uv run pytest tests/test_sse_governance_events.py -x --tb=short && cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/eaasp-cli-v2 && uv run pytest tests/test_cli_approval.py tests/test_cmd_session.py -x --tb=short && cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/tools/mock-scada && uv run pytest tests/test_server.py tests/test_snapshots.py -x --tb=short && cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox && cargo test --manifest-path tools/eaasp-skill-registry/Cargo.toml skill_parser -- --nocapture && cargo check --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `scada_set_setpoint` exists in MCP manifest/dispatch and tests prove 65.0→70.0 only on approval, with denial and invalid input preserving 65.0.
    - `examples/skills/scada-set-setpoint/SKILL.md` parses and contains exactly `risk_level: write_external` and required tool `l2:scada_set_setpoint`.
    - The hermetic S8 test proves ordered gate_request → request event → approve/deny audit → decision event → approved side effect, with no side effect on denial.
    - Walkthrough is at least 170 lines, documents exact prerequisites and commands, and contains only S8.
    - Dated status file uses the actual date; transcript is labelled actual or mock and never fabricates a live LLM run.
    - Targeted test commands and `cargo check --workspace` pass; no full workspace test suite runs.
    - Existing L3 tests, existing CLI session tests, and existing mock-SCADA tests remain green.
    - No files under `proto/`, `crates/grid-*`, `lang/`, `grid-server/`, `web/`, `web-platform/`, or `grid-desktop/` are changed.
  </acceptance_criteria>
  <done>REQ-EAASP-06..08 and the phase goal are closed by one observable S8 scenario, hermetic evidence, honest dated transcript, and clean targeted regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill manifest → L3 policy engine | Skill-authored `risk_level` is untrusted metadata; unknown values or omission must not bypass deterministic validation/default rules. |
| L3 gate → external tool dispatch | This is the critical pre-side-effect boundary: approval must resolve before `scada_set_setpoint` mutates state. |
| CLI user → governance decision | Interactive input and `--yes`/`--no` determine whether a write proceeds; conflicting flags and default-No behavior must be deterministic. |
| L3/L4 → SQLite | Concurrent writers record request/final decisions and session events; partial or overwritten audit rows would destroy evidence. |
| L4 event stream → observers | Event append is observable telemetry, not the authority for allow/deny; delivery failure must not invert the decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.7.3-04 | Tampering | `risk_level` deserialization | mitigate | Closed enum validation at Python and Rust boundaries; unknown values fail before persistence; absent value defaults to read per D-01/D-07. |
| T-3.7.3-05 | Elevation of Privilege | CLI gate bypass | mitigate | Enforced write risks produce `gate_request`; side-effect callback is unreachable until approve; denial/default-No exits 4 and tests assert zero tool calls. |
| T-3.7.3-06 | Spoofing | `approver` field | mitigate | Use fixed provenance values `cli:--yes`, `cli:--no`, and `cli:interactive`; do not infer or store personal email identity. |
| T-3.7.3-07 | Repudiation | governance audit | mitigate | Append-only request and final decision rows with unique IDs, rationale link, timestamps, and `BEGIN IMMEDIATE` transactions. |
| T-3.7.3-08 | Tampering | concurrent audit write | mitigate | Parameterized SQL plus `BEGIN IMMEDIATE`, commit-on-success, rollback-on-error, and integration rollback test. |
| T-3.7.3-09 | Denial of Service | SSE/event persistence failure | mitigate | Catch/log event append failures and return `None`; authoritative gate decision and audit persistence continue. |
| T-3.7.3-10 | Information Disclosure | `action_preview` and walkthrough | mitigate | Use deterministic mock values, truncate/sanitize where displayed, and prohibit secrets, real customer endpoints, or credentials in events/docs. |
| T-3.7.3-11 | Elevation of Privilege | full OPA/sandbox bypass assumptions | accept | Phase explicitly provides credible local in-process simulation, not production enforcement; dated record and walkthrough state this boundary and deferred capabilities. |
</threat_model>

<verification>
1. REQ-EAASP-01..08 are each implemented and mapped to D-01..D-10.
2. Risk metadata is backward compatible and validated in both Python L3 and Rust skill registry.
3. Gate decisions and final approvals/denials are append-only and transactional.
4. CLI primary UX supports `--yes`, `--no`, and `Approve? [y/N]`; denial prevents tool dispatch and exits 4.
5. L4 exposes exact `governance.request`/`governance.decision` events through existing session events without a new endpoint.
6. S8 updates the deterministic external setpoint only after approval and documents request, decision, action, audit, and event evidence.
7. New L3, L4, CLI, mock-SCADA, and skill parser tests plus existing targeted regressions pass; `cargo check --workspace` is clean.
8. Full OPA, multi-party approval, sandbox tiers, L5 consumers, additional scenarios, L1/proto, grid-server, web/platform/desktop, and shared core remain untouched.
9. Status transcript honestly distinguishes actual live execution from hermetic/mock execution.
</verification>

<success_criteria>
A non-developer can run the single S8 EAASP local scenario, observe a write_external gate, approve or deny synchronously, see request/decision events, inspect append-only audit records, and verify that mock-SCADA changes only after approval; the behavior is hermetically tested and the production OPA boundary remains explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/03.7.3-eaasp-phase-0-2-5-phase-3-governance-hooks/03.7.3-02-SUMMARY.md` following the summary template. Include REQ-EAASP closure, exact test counts/commands, gate decision table, audit schema, event payloads, S8 pre/post values, live-vs-mock transcript status, Cargo check result, commit SHAs, and confirmation that deferred scope was untouched.
</output>
