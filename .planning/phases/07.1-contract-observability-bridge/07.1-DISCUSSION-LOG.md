# Phase 7.1: contract observability + bridge - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-01
**Phase:** 07.1-contract-observability-bridge
**Areas discussed:** D137 scope, D138 mock approach, D5/D6 coupling, D55 scope, plan structure

---

## D137 scope — multi-turn + MCP bridge + PreCompact

| Option | Description | Selected |
|--------|-------------|----------|
| All 3 sub-tasks | Multi-turn session replay framework (4 xfail) + MCP subprocess fixture (5 xfail) + scripted PreCompact threshold (1 xfail). 10/10 D137 xfails closed. Largest phase but matches CONTEXT.md domain wording. | ✓ |
| Multi-turn + PreCompact only — defer MCP bridge to v3.4+ | Close 5/10 D137 xfails (test_event_type.py 4 + test_proto_shape.py 1); defer 5 MCP bridge xfails (test_mcp_bridge.py) to v3.4+ as new D-row. Smaller phase. | |
| MCP bridge + PreCompact only — defer multi-turn replay to v3.4+ | Close 6/10 D137 xfails (MCP 5 + PreCompact 1); defer multi-turn observability (test_event_type.py 4) to v3.4+. Useful if MCP integration is higher-priority capability. | |
| Just scripted PreCompact threshold (1 xfail) | Minimum-viable D137 close: replace hard-coded threshold with ChunkType-triggered emission; 1 xfail closed. Defer 9 xfails. Lightest phase. | |

**User's choice:** All 3 sub-tasks (recommended).
**Notes:** ROADMAP success criterion #1 explicitly names all three; partial close would leave D137 row only half-resolved.

---

## D138 mock approach

| Option | Description | Selected |
|--------|-------------|----------|
| Extend mock_openai/anthropic servers with scenario routing | LEDGER L250 option (a): support `tool_choice` + scripted deny via scenario header. Broader coverage. Reuses existing fixture infra. | ✓ |
| gRPC bypass — inject synthetic ToolCallEvent (LEDGER option (b)) | Bypass LLM; inject events via gRPC. Tighter scope; tests enforcement layer only (not LLM-call → deny round-trip). | |
| Both — scripted mock + gRPC bypass for finer-grained tests | Maximum coverage; scripted mock tests integration, bypass tests unit-level enforcement. More LOC. | |

**User's choice:** Scripted mock (recommended option).
**Notes:** Tests the production LLM-call → deny round-trip; gRPC bypass misses that round-trip.

---

## D5 + D6 (telemetry envelope migration + certifier SessionPayload assertions)

| Option | Description | Selected |
|--------|-------------|----------|
| Pair them — verify D5 migrated + add D6 SessionPayload assertions | Verify `telemetry_v1` is comment-only in grpc_integration.rs; close D5 as 'verified migrated' with rg evidence. Then add SessionPayload P1-P5 field assertions to certifier blindbox.rs + verifier.rs. | ✓ |
| D5 deferred close + D6 standalone full work | If grpc_integration.rs already on v2 envelope (scout suggests yes), D5 closes as 'no-op verified clean'. D6 stays as concrete certifier additions. Less coupling. | |
| Defer both D5 + D6 to v3.4+ | Focus phase on D137 + D138 P2 only. CONTRACT-03/04 close as deferred. Risk: under-delivers on REQUIREMENTS coverage. | |

**User's choice:** Pair them (recommended option).
**Notes:** LEDGER L109 explicitly says D6 depends on D5 decision; natural coupling.

---

## D55 proto3 HasField unification

| Option | Description | Selected |
|--------|-------------|----------|
| Cross-runtime parity test | Add absence-path parity tests across Python + Rust + TS for proto3 submessage presence. Closes D55 with mechanically enforced consistency. ~3 small additions to existing parity tests. | ✓ |
| Doc-only close + grep-canary CI gate | Single grep-canary CI step rejects truthy-fallback patterns; close LEDGER row with 'no current bug, mechanical guard added'. Lighter. | |
| Defer D55 to v3.4+ | LEDGER L175 says 'touch when proto3 layer next refactors'. No current bug. CONTRACT-05 closes as deferred. Saves scope room for D137. | |

**User's choice:** Cross-runtime parity test (recommended option).
**Notes:** D55 is small enough to do now; deferring then re-establishing context costs more than just doing it.

---

## Plan structure

| Option | Description | Selected |
|--------|-------------|----------|
| Single plan with 8-10 tasks | All 5 REQ-IDs in one PLAN file. T01-T09 covering multi-turn / PreCompact / MCP / scenario routing / D138 wiring / D5 verify / D6 assertions / D55 parity / LEDGER consolidation. Matches Phase 7.0 precedent. | ✓ |
| Two plans by language: contract harness (Plan 01) + Rust certifier/parity (Plan 02) | Plan 01 = D137 + D138 (Python-heavy). Plan 02 = D5 + D6 + D55 (Rust/TS-heavy). Cleaner PR review boundary. | |
| Three plans: D137 / D138 / D5+D6+D55 | Each closure-domain gets its own PLAN. Most focused PR review. Most git history overhead. | |

**User's choice:** Single plan (recommended).
**Notes:** Although the work spans Python and Rust, it's a single coherent observability+bridge story. Phase 7.0 single-plan precedent worked well.

---

## the agent's Discretion

(Per CONTEXT.md `decisions` section "the agent's Discretion" subsection)

- Multi-turn session replay framework API shape
- MCP subprocess choice (eaasp-l2-memory-engine vs thin test stub)
- Scenario-header name
- SessionPayload schema check report-line placement
- D6 strictness (strict per ADR-V2-028)
- LEDGER close-out wording precedent

---

## Deferred Ideas

(Mirrors CONTEXT.md `deferred` section)

- D74 (EmitEvent gRPC reverse channel) — v3.4+
- D139 (dual Terminate + unknown session semantics) — v3.4+
- Distributed tracing / OpenTelemetry export — v3.4+
- MCP bridge production hardening (TLS, auth, multiplexing) — separate stream
- New ADR for multi-turn replay framework if shape stabilizes
- Mock server gRPC-bypass mode (if use case emerges)
