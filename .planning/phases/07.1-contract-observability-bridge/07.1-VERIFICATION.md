---
phase: "07.1"
status: PASS-with-observations
verified: 2026-06-02
must_haves_pass: 5/5
req_ids_closed: 5/5
ledger_closures_verified: 5/5
xfails_flipped: 15/15
new_unsafe_introduced: 0
cargo_dep_changes: 0
pre_existing_regressions_acknowledged: 1 (api_security_policy:24)
---

# Phase 7.1 Verification — contract observability + bridge

## Overall verdict

Phase 7.1 Plan 01 delivered all 5 ROADMAP success criteria, closed all 5 REQ-IDs (CONTRACT-01..05), flipped all 15 D137+D138 xfails, and held every anti-canary (no proto change, no `CHUNK_TYPE_PRE_COMPACT`, no `GRID_TEST_SCENARIO` env-var shim in production). Two reviewer WARNINGs (F1 LEDGER L249 wire-path narrative inaccuracy, F2 process-global `ScenarioState` despite `*_session_*` naming) survive into the final state as documentation/maintainability nits — neither blocks the phase but both are worth folding into Phase 7.2 startup or a tiny follow-up commit before milestone close. PASS-with-observations.

## ROADMAP success criteria

### SC#1 — multi-turn + MCP + PRE_COMPACT non-hardcoded (D137 10 xfails closed)

- [x] PASS
  - `grep -cE 'pytest\.xfail.*D137' tests/contract/contract_v1/{test_event_type,test_mcp_bridge,test_proto_shape}.py` returns 0 / 0 / 0 — all 10 D137 xfails flipped.
  - PRE_COMPACT lives on the EVENT channel: `git diff 197b3ae^..HEAD -- proto/eaasp/runtime/v2/` is empty; `grep -c CHUNK_TYPE_PRE_COMPACT proto/eaasp/runtime/v2/common.proto` = 0; `grep -rn 'PreCompact\|PRE_COMPACT' crates/grid-runtime/src/` shows `HookPoint::PreCompact` registration at `harness.rs:811` and `HookEventType::PreCompact = 8` referenced in `pre_compact_emitter.rs:4`.
  - Multi-turn replay framework exists: `tests/contract/harness/multi_turn.py` (6871 bytes, `MultiTurnFixture` + `TurnScript` + `drive_session`).
  - MCP subprocess fixture exists: `tests/contract/harness/mcp_subprocess.py` (2376 bytes) + `tests/contract/harness/test_mcp_server.py` (4769 bytes thin stdio MCP echo-tool server).

### SC#2 — skill-workflow deny-path mock LLM (D138 5 xfails closed)

- [x] PASS
  - `grep -cE 'pytest\.xfail.*D138' tests/contract/contract_v1/test_skill_workflow.py` = 0 — all 5 D138 xfails flipped.
  - Mock servers support scenario routing: `mock_openai_server.py` has 4 matches for `X-Test-Scenario|x-test-scenario` and 9 matches for `tool_choice`; `mock_anthropic_server.py` has 3 matches for the scenario header.
  - T05 used the in-contract `UserMessage.metadata` path, NOT an env-var shim:
    - `grep -rcE 'GRID_TEST_SCENARIO' crates/grid-engine/src/ crates/grid-runtime/src/` = 0 (production code clean).
    - `crates/grid-runtime/src/harness.rs:870` reads `message.metadata.get("x-test-scenario")`.
    - `crates/grid-engine/src/providers/openai.rs:483` and `:649` set `X-Test-Scenario` on both `complete` and `stream` request paths.
    - `crates/grid-engine/src/providers/test_scenario.rs` is the bridge module.

### SC#3 — telemetry envelope v2 (D5 verified)

- [x] PASS
  - `rg "telemetry_v1" -t rust crates/` returns 0 hits — production code is fully migrated to v2 envelope.
  - LEDGER L108 (D5) cites the rg evidence verbatim, names commit `e2a3f91`, and notes the `harness_payload_integration.rs:1-20` planned no-op (also flagged as F5 NIT — already pre-documented in two places).

### SC#4 — certifier SessionPayload P1-P5 (D6 closed)

- [x] PASS
  - `grep -c "SessionPayload schema\|p1_p5\|assert_session_payload\|build_certifier_payload" tools/eaasp-certifier/src/{report,verifier,blindbox}.rs` = 4 (report) / 12 (verifier) / 0 (blindbox). Total 16, well above the ≥3 minimum. Blindbox is 0 because all assertions live in verifier — acceptable per CONTEXT.md D-03 ("Extend `verifier.rs`" was the operative direction; blindbox already exposes the underlying builder).
  - Strict-mode confirmed: `verifier.rs:388-416` `assert_session_payload_p1_p5()` returns `Err(...)` for each missing P-block (P1/P2/P3/P4/P5/session_id+user_id empty), NOT a warning. Failure flows through `verify_initialize` → `MethodResult.error` → `session_payload_schema_passed = false` on the markdown report (per the strict-by-default ADR-V2-028 lineage).
  - Targeted test run executed in this verification session: `cargo test -p eaasp-certifier --tests` → **33 passed; 0 failed; 0 ignored** — matches SUMMARY claim.

### SC#5 — proto3 HasField parity (D55 closed)

- [x] PASS
  - All 3 language test files exist:
    - Python: `tests/contract/contract_v1/test_proto3_hasfield_parity.py` (3215 bytes).
    - Rust: `crates/grid-runtime/tests/proto3_hasfield_parity.rs` (2380 bytes).
    - TypeScript: `lang/ccb-runtime-ts/tests/proto3-hasfield-parity.test.ts` (2375 bytes).
  - Targeted Rust run executed in this verification session: `cargo test -p grid-runtime --test proto3_hasfield_parity` → **6 passed; 0 failed** (P1/P4/P5 absent + present round-trip).
  - TS scoped to P4 only per SUMMARY §Deviation #2 and F4 NIT — accepted as documented limitation of the current TS type surface (`lang/ccb-runtime-ts/src/proto/types.ts:34-44` only declares `skillInstructions` on `SessionPayload`).

## REQ-ID closure verification

| REQ-ID | LEDGER row | Closure commit | Verified |
|--------|-----------|----------------|----------|
| CONTRACT-01 | L249 (D137) | `03d420c` (rolls T01 `197b3ae` + T02 `a52b2c4`) | ✓ |
| CONTRACT-02 | L250 (D138) | `e2460df` (rolls T04 `466308a`) | ✓ |
| CONTRACT-03 | L108 (D5)   | `e2a3f91` | ✓ |
| CONTRACT-04 | L109 (D6)   | `5807fb7` | ✓ |
| CONTRACT-05 | L175 (D55)  | `0842f60` | ✓ |

All 5 closure rows contain real 7-char hex hashes — `grep -cE '✅ CLOSED 2026-06-0[12] Phase 7.1 Plan 01' docs/design/EAASP/DEFERRED_LEDGER.md` = 5; `grep -c '<commit>\|<auto-link>' docs/design/EAASP/DEFERRED_LEDGER.md` = 0. Each cited hash exists in `git log 197b3ae^..HEAD`.

## Anti-check results

- New `unsafe` blocks: 0 (`git diff 197b3ae^..HEAD | grep -cE '^\+.*\bunsafe\s'` = 0)
- Cargo.toml/Cargo.lock pairing: OK — `git diff 197b3ae^..HEAD --stat` shows no `Cargo.toml` or `Cargo.lock` changes whatsoever (no new deps introduced)
- Proto/ untouched: VERIFIED — `git diff 197b3ae^..HEAD -- proto/eaasp/runtime/v2/` is empty
- `CHUNK_TYPE_PRE_COMPACT` introduced: 0 (anti-canary held — no closed enum amendment)
- `GRID_TEST_SCENARIO` leaked to production: 0 (anti-canary held — env-var shim correctly avoided)
- `<auto-link>` / `<commit>` placeholders remaining in LEDGER: 0
- Code reviewer findings escalated: F1/F2/F3 WARNING + F4/F5 NIT all acknowledged in REVIEW.md; none upgraded to BLOCKER

## Commit log integrity

`git log --oneline 197b3ae^..HEAD` shows 12 commits in expected sequence:

```
1106d0a docs(phase-07.1): update tracking after wave 1 (plan 01 complete)
6883f90 chore(contract-harness): tighten _run_hook_probe return type
cee5138 docs(07.1-01): SUMMARY.md — plan complete
d7cd524 docs(ledger): finalize Phase 7.1 D137 close + commit-hash backfill (T09)
0842f60 test(contract): cross-runtime proto3 HasField parity (T08 / D55)
5807fb7 feat(eaasp-certifier): SessionPayload P1-P5 strict assertions (T07 / D6)
e2a3f91 docs(ledger): close D5 verified-clean (T06)
e2460df feat(grid-engine,contract-tests): flip 5 D138 skill-workflow xfails (T05 / D138)
466308a feat(contract-harness): mock-server scenario routing + tool_choice (T04 / D138 infra)
03d420c test(contract-harness): MCP subprocess fixture + flip 5 D137 mcp_bridge xfails (T03)
a52b2c4 feat(grid-runtime): PRE_COMPACT event-channel emission via emit_event (T02)
197b3ae feat(contract-harness): multi-turn session replay framework (T01)
```

Matches PLAN T01-T09 + SUMMARY commit + type fix `6883f90` (called out as pre-existing drift correction in REVIEW self-check L123) + tracking docs `1106d0a`. No surprise commits.

## Reviewer's findings disposition

- **F1 (WARNING — LEDGER L249 narrative claims `EventStreamEntry → emit_event RPC at service.rs:597` for PRE_COMPACT, actual impl is a file-sink hook)**: ACCEPT as documentation NIT; recommend ≤5-line LEDGER edit in Phase 7.2 startup or as a small follow-up before milestone close. Verified the misleading phrase is still present (`grep -c "emit_event\` RPC at \`service.rs:597\`" docs/design/EAASP/DEFERRED_LEDGER.md` = 1). The wire-level anti-canary (no chunk channel, no proto change) holds — only the prose mis-describes the observation surface. Not blocking.

- **F2 (WARNING — `test_scenario` cell is process-global `Option<String>`, NOT a per-session map despite `*_session_*` naming)**: ACCEPT with the same posture. Contract suite serializes test execution, so the latent concurrent-Send race is bounded. Suggest opportunistic rename (`set_current_scenario` / `clear_current_scenario`) or one-line rustdoc caveat in Phase 7.2 if a touching PR appears.

- **F3 (WARNING — Send-path xfail flips NOT exercised end-to-end locally; pre-existing runtime Send-path hang)**: ACCEPT per SUMMARY §Deviation #1 (stash-based regression proof shows hang is pre-existing). CI is the authoritative gate for Send-path tests. Phase-orchestrator should watch CI for any deny-path failure tracing into `openai.rs:483` `builder.header("X-Test-Scenario", ...)` or `harness.rs:870` metadata read.

- **F4 (NIT — TS HasField parity scoped to P4 only)**: ACCEPT per SUMMARY §Deviation #2. The TS type surface limits coverage; documented in test docstring and LEDGER L175 closure. Follow-up D-row in v3.4+ INBOX recommended once TS-types extend.

- **F5 (NIT — T06 `harness_payload_integration.rs:4` comment update is a no-op)**: ACCEPT per SUMMARY §Deviation #4. Already pre-documented in two places (LEDGER L108 + SUMMARY). No action.

## Pre-existing regressions (NOT Phase 7.1's fault)

- `crates/grid-server/tests/api_security_policy.rs:24 security_policy_update_returns_not_implemented` — same pre-existing failure noted in the Phase 7.0 verifier output. NOT a Phase 7.1 regression; touched no `grid-server` security-policy code in this phase.
- Pre-existing runtime Send-path hang (F3) — confirmed via stash-revert by executor. Unrelated to Phase 7.1 changes.

## Recommendations

- **Small follow-up before milestone close**: a single LEDGER L249 edit to address F1 (≤5 lines) plus an optional rustdoc note on `test_scenario.rs` for F2 — both well within "polish-before-ship" scope and small enough to land as one commit.
- **Phase 7.2 startup**: re-evaluate the Send-path hang as a blocker for v3.3 contract suite CI fidelity. If the hang persists into Phase 7.2 verification, file as a P2 D-row and consider it blocking for milestone close.
- **v3.4+ INBOX**: file a follow-up D-row for TS `SessionPayload` type surface expansion + parity-test extension (per F4) — defer per CONTEXT §deferred.
- **CI watch**: monitor first CI run that exercises the 14 flipped Send-path xfails. Any failure stack-tracing into `crates/grid-engine/src/providers/openai.rs:483/649` or `crates/grid-runtime/src/harness.rs:870` would indicate a metadata-forwarding bug not caught by local verification.
- **Phase verdict for ROADMAP**: Phase 7.1 ready to mark COMPLETE. Update ROADMAP §"Phase 7.1" status from 🟡 STARTED to 🟢 COMPLETE.
