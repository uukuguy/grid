---
phase: 03.7.1-grid-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03.7.1-grid-cli/3.7.1-01-PLAN.md
  - docs/audit/3.7.1-GAP-AUDIT.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "An executor can read `docs/audit/3.7.1-GAP-AUDIT.md` and decide which gaps block Plan 02"
    - "The audit table covers all 5 scenarios (S1-S5) cross-referenced against the actual 17-command CLI surface"
    - "Each row of the audit table is one of three statuses: PASS / MANUAL TUNING / FAIL — with concrete evidence (file:line, command output, or env var)"
    - "The audit identifies which `grid` subcommands are stubs (e.g. `eval run` printing 'cargo run -p octo-eval' instead of executing)"
    - "The audit identifies whether scenario S5 (parallel/batch) has any first-class command surface, or needs a new flag/subcommand"
  artifacts:
    - path: "docs/audit/3.7.1-GAP-AUDIT.md"
      provides: "Gap audit matrix (scenario × command) with PASS/MANUAL TUNING/FAIL status and concrete evidence per row"
      min_lines: 120
    - path: ".planning/phases/03.7.1-grid-cli/3.7.1-01-PLAN.md"
      provides: "This plan (executable by /gsd-execute-phase)"
      min_lines: 50
  key_links:
    - from: "docs/audit/3.7.1-GAP-AUDIT.md"
      to: ".planning/phases/03.7.1-grid-cli/3.7.1-CONTEXT.md"
      via: "Scenarios S1-S5 referenced by D-01 / D-02"
      pattern: "S[1-5]"
    - from: "docs/audit/3.7.1-GAP-AUDIT.md"
      to: "crates/grid-cli/src/commands/"
      via: "Per-command audit entries cite the actual handler file:line"
      pattern: "crates/grid-cli/src/commands/[a-z_]+\\.rs"
    - from: "docs/audit/3.7.1-GAP-AUDIT.md"
      to: "Plan 3.7.1-02"
      via: "FAIL rows become Plan 02 task inputs (remediation backlog)"
      pattern: "REQUIREMENTS|MANUAL TUNING|FAIL"

---

<objective>
Produce a read-only gap audit that maps the 5 enterprise scenarios (S1-S5) from CONTEXT.md D-01 against the current 17-command `grid` CLI surface, so that Plan 3.7.1-02 has an evidence-backed backlog of exactly which gaps to close. No code changes. No scenario execution. The audit is a single document — `docs/audit/3.7.1-GAP-AUDIT.md` — containing the matrix plus per-row evidence.

Purpose: surface every gap between Activation-9.0 (code-quality) and 实战可用 (end-to-end runnability) BEFORE committing remediation scope. Plan 02's REQ-IDs derive directly from the FAIL and MANUAL TUNING rows.

Output: `docs/audit/3.7.1-GAP-AUDIT.md` (committed) + this PLAN.md (committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.7.1-grid-cli/3.7.1-CONTEXT.md
@.planning/phases/03.7.1-grid-cli/3.7.1-DISCUSSION-LOG.md
@crates/grid-cli/src/lib.rs
@crates/grid-cli/src/main.rs
@crates/grid-cli/src/commands/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the 17-command surface and map to scenarios</name>
  <read_first>
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/crates/grid-cli/src/lib.rs (full file; this is the canonical `Cli` enum and `Commands` enum — gives the authoritative list of 17 subcommands at lines 65-184)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/crates/grid-cli/src/commands/mod.rs (full file; lines 31-52 show the handler re-exports, lines 3-28 show the per-command .rs modules)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.1-grid-cli/3.7.1-CONTEXT.md (read D-01 scenario table at lines 20-29, D-02 walkthrough requirement at line 30, D-08 plan split at lines 57-60)
  </read_first>
  <files>docs/audit/3.7.1-GAP-AUDIT.md (new file, this task creates the file skeleton + Section 1 only)</files>
  <action>
    1. Create the file `docs/audit/3.7.1-GAP-AUDIT.md` with the exact skeleton below. Do not invent section names; the table in Section 2 must use the exact 17 subcommands listed in `crates/grid-cli/src/lib.rs` lines 65-184 (`Run`, `Ask`, `Agent`, `Session`, `Memory`, `Tool`, `Mcp`, `Config`, `Auth`, `Skill`, `Root`, `Eval`, `Sandbox`, `Init`, `Doctor`, `Completions` — and confirm whether `Dashboard` / `Tui` are listed under `StudioCommands` rather than the main `Commands` enum, and document that).

    2. Section 1 (Command Inventory) must be a verbatim list copied from the source — do not paraphrase. For each of the 17 (or 16 + 2-studio) entries: subcommand name, handler file path under `crates/grid-cli/src/commands/`, and the `pub use` line number from `commands/mod.rs` (e.g. `pub use run::execute_run` at line 48). This locks the audit to the actual code, not the user's recollection.

    3. Do NOT execute the CLI, do NOT run scenarios, do NOT modify any Rust source. This task is purely structural — building the document skeleton with command inventory and scenario overview sections.
  </action>
  <verify>
    <automated>test -f docs/audit/3.7.1-GAP-AUDIT.md && wc -l docs/audit/3.7.1-GAP-AUDIT.md | awk '{print $1}' | grep -qE '^[0-9]+$' && grep -c 'crates/grid-cli/src/commands/' docs/audit/3.7.1-GAP-AUDIT.md | awk '{print ($1>=16)?"OK":"FAIL"}' && grep -q '## 1. Command Inventory' docs/audit/3.7.1-GAP-AUDIT.md && grep -q '## 2. Scenario × Command Matrix' docs/audit/3.7.1-GAP-AUDIT.md</automated>
  </verify>
  <done>
    - `docs/audit/3.7.1-GAP-AUDIT.md` exists
    - File contains sections `## 1. Command Inventory` and `## 2. Scenario × Command Matrix`
    - File contains at least 16 distinct references to `crates/grid-cli/src/commands/<name>.rs` paths
    - File is committed (single git commit after Task 3 finishes the whole audit)
  </done>
</task>

<task type="auto">
  <name>Task 2: Walk each of S1-S5 against the command surface (read-only static analysis)</name>
  <read_first>
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/audit/3.7.1-GAP-AUDIT.md (Section 1 from Task 1)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.1-grid-cli/3.7.1-CONTEXT.md (D-01 lines 20-29 for scenario acceptance markers; D-02 line 30 for walkthrough doc requirement; D-05/D-06 lines 42-51 for error UX; D-07 line 53 for TTY/JSON; D-08 lines 57-60 for plan split)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/crates/grid-cli/src/commands/eval_cmd.rs (full file; lines 124-148 show `cmd_run` prints `cargo run -p octo-eval` instead of executing — this is the canonical stub pattern; lines 151-169 `cmd_compare` and 171-197 `cmd_benchmark` show the same pattern; lines 667-679 `cmd_watch` same)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/crates/grid-cli/src/commands/mcp.rs (lines 263-280 show `show_logs` says "live log streaming not yet available" — second stub pattern)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/crates/grid-cli/src/commands/agent.rs (full file; `update_agent_state` at line 119-127 and `delete_agent` at 130-139 use `eprintln!` then `anyhow::bail!` instead of typed `GridError` — third pattern; `show_agent_info` at 86-89 `eprintln!` + `Ok(())` silent on missing agent — fourth pattern)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/crates/grid-cli/src/main.rs (lines 92-104 show the eprintln!("Error: {}", e) + typed-downcast exit — current error UX; lines 73-82 show output_config setup including `is_terminal` detection at line 79 — relevant for D-07 TTY awareness but NOT yet propagated to error messages)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/crates/grid-cli/src/commands/run.rs (full file; only forwards to `repl::run_repl`; no `--parallel`, no `--retry`, no batch mode)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/crates/grid-cli/src/commands/ask.rs (full file; lines 49-55 create session if no --session; lines 67-76 `start_primary`; lines 142-146 show existing `RetryingMalformedToolCall` retry inside agent loop but NOT user-facing `--retry` flag)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/crates/grid-cli/src/commands/session.rs (full file; lines 9-25 show the `match` for SessionCommands variants — NO `Resume` variant, only `Kill --purge`. `grid session resume <id>` referenced in CONTEXT.md D-01 S4 acceptance does NOT exist as a typed subcommand. This is a critical gap.)
  </read_first>
  <files>docs/audit/3.7.1-GAP-AUDIT.md (extend Section 2 matrix and add Section 3 evidence + Section 4 remediation backlog)</files>
  <action>
    1. In `docs/audit/3.7.1-GAP-AUDIT.md` Section 2, fill the matrix. Use this exact table shape (markdown table) — DO NOT add columns; keep it 5 columns (Scenario | Command(s) exercised | Status | Evidence (file:line) | Remediation hint):

    ```
    | Scenario | Command(s) exercised | Status | Evidence (file:line) | Remediation hint |
    |----------|----------------------|--------|----------------------|-------------------|
    | S1 Multi-step tool use | `grid ask "<prompt with tools>"` | <PASS\|MANUAL TUNING\|FAIL> | crates/grid-cli/src/commands/ask.rs:L<N> | <hint> |
    | S1 | `grid run` | <status> | crates/grid-cli/src/commands/run.rs:L<N> | <hint> |
    ...
    ```

    2. Required evidence anchors (cite the EXACT file:line for each):

    - **S1 Multi-step tool use** — `grid ask` and `grid run` are the two execution paths. Look at `crates/grid-cli/src/commands/ask.rs:43-98` (execute_ask): does it stream tool calls? Yes (lines 117-141 ToolStart/ToolResult events to stderr). Status = PASS for streaming. But: does `grid ask` show tool calls WITHOUT `--debug`? Per CONTEXT.md D-01 S1 acceptance "each tool call observable in CLI output without --debug" — `ask.rs:117-141` uses `if !quiet { eprintln!(...) }` so default = observable. PASS.
    - **S2 Memory-driven session** — `grid memory add` + `grid memory search` + `grid ask` after restart. Look at `crates/grid-cli/src/commands/memory.rs:177-203` (add_memory writes via `memory_store.store`). PASS for write+search. Cross-session: `ask.rs:49-55` loads history via `session_store.get_messages`. PASS for cross-session read. Status = PASS.
    - **S3 Hook-driven governance** — `grid run` with a hooks config. Look at `crates/grid-cli/src/repl/hooks.rs` (8.6K — hooks live in REPL, not in Commands). Check whether `grid doctor` validates `GRID_HOOKS_FILE` (`doctor.rs:172-196` only checks LLM key, NOT hooks). FAIL — no hooks validation in doctor, no CLI path to test PreToolUse pause without REPL.
    - **S4 Long-running streaming stop/resume** — `grid session resume <id>`. Look at `commands/types.rs:55-102` (SessionCommands enum): NO `Resume` variant. Look at `commands/session.rs:9-25`: NO resume handler. FAIL — `grid session resume` does not exist as a subcommand; only `grid ask --session <id>` is the current resume path.
    - **S5 Parallel agent runs / batch mode** — `grid run --parallel` (per CONTEXT.md D-01 S5 acceptance). Look at `commands/run.rs:7-22` (RunOptions): no `parallel: bool` field. Look at `lib.rs:67-87` (Run variant): no `--parallel` flag. FAIL — no first-class batch surface.

    3. Cross-cutting audit rows (add to Section 2 table):
    - `grid eval run` — FAIL stub. Cite `crates/grid-cli/src/commands/eval_cmd.rs:124-148` (cmd_run prints `cargo run -p octo-eval -- run --suite` instead of executing).
    - `grid mcp logs` — FAIL stub. Cite `crates/grid-cli/src/commands/mcp.rs:263-280` (show_logs says "live log streaming not yet available").
    - Error UX per D-05/D-06/D-07 — `main.rs:92-104` prints `Error: {e}` to stderr — does NOT include `fix: grid doctor --repair` line, does NOT distinguish retryable vs permanent, does NOT detect TTY for text-vs-JSON. FAIL on all 3 of D-05, D-06, D-07.
    - `grid quickstart` — does not exist (`lib.rs:65-184` Commands enum has no Quickstart variant). FAIL per D-04.

    4. Section 3 (Evidence Detail) — for every FAIL row, paste the exact 1-3 line code snippet from the cited file:line as a fenced block. This is the audit trail Plan 02 will rely on.

    5. Section 4 (Remediation Backlog) — list each FAIL row as `REQ-AUDIT-NN: <one-line description>` (NN starts at 01). These REQ-IDs become Plan 02's task input. Do NOT assign REQ-IDs to PASS rows.

    Use these exact minimum REQ-IDs based on the audit findings:
    - REQ-AUDIT-01: `grid session resume <id>` subcommand missing (S4)
    - REQ-AUDIT-02: `grid run --parallel N` flag missing (S5)
    - REQ-AUDIT-03: `grid eval run` is a stub printing cargo instructions
    - REQ-AUDIT-04: `grid mcp logs` is a stub returning "live log streaming not yet available"
    - REQ-AUDIT-05: error UX does not match D-05/D-06/D-07 (no `fix:` line, no retryable/permanent class, no TTY→JSON switch)
    - REQ-AUDIT-06: `grid quickstart` subcommand missing (D-04)
    - REQ-AUDIT-07: `grid doctor` does not validate `GRID_HOOKS_FILE` (S3 gap)
    - REQ-AUDIT-08: `update_agent_state` / `delete_agent` use `eprintln!` + `anyhow::bail!` instead of typed `GridError::agent_not_found` — typed-error parity gap

    Add more REQ-AUDIT-NN if the executor discovers additional concrete gaps during the walk. Do NOT exceed 12 REQ-IDs — if more gaps appear, defer to a Plan 03 per CONTEXT.md D-08.

  </action>
  <verify>
    <automated>grep -c '^| S[1-5] ' docs/audit/3.7.1-GAP-AUDIT.md | awk '{print ($1>=5)?"OK":"FAIL"}' && grep -c '^| `grid ' docs/audit/3.7.1-GAP-AUDIT.md | awk '{print ($1>=10)?"OK":"FAIL"}' && grep -c '^| FAIL|' docs/audit/3.7.1-GAP-AUDIT.md | awk '{print ($1>=5)?"OK":"FAIL"}' && grep -c '^## ' docs/audit/3.7.1-GAP-AUDIT.md | awk '{print ($1>=4)?"OK":"FAIL"}' && grep -c 'REQ-AUDIT-' docs/audit/3.7.1-GAP-AUDIT.md | awk '{print ($1>=8)?"OK":"FAIL"}' && grep -q 'crates/grid-cli/src/commands/' docs/audit/3.7.1-GAP-AUDIT.md</automated>
  </verify>
  <done>
    - Section 2 matrix has at least 5 rows starting with `| S1 `, `| S2 `, ..., `| S5 `
    - Section 2 matrix has at least 10 command rows starting with `| \`grid `
    - At least 5 rows are marked `| FAIL|` (the audit must surface real gaps, not rubber-stamp PASS)
    - At least 4 sections present (`## 1.`, `## 2.`, `## 3.`, `## 4.`)
    - At least 8 `REQ-AUDIT-NN` IDs in Section 4
    - File references `crates/grid-cli/src/commands/` (proof of evidence-backed audit, not guesses)
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify, commit, and surface overflow assessment</name>
  <read_first>
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/audit/3.7.1-GAP-AUDIT.md (complete file from Task 1 + Task 2)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.1-grid-cli/3.7.1-CONTEXT.md (D-08 last paragraph at lines 60: "Scope is bounded by audit findings; if findings exceed 1 plan, defer overflow to a Plan 03")
  </read_first>
  <files>docs/audit/3.7.1-GAP-AUDIT.md (append Section 5 overflow assessment)</files>
  <action>
    1. Append Section 5 (Overflow Assessment) to `docs/audit/3.7.1-GAP-AUDIT.md` containing:
       - Total REQ-AUDIT-NN count
       - For each REQ-AUDIT-NN: complexity estimate (S/M/L) based on whether it requires new commands, new error variants, or just wiring changes
       - Verdict: "fits in Plan 02" OR "needs Plan 03" — apply this rule: if any single REQ-AUDIT-NN is estimated L AND it requires new CLI subcommand registration (changes to `crates/grid-cli/src/lib.rs` Commands enum), recommend Plan 03. Otherwise fit in Plan 02.

    2. Verify the file is internally consistent:
       - Every REQ-AUDIT-NN in Section 4 has a matching FAIL row in Section 2
       - Every Section 3 evidence block matches a file:line that still exists in the codebase (`grep -rn` the cited file:line; if it does NOT exist, fix the citation)

    3. Stage and commit ONLY `docs/audit/3.7.1-GAP-AUDIT.md` (not PLAN.md — PLAN.md is committed by the orchestrator after this plan closes). Use this exact commit message:

       ```
       docs(audit): Phase 3.7.1 gap audit — S1-S5 vs 17-command CLI surface

       Read-only audit (per locked decision D-08). Surfaces REQ-AUDIT-01..NN
       gaps between Activation-9.0 code quality and end-to-end runnability
       for the 5 enterprise scenarios (multi-step tool use, memory-driven,
       hook-driven governance, streaming stop/resume, parallel batch).

       Generated-By: Claude (claude-opus-4-8) via Claude Code CLI
       Co-Authored-By: claude-flow <ruv@ruv.net>
       ```

       Do NOT include Co-Authored-By with the user's email per global CLAUDE.md "Identity & Email Sources" rule. Use `noreply@anthropic.com` only if needed for the system footer; otherwise omit.
  </action>
  <verify>
    <automated>grep -q '^## 5. Overflow Assessment' docs/audit/3.7.1-GAP-AUDIT.md && wc -l docs/audit/3.7.1-GAP-AUDIT.md | awk '{print $1}' | awk '{print ($1>=120)?"OK":"FAIL"}' && git -C /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox status --short docs/audit/3.7.1-GAP-AUDIT.md | head -1 && git -C /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox log -1 --format='%s' -- docs/audit/3.7.1-GAP-AUDIT.md | grep -q 'Phase 3.7.1 gap audit'</automated>
  </verify>
  <done>
    - Section 5 (Overflow Assessment) appended
    - File is ≥120 lines (substantive audit, not skeleton)
    - File is committed with subject containing "Phase 3.7.1 gap audit"
    - `git status` is clean for `docs/audit/3.7.1-GAP-AUDIT.md` (no uncommitted changes)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new (audit is read-only) | This plan performs zero code execution against any user system. Only file reads + a single git commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.7.1-01 | Information Disclosure | `docs/audit/3.7.1-GAP-AUDIT.md` | accept | Audit is a public-facing status doc (matches existing `docs/status/PRODUCT_STATUS_*.md` pattern). No secrets / API keys / file paths beyond repo-relative. |
| T-3.7.1-02 | Tampering | audit file content | mitigate | Executor MUST cite real file:line via grep before pasting evidence block. Section 5 "internal consistency check" verifies every citation still resolves. |
| T-3.7.1-03 | Repudiation | audit authorship | accept | Plan 01 is fully autonomous; commit footer `Generated-By: Claude (claude-opus-4-8)` + `Co-Authored-By: claude-flow` is sufficient provenance per project MEMORY. |

Note: Plan 01 makes NO changes to code that runs. Threat surface is limited to the audit doc itself.
</threat_model>

<verification>
1. `docs/audit/3.7.1-GAP-AUDIT.md` exists and is ≥120 lines.
2. The file contains sections 1-5 (Command Inventory, Scenario × Command Matrix, Evidence Detail, Remediation Backlog, Overflow Assessment).
3. Section 2 has at least 5 scenario rows (S1-S5) and at least 10 command rows.
4. At least 5 rows are marked `| FAIL|`.
5. Section 4 contains at least 8 REQ-AUDIT-NN IDs.
6. Every REQ-AUDIT-NN maps to a FAIL row in Section 2.
7. Every evidence block in Section 3 cites a file:line that resolves in the repo.
8. The file is committed with the documented subject.
9. `git status` is clean (only this commit + the PLAN.md commit from the orchestrator).
</verification>

<success_criteria>
1. Plan 02 can read `docs/audit/3.7.1-GAP-AUDIT.md` and derive its task list with zero further investigation.
2. The audit distinguishes PASS / MANUAL TUNING / FAIL with concrete evidence per row (no subjective language).
3. Overflow Assessment verdict in Section 5 explicitly says either "fits in Plan 02" or "needs Plan 03" — no ambiguity.
4. No code in `crates/grid-cli/`, `web/`, `tools/eaasp-*/`, shared core, or `grid-server/` is touched.
5. `cargo check -p grid-cli` not required (no Rust changes).
</success_criteria>

<output>
After completion, create `.planning/phases/03.7.1-grid-cli/3.7.1-01-SUMMARY.md` following the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`. The summary must include the REQ-AUDIT-NN count and the overflow verdict, so the orchestrator can decide whether to commit to Plan 02 directly or open a Plan 03 first.
</output>
