# Phase 6.0: CI Red Clearance - Context

**Gathered:** 2026-05-23
**Status:** Ready for planning

<domain>
## Phase Boundary

pytest fixture-scope mismatch fix — `@pytest.mark.parametrize("runtime_name", ...)` (function-scoped) shadows the session-scoped `runtime_name` fixture in `tests/contract/conftest.py:113`, causing `ScopeMismatch: You tried to access the function scoped fixture runtime_name with a session scoped request object` errors that cascade through 3 test sites and block the entire Phase 3 Contract Matrix CI workflow (RED since 2026-05-04).

**In scope:**
- Rename `parametrize("runtime_name", ...)` → `parametrize("expected_runtime", ...)` at 3 sites (1 in `test_chunk_type_contract.py`, 2 in `test_hook_event_contract.py`)
- Update body var refs + error message strings to match new name
- Local 1-runtime verify (`pytest --runtime=grid`) + `--collect-only` 7-runtime inventory check
- Push to origin, wait Phase 3 Contract Matrix CI green
- LEDGER row close NEW-X4 ✅ + commit hash + CI run URL
- SUMMARY.md

**Out of scope (NOT for plan-phase to add):**
- Do NOT modify `ADR_V2_025_ACTIVE_RUNTIMES` list (Phase 5.1 ADR-V2-025 lock)
- Do NOT change fixture scope strategy (session-scoped subprocess launch is the design)
- Do NOT refactor `tests/contract/conftest.py` (650+ LOC, large surface)
- Do NOT optimize subprocess startup time
- Do NOT preemptively start Phase 6.1/6.2 work
- Do NOT add new tests (only fix existing 3 parametrize sites)

**Out of scope (defer to v3.3+):**
- Hybrid approach (separate inventory test function) — kept as future opt
- 7-runtime local full matrix smoke test infrastructure
- Other fixture-scope conflicts elsewhere in `tests/contract/` (not surveyed)

</domain>

<decisions>
## Implementation Decisions

### GA1 — Fix shape

- **D-01:** **Rename parametrize param `runtime_name` → `expected_runtime`** at all 3 sites. Preserves the `--collect-only` 7-runtime inventory visibility design (explicitly documented in `test_hook_event_contract.py:200-217` docstring "so the inventory is visible at --collect-only time"). Code change shape per site: rename parametrize argument string + function parameter + body var refs + 1-2 error message strings using the var. Session fixture `runtime_name` at `conftest.py:113` untouched. ~5-8 LOC per site × 3 sites = ~15-25 LOC total.
- **D-01 alternative considered + rejected:** (B) Delete parametrize — shorter (~3 LOC removed per site) but loses inventory + needs docstring deletion at hook_event L195-201 (doc would lie). (C) Change fixture scope to function — ~5-10× slowdown (subprocess + grpc + mock-llm per test instead of per session), dominated.

### GA2 — Site scope

- **D-02:** **3 sites in 2 files**, NOT just the 1 site REQUIREMENTS CI-01 字面 mentions:
  - `tests/contract/cases/test_chunk_type_contract.py:139` `test_chunk_type_contract`
  - `tests/contract/cases/test_hook_event_contract.py:203` `test_subagent_start_envelope_live`
  - `tests/contract/cases/test_hook_event_contract.py:238` `test_task_checkpoint_envelope_live`
  Rationale: SC#2 (workflow ≥4/7 jobs PASS) is the ground-truth gate; fixing only 1 of 3 sites leaves hook_event failures, workflow stays RED.
- **D-03:** **plan-phase task includes a small REQUIREMENTS.md edit** to stretch CI-01 字面 from `test_chunk_type_contract.py` only → `3 parametrize sites across test_chunk_type_contract.py + test_hook_event_contract.py`. This is the same precedent as Phase 5.4 SC#4 wording-fix and Phase 5.5 ROADMAP SC#3 wording-fix — document drift after scout reveals broader surface.

### GA3 — Verify depth

- **D-04:** **Local verify = 1-runtime (grid) + collect-only inventory check.** Specific commands:
  - `cd tests/contract && uv run pytest cases/test_chunk_type_contract.py cases/test_hook_event_contract.py --runtime=grid -v 2>&1 | tee /tmp/grid-runtime-verify.log` — expect NO ScopeMismatch; chunk_type assertion PASS/FAIL/XFAIL per actual runtime behavior is acceptable
  - `cd tests/contract && uv run pytest cases/test_chunk_type_contract.py cases/test_hook_event_contract.py --collect-only 2>&1 | grep -c "expected_runtime"` — expect ≥3 collected IDs containing `expected_runtime` parametrize values (≥7 runtimes × ≥3 test sites = ≥21 collected IDs ideally; ≥3 minimum satisfies inventory preservation)
  - Push to origin/main, wait `gh run watch` or `gh run list --workflow="Phase 3 Contract Matrix"` until completion
  - Verify CI: ≥4 of 7 jobs PASS per ROADMAP SC#2
- **D-04 rejected:** 本地 7-runtime spike — overkill for triage milestone (~30-45 min cold cache subprocess overhead). CI matrix IS the 7-runtime verification.

### GA4 — Plan structure

- **D-05:** **1 plan**, ~5-6 task: (1) Wave 0 — scout existing test files + lock baseline. (2) Edit `test_chunk_type_contract.py:139` rename. (3) Edit `test_hook_event_contract.py:203 + 238` rename. (4) REQUIREMENTS.md CI-01 字面 stretch edit. (5) Local 1-runtime + collect-only verify + commit + push. (6) Wait CI green + LEDGER NEW-X4 row close + SUMMARY.md. **NOT autonomous on CI wait** — task surfaces `gh run watch` to user or polls with status report.
- **D-06:** **Scope-creep guard** — any deviation discovered during execution (e.g., a 4th parametrize site found, fixture refactor temptation, unrelated conftest cleanup) goes to LEDGER as new deferred row (NEW-X5 series), not implemented this phase.

### the agent's Discretion

Items left for planner / researcher to resolve at task-authoring time:

- **Exact rename strings**: planner reads the 3 sites at planning time and confirms which body var refs + error message strings need updating (already inventoried at L142, L162, L170, L172 chunk_type / L206, L223-227 hook_event ×2). Sed-style multi-line edit vs per-line Edit chain — planner picks.
- **REQUIREMENTS.md CI-01 wording**: exact new text — planner drafts at planning time, ROADMAP SC#1 may also need similar stretch (3 sites mention). Cross-check during PLAN.
- **CI URL capture mechanism**: `gh run list --workflow="Phase 3 Contract Matrix" --limit 1 --json url --jq '.[0].url'` or web-link grab — planner picks.
- **Task 6 wait-CI gate semantics**: `gh run watch` (blocks until done) vs poll-based with status updates — planner picks based on Tasks tool / Bash background availability. (If `gh run watch` works in the executor environment, single bash call; else loop with sleep < 270s per CLAUDE.md cache rule.)
- **Whether to push immediately or batch with v3.2 other phase work**: per CLAUDE.md push policy "Push is autonomous", but for milestone scoping clarity recommend push after each phase. Planner can include push as task step or split as user-decision checkpoint.

### Folded Todos

None (`/gsd-add-todo` not used).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents (researcher, planner, executor) MUST read these before planning or implementing.**

### Bug surface (the 3 sites)

- `tests/contract/cases/test_chunk_type_contract.py` L139-156 — `@pytest.mark.parametrize("runtime_name", ADR_V2_025_ACTIVE_RUNTIMES)` + body uses `runtime_name` at L142, L162, L170, L172, L247, L252, L256
- `tests/contract/cases/test_hook_event_contract.py` L203-217 — `test_subagent_start_envelope_live`
- `tests/contract/cases/test_hook_event_contract.py` L238+ — `test_task_checkpoint_envelope_live`
- `tests/contract/cases/test_chunk_type_contract.py` L50 — `ADR_V2_025_ACTIVE_RUNTIMES` constant (DO NOT modify)

### Conflict source

- `tests/contract/conftest.py` L113 — `@pytest.fixture(scope="session") def runtime_name(request) -> str` reads `--runtime=<X>` CLI option, returns ONE value per pytest session
- `tests/contract/conftest.py` L241 — `runtime_config` fixture (session-scoped) depends on `runtime_name` — session-scoped fixture can ONLY depend on session-scoped (or higher) fixtures, not function-scoped parametrize → ScopeMismatch error

### ADR governance

- `docs/design/EAASP/adrs/ADR-V2-025-l1-runtime-contract-tier-strategy.md` — tier matrix that justifies parametrize pattern (4 tiers × 7 runtime); DO NOT modify in 6.0
- `docs/design/EAASP/adrs/ADR-V2-017-l1-runtime-ecosystem-strategy.md` — 7-runtime ecosystem; DO NOT modify in 6.0

### CI workflow + LEDGER

- `.github/workflows/phase3-contract.yml` (likely path; planner verifies) — Phase 3 Contract Matrix workflow definition
- `docs/design/EAASP/DEFERRED_LEDGER.md` L503+ — NEW-X4 row to close after CI green
- `gh run list --workflow="Phase 3 Contract Matrix"` — current CI status (RED since 2026-05-04)

### v3.2 milestone context

- `.planning/REQUIREMENTS.md` §A CI-01 — wording stretch needed (D-03)
- `.planning/ROADMAP.md` Phase 6.0 — SC#1-3 + Coverage table
- `.planning/STATE.md` — Current Position = Phase 6.0 ready
- `.planning/phases/05.5-interface-adr-milestone-close/05.5-CONTEXT.md` — precedent for "scope wording stretch after scout reveals broader surface"

### Project rules (CLAUDE.md / MEMORY.md)

- `CLAUDE.md` § Behavioral Rules — never run `pytest tests/contract/ -v` autonomously without `--runtime=X` (would try all runtimes sequentially)
- MEMORY `feedback_no_full_tests` — phase gate uses targeted tests
- MEMORY `feedback_check_ci_after_push` — `gh run list` scan post-push, file NEW-X row if FAIL pre-existing

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **Phase 5.4 W0-spike pattern** — not reused here (D-04 rejects spike approach), but precedent for "small verifier task before bulk work"
- **Phase 5.5 NEW-A3 LEDGER row-close pattern** — analog: surgical fix + LEDGER ✅ CLOSED with commit hash + verify URL
- **`gh run list --workflow="X" --json`** — already used in Phase 5.5 post-push CI scan, executor familiar

### Established Patterns

- **`@pytest.fixture(scope="session")`** dominates in `tests/contract/conftest.py` for performance (subprocess + grpc + mock-llm launch once per pytest session) — preserve this; the bug is at the parametrize layer, not the fixture layer
- **CI matrix one-runtime-per-job** via `--runtime=<X>` CLI option — the parametrize was always intended to skip 6/7, run 1/7 per job; rename keeps this contract
- **ADR-V2-025 tier strategy** — defines which runtimes are "active" (vs frozen hermes); planner must NOT alter the list

### Integration Points

- `.github/workflows/phase3-contract.yml` (likely) — workflow drives the matrix; no edit needed (the bug is in the test, not the workflow)
- Local pytest invocation pattern: `cd tests/contract && uv run pytest <case-file> --runtime=<X>` — CI mirrors this per job

</code_context>

<specifics>
## Specific Ideas

- **Rename target = `expected_runtime`** (not `target_runtime` or `runtime_id`) — captures intent "this is the runtime ID we expect the CLI option to match" without colliding with other naming in the file (`runtime_id` is used at L189 as a parameter to `Session.create`).
- **Error message wording** must update too — `f"parametrized runtime={runtime_name!r}..."` → `f"parametrized expected_runtime={expected_runtime!r}..."` so debugging output is consistent with the new var name.
- **Inventory check via collect-only** — `pytest --collect-only` output should still show 7 parametrize IDs per test (chunk_type 7 + subagent 7 + task_checkpoint 7 = 21 IDs total), confirming the "inventory visible at --collect-only" docstring promise is preserved.
- **CI win condition wording** in SUMMARY.md should match SC#2 exactly: "Phase 3 Contract Matrix workflow ≥4 of 7 jobs PASS (per ADR-V2-025 tier strategy allows reference-tier runtimes to XFAIL, but no ScopeMismatch errors)".

</specifics>

<deferred>
## Deferred Ideas

- **Hybrid GA1 approach (separate `test_*_inventory()` function)** — rejected here for scope reasons; revisit in v3.3+ if `--collect-only` inventory pattern proves insufficient
- **7-runtime local full matrix infrastructure** — would need `make verify-contract-matrix` target + 7-runtime parallel subprocess orchestration. Defer to v3.3+ if CI cycle pain motivates it.
- **Other potential fixture-scope conflicts in `tests/contract/conftest.py`** — D-06 scope-creep guard says don't survey; if 6.1 or 6.2 work surfaces another conflict, file NEW-X5 LEDGER row and address in v3.3+
- **`tests/contract/conftest.py` general refactor** — file is 650+ LOC, mixed concerns (proto stub injection, CLI options, fixtures, helpers). Out of scope; revisit if v3.3+ TRIAGE-03 INBOX surfaces module under test infra needing cleanup

### Reviewed Todos (not folded)

(None — `/gsd-add-todo` not used in v3.2.)

</deferred>

---

*Phase: 06.0-ci-red-clearance*
*Context gathered: 2026-05-23*
