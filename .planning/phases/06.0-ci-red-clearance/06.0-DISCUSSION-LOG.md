# Phase 6.0: CI Red Clearance - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-23
**Phase:** 6.0 — CI Red Clearance
**Areas discussed:** GA1 (fix shape), GA2 (site scope), GA3 (verify depth), GA4 (plan structure + scope guard)

---

## GA1: Fix shape

| Option | Description | Selected |
|---|---|---|
| A. Rename param `runtime_name` → `expected_runtime` | Preserve --collect-only 7-runtime inventory; rename local var + error msg strings; ~5-8 LOC × 3 sites | ✓ |
| B. Delete parametrize | Shorter (~3 LOC removed per site) but loses inventory and breaks documented design intent | |
| C. Change fixture scope to function | ~5-10× slowdown (subprocess + grpc + mock-llm per test instead of per session); dominated | |
| Hybrid — A + separate inventory test | Rename + extract `test_*_inventory()` for explicit inventory; scope creep | |

**User's choice:** A.
**Notes:** Hybrid 留 v3.3+ 如果 --collect-only 模式不够。

---

## GA2: Site scope

Real surface: 3 sites in 2 files (REQUIREMENTS CI-01 字面只提 1 site).

| Option | Description | Selected |
|---|---|---|
| 3 site 全修 + REQUIREMENTS CI-01 字面 stretch | SC#2 要求 workflow ≥4/7 PASS, hook_event 2 site 不修就达不成 | ✓ |
| 1 site 修 + file NEW-X5 defer | 严守 CI-01 字面但 CI 仍 RED, SC#2 失败 | |
| 全修 + grep sweep 全仓 | 防 4th site 漏网, 多一个验证 task | |

**User's choice:** 3 site 全修 + REQUIREMENTS CI-01 字面 stretch.
**Notes:** Plan-phase 包含 REQUIREMENTS.md CI-01 wording edit (与 ROADMAP SC#3 wording stretch precedent一致).

---

## GA3: Verify depth

| Option | Description | Selected |
|---|---|---|
| (a) 本地 1-runtime (grid) + collect-only + CI 签收 7 | ~5-10 min 本地 + push 后 CI 验 | ✓ |
| (b) 本地 7-runtime spike | ~30-45 min cold cache; triage milestone overkill | |
| (a) + W0 spike 验证 fix 可行 | 5.4 W0-03 模式; 但 fix 太简单不需要 | |

**User's choice:** (a) 本地 1 runtime + CI 签收.
**Notes:** Specific commands locked in CONTEXT D-04.

---

## GA4: Plan structure + scope-creep guard

| Option | Description | Selected |
|---|---|---|
| 1 plan + 严格 scope guard | ~5-6 task; CONTEXT NOT-in-scope list 锁死扩张方向 | ✓ |
| 1 plan + 顺手 conftest review | 防御性 read 一遍; 容易诱惑手痒 | |
| Split 2 plan (fix / wait-CI) | phase 本身小, 过重 | |

**User's choice:** 1 plan + 严格 scope-creep 限定.

---

## the agent's Discretion

- Exact rename body var updates (sed vs Edit chain)
- REQUIREMENTS.md CI-01 + ROADMAP SC#1 exact wording stretch
- CI URL capture mechanism
- `gh run watch` vs poll-based wait
- Push timing (immediate vs batch)

## Deferred Ideas

- Hybrid GA1 with separate inventory test (v3.3+)
- 7-runtime local matrix infrastructure (v3.3+)
- Other potential fixture-scope conflicts (v3.3+ if surfaced)
- `tests/contract/conftest.py` general refactor (v3.3+)
