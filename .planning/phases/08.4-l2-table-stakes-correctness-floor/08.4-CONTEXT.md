# Phase 8.4: L2 Table Stakes (Correctness Floor) - Context

**Gathered:** 2026-06-10
**Status:** Ready for planning

<domain>
## Phase Boundary

8 mechanical P3 correctness fixes for L2 memory engine (`tools/eaasp-l2-memory-engine/`). All 🧹/🔵 — fresh implementation. ~75 LOC total. No architecture decisions needed.

</domain>

<decisions>
## Implementation Decisions

### All mechanical — no decisions needed

- **D14**: `_row_to_memory` private → `row_to_memory` public. ~5 LOC rename.
- **D15**: Add `[tool.ruff]` + `[tool.mypy]` to pyproject.toml. Copy L3 config pattern. ~15 LOC.
- **D59**: Makefile `mcp-orch-start` port from `L2_MCP_ORCH_PORT` env var (not hardcoded 8082). ~5 LOC.
- **D92**: `MockEmbedding.seed` widen from 64-bit → 32-byte digest (`hashlib.sha256(text[:256])`). ~5 LOC.
- **D96**: `memory_id` HNSW key use `rsplit(":v", 1)` not `split(":v")` (handles versioned IDs like `doc:v1.2`). ~3 LOC.
- **D97**: `HybridIndex` weights=(0,0) → `logger.warning("Both weights are zero...")`. ~2 LOC.
- **D99**: MCP dispatcher `int()`/`float()` wrap in try/except → `ToolError("invalid_arg")`. ~20 LOC.
- **D101**: FastAPI `HTTPException(detail=dict)` nesting → response shape matches doc contract. ~20 LOC.

### the agent's Discretion
- Plan structure (single plan with all 8 items, or split into 2 parallel plans)
- Test strategy (existing L2 pytest suite is the verification gate)
</decisions>

<canonical_refs>
## Canonical References

- `docs/design/EAASP/DEFERRED_LEDGER.md` — SSOT for D-row status
- `tools/eaasp-l2-memory-engine/pyproject.toml` — D15 target
- `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/` — D14/D96/D97/D99/D101 targets
- `tools/eaasp-l2-memory-engine/Makefile` — D59 target
- `tools/eaasp-l2-memory-engine/tests/` — D92 test fixture target
- `.planning/ROADMAP.md` — Phase 8.4 success criteria
- `.planning/REQUIREMENTS.md` — L2-09..16 REQ-IDs

</canonical_refs>

<deferred>None</deferred>

*Context gathered: 2026-06-10*
