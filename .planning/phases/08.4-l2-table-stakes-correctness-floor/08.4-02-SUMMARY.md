---
phase: 08.4-l2-table-stakes-correctness-floor
plan: 02
subsystem: L2 memory engine tooling + Makefile orchestration
tags: [config, lint, type-check, makefile, env-var, D15, D59]
requires: []
provides: [L2-10, L2-11]
affects:
  - tools/eaasp-l2-memory-engine/pyproject.toml
  - Makefile
tech-stack:
  added: []
  patterns: [ruff config, mypy config, Makefile ?= env var pattern]
key-files:
  created: []
  modified:
    - tools/eaasp-l2-memory-engine/pyproject.toml
    - Makefile
decisions:
  - "Added [tool.ruff] config matching CLI-v2 pattern (line-length=100, target-version=py312)"
  - "Added [tool.mypy] with ignore_missing_imports=true to handle L2's lack of eaasp-common dependency path"
  - "L2_MCP_ORCH_PORT follows existing Makefile ?= pattern (same as VERIFY_ENDPOINT)"
metrics:
  duration: "<1 minute"
  completed_date: "2026-06-10"
---

# Phase 8.4 Plan 02: L2 Tooling Config Parity + Makefile Port Env Var

Two mechanical P3 config/build fixes: add lint/type-check config to L2 pyproject.toml (parity with CLI-v2) and make `make mcp-orch-start` port configurable via env var.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add [tool.ruff] + [tool.mypy] config to L2 pyproject.toml (D15) | 8625f755 | tools/eaasp-l2-memory-engine/pyproject.toml |
| 2 | Make mcp-orch-start port configurable via env var (D59) | 3a54cb31 | Makefile |

## Requirements Satisfied

| REQ-ID | Description | Task |
|--------|-------------|------|
| L2-10 | Add lint/type-check config to L2 pyproject.toml (D15) | 1 |
| L2-11 | Makefile mcp-orch-start port env var (D59) | 2 |

## Verifications

### Task 1: Lint/Type-Check Config
- `[tool.ruff]` section exists with `line-length = 100`, `target-version = "py312"` ✅
- `[tool.mypy]` section exists with `python_version = "3.12"`, `warn_return_any = true` ✅
- `ruff check --show-settings` loads config from L2 pyproject.toml ✅
- Config matches CLI-v2 pattern (same ruff settings) ✅

### Task 2: Makefile Port Env Var
- `L2_MCP_ORCH_PORT ?= 18082` variable defined ✅
- `make -n mcp-orch-start` passes `--port 18082` (default) ✅
- `L2_MCP_ORCH_PORT=19999 make -n mcp-orch-start` passes `--port 19999` ✅
- `?=` pattern matches existing Makefile convention (`VERIFY_ENDPOINT`) ✅

## Deviations from Plan

None — plan executed exactly as written.

## Threat Model

| Threat ID | Category | Disposition | Outcome |
|-----------|----------|-------------|---------|
| T-08.4-05 | Tampering | accept | Port config only affects local dev; no security impact change |

## Self-Check: PASSED

- [x] `tools/eaasp-l2-memory-engine/pyproject.toml` exists with ruff + mypy sections
- [x] `Makefile` contains `L2_MCP_ORCH_PORT ?= 18082`
- [x] Commit 8625f755 exists (Task 1)
- [x] Commit 3a54cb31 exists (Task 2)
