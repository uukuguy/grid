---
phase: 05.4-server-hardening
plan: 02
subsystem: grid-server + grid-engine + grid-runtime + claude-code-runtime-python
tags: [sqlite-migrate, atomic-transaction, provider-config, fail-fast, auth-hmac, audit-log, rate-limit, hot-reload, deployment-mode, eaasp-env, adr-governance]
status: complete
completed: 2026-05-21
duration_min: 671  # 11h11m, but ~6h of active execution wrapping in/out of orchestrator handoffs

dependency_graph:
  requires:
    - 05.4-01 — SERVER-01/02/03 backbone (ws_chunk-native envelope, in-process verification, grid-engine::l2 module + Stop hook → L2 write)
    - rusqlite 0.32 BEGIN EXCLUSIVE writer-lock semantics
    - subtle 2.x ConstantTimeEq (HMAC compare)
    - hmac 0.12 + sha2 0.10 (HMAC-SHA256)
  provides:
    - grid_engine::db::migrate() — atomic under multi-process concurrent invocation (BEGIN EXCLUSIVE + double-check user_version)
    - grid_engine::providers::config::ProviderConfig::try_from_env — strict env loader returning Err(ConfigError::MissingEnv); production callers use this path
    - grid_server middleware/auth — 3 modes (None / ApiKey / Full+HMAC), ±5min replay window, constant-time compare
    - grid_server middleware/audit — request_id column, per-key rate-limit gate
    - grid_server POST /api/v1/admin/reload — 4 reloadable fields + RESTART_REQUIRED_FIELDS → 422
    - grid-runtime + claude-code-runtime-python — EAASP_DEPLOYMENT_MODE env wire + per_session gate (ADR-V2-019 §D2)
    - ADR-V2-028 (Strict-by-default Config Validation) — Accepted, new contract ADR
    - ADR-V2-019 enforcement.trace — filled with 4 implementation anchors (status untouched)
    - DEFERRED_LEDGER 5-row close (D142, D143, NEW-A2, NEW-E3, NEW-F3) per Phase 4.0 CLEANUP-02 row-edit convention
  affects:
    - .planning/STATE.md (plan-02-complete; WATCH-04 + WATCH-07 closed)
    - .planning/ROADMAP.md (Phase 5.4 progress row → 2/2 plans complete)
    - docs/design/EAASP/DEFERRED_LEDGER.md (5 rows ✅ CLOSED; last-updated timestamp moved forward)
    - docs/design/EAASP/adrs/ADR-V2-019-l1-runtime-deployment-model.md (enforcement.trace filled)
    - docs/design/EAASP/adrs/ADR-V2-028-strict-config-validation.md (new Accepted ADR)

tech_stack:
  added:
    - subtle 2.x (constant-time HMAC compare)
    - hmac 0.12 + sha2 0.10 (HMAC-SHA256 signature path)
  patterns:
    - "BEGIN EXCLUSIVE + double-check transaction pattern for SQLite multi-writer atomicity (rusqlite execute_batch + PRAGMA user_version read-after-lock)"
    - "Strict env loader: try_from_env returns Err(ConfigError::MissingEnv); silent-fallback Default impl retained only as test fixture (ADR-V2-028)"
    - "3-mode auth ladder (None / ApiKey / Full+HMAC) — explicit mode enum with constant-time compare via subtle crate, ±5min timestamp window for replay protection"
    - "POST /admin/reload with field-allowlist + RESTART_REQUIRED_FIELDS reject — explicit 422 + structured error body, no silent ignore"
    - "EAASP_DEPLOYMENT_MODE per-session admission gate at handler entry — returns RESOURCE_EXHAUSTED on 2nd session attempt under per_session mode"
    - "Multi-process race regression test via test-only [[bin]] migrate-tester spawned by integration test (4 concurrent processes against same db file)"

key_files:
  created:
    - crates/grid-engine/tests/bin/migrate_tester.rs
    - crates/grid-server/tests/migrate_race.rs
    - crates/grid-server/tests/test_auth_modes.rs
    - crates/grid-server/tests/test_audit_and_rate_limit.rs
    - crates/grid-server/tests/test_hot_reload.rs
    - crates/grid-runtime/tests/test_deployment_mode_gate.rs
    - lang/claude-code-runtime-python/tests/test_deployment_mode.py
    - docs/design/EAASP/adrs/ADR-V2-028-strict-config-validation.md
    - .planning/phases/05.4-server-hardening/deferred-items.md
  modified:
    - crates/grid-engine/src/db/mod.rs (migrate() → BEGIN EXCLUSIVE + double-check user_version)
    - crates/grid-engine/Cargo.toml ([[bin]] migrate-tester registration)
    - crates/grid-engine/src/providers/config.rs (+try_from_env strict API + ConfigError enum)
    - crates/grid-server/src/middleware/auth.rs (3-mode + HMAC + constant-time compare)
    - crates/grid-server/src/middleware/audit.rs (+request_id column + per-key rate-limit)
    - crates/grid-server/src/config.rs (+RuntimeOverrides reloadable surface)
    - crates/grid-server/src/state.rs (+runtime_overrides RwLock; +audit_storage wiring)
    - crates/grid-server/src/api/admin.rs (POST /admin/reload + RESTART_REQUIRED_FIELDS)
    - crates/grid-server/src/api/mod.rs (admin route registration)
    - crates/grid-server/src/router.rs (audit + rate-limit middleware insertion)
    - crates/grid-server/Cargo.toml (+subtle, +hmac, +sha2; +chrono dev-dep for timestamp tests)
    - crates/grid-runtime/src/config.rs (+deployment_mode field reading EAASP_DEPLOYMENT_MODE)
    - crates/grid-runtime/src/service.rs (RuntimeServiceImpl::create_session per_session gate)
    - lang/claude-code-runtime-python/src/claude_code_runtime/service.py (_deployment_mode + Initialize gate)
    - docs/design/EAASP/adrs/ADR-V2-019-l1-runtime-deployment-model.md (enforcement.trace filled, 4 anchors)
    - docs/design/EAASP/DEFERRED_LEDGER.md (5-row close + last-updated 2026-05-21)
    - Cargo.lock (HMAC stack fold-in)

decisions:
  - "T-02 mitigation: migrate() uses execute_batch(\"BEGIN EXCLUSIVE\") not BEGIN IMMEDIATE — EXCLUSIVE guarantees no concurrent reader during DDL execution, double-check pattern (read user_version inside txn) handles the case where another process committed migrations after the first PRAGMA read but before this writer acquired the lock. ASVS V14.1.5."
  - "T-04 mitigation: ProviderConfig::try_from_env returns Err(ConfigError::MissingEnv); the silent-fallback Default impl is retained only as test fixture, NOT used by production binaries. ADR-V2-028 formalizes the policy. ASVS V6.4.2."
  - "T-05 mitigation: auth Full mode uses subtle::ConstantTimeEq for HMAC comparison (timing-attack resistance per ASVS V2.10), 5-minute timestamp window (±300s clock skew) for replay protection per ASVS V13.1.3. Audit log captures user_id + request_id for forensics."
  - "T-06 mitigation: POST /admin/reload uses explicit RESTART_REQUIRED_FIELDS const array; HOST/PORT/AUTH_MODE/API_KEY etc. return HTTP 422 with structured error body {\"error\":\"FIELD_REQUIRES_RESTART\",\"field\":<name>,\"message\":...} — no silent ignore. ASVS V8.3.4."
  - "T-07 mitigation: both grid-runtime (Rust) + claude-code-runtime-python (Python) read EAASP_DEPLOYMENT_MODE env at startup, store on service instance, and gate at handler entry (CreateSession / Initialize). Returns Status::resource_exhausted / RESOURCE_EXHAUSTED on 2nd session attempt under per_session mode. ADR-V2-019 enforcement.trace (Task 10) anchors the 4 implementation points."
  - "WATCH-07 / Q9 correction: ADR-V2-019 was Accepted 2026-04-20 — Task 10 fills the empty enforcement.trace array with implementation anchors. status: Accepted UNCHANGED, accepted_at: 2026-04-20 UNCHANGED. This is a trace fill, NOT a status flip."
  - "NEW-A2 row-creation policy: NEW-A2 and NEW-E3 did not have existing rows in DEFERRED_LEDGER.md (Phase 4a session-04-26 tracked them in STATE.md only). Task 11 added the rows under a new NEW-A / NEW-E namespace subsection with full Resolution body — preserves row-edit-on-close convention by adding-then-closing in a single commit rather than retroactively backfilling history."
  - "Pre-existing test drift NOT in scope: api_security_policy::security_policy_update_returns_not_implemented expects HTTP 501 but impl returns 200 — drift introduced at Phase BA (9432e1e) rename, never touched in Phase 5.4. Logged to deferred-items.md, gate command run with --skip flag. Per CLAUDE.md scope boundary rule, not auto-fixed."

metrics:
  duration_min: 671  # see Performance for breakdown
  tasks_completed: 12  # 5.4-02-01 through 5.4-02-12
  files_created: 9
  files_modified: 17
  tests_added: 31  # 4 migrate + 5 provider config + 6 auth + 3 audit + 2 rate-limit + 7 hot-reload + 2 grid-runtime + 2 claude-code-runtime
  tests_passing: 31
  commits: 12  # tasks 1-11 each one commit (10 task commits + 1 Cargo.lock fold-in + 1 task-12 phase-gate-only-no-commit = 12 source commits; Task 12 commits ONLY the SUMMARY/STATE/ROADMAP triplet which is the +1 closure commit)

requirements_completed:
  - SERVER-04
  - SERVER-05
  - WATCH-04
  - WATCH-07
---

# Phase 5.4 Plan 02: NEW-A2 + NEW-F3 + SERVER-04 + SERVER-05 + WATCH-04 + WATCH-07 + ADR-V2-028 Summary

**Atomic migrate() via BEGIN EXCLUSIVE + double-check, strict ProviderConfig fail-fast via ADR-V2-028, 3-mode auth ladder with HMAC + audit + rate-limit, /admin/reload with RESTART_REQUIRED_FIELDS gate, and EAASP_DEPLOYMENT_MODE wiring on both Rust and Python runtimes — 5 STRIDE threats mitigated + 5 deferred items closed.**

## Performance

- **Duration:** 11h 11m (wall-clock) / ~6h active execution
- **Started:** 2026-05-21T06:16:08Z (commit `74e6472` — first task commit)
- **Completed:** 2026-05-21T17:26:38Z (commit `279b639` — task-11 ledger close)
- **Tasks:** 12 (5.4-02-01 through 5.4-02-12)
- **Files created:** 9
- **Files modified:** 17
- **Tests added:** 31 (all PASS in release-mode 3-crate gate)

## Accomplishments

- **NEW-A2 closed**: `migrate()` in `crates/grid-engine/src/db/mod.rs` made atomic via `BEGIN EXCLUSIVE` writer-lock + double-check `user_version` read-after-lock; 4-process concurrent regression test (`crates/grid-server/tests/migrate_race.rs`) PASSES against the same db file with no `duplicate column name: user_id` panic and final user_version converging to 13.
- **NEW-F3 closed**: `ProviderConfig::try_from_env()` strict API added with `ConfigError::MissingEnv` variant; production code paths in `grid-server` + `grid-runtime` entrypoints route through this strict loader, silent-fallback `Default` impl retained only as test fixture.
- **ADR-V2-028 Accepted**: Strict-by-default Config Validation — new contract ADR formalizes the no-fallback policy with trace anchors at `crates/grid-engine/src/providers/config.rs` + `crates/grid-runtime/src/config.rs` + `crates/grid-server/src/config.rs`.
- **SERVER-04 hardened**: 3 actual auth modes (None / ApiKey / Full+HMAC) replace the previous documented-but-not-implemented 2-mode setup; HMAC-SHA256 signature over `timestamp + body` with ±5min replay window + constant-time compare via `subtle::ConstantTimeEq`; audit log columns `user_id` + `request_id`; per-key rate-limit middleware.
- **SERVER-05 implemented**: `POST /api/v1/admin/reload` accepts 4 reloadable fields, returns HTTP 422 with structured error body for `RESTART_REQUIRED_FIELDS` (HOST/PORT/AUTH_MODE/API_KEY) — no silent ignore.
- **WATCH-04 closed (D142+D143)**: Both `grid-runtime` (Rust) and `claude-code-runtime-python` (Python) now read `EAASP_DEPLOYMENT_MODE` env and gate `CreateSession` / `Initialize` with `RESOURCE_EXHAUSTED` on 2nd session under `per_session` mode.
- **WATCH-07 closed (NEW-E3)**: ADR-V2-019 `enforcement.trace` array filled with 4 implementation anchors pointing at D142+D143 closures — status `Accepted` UNCHANGED, this is a trace fill not a status flip (per Q9 correction).
- **5-row LEDGER close**: D142, D143, NEW-A2, NEW-E3, NEW-F3 marked `✅ CLOSED` in `docs/design/EAASP/DEFERRED_LEDGER.md` per Phase 4.0 CLEANUP-02 row-edit-on-close convention; NEW-A2 + NEW-E3 rows added under a new NEW-A / NEW-E namespace subsection with full Resolution bodies.

## Task Commits

Each task committed atomically (in chronological order, all on `main`):

1. **Task 5.4-02-01: atomic migrate() via BEGIN EXCLUSIVE + double-check (NEW-A2)** — `74e6472` (fix)
2. **Task 5.4-02-02: 4-process migrate() race regression test (NEW-A2)** — `bf26cb8` (test)
3. **Task 5.4-02-03: ProviderConfig::try_from_env strict API (NEW-F3)** — `9007935` (feat)
4. **Task 5.4-02-04: SERVER-04 auth — 3 modes + HMAC signature (T-05)** — `83f594c` (feat)
5. **Task 5.4-02-05: SERVER-04 audit request_id column + per-key rate-limit (SC#4)** — `e757f9e` (feat)
6. **Task 5.4-02-06: SERVER-05 POST /admin/reload hot-reload endpoint (T-06)** — `d9aa08c` (feat)
7. **Task 5.4-02-07: WATCH-04 D142 — grid-runtime EAASP_DEPLOYMENT_MODE per-session gate** — `d12f6ec` (feat)
8. **Task 5.4-02-08: WATCH-04 D143 — claude-code-runtime EAASP_DEPLOYMENT_MODE per-session gate** — `2453447` (feat)
9. **Task 5.4-02-09: ADR-V2-028 Strict-by-default Config Validation Accepted** — `5b5c892` (docs)
10. **(side) Cargo.lock fold-in for HMAC stack** — `5542ff0` (chore)
11. **Task 5.4-02-10: ADR-V2-019 enforcement.trace filled (WATCH-07 close)** — `70b5e94` (docs)
12. **Task 5.4-02-11: DEFERRED_LEDGER 5-row close** — `279b639` (docs)
13. **Task 5.4-02-12: Phase gate** — verification-only, no source edits — see Phase Gate Run section below.

**Plan closure commit:** SUMMARY.md + STATE.md + ROADMAP.md (this commit, single atomic).

## Phase Gate Run

Per VALIDATION.md GATE row + Task 5.4-02-12 spec, the following 3 gate steps were executed:

### Step 1: Full release-build test sweep (3 crates)

```bash
cargo test -p grid-server -p grid-engine -p grid-runtime --features grid-server/testing --release -- --test-threads=1 \
  --skip security_policy_update_returns_not_implemented
```

- **Result**: ✅ PASS — **2698 passed / 0 failed / 6 ignored** across the 3 crates
- **Note 1 (Rule 1 — Blocking fix)**: Initial invocation FAILED at `tests/ws_chunk_envelope.rs` with E0599 `method not found: test_inject_events`. Root cause: `test_inject_events` is gated on `cfg(any(test, feature = "testing"))` (per Plan 01 W3 plan-checker requirement) — integration tests under `tests/` link the lib WITHOUT `cfg(test)`, requiring `--features grid-server/testing` to be passed. Plan 01 SUMMARY line 187-189 documents this. VALIDATION.md and RESEARCH.md §9.9 gate spec omitted the feature flag — Phase 5.4-02 gate command updated to include it. Pre-existing finding from Plan 01, not Phase 5.4-02 work.
- **Note 2 (out-of-scope)**: `security_policy_update_returns_not_implemented` expects HTTP 501 but impl returns 200 — pre-existing drift from Phase BA rename (9432e1e), never touched in Phase 5.4. Logged to `.planning/phases/05.4-server-hardening/deferred-items.md`. Not auto-fixed per CLAUDE.md scope boundary rule.

### Step 2: Full ADR audit

```bash
python3 .adr-plugin/scripts/adr_lint.py --ci
```

- **Result**: ✅ PASS — **164 PASS / 0 FAIL / 105 WARN** (advisory only)
- **Exit code**: 0
- **F1-F2 status**: PASS for all ADRs including newly-edited V2-019 + newly-Accepted V2-028.
- **F3 WARN**: 105 advisory warnings about trace files not appearing in CI workflow include lists — expected for ADRs not gated by CI (e.g., V2-028 is policy-level, not runtime-gated). Not a blocker per CLAUDE.md "F3 advisory WARN, exit 0".

### Step 3: Schema coverage grep

```bash
TMPDB=$(mktemp); cargo run --release -p grid-engine --bin migrate-tester -- "$TMPDB"
sqlite3 "$TMPDB" ".schema" | grep -E 'CREATE TABLE.*(sessions|turns)\b'
```

- **Result**: ✅ PASS
- **Output**: `user_version=13` (current schema version); schema dump shows both `CREATE TABLE sessions (` and `CREATE TABLE turns (` lines.
- **Conclusion**: SC#3 schema-coverage clause demonstrably satisfied; migrate() lands a coherent schema with both backbone tables.

## Files Created/Modified

### Created (9)

- `crates/grid-engine/tests/bin/migrate_tester.rs` — test-only `[[bin]]` spawned by integration test for multi-process race exercise
- `crates/grid-server/tests/migrate_race.rs` — 4-process concurrent migrate() regression test
- `crates/grid-server/tests/test_auth_modes.rs` — 3-mode auth + HMAC + audit + rate-limit integration tests
- `crates/grid-server/tests/test_audit_and_rate_limit.rs` — audit columns + per-key rate-limit assertion tests
- `crates/grid-server/tests/test_hot_reload.rs` — 4 reloadable + ≥4 restart-required field integration tests
- `crates/grid-runtime/tests/test_deployment_mode_gate.rs` — per-session gate tests (Rust)
- `lang/claude-code-runtime-python/tests/test_deployment_mode.py` — per-session gate tests (Python pytest)
- `docs/design/EAASP/adrs/ADR-V2-028-strict-config-validation.md` — Accepted contract ADR
- `.planning/phases/05.4-server-hardening/deferred-items.md` — pre-existing test drift log

### Modified (17)

See key_files frontmatter — full list with one-line "what changed" per file.

## Decisions Made

See `decisions` frontmatter for the 8 key decisions with rationale. Highlights:

- **Q9 correction (ADR-V2-019)**: Task 10 is a trace fill, NOT a status flip. Status `Accepted` and `accepted_at: 2026-04-20` UNCHANGED.
- **NEW-A2 + NEW-E3 row creation**: These items were tracked in STATE.md but never folded into DEFERRED_LEDGER.md. Task 11 added rows under a new NEW-A / NEW-E namespace subsection — preserves row-edit-on-close convention by adding and closing in a single commit.
- **Pre-existing test drift NOT in scope**: `security_policy_update_returns_not_implemented` failure existed since Phase BA — logged to deferred-items.md, gate command run with --skip flag.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Blocking] `--features grid-server/testing` flag added to Phase Gate command**

- **Found during**: Task 5.4-02-12 (Phase Gate, Step 1 first invocation)
- **Issue**: VALIDATION.md and RESEARCH.md §9.9 gate spec wrote `cargo test -p grid-server -p grid-engine -p grid-runtime --release -- --test-threads=1` without the feature flag. The `test_inject_events` backdoor (Plan 01 W3 cfg-gate) is invisible to integration tests under `tests/` without `--features grid-server/testing`, causing 8 E0599 errors compiling `ws_chunk_envelope.rs`.
- **Fix**: Re-ran gate with `--features grid-server/testing` appended. Documented in this SUMMARY's Phase Gate Run section so future Phase 5.4+ gate invocations don't repeat the mistake. Plan 01 SUMMARY line 187-189 already documents this requirement at integration-test invocation level; the omission is in the phase-level gate spec, not the per-task verify commands.
- **Files modified**: None (command-line argument only)
- **Verification**: Re-run produced 2698 PASS / 0 FAIL output
- **Committed in**: N/A (verification-step adjustment, not a source edit)

### Out-of-Scope Items Logged

**1. Pre-existing test drift: `security_policy_update_returns_not_implemented`**

- **Found during**: Task 5.4-02-12 (Phase Gate Step 1)
- **Determination**: NOT introduced by Phase 5.4 — drift exists since Phase BA grid-rename (`9432e1e`). `crates/grid-server/src/api/security.rs` and `crates/grid-server/tests/api_security_policy.rs` were not touched by any Plan 5.4-01 or 5.4-02 commit.
- **Action**: Logged to `.planning/phases/05.4-server-hardening/deferred-items.md` with full root-cause analysis + recommended fix (5 LOC test update). Gate command run with `--skip` flag.
- **Per CLAUDE.md scope boundary**: pre-existing failures are NOT auto-fixed during plan execution; logged for follow-up.

---

**Total deviations**: 1 auto-fixed (Rule 1 — blocking gate command fix). 1 out-of-scope item logged.
**Impact on plan**: Both adjustments necessary for gate execution. No scope creep — Phase 5.4 source code changes match the plan exactly.

## Issues Encountered

- **Cargo.lock fold-in (`5542ff0`)**: Task 5.4-02-04 added `subtle`, `hmac`, `sha2` to grid-server `Cargo.toml`; the lockfile update was committed separately to keep the SERVER-04 source diff focused on auth code. Standard pattern.
- **None blocking** — all 12 tasks completed sequentially without architectural escalation.

## Threat Mitigation Summary (from Plan threat_model)

| Threat | Status | Mitigation |
|--------|--------|------------|
| T-02 (DB concurrent migration race) | ✅ mitigated | Task 5.4-02-01 BEGIN EXCLUSIVE + double-check; Task 5.4-02-02 4-process regression test. ASVS V14.1.5. |
| T-04 (Silent fallback → wrong provider) | ✅ mitigated | Task 5.4-02-03 try_from_env strict API; Task 5.4-02-09 ADR-V2-028 Accepted. ASVS V6.4.2. |
| T-05 (Auth replay + timing attack) | ✅ mitigated | Task 5.4-02-04 ±5min window + HMAC-SHA256 + constant-time compare; Task 5.4-02-05 audit log. ASVS V2.10 + V13.1.3. |
| T-06 (Hot-reload silently ignoring restart-required fields) | ✅ mitigated | Task 5.4-02-06 RESTART_REQUIRED_FIELDS + 422. ASVS V8.3.4. |
| T-07 (Per-session deployment-mode bypass) | ✅ mitigated | Tasks 5.4-02-07 + 5.4-02-08 env-read + handler-entry gate; Task 5.4-02-10 ADR-V2-019 trace fill. ASVS V1.4.4. |

T-01 + T-03 were addressed in Plan 01 (per threat_model footer note).

## Self-Check: PASSED

**Created files exist** (9/9 verified):
- ✅ crates/grid-engine/tests/bin/migrate_tester.rs
- ✅ crates/grid-server/tests/migrate_race.rs
- ✅ crates/grid-server/tests/test_auth_modes.rs
- ✅ crates/grid-server/tests/test_audit_and_rate_limit.rs
- ✅ crates/grid-server/tests/test_hot_reload.rs
- ✅ crates/grid-runtime/tests/test_deployment_mode_gate.rs
- ✅ lang/claude-code-runtime-python/tests/test_deployment_mode.py
- ✅ docs/design/EAASP/adrs/ADR-V2-028-strict-config-validation.md
- ✅ .planning/phases/05.4-server-hardening/deferred-items.md

**Commits exist** (12/12 verified in `git log`):
- ✅ 74e6472, bf26cb8, 9007935, 83f594c, e757f9e, d9aa08c, d12f6ec, 2453447, 5b5c892, 5542ff0, 70b5e94, 279b639

**Phase gate** (3/3 verified): release suite 2698 PASS + ADR audit 164/0/0 exit 0 + schema grep sessions+turns PASS.

## Next Phase Readiness

- **Phase 5.4 COMPLETE**: Both Plan 01 + Plan 02 closed. ROADMAP Phase 5.4 → 2/2 plans complete.
- **WATCH-04 + WATCH-07 closed** per Phase 4a session-04-26 audit follow-ups.
- **D142 + D143 closed** in DEFERRED_LEDGER (Phase 2.5 post-ADR-V2-019 compliance debt cleared).
- **NEW-A2 + NEW-E3 + NEW-F3 closed** in DEFERRED_LEDGER (new NEW-A / NEW-E namespace subsection created).
- **ADR-V2-028 Accepted** — new contract ADR formalizing strict-by-default config validation.
- **One deferred-items.md entry** for follow-up: pre-existing `security_policy_update_returns_not_implemented` drift (5 LOC test update, P3, opportunistic Phase 5.5 cleanup).
- **Recommended next**: `/gsd-verify-work 5.4` to verify Phase 5.4 closure end-to-end, then `/gsd-discuss-phase 5.5`.

---
*Phase: 05.4-server-hardening*
*Plan: 02*
*Completed: 2026-05-21*
