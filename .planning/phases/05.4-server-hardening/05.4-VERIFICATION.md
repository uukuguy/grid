---
phase: 05.4-server-hardening
verified: 2026-05-21T00:00:00Z
status: passed
score: 5/5 success criteria + 7/7 REQ-IDs verified
overrides_applied: 0
re_verification:
  previous_status: initial
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification: []
---

# Phase 5.4 — Verification

**Date:** 2026-05-21
**Verifier:** gsd-verifier
**Verdict:** PASS

## VERIFICATION PASSED

Phase 5.4 (Server Hardening) goal is achieved in the codebase. All 5 ROADMAP success criteria PASS, all 7 REQ-IDs are wired with concrete artifacts + tests, ADR-V2-028 is Accepted, ADR-V2-019 trace is filled (status correctly UNCHANGED per Q9), and the 5-row DEFERRED_LEDGER close is in place with commit hashes. Out-of-scope items (NEW-A3, NEW-F4, grid-cli `mod output` E0583, NEW-L1) correctly remain OPEN per CONTEXT D-13.

---

### SC#1 — WebSocket ChunkType stream + simple reconnect + ordering (≥5 cases)

**ROADMAP text (post D-15 reword):** WebSocket endpoint `ws://127.0.0.1:3001/v1/sessions/{id}/stream` 支持 ChunkType stream 端到端流式输出 + backpressure (1000-msg load test 0 dropped chunks) + simple reconnect (new connection succeeds + first SessionCreated frame replay; **chunk_id resume defer 到 5.5+** per Phase 5.4 D-15) + message ordering; 集成测试 ≥5 cases PASS.

**Status:** VERIFIED

Evidence:
- `crates/grid-server/src/router.rs:267` registers `.route("/v1/sessions/{id}/stream", get(ws_handler_with_path))` — canonical WS path.
- `crates/grid-server/src/router.rs:266` keeps `.route("/ws", get(ws_handler))` as deprecation alias.
- `crates/grid-server/src/ws.rs:65-73` `ws_handler` emits `warn!("/ws WebSocket path is deprecated; use /v1/sessions/{id}/stream ...")` on legacy path; `ws_handler_with_path` (line 132) is silent.
- `crates/grid-server/src/ws_chunk.rs:48-66` defines `WsServerMessage::{Chunk(ChunkEnvelope), Control(ControlMessage)}` with wire-int `chunk_type` field (D-03 envelope). `map_event` at line 158 dispatches AgentEvent → either chunk wire 1-9 (D-03) or control message (D-04).
- `crates/grid-server/tests/ws_chunk_envelope.rs` carries 8 functions: `test_path_alias_warns_on_legacy`, `test_path_v1_native_no_warn`, `test_backpressure_1000_chunks`, `test_message_ordering_serial`, `test_ws_reconnect_simple` (these 5 satisfy SC#1) + `test_chunk_text_delta_envelope`, `test_chunk_tool_lifecycle`, `test_control_session_created_first` (SC#2).
- SC#1 reconnect explicitly scope-limited to "simple reconnect" per D-15. ROADMAP text matches: contains "chunk_id resume defer 到 5.5+".
- Plan 02 phase-gate output: 2698 PASS / 0 FAIL across all 3 crates includes these tests.

Threat T-01 (WS DoS / slow consumer) mitigated: backpressure test asserts exact 1000 wire-1 chunks AND `!logs.contains("broadcast channel lagged")` (D-A8 reinforcement).

---

### SC#2 — in-process grid-engine integration (post D-02 rewrite)

**ROADMAP text (post D-02 rewrite):** `grid-server` (`:3001`) **in-process** 调 `grid-engine` 走 Initialize + SendResponse + Terminate 三主路径 + chunk relay + hook envelope 透传; session id 在 server-side state 与 engine-side state 一致; 集成测试 `crates/grid-server/tests/ws_chunk_envelope.rs` 内 ≥3 cases PASS (per Phase 5.4 D-01/D-02 lock — gRPC subprocess path defer 到 v3.2+ EAASP-via-server).

**Status:** VERIFIED

Evidence:
- D-02 rewrite confirmed in ROADMAP line containing "**in-process**" + "≥ 3 case PASS (per Phase 5.4 D-01/D-02 lock — gRPC subprocess path defer 到 v3.2+ EAASP-via-server)".
- ROADMAP no longer contains `:50051`, `make verify-dual-runtime`, or "16-method gRPC" phrases in SC#2 (D-02 rewrite verified by grep — only Phase 5.5 references survive at the meta level).
- `crates/grid-server/src/state.rs` keeps `agent_supervisor: Arc<AgentRuntime>` + `agent_handle: AgentExecutorHandle` (in-process; no tonic-client dep added to grid-server Cargo.toml).
- ≥3 lifecycle tests PASS: `test_chunk_text_delta_envelope` (SendResponse path), `test_chunk_tool_lifecycle` (tool lifecycle on Initialize+SendResponse path), `test_control_session_created_first` (Initialize → session_created control before any chunk).

---

### SC#3 — SQLite schema + L2 Stop hook + tokio-rusqlite migration idempotent (incl. NEW-A2)

**ROADMAP text:** SQLite (`data/grid.db`) schema 新增 / 演进 covering session record + turn record + L2 memory FTS5+HNSW+time-decay 索引; Stop hook fire 时 server 写 trajectory 到 L2 memory engine 的端到端单元测试 ≥ 2 case PASS; tokio-rusqlite migration 跑通且幂等.

**Status:** VERIFIED

Evidence:
- `crates/grid-engine/src/db/migrations.rs` defines `CREATE TABLE IF NOT EXISTS sessions` AND `CREATE TABLE IF NOT EXISTS turns` (schema coverage).
- `crates/grid-engine/src/db/mod.rs:46` uses `conn.execute_batch("BEGIN EXCLUSIVE")` followed by double-check of `PRAGMA user_version` (NEW-A2 fix per ASVS V14.1.5).
- `crates/grid-server/tests/migrate_race.rs` (140 lines) spawns 4 concurrent migrate-tester processes — passes per Plan 02 SUMMARY phase-gate output.
- `crates/grid-engine/src/agent/l2_trajectory_hook.rs:32` `impl StopHook for L2TrajectoryStopHook` — fire-and-forget (returns `Ok(Noop)` per LC-5).
- L2 client extracted to `crates/grid-engine/src/l2/{mod.rs,client.rs}` (D-08); `crates/grid-runtime/src/l2_memory_client.rs` is the 7-line re-export shim.
- `crates/grid-server/tests/test_l2_stop_hook.rs` carries 3 tests: `test_stop_hook_writes_anchor_to_l2`, `test_stop_hook_writes_file_to_l2`, `test_schema_coverage_session_and_turn_tables` (≥2 SC#3 cases + 1 schema coverage proof).
- Phase-gate Step 3 schema dump confirms `user_version=13` + both `CREATE TABLE sessions` and `CREATE TABLE turns` present.

Threat T-02 (DB concurrent migration race) mitigated; T-03 (Stop hook L2 write failure) mitigated by fire-and-forget pattern.

---

### SC#4 — Auth 3-mode + HMAC + audit + rate limit

**ROADMAP text:** `GRID_AUTH_MODE` / `GRID_API_KEY` / `GRID_API_KEY_USER` / HMAC (ADR-003) 端到端验证: 4 种 auth 模式各有 ≥ 1 个集成测试覆盖 + audit log 写入 + rate limit 基础生效; config hot-reload 在 4 类 hot-reloadable 字段上.

**Status:** VERIFIED (with Q3 wording correction noted)

Evidence:
- `crates/grid-server/src/middleware/auth.rs:76-149` implements 3 AuthMode variants (None / ApiKey / Full); HMAC signature validation inline inside `ApiKey` arm at lines 99-145 (NOT a 4th variant — per Q3 correction in RESEARCH).
- `verify_hmac_signature` uses `subtle::ConstantTimeEq` for timing-resistance (ASVS V2.10) at line 30.
- ±5min replay window enforced (ASVS V13.1.3).
- `crates/grid-server/tests/test_auth_modes.rs` (244 LOC) carries 8 test functions covering: None mode pass-through, ApiKey valid 200, ApiKey invalid 401, ApiKey missing header 401, ApiKey+HMAC valid 200, ApiKey+HMAC replay-window-old 401, ApiKey+HMAC tampered-body 401, ApiKey+HMAC missing-timestamp 401 — exceeds "≥1 per mode".
- `crates/grid-server/src/middleware/audit.rs:49-128` writes `user_id` + `request_id` columns to audit log.
- `crates/grid-server/src/middleware/rate_limit.rs` exists; per-key rate-limit gate.
- `crates/grid-server/tests/test_audit_and_rate_limit.rs` (199 LOC) validates both audit columns + rate-limit behavior.
- **Q3 wording note**: ROADMAP SC#4 still reads "4 种 auth 模式" but Q3 in RESEARCH.md corrected this to 3-actual-modes (None/ApiKey/Full with HMAC as a signature layer inside ApiKey). The 3+HMAC implementation provides 4 distinct behavioral test paths; the SUMMARY documents the correction explicitly. This is a doc-vs-implementation drift in ROADMAP text only; coverage is satisfied. Not a blocker.

Threat T-05 (auth replay + timing attack) mitigated per ASVS V2.10 + V13.1.3.

---

### SC#5 — Config hot-reload + EAASP_DEPLOYMENT_MODE (WATCH-04) + ADR-V2-019 trace fill (WATCH-07)

**ROADMAP text (SC#4 second half + SC#5):** config hot-reload 在 4 类 hot-reloadable 字段上 (GRID_HOOKS_FILE / GRID_POLICIES_FILE / GRID_LOG / GRID_CORS_ORIGINS) 不重启生效, GRID_HOST / GRID_PORT 列入 require-restart 白名单且 hot-reload 触发时报清晰错误. SC#5: D142 + D143 closed + NEW-E3 closed (ADR-V2-019 status flip Proposed → Accepted, F1-F4 lint exit 0).

**Status:** VERIFIED (with WATCH-07/Q9 correction: trace fill, not status flip)

Evidence:
- `crates/grid-server/src/api/admin.rs:22` `pub const RESTART_REQUIRED_FIELDS: &[&str] = &["host", "port", "auth_mode", "api_key"]` — explicit allowlist.
- `crates/grid-server/src/api/admin.rs:46-55` returns HTTP 422 with `{"error":"FIELD_REQUIRES_RESTART","field":<name>,"message":"..."}` for restart-required fields.
- `crates/grid-server/src/api/admin.rs:83` `state.runtime_overrides.write().await` applies the 4 reloadable fields.
- `crates/grid-server/src/api/mod.rs:145` `.route("/admin/reload", post(admin::reload_config))` registers the endpoint.
- `crates/grid-server/tests/test_hot_reload.rs` carries 8 tests: 4 reloadable cases (hooks_file, policies_file, log_level, cors_origins) + 4 restart-required cases (host, port, auth_mode, api_key).
- **WATCH-04 D142 (grid-runtime)**: `crates/grid-runtime/src/config.rs:87-95` reads `EAASP_DEPLOYMENT_MODE` env mapping to `DeploymentMode::{Shared, PerSession}`; `crates/grid-runtime/src/service.rs:256` returns `Status::resource_exhausted` on per-session gate violation.
- **WATCH-04 D143 (claude-code-runtime)**: `lang/claude-code-runtime-python/src/claude_code_runtime/service.py:137` reads `EAASP_DEPLOYMENT_MODE`; line 166-169 returns `grpc.StatusCode.RESOURCE_EXHAUSTED` on `_deployment_mode == "per_session"` + already-initialized.
- `crates/grid-runtime/tests/test_deployment_mode_gate.rs` (151 LOC) + `lang/claude-code-runtime-python/tests/test_deployment_mode.py` (80 LOC) cover both runtimes.
- **WATCH-07/Q9 correction**: ADR-V2-019 frontmatter `status: Accepted` UNCHANGED + `accepted_at: 2026-04-20` UNCHANGED. Action was trace fill, NOT status flip. `git show 70b5e94 -- docs/design/EAASP/adrs/ADR-V2-019-l1-runtime-deployment-model.md` confirms no `status:` line modified. Status was already flipped to Accepted at commit `d14893d` (Phase 4a).
- `enforcement.trace` array populated with 4 implementation anchors: `crates/grid-runtime/src/config.rs::RuntimeConfig::deployment_mode`, `crates/grid-runtime/src/service.rs::RuntimeServiceImpl::create_session`, `lang/claude-code-runtime-python/.../service.py::__init__::_deployment_mode`, `lang/claude-code-runtime-python/.../service.py::Initialize`.
- ADR-V2-028 (NEW) `status: Accepted` + `accepted_at: 2026-05-21` + trace anchors at `crates/grid-engine/src/providers/config.rs` + `crates/grid-runtime/src/config.rs`.

Threats T-06 (hot-reload silent ignore) + T-07 (per-session bypass) mitigated.

---

## REQ-ID Coverage

| REQ-ID | Plan | Closing Commit(s) | Status |
|--------|------|-------------------|--------|
| SERVER-01 | 05.4-01 | 2411edb, 6ba7dac, f93a7ee, dd1df1b | VERIFIED — WS ChunkType envelope + /v1/sessions/{id}/stream path + 5 SC#1 tests |
| SERVER-02 | 05.4-01 | 2411edb, 6ef59e1 | VERIFIED — in-process verification (D-02 rewrite landed) + 3 lifecycle tests |
| SERVER-03 | 05.4-01 | 180beac, c8c5f74 | VERIFIED — `grid-engine::l2` module + Stop hook → L2 write + schema coverage |
| SERVER-04 | 05.4-02 | 83f594c, e757f9e | VERIFIED — 3-mode auth + HMAC + audit columns + rate limit (Q3 wording note) |
| SERVER-05 | 05.4-02 | d9aa08c | VERIFIED — POST /api/v1/admin/reload + RESTART_REQUIRED_FIELDS 422 |
| WATCH-04 | 05.4-02 | d12f6ec, 2453447 | VERIFIED — D142 + D143 both gated; 2+2 tests PASS |
| WATCH-07 | 05.4-02 | 70b5e94 | VERIFIED — ADR-V2-019 trace fill (Q9 correction: NOT status flip) |

7/7 REQ-IDs verified.

---

## ADR Status Check

| ADR | Expected | Actual | Status |
|-----|----------|--------|--------|
| ADR-V2-028 | New, Accepted 2026-05-21, F1-F4 lint clean | `status: Accepted`, `accepted_at: 2026-05-21`, lint PASS | VERIFIED |
| ADR-V2-019 | Status UNCHANGED (already Accepted 2026-04-20 per Q9), trace populated | `status: Accepted`, `accepted_at: 2026-04-20` UNCHANGED, 4 trace anchors present | VERIFIED |
| Full audit lint | 164 PASS / 0 FAIL / 105 WARN advisory, exit 0 | `python3 .adr-plugin/scripts/adr_lint.py --ci` returns "Summary: 164 PASS \| 0 FAIL \| 105 WARN" | VERIFIED |

---

## DEFERRED_LEDGER 5-row close

| Item | Closed In | Status |
|------|-----------|--------|
| D142 | `d12f6ec` (5.4-02-07) | VERIFIED — ✅ CLOSED row in DEFERRED_LEDGER.md with full Resolution body |
| D143 | `2453447` (5.4-02-08) | VERIFIED — ✅ CLOSED row in DEFERRED_LEDGER.md with full Resolution body |
| NEW-A2 | `74e6472`+`bf26cb8` (5.4-02-01 + 5.4-02-02) | VERIFIED — ✅ CLOSED row added under new NEW-A namespace subsection with full Resolution body |
| NEW-E3 | `70b5e94` (5.4-02-10) | VERIFIED — ✅ CLOSED row added under new NEW-E namespace subsection with Resolution emphasizing Q9 correction |
| NEW-F3 | `9007935`+`5b5c892` (5.4-02-03 + 5.4-02-09) | VERIFIED — ✅ CLOSED row with full Resolution body |

All 5 rows carry phase=5.4 + commit hash markers per Phase 4.0 CLEANUP-02 row-edit-on-close convention.

---

## Out-of-Scope Items (per CONTEXT D-13) — should still be OPEN

| Item | Status | Defer Target |
|------|--------|--------------|
| NEW-A3 (`kill_session` exit 1 → 4) | OPEN | Phase 5.5 (per CONTEXT D-13) — confirmed not closed in 5.4 |
| NEW-F4 (`./logs/tui.log` + `GRID_TUI_LOG` ADR) | OPEN — DEFERRED_LEDGER row `🟡 P2` | Phase 5.5 (CONTEXT D-13 says defer 到 5.5; ledger says "Pushed to Phase 5.4" — outdated note but no closure happened) — confirmed not closed in 5.4 |
| grid-cli `mod output` E0583 | OPEN | Phase 5.5 — feature-flag gate fix |
| NEW-L1 (HNSW disk leak in `vector_index.py`) | OPEN | Phase 5.5 — Python L2 engine domain, separate from server hardening |

Confirmed via `grep -E "(NEW-A3\|NEW-L1).*(CLOSED\|✅)"` — only the "✅ deleted" data note for NEW-L1 (referring to disk reclamation, not code fix). All four items correctly remain OPEN per D-13 boundary.

One new deferred item logged in `.planning/phases/05.4-server-hardening/deferred-items.md`:
- `security_policy_update_returns_not_implemented` pre-existing test drift (P3, Phase BA-era, not introduced by 5.4) — handled via `--skip` flag in phase gate; logged for Phase 5.5 opportunistic touch.

---

## Summary

- **Total commits in Phase 5.4:** 23 (Plan 01: 11 commits 418bc77→f937ffc, Plan 02: 12 commits 74e6472→a2ec608). Plus 6 setup commits (CONTEXT/RESEARCH/PATTERNS/VALIDATION/PLAN-01/PLAN-02 + revision) before any code = 29 total Phase 5.4-related commits since pre-phase tip 8c93713.
- **Plans complete:** 2/2 (05.4-01-PLAN, 05.4-02-PLAN)
- **Tasks complete:** 22/22 (10 Plan 01 [W0-01/02/03 + 5.4-01-01..07] + 12 Plan 02 [5.4-02-01..12])
- **Tests added:** 64 (Plan 01: 33 + Plan 02: 31) all PASS per per-Plan SUMMARY tables.
- **Phase gate output (Plan 02 SUMMARY §Phase Gate Run):**
  - Step 1 release suite: 2698 PASS / 0 FAIL / 6 ignored (with `--features grid-server/testing` + `--skip security_policy_update_returns_not_implemented`)
  - Step 2 ADR audit: 164 PASS / 0 FAIL / 105 WARN advisory, exit 0 (re-verified inline in this verification at 2026-05-21)
  - Step 3 schema coverage: `user_version=13`; both `CREATE TABLE sessions` + `CREATE TABLE turns` present
- **STRIDE threats mitigated:** T-01 + T-02 + T-03 + T-04 + T-05 + T-06 + T-07 (all 7 from both plans' threat_model arrays)
- **Files created (Phase 5.4 cumulative):** 17 (8 in Plan 01 + 9 in Plan 02)
- **Files modified (Phase 5.4 cumulative):** 28

---

## Recommendation

**PASS — proceed to Phase 5.4 closure.**

All 5 ROADMAP success criteria verified by codebase evidence (file existence + substantive content + key-link wiring + threat mitigation tests). 7/7 REQ-IDs covered with concrete test artifacts. ADR governance clean (V2-028 Accepted, V2-019 trace fill per Q9). 5-row LEDGER close in place. Out-of-scope items correctly remain OPEN per CONTEXT D-13.

**Minor drift to note (advisory, non-blocking):**
- ROADMAP SC#4 text retains "4 种 auth 模式" phrasing despite Q3 correction in RESEARCH/PLAN documenting 3 actual AuthMode variants + HMAC-as-signature-layer-inside-ApiKey. Implementation provides 4 distinct test paths satisfying the spirit of the criterion, but ROADMAP text was not synchronously updated. Consider Phase 5.5 milestone-close to fix the ROADMAP wording to match Q3.
- DEFERRED_LEDGER NEW-F4 row says "Pushed to Phase 5.4" but the item remains OPEN — CONTEXT D-13 correctly defers it; ledger note is stale. Consider Phase 5.5 to mark NEW-F4 as "Defer-to-5.5" or close at that time.

**Next action:** `/gsd-discuss-phase 5.5` — Interface ADR (ADR-V2-026) + milestone close cascade.

---

*Verified: 2026-05-21*
*Verifier: Claude (gsd-verifier, goal-backward, single-pass codebase verification)*
