# Phase 5.4: Server Hardening - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-20
**Phase:** 05.4-server-hardening
**Areas discussed:** GA1 (SERVER-02 形态), GA2 (SERVER-01 wire alignment), GA3 (SERVER-03 L2 wiring), GA4+5 (Scope fold-in)

---

## Initial Gray Area Selection

**Question:** Phase 5.4 gray areas — 你想讨论哪几个? (multi-select; GA6/7/8 default-policy 问题留给 planner)

| Option | Description | Selected |
|--------|-------------|----------|
| GA1: SERVER-02 形态 | gRPC client wiring 范围 — in-process vs subprocess+gRPC vs hybrid. 决定整 phase 工时分布 | ✓ |
| GA2: SERVER-01 wire alignment | WS wire 格式: ChunkType-native vs 14-variant + ChunkType 通道 vs 完全切换. /ws path 是否切到 /v1/sessions/{id}/stream | ✓ |
| GA3: SERVER-03 L2 wiring | grid-server → L2: Rust in-process reimpl vs HTTP/gRPC 调 Python L2 vs Stop hook 写本地 SQLite + EAASP 自己 pull | ✓ |
| GA4+5: Scope fold-in | WATCH-04/07 + 9 sideband 项 (NEW-A2/A3/F3/F4/L1/grid-cli E0583) 哪几个 fold 哪几个 defer | ✓ |

**User's choice:** 全部选中

---

## GA1: SERVER-02 形态 (gRPC client wiring)

**Code 现状:** `AppState.agent_supervisor: Arc<AgentRuntime>` + `agent_handle: AgentExecutorHandle` — grid-server 用 grid-engine in-process。`crates/grid-runtime/` 才是 `:50051` gRPC server。

### Q1: grid-server 如何与 L1 runtime 集成?

| Option | Description | Selected |
|--------|-------------|----------|
| A. 保持 in-process (ROADMAP 重新描述) | 现有 in-process AgentRuntime, ROADMAP SC#2 重写; 零重构, 遵循 Grid 独立产品 dormancy 原则 | ✓ |
| B. 完全切换到 subprocess + gRPC client | tonic gRPC client 调 16 method; 严格匹配 ROADMAP, 与 EAASP L4 同构; 但大工作量 + 双进程运维 + gate 跨进程重设 | |
| C. Hybrid: in-process 默认 + gRPC 可选 | Optional gRPC client config flag; 两套代码路径维护 | |
| D. Minimal validation, 不动 in-process | thin verify gate, SC#2 名不副实 | |

**User's choice:** A. 保持 in-process (现状为准, ROADMAP 重新描述)

### Q2: SC#2 重写后的验收标准应该是?

| Option | Description | Selected |
|--------|-------------|----------|
| A1. 验证 in-process 逻辑路径 | "grid-server in-process 调 grid-engine 走完 16-method 逻辑路径 + chunk relay + hook envelope 透传 + 集成测试 ≥3 case PASS"; 不提 gRPC/50051/dual-runtime | ✓ |
| A2. 验证 in-process + 额外 in-tree gRPC handshake test | + 启动时选择性判断 50051 可达 + 记入 doctor/metrics | |
| A3. 你决 (wording 留给 planner) | the agent's discretion | |

**User's choice:** A1. 验证 in-process 逻辑路径 (不提 gRPC/50051/dual-runtime)

**Notes:** 决定遵循 ADR-V2-024 双轴模型 Grid 独立产品 dormancy 原则。SERVER-02 实质是"再加一层"(network hop) — 5.4 不 pre-invest。EAASP-via-server 路径是未来路径,等真有 consumer 时再做。

---

## GA2: SERVER-01 WS wire alignment + path

**Code 现状:**
- `ws.rs:40-180` ServerMessage enum = 14 个 ad-hoc variants (text_delta / tool_start / approval_required / interaction_requested 等), NOT ChunkType wire
- ChunkType wire 8/9 在 5.3 已扩到 contract-v1.2.0, wire 值 0-9
- Web frontend (`web/`) 目前 dormant — 没有活跃消费者
- ROADMAP path `/v1/sessions/{id}/stream` vs 现有 `/ws?session_id=`

### Q1: WS wire 格式 — server→client 应如何序列化?

| Option | Description | Selected |
|--------|-------------|----------|
| A. ChunkType-native | thin envelope, 删除 14-variant model-output variants; 与 contract-v1.2.0 对齐, doctor proto-cli-sync check 复用 | ✓ |
| B. 双轨 — 保留 14-variant + 加 ChunkType 通道 | ChunkRelay variant + 旧 14 保留; 维护成本高 | |
| C. 完全切换到 ChunkType + 提供 legacy mapper | mapper 是额外代码路径 | |

**User's choice:** A. ChunkType-native

### Q2: WS path — ROADMAP 写的 /v1/sessions/{id}/stream 跳上去?

| Option | Description | Selected |
|--------|-------------|----------|
| A. 切到 /v1/sessions/{id}/stream + 保留 /ws alias | 复用 5.2 D-03 deprecation 模式 + stderr warn | ✓ |
| B. 只加 /v1/sessions/{id}/stream, 不保留 /ws | 硬切, 与 5.2 D-03 deprecation 习惯不一致 | |
| C. 保留 /ws, 不加 /v1/ path | ROADMAP 重描述为 /ws 是唯一 path | |

**User's choice:** A. 切到 /v1/sessions/{id}/stream + 保留 /ws alias

### Q3: ChunkType-native 切换范围 — client→server 也要重设吗?

| Option | Description | Selected |
|--------|-------------|----------|
| A. 只重设 server→client, client→server 不动 | ClientMessage 4 variants 保留 — user intent 不是 ChunkType 语义 | ✓ |
| B. 双向都上 ChunkType wire | 完全对称 | |

**User's choice:** A. 只重设 server→client

**Notes:** server-only control messages (session_created / error / done / approval_required / interaction_requested / memory_flushed / context_degraded) 保留 ad-hoc shape — 这些不是 model output。

---

## GA3: SERVER-03 L2 wiring

**Code 现状:**
- `tools/eaasp-l2-memory-engine/` Python tool 提供 7 MCP tools + FTS5+HNSW+time-decay 索引
- `crates/grid-runtime/src/l2_memory_client.rs` 已有 Rust gRPC client
- `crates/grid-server/src/state.rs` 无 L2 client — server 当前不写 L2

### Q1: grid-server → L2 memory engine 接入机制?

| Option | Description | Selected |
|--------|-------------|----------|
| A. 复用 grid-runtime/l2_memory_client.rs (抽到 shared) | 与 grid-runtime 一致, L2 仍是 SSOT, ADR-V2-015 不动 | ✓ |
| B. Stop hook 写本地 SQLite + EAASP pull (松耦合) | server 不依赖 Python L2 运行, deployment-mode 中立; 但 SC#3 字面 "写入 L2 memory engine" 不满足 | |
| C. Rust in-process FTS5+HNSW reimpl | 双套 schema, ADR-V2-015 重写, 工作量太大 | |
| D. ADR-V2-019 deployment-mode 锁后 L2 接入方式 follow | 推迟决策到 GA4+5 ADR-V2-019 Accept | |

**User's choice:** A. 复用 grid-runtime/l2_memory_client.rs

### Q2: l2_memory_client.rs 抽离位置?

| Option | Description | Selected |
|--------|-------------|----------|
| A. 拼到 grid-engine 新 module grid-engine::l2 | 与 audit/metering/mcp layering 一致, 零新 crate | ✓ |
| B. 新建 crates/grid-l2-client 共享 crate | 职责单一; 但 2 consumer 过重 | |
| C. 你决 (planner 决定) | discretion | |

**User's choice:** A. 拼到 grid-engine::l2 新 module

---

## GA4+5: Scope fold-in 决策

### Q1: WATCH-04 + WATCH-07 (D142/D143/NEW-E3 ADR-V2-019) 在 5.4 怎么处理?

| Option | Description | Selected |
|--------|-------------|----------|
| A. 都 fold 进 5.4 | WATCH-04 ~40 LOC + WATCH-07 ADR flip; SC#5 完整交付 | ✓ |
| B. 只 fold WATCH-04, WATCH-07 defer 到 5.5 | 代码进 5.4, ADR flip 进 5.5 | |
| C. 都 defer 到 5.5 | SC#5 需重写 | |

**User's choice:** A. 都 fold 进 5.4

### Q2: NEW-A2 migrate() race fix 在 5.4 怎么处理?

| Option | Description | Selected |
|--------|-------------|----------|
| A. fold 进 5.4 | SC#3 "tokio-rusqlite migration 跑通且幂等" 直接对应 | ✓ |
| B. defer 到 5.5 | grid-engine 层, 不是 server SC 直接路径 | |
| C. 你决 (planner 看 workload) | discretion | |

**User's choice:** A. fold 进 5.4

### Q3: NEW-F3 silent-fallback removal 在 5.4 怎么处理?

| Option | Description | Selected |
|--------|-------------|----------|
| A. fold 进 5.4 | 与 SERVER-04 (auth config validation) + SERVER-05 (hot-reload 错误上报) 同主题, 与 MEMORY "No Fallback" rule 对齐 | ✓ |
| B. 只 fold 代码修改, ADR defer | 代码 5.4, ADR 5.5 | |
| C. defer 到 5.5 | 5.4 专注 server | |

**User's choice:** A. fold 进 5.4

### Q4: 剩 4 项 轻量 sideband (NEW-A3 / NEW-F4 / grid-cli E0583 / NEW-L1) 怎么处理?

| Option | Description | Selected |
|--------|-------------|----------|
| A. 4 项都 defer 到 5.5 | 域不一致 (CLI / TUI / Python L2), 加进来裂 phase domain | ✓ |
| B. NEW-L1 fold (因 GA3 走 L2), 其余 defer | L2 disk leak server CI 起来后会重发 | |
| C. NEW-A3 + grid-cli E0583 fold (CLI 15 LOC), 其余 defer | 顺手做 5.2 遗留 CLI fix | |

**User's choice:** A. 4 项都 defer 到 5.5

**Notes:** **最终 5.4 scope** = SERVER-01..05 + WATCH-04 + WATCH-07 + NEW-A2 + NEW-F3。4 项 sideband (NEW-A3 / NEW-F4 / grid-cli E0583 / NEW-L1) 全部 defer 到 5.5 milestone close phase。

---

## Done check

**Question:** We've discussed GA1+GA2+GA3+GA4+5. Remaining gray areas (GA6 auth default / GA7 hot-reload 机制 / GA8 ExecutionMode default) 是 the agent's discretion / 属 default-policy 问题 — 推荐留给 planner. 你还想讨论吗?

| Option | Description | Selected |
|--------|-------------|----------|
| I'm ready for context | 跳过 GA6/7/8, 写 CONTEXT.md, 其他交给 planner/researcher | ✓ |
| Explore more gray areas | 讨论 GA6/7/8 | |

**User's choice:** I'm ready for context

---

## the agent's Discretion (deferred to planner/researcher)

- **GA6 SERVER-04 auth default policy** — 4 mode (None/ApiKey/Hmac/Hybrid) 哪个 default, prod-ready vs dev-only 分类
- **GA7 SERVER-05 hot-reload 实现机制** — SIGHUP vs file watcher (notify crate) vs POST endpoint
- **GA8 ExecutionMode default for server-served sessions** — Conversational default + explicit opt-in header
- **GA2 sub-detail** — thin envelope JSON shape 细节 (嵌套 vs 平铺, key 命名 chunk_type vs kind)
- **GA3 sub-detail** — `grid-engine::l2` module API surface
- **D-10 sub-detail** — ADR-V2-019 Accept enforcement.trace 写法
- **D-12 sub-detail** — silent-fallback ADR 是新 ADR-V2-028 还是修订 ADR-V2-019

---

## Deferred Ideas

### To Phase 5.5 (Interface ADR + Milestone Close)
- NEW-A3 (kill_session exit code) — 2026-05-17 5.2 OOS
- NEW-F4 (TUI log path ADR) — 2026-05-19 5.3 OOS
- grid-cli mod output E0583 — 2026-05-20 5.3 OOS
- NEW-L1 (HNSW Python disk leak) — 2026-05-20 5.3 OOS

### To next milestone (v3.2+)
- gRPC client grid-server → grid-runtime (per GA1 D-01)
- ATTACHMENT_REF backend storage (5.3 wire-only locked)
- web/ frontend ChunkType wire 对齐 (Grid 独立产品 dormancy)
- NEW-C1/C3 — harness.rs / grid-eval 大文件拆分
- D-batch ~40 P3 items
