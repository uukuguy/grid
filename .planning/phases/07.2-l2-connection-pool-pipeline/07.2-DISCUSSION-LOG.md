# Phase 7.2: L2 connection-pool + Pipeline - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-02
**Phase:** 07.2-l2-connection-pool-pipeline
**Areas discussed:** D12+D94 scope, D91 tombstone policy, D93 concurrency model, D11 SQL fix, plan structure

---

## D12+D94 scope — connection pool already partially shipped

| Option | Description | Selected |
|--------|-------------|----------|
| Verify substantively done + harden + close | Audit each store; confirm shared-connection usage; add high-concurrency throughput test; close LEDGER as "verified done in S2.T1+T6 + this phase added throughput test". | ✓ |
| Full singleton refactor + remove all per-call connect() | Force a stricter API (MemoryStore singleton class). Larger scope; redoing what's already correct. | |
| Re-scope D94 — use freed scope room for D98 or D91 | Acknowledge D12/D94 partly done; redirect to deeper D98 / D91. Honest LEDGER but loses REQ-ID symmetry. | |

**User's choice:** Verify substantively done + harden + close.
**Notes:** Scout confirmed `get_shared_connection` + `get_write_lock` at db.py:15-44; all 3 stores consume. Real remaining work is throughput test + audit + close. Avoid churn.

---

## D91 HNSW tombstone rebuild trigger policy

| Option | Description | Selected |
|--------|-------------|----------|
| 30% tombstone threshold + sync rebuild | Trigger sync rebuild on next write when ratio crosses 30%. Matches ROADMAP success criterion #2 verbatim. | ✓ |
| 30% threshold + async background rebuild | Same threshold; rebuild in background task. No write-path latency hit; adds task lifecycle complexity. | |
| Adaptive: scale threshold by index size | Larger indexes amortize rebuild less frequently. Over-engineered for current usage. | |

**User's choice:** Sync at 30% (recommended).
**Notes:** Async rebuild deferred to v3.4+ if production usage surfaces latency-spike pain.

---

## D93 embed_batch concurrency model

| Option | Description | Selected |
|--------|-------------|----------|
| asyncio.gather() with provider rate-limit semaphore | Wrap individual embed() in `asyncio.gather` bounded by per-provider semaphore. Simplest; respects rate limits. | ✓ |
| Use provider's native batch endpoint where available | Ollama + TEI support batched HTTP. More plumbing; theoretically faster. | |
| Both: native batch when available, gather fallback | Maximum coverage; more LOC + maintenance. | |

**User's choice:** asyncio.gather + semaphore.
**Notes:** Native batch deferred to provider-specific perf passes; `asyncio.gather` gets ≥3× speedup with minimal LOC.

---

## D11 skill-registry fix — single WHERE clause or post-query keep?

| Option | Description | Selected |
|--------|-------------|----------|
| Rewrite as single SQL WHERE clause + add access_scope column | Migration + index update; LIMIT N gives accurate scope-filtered top-N. Cleanest. | ✓ |
| Keep post-query filter + LIMIT N*4 compensation | Avoids schema migration; wasteful; doesn't satisfy ROADMAP wording. | |
| Two-pass query: subquery for scope IDs, then JOIN | Comparable to option 1 with extra round-trip. | |

**User's choice:** Single WHERE clause + new column (recommended).
**Notes:** D11 lives in Rust (`tools/eaasp-skill-registry/src/store.rs:213-290`), not Python; mechanical migration.

---

## Plan structure

| Option | Description | Selected |
|--------|-------------|----------|
| Three plans split by subsystem | Plan 01 connection layer (D12+D94+D98+D30). Plan 02 perf (D91+D93). Plan 03 correctness (D11+D13). Matches ROADMAP §Notes. | ✓ |
| Two plans: L2 perf (P2) + cleanups (P3) | Plan 01 all 5 P2; Plan 02 3 P3. Plan 01 large. | |
| Single plan, ~10 tasks | Maximum cohesion; matches 7.0/7.1 precedent. 2000+ LOC plan; harder to review. | |
| Two plans: connection layer first + everything else | Sequential; less clean subsystem boundary. | |

**User's choice:** Three plans split by subsystem (recommended).
**Notes:** ROADMAP §Notes explicitly recommends ≥2 plans for keystone phase.

---

## the agent's Discretion

(Per CONTEXT.md `decisions` section)

- Exact denominator for D91 tombstone ratio
- Concurrency-limit defaults per provider for D93
- D13 implementation Option A (FTS delete on archive) vs B (query-time filter)
- Throughput test exact load (≥10 concurrent, may scale up)
- D11 migration UP/DOWN ordering
- LEDGER close-out commit-hash anchoring style

---

## Deferred Ideas

(Mirrors CONTEXT.md `deferred` section)

### To v3.4+ (L2 cross-cutting INBOX already there)
- D14, D15, D59, D65, D75-D80, D92, D95-D101

### To v3.4+ (this phase scope ceiling)
- Async background HNSW rebuild
- Native batched HTTP embed (Ollama/TEI native batch)
- `eaasp_common.connect()` shared helper
- Skill-registry full schema redesign
