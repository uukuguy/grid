# Phase 7.0: grid-engine harness wiring (Verify & Close + D3) - Context

**Gathered:** 2026-06-07
**Status:** Ready for planning

<domain>
## Phase Boundary

Verify-and-close 6 ENGINE carry-forward D-items from v3.3 Phase 7.0, plus implement the deferred D3 (user_preferences + trim_for_budget) in full. 5 of 6 D-items are already confirmed ✅ CLOSED in DEFERRED_LEDGER.md with commit hashes from 2026-06-01. D3 was closed as DEFERRED (scope-ceiling deferral) and needs full implementation now.

</domain>

<decisions>
## Implementation Decisions

### Verification depth
- **D-01:** Standard verification — confirm LEDGER ✅ CLOSED tags + running `cargo test -p grid-engine -p grid-server` to catch regressions. No commit-by-commit audit (those commits were made June 1 and are in git already). One plan for this phase.

### D3 — user_preferences + trim_for_budget (full implementation)
- **D-02:** Full D3 scope — both sub-items implemented within this phase (not deferred again).
- **D-02a:** harness reads `payload.user_preferences` (user_id, preferences map) and surfaces into `AgentLoopConfig` so the agent loop can use them. Precedent: `crates/grid-runtime/src/harness.rs` already reads `payload.user_preferences` at line 625.
- **D-02b:** `trim_for_budget()` becomes budget-driven (triggered by budget threshold) instead of turn-boundary driven. Currently `trim_for_budget` calls happen at turn boundaries in the harness loop at `crates/grid-engine/src/agent/harness.rs`. The change is to check `current budget < threshold` before trimming rather than trimming every turn.
- **D-03:** D3 LEDGER row (L106) re-opened in DEFERRED_LEDGER.md with new scope. Previous closure was "deferred per scope ceiling" — now implementing for real.

### Already-closed items (verify-only)
- **D102** (ENGINE-01): ✅ CLOSED @ `3a23ecb` — `CompactionPipelineConfig` YAML wiring with 5 tests. Verify `cargo test -p grid-server --test compaction_yaml_wiring` still passes.
- **D57** (ENGINE-03): ✅ CLOSED @ `60f99d5` — `build_memory_preamble` single source of truth. Verify no duplicate exists.
- **D58** (ENGINE-04): ✅ CLOSED @ `60f99d5` — Send-path assertion with preamble snapshot. Verify test passes.
- **D103** (ENGINE-05): ✅ CLOSED @ `3e0b055` — Profiled 21µs, perf canary at `crates/grid-engine/tests/find_tail_boundary_perf.rs`. Verify canary still green.
- **D104** (ENGINE-06): ✅ CLOSED @ `b32d985` — Doc-only close. Harness-local guard at `crates/grid-engine/src/agent/harness.rs:526` with revisit trigger documented. Verify rustdoc block still present.

### the agent's Discretion
- Exact D3 implementation approach — where to thread `user_preferences` into AgentLoopConfig, how to determine budget threshold for trim_for_budget
- Verification automation — whether to script LEDGER checks or do manual verification
- Plan structure — single plan or split verify + implement

</decisions>

<specifics>
## Specific Ideas

- D3 should follow the existing D102 wiring precedent: YAML/struct → AgentLoopConfig → executor consumption pattern at `crates/grid-engine/src/agent/executor.rs`
- `trim_for_budget()` budget-driven threshold should be configurable via `AgentLoopConfig` (not hardcoded), following ADR-V2-028 strict-by-default pattern
- D102 was wired via `compaction: CompactionPipelineConfig` field on `AgentRuntimeConfig`. D3 user_preferences should follow the same wiring pattern.

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### LEDGER (D-row SSOT)
- `docs/design/EAASP/DEFERRED_LEDGER.md` L106 — D3 status (currently CLOSED, needs re-open)
- `docs/design/EAASP/DEFERRED_LEDGER.md` L177 — D57 status (✅ CLOSED)
- `docs/design/EAASP/DEFERRED_LEDGER.md` L178 — D58 status (✅ CLOSED)
- `docs/design/EAASP/DEFERRED_LEDGER.md` L232 — D102 status (✅ CLOSED)
- `docs/design/EAASP/DEFERRED_LEDGER.md` L233 — D103 status (✅ CLOSED)
- `docs/design/EAASP/DEFERRED_LEDGER.md` L234 — D104 status (✅ CLOSED)

### ADRs
- `docs/design/EAASP/adrs/ADR-V2-028-strict-by-default-config-validation.md` — config validation strictness for D3 AgentLoopConfig wiring
- `docs/design/EAASP/adrs/ADR-V2-018-precompact-hook-protocol.md` §D5 — per-turn compact semantic that D104 guard enforces

### Phase docs
- `.planning/ROADMAP.md` — Phase 7.0 success criteria and verification pattern
- `.planning/REQUIREMENTS.md` — ENGINE-01..06 REQ-ID definitions

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `crates/grid-engine/src/agent/executor.rs:525-573` — `AgentLoopConfig` struct-literal construction pattern (D102 compaction wiring precedent). D3 user_preferences should add fields here.
- `crates/grid-server/src/config.rs` — YAML→struct config flow (D102 precedent for D3 wiring)
- `crates/grid-runtime/src/harness.rs:625` — existing `user_preferences` reading from `payload.user_preferences`
- `crates/grid-server/tests/compaction_yaml_wiring.rs` — D102 test pattern (round-trip + missing-fallback + reject-unknown). D3 should follow same pattern.

### Established Patterns
- **ADR-V2-028 strict-by-default**: All new config fields use `#[serde(deny_unknown_fields, default)]`. Missing → default(), unknown → error.
- **AgentLoopConfig wiring flow**: `Config` (YAML) → `AgentRuntimeConfig` → `AgentRuntime` → `AgentExecutor` → per-turn `AgentLoopConfig`. D102 walked this path; D3 follows the same pattern.
- **LEDGER close-out convention**: Every D-row closure gets ✅ CLOSED + commit hash + resolution paragraph.

### Integration Points
- `crates/grid-engine/src/agent/harness.rs:418-423` — current `MAX_TURNS_FOR_BUDGET=50` and budget computation. D3 trim_for_budget will interact with this.
- `crates/grid-runtime/src/harness.rs:149` — `build_memory_preamble()` public API (D57 closed)
- `crates/grid-engine/src/context/compaction_pipeline.rs:746-781` — `find_tail_boundary()` with rustdoc and perf canary (D103 closed)

</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 07.0-grid-engine-harness-wiring*
*Context gathered: 2026-06-07*
