# Phase 7.0: grid-engine harness wiring - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-01
**Phase:** 07.0-grid-engine-harness-wiring
**Areas discussed:** D102 wiring scope, D57 DRY fix, D103 boundary scan deliverable, D104 reactive guard placement, D58 send-path coverage, D3 in-scope/defer, plan structure

---

## D102 — AgentLoopConfig.compaction YAML wiring scope

| Option | Description | Selected |
|--------|-------------|----------|
| Minimum: derive serde + Config field + factory wire | Add Serialize/Deserialize to CompactionPipelineConfig, add `compaction: CompactionPipelineConfig` to grid-server Config with #[serde(default)], thread to AgentLoopConfigBuilder.compaction_config() at the runtime-factory site. Round-trip + strict-by-default per ADR-V2-028. ~30 LOC + 2 tests. | ✓ |
| Extended: also expose via grid-cli config-doctor / env override | Above + add GRID_COMPACTION_* env overrides per provider/budget pattern + `grid cli-config validate compaction:` echo. Larger surface, more thorough validation, more LOC. | |
| Conservative: serde derives + Config field only; defer factory wiring | Just unblock config-file authoring; agent runtime still ignores until separate follow-up. Cleanest atomic commit but leaves D102 only half-closed. | |

**User's choice:** Minimum viable path (recommended option).
**Notes:** Avoids the half-closed-D102 state of the conservative option, and keeps phase scope tight by deferring env-var surface to v3.4+ config-doctor pass.

---

## D57 — build_memory_preamble DRY violation

| Option | Description | Selected |
|--------|-------------|----------|
| Promote to pub + delete test copy | Change `pub(crate) fn build_memory_preamble` to `pub fn` in `grid-runtime/src/harness.rs:144` and import it in `tests/harness_payload_integration.rs:474` instead of `expected_preamble`. Single source of truth. Tests no longer pin-via-copy. | ✓ |
| Keep copy, add parity test enforcing equivalence | Keep test copy but add a new test that calls both implementations on shared fixtures and asserts byte equality. DRY violation persists in code but is mechanically enforced. | |
| Move into grid-types as a shared helper | Hoist into grid-types crate so both grid-runtime + test can depend without visibility surgery. Most architecturally clean; widest blast radius (Cargo.toml edits). | |

**User's choice:** Promote to pub + delete test copy (recommended option).
**Notes:** Pure function with no internal-state dependency makes the 1-LOC visibility change zero-risk; grid-types hoist would be over-engineered for a runtime-specific formatter.

---

## D103 — find_tail_boundary investigation deliverable

| Option | Description | Selected |
|--------|-------------|----------|
| Profile + decide + record in LEDGER close-out | Build a small bench (200-turn synthetic conversation, varying tail_protect_tokens), measure wall-clock + complexity. Then decide: fix vs doc-only warning. Either way LEDGER row gets closed with measurements + rationale. No commitment to actual code change. | ✓ |
| Doc-only: LEDGER row close-out + inline rustdoc complexity note | Skip the bench; just add `/// Complexity: O(sum of msg sizes)` doc and close LEDGER with verdict 'not actually O(N²); deferred'. Faster; no measured baseline. | |
| Fix it eagerly: pre-compute estimated tokens incrementally | Don't bother profiling; refactor to maintain running estimate so boundary search is O(N). Bigger change, but clears LEDGER cleanly. | |

**User's choice:** Profile + decide + record (recommended option).
**Notes:** Measurement evidence prevents speculative refactoring; one-shot assertion bench doubles as a regression canary for future v3.4+ work.

---

## D104 — Reactive guard placement

| Option | Description | Selected |
|--------|-------------|----------|
| Keep in harness + doc-only LEDGER close | LEDGER L234 already says 'currently ADR-V2-018 §D5 compliant, defer until pipeline re-architect'. Close out with verdict 'no other caller exists today, deferred per ADR-V2-018 §D5' + add rustdoc explaining the invariant. No ADR needed. | ✓ |
| Move to pipeline + draft new ADR | Hoist guard into CompactionPipeline (per-turn double-compact lock), which means the pipeline itself becomes the enforcement boundary. This touches ADR-V2-026 ExecutionMode lineage — draft ADR-V2-030 (candidate). Bigger phase. Forces ADR work in v3.3. | |
| Add a pipeline-level test asserting harness invariant; keep guard in harness | Compromise: locate the invariant 'a single turn cannot trigger reactive compaction twice' as a pipeline test (using mock harness), keep code in harness. Documents the contract without moving code. | |

**User's choice:** Keep in harness + doc-only LEDGER close (recommended option).
**Notes:** No ADR drafted; consistent with the milestone's "0 ADRs Accepted" trend continuing from v3.2's intentional light-triage.

---

## D58 — Send-path test coverage scope

| Option | Description | Selected |
|--------|-------------|----------|
| Extend existing test to assert Send full-path | Augment `test_initialize_injects_memory_refs_preamble` to also drive a Send call and assert the preamble survives into the request body. Keeps existing test intent; one assertion add. | ✓ |
| Add a new test alongside the existing one | Leave the existing test as-is, add `test_send_path_carries_memory_preamble` next to it. Cleaner separation, more LOC. | |
| Skip — mark D58 as covered by ENGINE-03 fix | If D57 promotes `build_memory_preamble` to pub and the cross-crate test drives both Initialize + Send via shared helper, D58 is functionally satisfied. Close both rows together. | |

**User's choice:** Extend existing test (recommended option).
**Notes:** Preserves the test's Initialize → preamble propagation narrative; benefits from D-02's pub helper for clean shared fixture data.

---

## D3 (ENGINE-02) — user_preferences + trim_for_budget scope

| Option | Description | Selected |
|--------|-------------|----------|
| Defer entirely — leave ENGINE-02 in this phase as no-op | D3 is the largest P3 item (touches payload schema + harness flow + trim budget logic). Defer to v3.4+ as part of payload-schema review; close ENGINE-02 with 'deferred per scope ceiling'. Keeps Phase 7.0 shippable. | ✓ |
| Minimal: thread user_preferences through harness payload, skip trim_for_budget | Smaller subset: just surface user_preferences from payload into agent loop. Skip the budget-driven trim refactor. Partial close on ENGINE-02. | |
| Full: both user_preferences + trim_for_budget refactor | Do the whole D3 item. Bigger surface, probably forces phase >6 rows. | |

**User's choice:** Defer entirely (recommended option).
**Notes:** Keeps phase row-count ≤6 per ROADMAP §Notes; ENGINE-02 LEDGER row close-out anchors back to this CONTEXT.md scope-ceiling rationale.

---

## Plan structure

| Option | Description | Selected |
|--------|-------------|----------|
| Single plan with 5-6 tasks | All ENGINE-01..06 in one plan. Atomic phase, easier review. ROADMAP §Notes already implied this scale ("≤6 rows in phase 7.0"). | ✓ |
| Two plans: D102 wiring (Plan 01) + harness/test cleanup (Plan 02) | Plan 01 = D102 + serde derives + factory wire (config-touching). Plan 02 = D57/D58 + D103/D104 doc-only closes. Cleaner separation, more git history overhead. Lets reviewer focus narrowly on Plan 01 (config layer). | |

**User's choice:** Single plan (recommended option).
**Notes:** All tasks touch `crates/grid-engine/src/` or `crates/grid-runtime/src/`, splitting creates artificial PR seam.

---

## the agent's Discretion

(Per CONTEXT.md `decisions` section "the agent's Discretion" subsection)

- Exact rustdoc wording for D-04 complexity note and D-05 invariant note
- Bench harness style for D-04 (cargo bench vs custom #[test] with Instant::now())
- Factory wiring exact call site (LEDGER L232 hint is a starting point, not binding)
- LEDGER close-out commit-hash anchoring style (match recent v3.x precedent)

---

## Deferred Ideas

(Mirrors CONTEXT.md `deferred` section — see there for full list)

- ENV var override surface for compaction (GRID_COMPACTION_*)
- grid cli-config validate echo for compaction section
- D3 (ENGINE-02) full payload.user_preferences + trim_for_budget refactor
- D104 candidate ADR-V2-030 (pipeline-guard re-architect)
- D105 (HookPoint::ContextDegraded string alias) + D106 (MAX_TURNS_FOR_BUDGET hardcode)
- find_tail_boundary actual O(N) refactor (if D-04 measurement surfaces regression risk)
