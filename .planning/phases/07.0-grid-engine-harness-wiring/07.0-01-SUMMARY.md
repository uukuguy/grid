---
phase: 07.0-grid-engine-harness-wiring
plan: 01
subsystem: grid-engine harness
tags: [user-preferences, compaction, budget-driven, D3, LEDGER-closeout]
depends_on: []
requires: [ENGINE-01, ENGINE-02, ENGINE-03, ENGINE-04, ENGINE-05, ENGINE-06]
provides: [AgentLoopConfig.user_preferences, budget-driven proactive compaction gate]
affects:
  - crates/grid-engine/src/agent/loop_config.rs
  - crates/grid-engine/src/agent/harness.rs
  - crates/grid-engine/src/agent/executor.rs
  - crates/grid-engine/src/agent/runtime.rs
  - crates/grid-engine/src/agent/mod.rs
  - crates/grid-runtime/src/harness.rs
  - docs/design/EAASP/DEFERRED_LEDGER.md
tech-stack:
  added: []
  patterns:
    - "AgentLoopConfig wiring (config → runtime → executor → loop-config struct-literal)"
    - "budget-driven compaction gate (< MIN_TURN_BUDSET threshold)"
key-files:
  created:
    - crates/grid-engine/tests/test_d3_user_preferences.rs
    - crates/grid-engine/tests/test_d3_budget_driven_compact.rs
  modified:
    - crates/grid-engine/src/agent/loop_config.rs (UserPreferences type + AgentLoopConfig field)
    - crates/grid-engine/src/agent/harness.rs (is_budget_tight + budget gate + debug log)
    - crates/grid-engine/src/agent/executor.rs (AgentExecutor field + setter + struct-literal)
    - crates/grid-engine/src/agent/runtime.rs (AgentRuntimeConfig + AgentRuntime + set_user_preferences)
    - crates/grid-engine/src/agent/mod.rs (export UserPreferences)
    - crates/grid-runtime/src/harness.rs (initialize() payload.user_preferences → engine prefs)
    - docs/design/EAASP/DEFERRED_LEDGER.md (D3 re-open/close + verification entries + close-out)
decisions:
  - "D3 user_preferences type defined in grid-engine loop_config.rs (independent of grid-runtime contract type)"
  - "D3 wire path follows D102 compaction precedent: Config → Runtime → Executor → AgentLoopConfig"
  - "Budget-driven compaction gate uses pure helper is_budget_tight() (remaining < MIN_TURN_BUDGET=4096)"
  - "Thread-safe user_preferences on AgentRuntime via StdMutex (sync setter, replicated at executor spawn)"
metrics:
  duration: "~14 min"
  completed_date: "2026-06-07T23:50:22+08:00"
---

# Phase 7.0 Plan 01: grid-engine-harness-wiring — Verify & Close + D3 Implementation

**One-liner:** Closed all 6 ENGINE carry-forward D-items — verified 5 already-closed, implemented D3 (user_preferences wiring into AgentLoopConfig + budget-driven proactive compaction gate), zero regressions across grid-engine / grid-runtime.

## Summary

Phase 7.0 Plan 01 executed three tasks:

1. **Task 1 (verify-only):** Confirmed all 5 already-closed D-rows (D57/D58/D102/D103/D104) remain ✅ CLOSED in DEFERRED_LEDGER.md with correct commit hashes. Ran full regression suite (grid-engine 1598 PASS, grid-runtime all PASS, compaction_yaml_wiring 3/3, find_tail_boundary_perf 1/1). Added verification entry to LEDGER 状态変更日志.

2. **Task 2 (TDD RED→GREEN):** Implemented D3 (ENGINE-02) in full, superseding the v3.3 DEFERRED close:
   - **Sub-item A — user_preferences wiring:** New `UserPreferences` type in `grid_engine::agent::loop_config` (user_id, language, timezone, llm_provider, llm_model). Added `user_preferences: Option<UserPreferences>` to `AgentLoopConfig`, `AgentExecutor`, `AgentRuntimeConfig`, and `AgentRuntime`. Wire path: `grid-runtime::GridHarness::initialize()` converts payload `UserPreferences` → engine type → `runtime.set_user_preferences()` → `executor.set_user_preferences()` at spawn → `AgentLoopConfig` struct-literal per round.
   - **Sub-item B — budget-driven compaction:** Added `is_budget_tight(remaining) -> bool` pure helper. Proactive compaction at `harness.rs` now gated on `task_budget_remaining < MIN_TURN_BUDGET` (4096 tokens). When budget is sufficient, compaction is skipped with a `debug!` log. Only proceeds when genuinely tight.
   - **Tests:** 15 new D3 tests (5 user_preferences + 10 budget arithmetic/gate). All PASS.

3. **Task 3 (close-out):** Verified all 6 ENGINE-01..06 REQ-IDs are ✅ CLOSED. ROADMAP success criteria 1-6 confirmed. Final LEDGER close-out entry added. Zero regressions.

## Tasks Executed

| Task | Name | Type | Status | Commit |
|------|------|------|--------|--------|
| 1 | Verify 5 closed items + run regression suite | auto | ✅ Complete | `6aae3993` |
| 2a | D3 RED phase — failing tests | auto (tdd) | ✅ Complete | `a57df360` |
| 2b | D3 GREEN phase — implementation | auto (tdd) | ✅ Complete | `65aa7bfe` |
| 2c | D3 LEDGER hash fix | auto | ✅ Complete | `116d9744` |
| 3 | Final close-out + LEDGER audit | auto | ✅ Complete | `7c81c370` |

## Verification Results

### Test Suite
| Suite | Result |
|-------|--------|
| `grid-engine` (lib + all tests) | 1598+ passed, 0 failed ✅ |
| `grid-runtime` (all tests) | all passed ✅ |
| `compaction_yaml_wiring` (D102) | 3/3 passed ✅ |
| `find_tail_boundary_perf` (D103) | 1/1 passed ✅ |
| `test_d3_user_preferences` (new) | 5/5 passed ✅ |
| `test_d3_budget_driven_compact` (new) | 10/10 passed ✅ |

### LEDGER Audit
| D-row | REQ-ID | Status | Commit |
|-------|--------|--------|--------|
| D3 | ENGINE-02 | ✅ CLOSED | `65aa7bfe` |
| D57 | ENGINE-03 | ✅ CLOSED | `60f99d5` |
| D58 | ENGINE-04 | ✅ CLOSED | `60f99d5` |
| D102 | ENGINE-01 | ✅ CLOSED | `3a23ecb` |
| D103 | ENGINE-05 | ✅ CLOSED | `3e0b055` |
| D104 | ENGINE-06 | ✅ CLOSED | `b32d985` |

## Deviations from Plan

None — plan executed exactly as written. The pre-existing `ws_chunk_envelope` test failure (E0599 `test_inject_events` on grid-server) is out-of-scope for Phase 7.0 and logged to `deferred-items.md`.

## Known Stubs

None. All data flows are wired end-to-end (user_preferences from gRPC payload → AgentLoopConfig struct-literal; budget gate active in harness proactive compaction path).

## Threat Flags

None. All three threats in the plan's threat model (T-7.0-01 Tampering, T-7.0-02 Denial of Service, T-7.0-03 Information Disclosure) have `accept` disposition — user_preferences is read-only metadata, the budget gate reduces (not increases) LLM calls, and no new disclosure surface is introduced.

## Self-Check

All 5 commits verified existing in git log. All created/modified files verified present on disk:

- ✅ `crates/grid-engine/tests/test_d3_user_preferences.rs` exists
- ✅ `crates/grid-engine/tests/test_d3_budget_driven_compact.rs` exists
- ✅ `crates/grid-engine/src/agent/loop_config.rs` has `UserPreferences` type and `user_preferences` field
- ✅ `crates/grid-engine/src/agent/harness.rs` has `is_budget_tight()` and budget gate
- ✅ `crates/grid-engine/src/agent/executor.rs` has `user_preferences` field + setter + struct-literal
- ✅ `crates/grid-runtime/src/harness.rs` has `initialize()` user_preferences extraction
- ✅ `docs/design/EAASP/DEFERRED_LEDGER.md` has D3 CLOSED entry + close-out log
- ✅ `cargo test -p grid-engine -p grid-runtime` exits 0

**Self-Check: PASSED**
