# Phase 3.6.1: docs/PROJECT_PRODUCT_OVERVIEW.md SSOT + dated snapshot - Context

**Gathered:** 2026-07-18
**Status:** Ready for planning

<domain>
## Phase Boundary

Establish the maintained product-status SSOT (`docs/PROJECT_PRODUCT_OVERVIEW.md`) and an immutable date-stamped audit snapshot (`docs/status/PRODUCT_STATUS_2026-07-18.md`) that together lock the post-Activation product-status facts. This phase does NOT touch `AGENTS.md`, `CLAUDE.md`, `README.md`, `README.zh.md`, or `.planning/PROJECT.md` / `.planning/STATE.md` — those belong to Phases 3.6.2 and 3.6.3.

</domain>

<decisions>
## Implementation Decisions

### SSOT structure (where new content goes)
- **D-01:** Add a new Section 3.0 状态快照 (status snapshot) at the top of the existing Section 3 area, containing the 5 canonical facts as a 5-row table with the lock-and-anchor rule that any change must go through `/adr:status` + `/adr:new` (per GSD governance).
- **D-02:** Add a new Section 3.1.1 (sub-table under existing 3.1) titled "EAASP Phase 演化路线对应里程碑", with a row per EVOLUTION_PATH Phase (0 / 0.5 / 0.75 / 1 / 2 / 2.5 / 3 / 3.5 / 3.6 / 4a / 4 / 5 / 6) and a final "Grid 独立产品 Activation (A.0–A.8)" row. This is the source-of-truth phase→milestone mapping table; future v3.6.x phases may append without re-asking the user.
- **D-03:** Patch Section 3.2 (KPI table) to add an "EAASP 平台未来工作 (OOS, 未实现)" row, listing the 4 platform-evolution gaps (production OPA approval chain / A2A Router + Event Room / L5 Cowork UI / ecosystem expansion Marketplace/多租户/SDK).
- **D-04:** Patch Section 2.2 (EAASP 全栈) L0 Protocol row to disambiguate the 16/17/21 RPC distinction in one inline block:
  - L1 contract 16 方法 = 12 MUST + 4 Optional (per v2.0 spec §8.5 + EVOLUTION_PATH §2.4)
  - runtime.proto 17 RPC = 16 spec + 1 EmitEvent (OPTIONAL per ADR-V2-001)
  - 协议总 21 RPC = 17 runtime + 4 hook (hook.proto is a separate RPC service)
- **D-05:** Add Section 5 显式平台演化缺口 (explicit platform-evolution gaps) with 4 numbered rows, one per gap, each cross-linked from Section 3.0 snapshot and Section 3.1.1.
- **D-06:** Add a "See also" cross-link block at the top of the SSOT pointing to the dated snapshot `docs/status/PRODUCT_STATUS_2026-07-18.md` (and vice versa from the snapshot).

### Snapshot depth (how much detail in the audit snapshot)
- **D-07:** Snapshot is fully self-contained with 7 sections, not a pointer summary:
  1. Executive conclusion (5 canonical facts + 4 future gaps)
  2. Grid A.0–A.8 completion table
  3. EAASP Phase 0–4a + later hardening tables
  4. L0/L1/L2/L3/L4 delivered capabilities (横切)
  5. Explicit remaining gaps
  6. Canonical terminology + source links
  7. Verification date + commit SHA range
- **D-08:** Snapshot frontmatter includes `doc_type: project-status-snapshot`, `status: canonical (<date> 快照)`, `sibling_sssot: docs/PROJECT_PRODUCT_OVERVIEW.md`, and `purpose` explaining it is a date-stamped audit snapshot.
- **D-09:** Snapshot explicitly states it is immutable: any future change goes to SSOT, then a new date-stamped snapshot is created (do not modify the existing snapshot).

### Snapshot versioning
- **D-10:** Use date-only filenames (`PRODUCT_STATUS_2026-07-18.md`). No revision number. If a same-day re-cut is needed, the second snapshot supersedes the first (delete the first and commit only the canonical replacement) — do not retain multiple snapshots for the same date.

### Validation gates
- **D-11:** 9-token parity check (Python heredoc) across both files must pass: `2026-06-17`, `A.8`, `7`, `6 comparison`, `contract-v1.1.0`, `contract-v1.2.0`, `模拟器级参考实现`, `A2A`, `Cowork`.
- **D-12:** Each commit lands with footer `Generated-By: Claude (claude-opus-4-8) via Claude Code CLI` + `Co-Authored-By: claude-flow <ruv@ruv.net>` (per project convention).
- **D-13:** Final `git status --short` shows only `docs/PROJECT_PRODUCT_OVERVIEW.md` and `docs/status/PRODUCT_STATUS_2026-07-18.md` (or staged, in two separate commits per plan-checker guidance, but no other files).

### the agent's Discretion
- Whether to commit the SSOT change and the snapshot creation as one commit or two commits. Recommendation: two commits (SSOT first, snapshot second) so the diff for the snapshot is purely additive and easy to review.
- Whether to write Phase 3.6.1 SUMMARY.md and VERIFICATION.md as part of this phase. Recommendation: yes, both are required for GSD phase close.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### EAASP design / governance
- `docs/design/EAASP/EAASP-Design-Specification-v2.0.docx` — official spec; §8.5 is the source for the 12 MUST + 4 Optional = 16 L1 contract split
- `docs/design/EAASP/EAASP_v2_0_EVOLUTION_PATH.md` — §三 8 Phase 演化路线 (the source for §3.1.1 phase→milestone mapping); §2.4 confirms L1 contract = 12 MUST + 4 Optional; §2.2 四元范式 I confirms 5-stage approval chain (one of the 4 platform gaps)
- `docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md` — strategic anchor; supersedes ADR-V2-023 Leg A/B framing with engine vs data/integration dual-axis model
- `docs/design/EAASP/adrs/ADR-V2-001` — confirms EmitEvent is OPTIONAL in runtime.proto (L1 contract = 16, runtime.proto = 17)
- `docs/design/EAASP/DEFERRED_LEDGER.md` — cross-phase D-item SSOT, 100% ✅ CLOSED on 2026-06-16
- `docs/design/EAASP/PHASE_3_DESIGN.md` — Phase 3 OPA design (intentionally not implemented yet; one of the 4 future gaps)

### Project memory
- `AGENTS.md` — current state. Lines 9-13 (LEG A/B anchored to ADR-V2-024) and lines 18-20 (SSOT reference) are the baseline that §1 of the SSOT must remain compatible with.
- `README.md` and `README.zh.md` — current public-facing narrative. Phase 3.6.2 will add Product status sections; Phase 3.6.1 should ensure the SSOT additions do not contradict current README claims.
- `.planning/STATE.md` — frontmatter `milestone: v3.6` + `status: in_progress` as of 2026-07-18; Phase 3.6.3 will update this file's body text.
- `.planning/PROJECT.md` — the pre-Activation `Current Phase` block (lines 19-30) is the most stale surface in the project; Phase 3.6.3 rewrites it. Phase 3.6.1 SSOT must not contradict the planned Phase 3.6.3 rewrite.

### Verification anchors (must reference real SHAs in the snapshot)
- `05c6d7db9cedd242a9beaf082a6ed0c59ae9ff8b` — `wip: docs-sync-2026-07-17 paused at completed+verified` (the commit Phase 3.6.1 will land on top of; pre-docs-sync baseline)
- The Phase 3.6.1 commit SHA itself — frontmatter's `verification_commit_sha` should be `05c6d7db...` (the baseline), and the snapshot's commit-record block should record both SHAs.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `docs/PROJECT_PRODUCT_OVERVIEW.md` is the SSOT to be expanded. The file already has §1, §2.1, §2.2, §2.3, §三, §四, §五, §六 with a stable table-of-contents. New content slots into §三 / §2.2 / §六 without restructuring.
- `docs/status/` directory does not exist yet — Phase 3.6.1 creates it.

### Established Patterns
- All docs/ markdown is in Chinese per the project convention (`docs/` 目录规则, see global CLAUDE.md §4). SSOT additions in Chinese. Snapshot frontmatter keys in English (so they parse cleanly), but the body of the snapshot is in Chinese to match SSOT.
- Tables use a consistent 7-8 column layout with the `**bold**` for status. Maintain that.

### Integration Points
- `AGENTS.md` (via symlink `CLAUDE.md`) and `.planning/PROJECT.md` both currently link to `docs/PROJECT_PRODUCT_OVERVIEW.md`. Phase 3.6.1 does NOT need to update those — Phase 3.6.2 and 3.6.3 do.
- The new snapshot path `docs/status/PRODUCT_STATUS_2026-07-18.md` will be referenced from Phase 3.6.2 (AGENTS.md canonical-facts block) and Phase 3.6.3 (planning state) as a sibling canonical source.

</code_context>

<specifics>
## Specific Ideas

- The SSOT must explicitly call out the 4 EAASP platform gaps as **future work**, not as "missing" or "incomplete" — this is the language GSD/ADR governance uses for OOS items. Use the same `🟡 STARTED` / `🚧` / `📦 long-term` markers as in the milestone blocks already in `.planning/PROJECT.md`.
- The snapshot's "Verification" section should pin BOTH SHAs: the baseline (`05c6d7db...`) and the Phase 3.6.1 SSOT commit SHA. Without the baseline SHA, future readers can't tell what state the snapshot freezes.
- The 4 EAASP platform gaps should be ordered by `EVOLUTION_PATH` 8-Phase sequence: Phase 3 OPA / Phase 4 A2A / Phase 5 L5 Cowork / Phase 6 ecosystem. This matches the 8-Phase route and the order future milestones will likely tackle.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within Phase 3.6.1 scope. (Phases 3.6.2 and 3.6.3 will discuss their own gray areas; no need to preempt.)

</deferred>

---

*Phase: 3.6.1*
*Context gathered: 2026-07-18*
