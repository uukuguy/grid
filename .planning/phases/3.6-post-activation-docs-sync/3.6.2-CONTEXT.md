# Phase 3.6.2: AGENTS.md canonical-facts block + CLAUDE.md symlink + README status sections - Context

**Gathered:** 2026-07-18
**Status:** Ready for planning

<domain>
## Phase Boundary

Surface the canonical-facts block (from Phase 3.6.1 SSOT) at the project's root entrypoint (`AGENTS.md` + `CLAUDE.md` symlink) and the public bilingual READMEs (`README.md` + `README.zh.md`). This phase does NOT touch `docs/PROJECT_PRODUCT_OVERVIEW.md` / `docs/status/PRODUCT_STATUS_2026-07-18.md` (Phase 3.6.1 territory) or `.planning/PROJECT.md` / `.planning/STATE.md` (Phase 3.6.3 territory).

</domain>

<decisions>
## Implementation Decisions

### AGENTS.md canonical-facts block
- **D-01:** Insert a new "Product status (canonical facts, 2026-07-17 sync)" block in AGENTS.md **between §1 (top-of-file project memory) and the "Product legs at a glance (ADR-V2-024 双轴模型)" table**. The block sits before the dual-axis framing so an agent scanning top-down encounters: (1) what this repo is, (2) what the canonical facts are, (3) how the dual-axis model organizes it.
- **D-02:** Use the 5-fact table format from the (planned) Phase 3.6.1 SSOT §3.0, with a leading "See also" pointer table that points to:
  - `docs/PROJECT_PRODUCT_OVERVIEW.md` (the maintained SSOT)
  - `docs/status/PRODUCT_STATUS_2026-07-17.md` (the dated audit snapshot, to be created by Phase 3.6.1)
  - `docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md` and `docs/design/EAASP/adrs/ADR-V2-029-engine-data-integration-boundary.md` (the dual-axis strategic anchors)
  - `docs/design/EAASP/EAASP-Design-Specification-v2.0.docx` (the EAASP v2.0 spec of record)
- **D-03:** Keep the existing "Product legs at a glance" table and the existing "## EAASP v2 Architecture (L0–L4)" diagram **byte-identical**. They already carry the post-Activation framing (8 phases SHIPPED 2026-06-17, 7 runtimes, contract-v1.2.0 current, simulator-level reference impls) — the new block is additive, not a rewrite.
- **D-04:** Preserve the existing "see ADR-V2-024 supersedes ADR-V2-023" anchors on every Leg A/B historical phrase. Do NOT scrub Leg A/B wording — the historical anchor notes are project convention.

### CLAUDE.md symlink
- **D-05:** Create `CLAUDE.md` as a **relative symlink** to `AGENTS.md` (target: `AGENTS.md`, not an absolute path). Recorded as a git symlink (mode 120000). The symlink target string is exactly `AGENTS.md\n` (the standard git symlink blob format).
- **D-06:** The symlink is created via `ln -s AGENTS.md CLAUDE.md` followed by `git add -A` so git records it as mode 120000. No separate content file; the symlink IS the file.
- **D-07:** If `CLAUDE.md` already exists as a regular file or a symlink with a different target in the working tree, the plan must abort and ask the user (do not silently overwrite).

### README.md (English) Product status section
- **D-08:** Insert a `## Product status` section **immediately before the existing `## Quick Start` section**. Same 5 canonical facts as the SSOT/AGENTS block, plus a single line: "For the maintained product-status overview, see `docs/PROJECT_PRODUCT_OVERVIEW.md`."
- **D-09:** **Do NOT modify** the existing README body (e.g. the `16-method gRPC contract` line mentioning `contract-v1.1.0 test suite` in §1). The new Product status section is the authoritative location for current-status claims; existing body text is preserved as historical product-design context.

### README.zh.md (Chinese) Product status section
- **D-10:** Insert a `## 产品状态` section immediately before the existing `## 快速开始` section.
- **D-11:** The Chinese text is **technical Chinese with retained technical terms** (gRPC, L1 runtime, SSOT, activation, A2A, Cowork, etc.) — the same bilingual writing style as the existing README.zh.md body and AGENTS.md. The wording is **genuinely Chinese-written, not a literal English-to-Chinese word-by-word translation**. Concrete examples:
  - "Grid 独立产品 Activation 八个阶段（A.0–A.8）已于 2026-06-17 全部交付" (not a literal "A.0–A.8 shipped on 2026-06-17"→"A.0–A.8 在 2026-06-17 装运")
  - "`contract-v1.1.0` 是 Phase 3 历史签字版合同" (not "Phase 3 historical sign-off"→"Phase 3 历史签收")
- **D-12:** Do NOT modify the existing README.zh.md body.

### Commit strategy
- **D-13:** Use **two separate commits** in the order: (1) AGENTS.md + CLAUDE.md symlink together as commit 1, (2) README.md + README.zh.md together as commit 2. This mirrors Phase 3.6.1's SSOT-then-snapshot ordering and keeps each commit's diff cleanly reviewable.
- **D-14:** Each commit ends with the project footer:
  ```
  Generated-By: Claude (claude-opus-4-8) via Claude Code CLI
  Co-Authored-By: claude-flow <ruv@ruv.net>
  ```

### Validation gates
- **D-15:** Symlink test: `test -L CLAUDE.md && test "$(readlink CLAUDE.md)" = "AGENTS.md"` must pass.
- **D-16:** Bilingual parity: both `README.md` and `README.zh.md` must contain `2026-06-17`, `A.8`, and `contract-v1.2.0` (3-token check). Additionally the English README must contain `6 comparison` and the Chinese README must contain `6 个对比` (or `6 个 comparison`).
- **D-17:** Final `git status --short` must show ONLY AGENTS.md (modified) + CLAUDE.md (new symlink) in commit 1, and ONLY README.md + README.zh.md (both modified) in commit 2. No untracked scratch, no edits outside the four intended files.
- **D-18:** No edits to `crates/`, `lang/`, `tools/`, `web/`, `web-platform/`, `proto/`, `Cargo.toml`, `Cargo.lock`, `package.json`, `uv.lock`, `pyproject.toml`, `.planning/`, or any non-documentation file.

### the agent's Discretion
- Exact placement of the "See also" table within the AGENTS.md block (above the 5-fact table vs. below it). Recommendation: below the 5-fact table.
- Whether to commit the AGENTS+CLAUDE pair as one commit and README pair as a second commit (D-13) or batch all four file changes into a single commit. Recommendation: two commits (D-13) for clean reviewability.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Maintained SSOT and dated snapshot
- `docs/PROJECT_PRODUCT_OVERVIEW.md` — maintained product-status SSOT (Phase 3.6.1 target). Phase 3.6.2 does NOT modify this file but its content is the source of the 5 canonical facts.
- `docs/status/PRODUCT_STATUS_2026-07-17.md` — date-stamped audit snapshot to be created by Phase 3.6.1. Phase 3.6.2's AGENTS.md and READMEs point to this file by path; if Phase 3.6.1 has not yet been merged when 3.6.2 executes, the forward-reference link will be broken in the working tree until 3.6.1 lands. The plan must note this transient broken-link window.

### Strategic ADRs
- `docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md` — dual-axis substance (engine vs data/integration).
- `docs/design/EAASP/adrs/ADR-V2-029-engine-data-integration-boundary.md` — dual-axis crate-level enforcement.
- `docs/design/EAASP/adrs/ADR-V2-017` — `hermes-runtime-python` frozen status (1+6=7 L1 runtimes rationale).
- `docs/design/EAASP/adrs/ADR-V2-026-` and `ADR-V2-027-` — `contract-v1.2.0` provenance (Phase 5.3, ExecutionMode + OpenAI-compat Quirks).

### Project memory
- `AGENTS.md` — current state. The 5 canonical facts being added to the new Product status block must NOT contradict any of AGENTS.md's existing claims about L1 runtimes, contract versions, EAASP architecture, or dual-axis framing. Specifically:
  - Line 11: "tools/eaasp-*/" is simulator-level reference implementation; no upstream EAASP project
  - Line 13: Activation SHIPPED 2026-06-17
  - Lines 18-20: SSOT reference (already points to `docs/PROJECT_PRODUCT_OVERVIEW.md` which Phase 3.6.1 will populate; this remains valid as a forward reference)
  - Line 28-30: "see ADR-V2-024 supersedes ADR-V2-023" anchor pattern
  - Line 54-55: 7 L1 runtimes (1 + 6 comparison + hermes frozen)
  - Line 65: `contract-v1.2.0` current
  - Line 99: `contract-v1.1.0` Phase 3 sign-off
  - Line 56-57: 21 RPC total = 17 runtime + 4 hook; "16 methods" is shorthand
- `README.md` and `README.zh.md` — current public-facing narrative. The new Product status sections must not contradict existing claims:
  - README.md line 24: "16-method gRPC contract... 6 comparison runtimes... pass the same `contract-v1.1.0` test suite" — historical accuracy, preserve
  - README.md line 220: "16-method gRPC, 6 comparison runtimes" — preserve
  - README.zh.md line 24: same content in Chinese — preserve

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `AGENTS.md` already contains a complete EAASP v2 architecture diagram (L0-L4) with 5-layer / 3-pipeline / 4-paradigm / 7-phase framing. The new canonical-facts block references this existing diagram but does not duplicate it.
- `AGENTS.md` already references `docs/PROJECT_PRODUCT_OVERVIEW.md` as the "项目级 single source of truth" (lines 18-20). The new block's "See also" table extends this reference with the dated snapshot and the dual-axis ADRs.
- `README.md` and `README.zh.md` are bilingual. The Chinese version uses genuine technical Chinese with retained English technical terms; the new `产品状态` section follows the same style.

### Established Patterns
- AGENTS.md uses Chinese for body prose with retained English technical terms (e.g. "gRPC L1 contract", "runtime", "shadows", etc.). New block matches this.
- README.md uses English, README.zh.md uses Chinese. New Product status sections are bilingual but each language is independent prose, not literal translation.
- Commit footer pattern is two-line: `Generated-By: ...` + `Co-Authored-By: ...`.

### Integration Points
- Phase 3.6.1's `docs/status/PRODUCT_STATUS_2026-07-17.md` will exist after 3.6.1 ships. Phase 3.6.2's AGENTS.md block points to this path; if 3.6.2 executes before 3.6.1 lands, the file is forward-referenced.
- Phase 3.6.3 will update `.planning/STATE.md` to remove the "激活后现实未对齐" framing. Phase 3.6.2's AGENTS.md and READMEs update the public surfaces first; the planning state aligns last.

</code_context>

<specifics>
## Specific Ideas

- The AGENTS.md "See also" table should follow the format pattern already in use at `docs/PROJECT_PRODUCT_OVERVIEW.md` §6.2: a small table with 3-4 rows pointing to key references, each row a single relative path with one-line description.
- The Chinese `产品状态` section should mirror the structure of the English `Product status` section but be genuinely rewritten in Chinese, not literal translation. Example: instead of "The repository contains 7 L1 runtimes in total, including 6 comparison runtimes", the Chinese version should read something like "仓库共包含 7 个 L1 运行时（含 6 个对比运行时）" — same meaning, different sentence structure.
- When Phase 3.6.2 executes before Phase 3.6.1, the "See also" forward-reference to `docs/status/PRODUCT_STATUS_2026-07-17.md` will be a broken link in the working tree. The plan must validate that the forward-reference path is the **same path Phase 3.6.1 will create** (so the link is future-correct, just transiently broken).
- If the user requests sequencing, Phase 3.6.2 should land AFTER 3.6.1 to avoid transient broken links in `main`. The order is `3.6.1 → 3.6.2 → 3.6.3`.

</specifics>

<deferred>
## Deferred Ideas

- **README screenshots / diagrams** — Adding a visual diagram of the L0–L4 architecture in the Product status section is nice-to-have but not in scope. The existing EAASP architecture diagram in `AGENTS.md` (lines 43-58) is sufficient.
- **Translated CLAUDE.md body** — Some projects keep a Chinese mirror at `CLAUDE.zh.md` for Codex + Claude Code bilingual coverage. Out of scope here; the `README.zh.md` + `AGENTS.md` Chinese body is sufficient.
- **i18n automation** — Future workflow could generate `README.<lang>.md` from a single source. Out of scope.
- **Phase 3.6.1 + 3.6.3 sequencing** — Whether to execute 3.6.2 before or after 3.6.1 is a sequencing decision, not a 3.6.2 scope decision. The 3.6.2 plan's execution must be tolerant of either order (forward-references resolve to whichever file lands last).

</deferred>

---

*Phase: 3.6.2*
*Context gathered: 2026-07-18*
