---
phase: 3.6-post-activation-docs-sync
plan: 3.6.2-01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - README.md
  - README.zh.md
autonomous: true
requirements:
  - DOCS-3.6.2
must_haves:
  truths:
    - "Agents entering through AGENTS.md or CLAUDE.md see the same five post-Activation canonical facts before the dual-axis product table."
    - "CLAUDE.md is a repository-tracked relative symlink whose target is exactly AGENTS.md."
    - "English and Chinese readers see matching current product-status claims immediately before Quick Start, while the existing README bodies remain unchanged."
    - "The work lands as two ordered documentation commits: AGENTS.md + CLAUDE.md first, then README.md + README.zh.md."
    - "No source, dependency, planning-state, superpowers, lwm, project-state, or scratch artifact enters either commit."
  artifacts:
    - path: "AGENTS.md"
      provides: "Canonical five-fact block and reference table"
      contains: "## Product status (canonical facts, 2026-07-17 sync)"
    - path: "CLAUDE.md"
      provides: "Claude Code entrypoint sharing AGENTS.md content"
      contains: "AGENTS.md"
    - path: "README.md"
      provides: "English Product status section"
      contains: "## Product status"
    - path: "README.zh.md"
      provides: "Genuinely written technical-Chinese 产品状态 section"
      contains: "## 产品状态"
  key_links:
    - from: "CLAUDE.md"
      to: "AGENTS.md"
      via: "relative symbolic link"
      pattern: "^AGENTS\\.md$"
    - from: "AGENTS.md"
      to: "docs/PROJECT_PRODUCT_OVERVIEW.md"
      via: "See also reference"
      pattern: "docs/PROJECT_PRODUCT_OVERVIEW\\.md"
    - from: "AGENTS.md"
      to: "docs/status/PRODUCT_STATUS_2026-07-17.md"
      via: "date-stamped audit snapshot forward-reference"
      pattern: "docs/status/PRODUCT_STATUS_2026-07-17\\.md"
    - from: "README.md"
      to: "docs/PROJECT_PRODUCT_OVERVIEW.md"
      via: "maintained product-status overview pointer"
      pattern: "For the maintained product-status overview"
    - from: "README.zh.md"
      to: "docs/PROJECT_PRODUCT_OVERVIEW.md"
      via: "maintained SSOT pointer"
      pattern: "docs/PROJECT_PRODUCT_OVERVIEW\\.md"
---

<objective>
Surface the five canonical post-Activation facts through the repository's agent entrypoints and bilingual public READMEs.

Purpose: Ensure agents and external readers encounter the current Grid/EAASP status without erasing historically accurate contract-v1.1.0 context.
Output: One additive AGENTS.md block, a relative CLAUDE.md symlink, and paired English/Chinese README status sections, delivered in two ordered commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/3.6-post-activation-docs-sync/3.6.2-CONTEXT.md
@.planning/phases/3.6-post-activation-docs-sync/3.6.1-CONTEXT.md
@AGENTS.md
@README.md
@README.zh.md

Phase 3.6.1 owns `docs/PROJECT_PRODUCT_OVERVIEW.md` and its dated snapshot; this plan only links to those outputs. The locked 3.6.2 path is `docs/status/PRODUCT_STATUS_2026-07-17.md`. If Phase 3.6.1 has not landed, that link is intentionally a transient forward-reference; verify the path text, do not create or modify the target here.

<interfaces>
Exact current edit anchors, each unique in its file:

- `AGENTS.md`: insert immediately before the single line `## Product legs at a glance (ADR-V2-024 双轴模型, supersedes ADR-V2-023)`. Preserve the existing separator immediately above it and the entire existing Product legs table and EAASP architecture section byte-for-byte per D-03/D-04.
- `README.md`: insert immediately before the single line `## Quick Start`; preserve the existing separator immediately above it and every pre-existing README line per D-08/D-09.
- `README.zh.md`: insert immediately before the single line `## 快速开始`; preserve the existing separator immediately above it and every pre-existing README line per D-10/D-12.

Do not use line numbers as edit anchors; use the exact unique heading strings above.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical-facts block to AGENTS.md</name>
  <files>AGENTS.md</files>
  <action>
Read AGENTS.md, then insert the following exact additive block immediately before the unique `## Product legs at a glance (ADR-V2-024 双轴模型, supersedes ADR-V2-023)` heading, per D-01. Keep the existing separator above that heading, Product legs table, historical Leg A/B see-links, and EAASP architecture block byte-identical per D-03 and D-04. The five facts match the Phase 3.6.1 SSOT §3.0 decision set; the See also table follows the facts per Claude's discretion and D-02.

```markdown
## Product status (canonical facts, 2026-07-17 sync)

| Canonical fact | Current status |
|----------------|----------------|
| Grid 独立产品 Activation | A.0–A.8 八个阶段已于 **2026-06-17 全部交付**。 |
| EAASP core engineering | 当前参考实现的 L0/L1/L2/L3/L4 核心工程与 runtime-contract validation 已完成。 |
| L1 runtimes | 仓库共包含 **7 个 L1 runtime**，其中 **6 个 comparison runtime**。 |
| Runtime contract | `contract-v1.1.0` 是 Phase 3 历史签字版合同；`contract-v1.2.0` 是当前最新合同。 |
| EAASP platform scope | `tools/eaasp-*` 是 EAASP v2.0 平台合同的模拟器级参考实现；不存在独立的“上游 EAASP”项目。Phase 3 production OPA approval chain、Phase 4 A2A / Event Room、Phase 5 L5 Cowork UI、Phase 6 ecosystem expansion 仍待推进。 |

### See also

| Reference | Role |
|-----------|------|
| [`docs/PROJECT_PRODUCT_OVERVIEW.md`](docs/PROJECT_PRODUCT_OVERVIEW.md) | 持续维护的产品状态 SSOT。 |
| [`docs/status/PRODUCT_STATUS_2026-07-17.md`](docs/status/PRODUCT_STATUS_2026-07-17.md) | 2026-07-17 日期化审计快照；若 Phase 3.6.1 尚未落地，此链接是暂时的 forward-reference。 |
| [`ADR-V2-024`](docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md) / [`ADR-V2-029`](docs/design/EAASP/adrs/ADR-V2-029-engine-data-integration-boundary.md) | engine vs data/integration 双轴战略锚点与 crate-level 边界。 |
| [`EAASP-Design-Specification-v2.0.docx`](docs/design/EAASP/EAASP-Design-Specification-v2.0.docx) | EAASP v2.0 spec of record。 |

---
```

Do not rewrite nearby prose, normalize punctuation elsewhere, scrub historical Leg A/B terms, or touch any other file in this step.
  </action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; p=Path("AGENTS.md"); s=p.read_text(); h="## Product legs at a glance (ADR-V2-024 双轴模型, supersedes ADR-V2-023)"; n="## Product status (canonical facts, 2026-07-17 sync)"; assert s.count(h)==1 and s.count(n)==1 and s.index(n)&lt;s.index(h); required=["2026-06-17","A.0–A.8","7 个 L1 runtime","6 个 comparison runtime","contract-v1.1.0","contract-v1.2.0","模拟器级参考实现","docs/status/PRODUCT_STATUS_2026-07-17.md","ADR-V2-024-phase4-product-scope-decision.md","ADR-V2-029-engine-data-integration-boundary.md","EAASP-Design-Specification-v2.0.docx"]; assert all(x in s for x in required)'</automated>
  </verify>
  <done>AGENTS.md contains exactly one five-fact block before the unique Product legs heading, all D-02 references are present, and existing dual-axis/historical architecture content is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create the CLAUDE.md symlink, validate scope, and land commit 1</name>
  <files>AGENTS.md, CLAUDE.md</files>
  <action>
Per D-05 through D-07, inspect without following the path: `if [ -e CLAUDE.md ] || [ -L CLAUDE.md ]; then ... fi`. If CLAUDE.md exists as any regular file or symlink, abort and ask the user; do not remove or overwrite it. Otherwise create the exact relative symlink with `ln -s AGENTS.md CLAUDE.md` and stage it with `git add CLAUDE.md` so Git records mode 120000. Stage AGENTS.md explicitly with `git add AGENTS.md`; do not stage unrelated paths and do not use `git add -A` because the locked scope requires excluding pre-existing or scratch changes.

Run the D-15 symlink gate and assert `git ls-files -s CLAUDE.md` reports mode `120000`. Before commit, enforce D-17/D-18 by checking the staged path set is exactly `AGENTS.md` and `CLAUDE.md`, the unstaged/untracked set is empty, and no forbidden source/config/planning path is staged or modified. In particular, fail if any superpowers, lwm, project-state, `.planning/`, scratch, or non-documentation artifact appears in `git status --short`.

Create commit 1 with a concise docs subject such as `docs: expose canonical product facts to agents` and this exact footer:

```text
Generated-By: Claude (claude-opus-4-8) via Claude Code CLI
Co-Authored-By: claude-flow <ruv@ruv.net>
```

After committing, confirm the working tree is clean before Task 3. This cleanliness is what makes the next D-17 gate meaningfully isolate commit 2.
  </action>
  <verify>
    <automated>test -L CLAUDE.md &amp;&amp; test "$(readlink CLAUDE.md)" = "AGENTS.md" &amp;&amp; test "$(git ls-files -s CLAUDE.md | cut -d" " -f1)" = "120000" &amp;&amp; test -z "$(git status --short)" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r HEAD | sort)" = "$(printf '%s\n' AGENTS.md CLAUDE.md | sort)"</automated>
  </verify>
  <done>Commit 1 contains only AGENTS.md and CLAUDE.md; CLAUDE.md is a mode-120000 relative symlink to AGENTS.md; the working tree is clean and no forbidden path was touched.</done>
</task>

<task type="auto">
  <name>Task 3: Add matched English and Chinese README product-status sections</name>
  <files>README.md, README.zh.md</files>
  <action>
Read both READMEs. Per D-08/D-09, insert the following exact English section immediately before the unique `## Quick Start` heading, leaving the separator immediately above the heading and all existing body text unchanged:

```markdown
## Product status

- Grid Activation A.0–A.8 shipped on 2026-06-17.
- EAASP core L0/L1/L2/L3/L4 engineering and runtime-contract validation are complete for the current reference implementation.
- The repository contains 7 L1 runtimes in total, including 6 comparison runtimes.
- `contract-v1.1.0` is the Phase 3 historical sign-off; `contract-v1.2.0` is the current latest contract.
- `tools/eaasp-*` are simulator-level reference implementations of the EAASP v2.0 platform; there is no separate upstream EAASP project.
- Future EAASP platform work (Phase 3 production OPA approval chain, Phase 4 A2A / Event Room, Phase 5 L5 Cowork UI, Phase 6 ecosystem expansion) remains pending.

For the maintained product-status overview, see [`docs/PROJECT_PRODUCT_OVERVIEW.md`](docs/PROJECT_PRODUCT_OVERVIEW.md).

---
```

Per D-10 through D-12, insert the following exact genuinely rewritten technical-Chinese section immediately before the unique `## 快速开始` heading, preserving all existing Chinese README body text:

```markdown
## 产品状态

- Grid 独立产品 Activation 八个阶段（A.0–A.8）已于 2026-06-17 全部交付。
- 当前参考实现已经完成 EAASP core L0/L1/L2/L3/L4 工程与 runtime-contract validation。
- 仓库共包含 7 个 L1 运行时（含 6 个对比运行时）。
- `contract-v1.1.0` 是 Phase 3 历史签字版合同；`contract-v1.2.0` 是当前最新合同。
- `tools/eaasp-*` 是 EAASP v2.0 平台合同的模拟器级参考实现，不存在独立的“上游 EAASP”项目。
- 后续 EAASP 平台演进（Phase 3 生产级 OPA 审批链、Phase 4 A2A / Event Room、Phase 5 L5 Cowork UI、Phase 6 生态扩展）仍待推进。

持续维护的产品状态 SSOT 见 [`docs/PROJECT_PRODUCT_OVERVIEW.md`](docs/PROJECT_PRODUCT_OVERVIEW.md)。

---
```

Do not update the existing historical `contract-v1.1.0` passages, architecture diagrams, headings, punctuation, formatting, or other README content. Only these two insertions are permitted.
  </action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; en=Path("README.md").read_text(); zh=Path("README.zh.md").read_text(); assert en.count("## Product status")==1 and en.count("## Quick Start")==1 and en.index("## Product status")&lt;en.index("## Quick Start"); assert zh.count("## 产品状态")==1 and zh.count("## 快速开始")==1 and zh.index("## 产品状态")&lt;zh.index("## 快速开始"); common=["2026-06-17","A.8","contract-v1.2.0"]; assert all(x in en and x in zh for x in common); assert "6 comparison" in en and "6 个对比" in zh; assert "For the maintained product-status overview" in en and "持续维护的产品状态 SSOT" in zh'</automated>
  </verify>
  <done>Both status sections appear exactly once before their Quick Start headings, carry the locked claims and parity tokens, and leave the historical README bodies intact.</done>
</task>

<task type="auto">
  <name>Task 4: Validate bilingual parity and scope, then land commit 2</name>
  <files>README.md, README.zh.md</files>
  <action>
Stage only README.md and README.zh.md. Enforce D-16 with the automated bilingual parity check: both files contain `2026-06-17`, `A.8`, and `contract-v1.2.0`; English contains `6 comparison`; Chinese contains `6 个对比`. Enforce D-17 by requiring the staged path set to be exactly README.md and README.zh.md and no unstaged/untracked paths to exist. Enforce D-18 by failing on any status entry under `crates/`, `lang/`, `tools/`, `web/`, `web-platform/`, `proto/`, `.planning/`, dependency manifests/locks, or any superpowers/lwm/project-state/scratch artifact.

Create commit 2 only after those gates pass, with a concise docs subject such as `docs: publish bilingual product status` and this exact footer:

```text
Generated-By: Claude (claude-opus-4-8) via Claude Code CLI
Co-Authored-By: claude-flow <ruv@ruv.net>
```

After commit, verify HEAD contains exactly the README pair, HEAD^ contains exactly AGENTS.md and CLAUDE.md, the symlink still passes D-15, bilingual parity still passes D-16, and the working tree is clean. Do not amend, squash, reorder, or add a third commit.
  </action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; en=Path("README.md").read_text(); zh=Path("README.zh.md").read_text(); common=["2026-06-17","A.8","contract-v1.2.0"]; assert all(x in en and x in zh for x in common); assert "6 comparison" in en and "6 个对比" in zh' &amp;&amp; test -L CLAUDE.md &amp;&amp; test "$(readlink CLAUDE.md)" = "AGENTS.md" &amp;&amp; test -z "$(git status --short)" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r HEAD | sort)" = "$(printf '%s\n' README.md README.zh.md | sort)" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r HEAD^ | sort)" = "$(printf '%s\n' AGENTS.md CLAUDE.md | sort)"</automated>
  </verify>
  <done>Commit 2 contains only the bilingual README pair, follows commit 1, all D-15 through D-18 gates pass, and the repository is clean after exactly two scoped commits.</done>
</task>

</tasks>

<verification>
Run from the repository root after both commits:

1. Symlink and Git mode (D-05/D-15):
   `test -L CLAUDE.md && test "$(readlink CLAUDE.md)" = "AGENTS.md" && test "$(git ls-files -s CLAUDE.md | cut -d" " -f1)" = "120000"`
2. Bilingual parity (D-16): use Task 4's Python assertion; it checks the three shared tokens plus language-specific comparison-runtime wording.
3. Ordered commit isolation (D-13/D-17):
   - `git diff-tree --no-commit-id --name-only -r HEAD^` resolves to only AGENTS.md and CLAUDE.md.
   - `git diff-tree --no-commit-id --name-only -r HEAD` resolves to only README.md and README.zh.md.
4. Documentation-only cleanliness (D-18): `git status --short` is empty after commit 2; neither of the two commit path sets contains `.planning/`, source trees, manifests, lockfiles, superpowers, lwm, project-state, or scratch artifacts.
5. Anchor integrity: each new heading occurs once and precedes its unique target heading; the validation commands in Tasks 1 and 3 prove this mechanically.
6. Forward-reference fidelity: AGENTS.md contains exactly `docs/status/PRODUCT_STATUS_2026-07-17.md`; absence of that target before Phase 3.6.1 is allowed and must not trigger creation in this plan.
</verification>

<success_criteria>
- D-01 through D-04: AGENTS.md has the exact additive five-fact block and See also references before Product legs; existing Product legs and EAASP architecture content is preserved.
- D-05 through D-07: CLAUDE.md is created only when absent, points relatively to AGENTS.md, and is tracked with mode 120000.
- D-08 through D-12: exact English and genuinely written technical-Chinese status sections appear immediately before their Quick Start headings without changing existing bodies.
- D-13/D-14: two sequential commits land with the required model/footer text; commit 1 is AGENTS+CLAUDE and commit 2 is the README pair.
- D-15 through D-18: symlink, bilingual parity, exact commit path sets, and no-source/no-artifact gates all pass.
- The only phase implementation artifacts are AGENTS.md, CLAUDE.md, README.md, and README.zh.md; no deferred idea is implemented.
</success_criteria>

<source_audit>
| SOURCE | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Canonical facts at root agent entrypoints and public bilingual READMEs | 1-4 | COVERED | Two ordered commits |
| REQ | DOCS-3.6.2 | Phase 3.6.2 roadmap scope | 1-4 | COVERED | ROADMAP has no formal REQ-ID; local phase identifier preserves non-empty plan schema |
| RESEARCH | — | Research intentionally skipped | — | EXCLUDED | Locked docs-only decision |
| CONTEXT | D-01–D-04 | AGENTS placement, table, preservation, historical anchors | 1 | COVERED | Exact block and unique anchor supplied |
| CONTEXT | D-05–D-07 | Relative symlink, mode 120000, abort on existing path | 2 | COVERED | No overwrite path |
| CONTEXT | D-08–D-09 | English status before Quick Start; preserve body | 3 | COVERED | Exact English text supplied |
| CONTEXT | D-10–D-12 | Chinese status before 快速开始; genuine technical Chinese; preserve body | 3 | COVERED | Exact Chinese text supplied |
| CONTEXT | D-13–D-14 | Two ordered commits and exact footers | 2, 4 | COVERED | Commit isolation verified |
| CONTEXT | D-15–D-18 | Symlink, parity, exact status scope, no source edits | 2, 4 | COVERED | Automated gates supplied |
| CONTEXT | Deferred | Screenshots, translated CLAUDE body, i18n automation, sequencing decision | — | EXCLUDED | Explicitly deferred |
</source_audit>

<output>
After completion, create `.planning/phases/3.6-post-activation-docs-sync/3.6.2-01-SUMMARY.md`.
</output>
