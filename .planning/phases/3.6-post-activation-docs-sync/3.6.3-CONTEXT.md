# Phase 3.6.3: .planning/PROJECT.md + .planning/STATE.md sync - Context

**Gathered:** 2026-07-19
**Status:** Ready for planning

<domain>
## Phase Boundary

Bring the GSD planning state (`.planning/PROJECT.md` and `.planning/STATE.md`) into alignment with the post-Activation reality that Phases 3.6.1 and 3.6.2 have documented in the public surfaces (`docs/PROJECT_PRODUCT_OVERVIEW.md`, `AGENTS.md`, `CLAUDE.md`, `README.md`, `README.zh.md`). This phase is documentation-only and does NOT touch AGENTS.md, CLAUDE.md, READMEs, the docs/ tree, or any source code.

</domain>

<decisions>
## Implementation Decisions

### STATE.md frontmatter (GSD standard format)
- **D-01:** Rewrite `.planning/STATE.md` frontmatter to GSD's canonical schema, replacing the current non-standard `milestone: v3.6, milestone_name: ..., status: in_progress, v3.5_shipped: true, activation_shipped: true` block with:
  ```yaml
  ---
  gsd_state_version: 1.0
  milestone: v3.6
  milestone_name: v3.6 Post-Activation Docs Sync
  status: in_progress
  stopping: Phase 3.6 docs sync in progress; previous milestones v3.0–v3.5 and Grid 独立产品 Activation SHIPPED 2026-06-17.
  last_updated: "2026-07-19"
  last_activity: "2026-07-19"
  progress:
    total_phases: 3
    completed_phases: 0
    total_plans: 3
    completed_plans: 0
    percent: 0
  ---
  ```
- **D-02:** Frontmatter must remain a single YAML block at the top of the file. No additional fields beyond what GSD's `state begin-phase` / `init phase-op` recognizes (keeps the file gsd-tools-compatible).
- **D-03:** The `progress` block reports the **v3.6 milestone** phase count (3 sub-phases 3.6.1/3.6.2/3.6.3), NOT the cumulative project-wide count.

### STATE.md body
- **D-04:** Update the "Core value" line: replace `16-method gRPC contract` and `any contract-v1.1.0 compliant runtime` with `contract-v1.2.0` (current) + `contract-v1.1.0` (historical Phase 3 sign-off) — see `AGENTS.md` line 65/99 for the canonical phrasing.
- **D-05:** Update the "Current focus" line: replace `Grid 独立产品 Activation — ALL 8 PHASES COMPLETE` with `Post-Activation. Grid 独立产品 Activation SHIPPED 2026-06-17. v3.6 Post-Activation Docs Sync in progress.`
- **D-06:** Add a "Canonical product-status sources" block to the "Project Reference" section pointing to:
  - `docs/PROJECT_PRODUCT_OVERVIEW.md` (maintained SSOT)
  - `docs/status/PRODUCT_STATUS_2026-07-19.md` (dated audit snapshot — date updated to today; if 3.6.1 hasn't landed, the link is a forward reference)
- **D-07:** Fix the "4/7 components at 9.0+" prose at the end of the Audit Findings Summary to `5/7 components at 9.0+` (the scoreboard table has 5 entries at 9.0: grid-cli, web/, grid-server, grid-eval, grid-platform; web-platform/ at 7.5; grid-desktop at 6.5).
- **D-08:** Replace the "Blockers/Concerns" section: remove the false "Grid 独立产品 dormancy" entry; replace with:
  - **Quality gaps in shipped components**: `web-platform/` (Quality 7.5) and `grid-desktop` (Quality 6.5) shipped with Activation but remain below the 9.0+ bar. Need follow-on feature work.
  - **EAASP v2.0 platform-evolution gaps (explicit future work)**: production OPA approval chain (Phase 3), A2A / Event Room (Phase 4), L5 Cowork UI (Phase 5), ecosystem expansion (Phase 6) — per `docs/design/EAASP/EAASP_v2_0_EVOLUTION_PATH.md`. Out of post-Activation scope; future milestone candidates.
  - **138 unpushed commits**: accumulated across v3.2–v3.5. Push decision deferred to user.
  - **Local environment**: `.env` has `OPENAI_NO_PROXY=1` for Clash. `LLM_PROVIDER=openai` code default.
- **D-09:** Update "Session Continuity" with the 2026-07-19 docs-sync session entry at the top of the prior-sessions list.

### PROJECT.md body
- **D-10:** Rewrite the "Current Phase" block (lines 19-30) from "Grid 独立产品 Activation (Post-Debt)" framing to "Post-Activation (next milestone TBD)". Per-project "Activation targets" table at lines 25-30 must be removed (the per-crate activation status now lives in the SSOT §3.1 and the 2026-07-19 audit summary).
- **D-11:** Update "Activation has SHIPPED" framing: add a one-paragraph "Post-Activation scope" block after the Current Phase rewrite that explains: (1) `web-platform/` 7.5/10 + `grid-desktop` 6.5/10 quality gap closure is a candidate next milestone; (2) EAASP v2.0 platform-evolution gaps (4 items) are explicit future milestones, not part of post-Activation. Cite `docs/PROJECT_PRODUCT_OVERVIEW.md` and `docs/status/PRODUCT_STATUS_2026-07-19.md` as canonical sources.
- **D-12:** Move the v3.5 Debt Finalization entry from "Active" (line 75) to "Validated" (it's shipped; pre-Activation). The Active section must now list:
  - v3.6 Post-Activation Docs Sync (in progress)
  - Phase 3 OPA / Phase 4 A2A / Phase 5 L5 Cowork / Phase 6 ecosystem — future milestone candidates
- **D-13:** Update "Core Value" to state `contract-v1.2.0` is current, `contract-v1.1.0` is historical Phase 3 sign-off (same as STATE.md D-04).
- **D-14:** Add a "See also (canonical sources)" block to PROJECT.md's "Context" or "What This Is" section pointing to `docs/PROJECT_PRODUCT_OVERVIEW.md` and `docs/status/PRODUCT_STATUS_2026-07-19.md`.

### Commit strategy
- **D-15:** Two separate atomic commits: (1) `.planning/STATE.md`, (2) `.planning/PROJECT.md`. Each commit lands with the project footer:
  ```
  Generated-By: Claude (claude-opus-4-8) via Claude Code CLI
  Co-Authored-By: claude-flow <ruv@ruv.net>
  ```
- **D-16:** Commit order: STATE.md first (smaller, higher-impact, also re-establishes gsd-tools compat), PROJECT.md second (broader rewrite, depends on STATE.md's canonical-source pointer being defined first).

### Validation gates
- **D-17:** After both commits, the project can run `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init phase-op 3.6.1` (or any 3.6.x sub-phase) successfully with `phase_found: true` — confirming the frontmatter is GSD-compatible. If 3.6.1's plan has been merged, this is a no-op; if not yet, it confirms the standard format works for future plan-phase invocations.
- **D-18:** After both commits, `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init milestone-op` returns `milestone_version: "v3.6"` with no errors.
- **D-19:** 3-token parity check on both files must pass: `2026-06-17`, `contract-v1.2.0`, `PROJECT_PRODUCT_OVERVIEW.md`.
- **D-20:** No edits to `crates/`, `lang/`, `tools/`, `web/`, `web-platform/`, `proto/`, `Cargo.toml`, `Cargo.lock`, `package.json`, `uv.lock`, `pyproject.toml`, `AGENTS.md`, `CLAUDE.md`, `README.md`, `README.zh.md`, `docs/`, or `.planning/ROADMAP.md`.
- **D-21:** Final `git status --short` shows exactly two files modified: `.planning/STATE.md` and `.planning/PROJECT.md` (in their respective commits). No untracked scratch.

### the agent's Discretion
- Exact wording of the rewritten "Current Phase" block in PROJECT.md (D-10): the "Post-Activation scope" prose can be 2-4 paragraphs depending on how much detail is useful for a future reader.
- Whether the `last_activity` field in STATE.md frontmatter is `2026-07-19` (today) or `2026-07-18` (yesterday, the date the prior commits landed). Recommendation: `2026-07-19` since the 3.6.3 commits themselves happen today.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Maintained SSOT and dated snapshot
- `docs/PROJECT_PRODUCT_OVERVIEW.md` — maintained product-status SSOT (Phase 3.6.1 target). Phase 3.6.3 does NOT modify this file but its content is the source of the canonical facts. If 3.6.1 has not yet landed when 3.6.3 executes, the frontmatter `canonical sources` block has a forward reference to `docs/PROJECT_PRODUCT_OVERVIEW.md` (which exists) and to `docs/status/PRODUCT_STATUS_2026-07-19.md` (which may not yet exist).
- `docs/status/PRODUCT_STATUS_2026-07-19.md` — date-stamped audit snapshot to be created by Phase 3.6.1, dated to today. If 3.6.1 hasn't landed, the link is forward-referenced; the plan must note this transient broken-link window.

### EAASP design / governance
- `docs/design/EAASP/EAASP_v2_0_EVOLUTION_PATH.md` — source for the 4 EAASP platform-evolution gaps (Phase 3 OPA / Phase 4 A2A / Phase 5 L5 Cowork / Phase 6 ecosystem). Mentioned in both STATE.md and PROJECT.md.
- `docs/design/EAASP/DEFERRED_LEDGER.md` — 100% ✅ CLOSED on 2026-06-16. Phase 3.6.3 does NOT modify this file but its closure state is referenced in the "post-debt era" framing of PROJECT.md.
- `docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md` — strategic anchor; supersedes ADR-V2-023 Leg A/B framing with engine vs data/integration dual-axis model.

### Project memory
- `AGENTS.md` lines 65 + 99: `contract-v1.2.0` current, `contract-v1.1.0` historical Phase 3 sign-off — these are the canonical phrasings for STATE.md D-04 and PROJECT.md D-13.
- `AGENTS.md` line 13: Grid Activation SHIPPED 2026-06-17 — canonical phrasing for STATE.md D-05.
- `AGENTS.md` lines 11 + 33: "tools/eaasp-*/" is simulator-level reference implementation; no upstream EAASP project — preserved as the "post-Activation reality" framing in PROJECT.md and STATE.md.

### GSD tooling
- `$HOME/.claude/get-shit-done/bin/gsd-tools.cjs` — the standard `init phase-op` and `init milestone-op` calls. STATE.md frontmatter must remain compatible with these calls. After Phase 3.6.3, `init phase-op 3.6.1` / `3.6.2` / `3.6.3` all return `phase_found: true` with proper `phase_number`, `phase_name`, `phase_dir`.
- `$HOME/.claude/get-shit-done/workflows/discuss-phase.md` and `plan-phase.md` — already-read workflow files that establish the frontmatter schema.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `.planning/PROJECT.md` and `.planning/STATE.md` already use frontmatter in YAML format. The plan must preserve the YAML structure.
- `.planning/ROADMAP.md` already has the v3.6 milestone block with all 3 sub-phases (3.6.1 / 3.6.2 / 3.6.3). Phase 3.6.3 does NOT touch ROADMAP.md.
- `.planning/STATE.md`'s Audit Findings Summary scoreboard table (lines 50-55) has 5 rows at 9.0 ✅; the prose below (line 69) is the only place that says "4/7" — that single line is what 3.6.3 fixes (D-07).

### Established Patterns
- The project commits use the footer `Generated-By: Claude (claude-opus-4-8) via Claude Code CLI` + `Co-Authored-By: claude-flow <ruv@ruv.net>` (matching the v3.6 sub-phase commits).
- Documentation changes land as separate atomic commits per logical concern (matching Phase 3.6.1's SSOT-then-snapshot split).
- The `.planning/` directory contains Chinese-language docs and English-language frontmatter. Phase 3.6.3's edits maintain this mixed-language convention.

### Integration Points
- Phase 3.6.1 (when landed) will create `docs/PROJECT_PRODUCT_OVERVIEW.md` with the 5 canonical facts and `docs/status/PRODUCT_STATUS_2026-07-19.md` with the 7-section snapshot. Phase 3.6.3's STATE.md and PROJECT.md point to both files. If 3.6.1 lands first, the forward-references resolve naturally; if 3.6.3 lands first, the frontmatter/canonical-source blocks have a forward-referenced link (acceptable for sequenced local execution).
- The `.planning/STATE.md` frontmatter change from non-standard to GSD-standard format unblocks `gsd-tools` integration (the current `init phase-op` already finds 3.6.x phases, but full integration enables automated phase begin/end commands and validation).

</code_context>

<specifics>
## Specific Ideas

- The rewritten "Blockers/Concerns" section in STATE.md should preserve the "138 unpushed commits" and "Local environment" entries from the current text — those are real concerns that survived the docs sync and should be carried forward. The "false dormancy" line is what gets removed.
- The new "Post-Activation scope" paragraph in PROJECT.md should be specific about the next-milestone candidates: web-platform/ 7.5/10 + grid-desktop 6.5/10 quality gap closure (Grid side) and the 4 EAASP platform-evolution gaps (EAASP side). This gives future readers an explicit roadmap without scope-creep into a new milestone.
- The "Core Value" update (D-04 + D-13) must use the EXACT phrasing from `AGENTS.md` line 65 / 99, not paraphrased. AGENTS.md is the project memory; planning files cite it.
- The plan should validate via `gsd-tools init phase-op 3.6.1` (or any 3.6.x) that frontmatter changes don't break the tool's phase detection. If 3.6.1's plan has been merged, run the validation; if not yet, the validation is left as a manual check after the 3.6.3 commits land.
- The user has not requested a full project re-init (re-running `gsd-tools config-new-project` to regenerate the entire `.planning/` tree). Phase 3.6.3 is incremental: update STATE.md and PROJECT.md without touching other GSD artifacts.

</specifics>

<deferred>
## Deferred Ideas

- **Full `.planning/` re-init via `gsd-tools config-new-project`** — This would regenerate the entire planning tree from scratch. Out of scope; the user has not requested it. Phase 3.6.3 is incremental.
- **PHASE_3_DESIGN.md implementation** — The OPA backend design doc exists but is unimplemented. Phase 3.6.3 references it as future work; implementation is a separate future milestone.
- **`.planning/REQUIREMENTS.md`** — This file is referenced in gsd-tools output as `requirements_path: ".planning/REQUIREMENTS.md"` but the file may not exist (project switched from REQUIREMENTS.md to PROJECT.md-based requirement tracking). Phase 3.6.3 does NOT create it; if future GSD tooling needs REQUIREMENTS.md, that is a separate task.
- **Phase 3.6.1 + 3.6.2 execution sequencing** — Whether to execute 3.6.3 before or after 3.6.1 / 3.6.2 lands is a sequencing decision, not a 3.6.3 scope decision. The 3.6.3 plan's execution must be tolerant of any order (forward-references resolve to whichever file lands last).

</deferred>

---

*Phase: 3.6.3*
*Context gathered: 2026-07-19*
