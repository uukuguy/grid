# Phase 3.6.3: .planning/PROJECT.md + .planning/STATE.md sync - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-19
**Phase:** 3.6-post-activation-docs-sync / Phase 3.6.3
**Areas discussed:** (1) STATE.md frontmatter format, (2) Plan split, (3) Research skip, (4) Scoreboard 4/7 vs 5/7, (5) PROJECT Current Phase, (6) PROJECT Active list, (7) STATE blockers dormancy, (8) Scope of this discuss session

---

## 1. STATE.md frontmatter format

| Option | Description | Selected |
|--------|-------------|----------|
| Fully GSD-standard format (Recommended) | Use GSD's canonical schema: milestone / status / progress(total_phases / completed_phases / total_plans / completed_plans / percent). gsd-tools natively recognizes. | ✓ |
| Preserve project-existing format | Keep current frontmatter (milestone / milestone_name / status / v3.5_shipped / activation_shipped) and only adjust values. gsd-tools is partially broken but human-readable. | |
| Hybrid: standard fields + project extras | Frontmatter gets both GSD standard fields (progress, total_phases) AND project extensions (v3.5_shipped, activation_shipped). Compatible both ways. | |

**User's choice:** Fully GSD-standard format (recommended).
**Notes:** The current non-standard frontmatter was a leftover from v3.5-era STATE.md. Phase 3.6.3 normalizes to the GSD schema so `init phase-op` and `state begin-phase` work cleanly going forward.

## 2. Plan split

| Option | Description | Selected |
|--------|-------------|----------|
| 1 PLAN: PROJECT + STATE together (Recommended) | One plan covering both files. Two atomic commits (PROJECT / STATE each one). | ✓ |
| 2 PLANS: split them | Two independent plans: PLAN-01 fixes PROJECT.md, PLAN-02 fixes STATE.md. More files in `.planning/`, but execution is more flexible. | |

**User's choice:** 1 PLAN: PROJECT + STATE together (recommended).
**Notes:** Both files are tightly coupled — STATE.md's "canonical sources" pointer references the same SSOT/snapshot path that PROJECT.md's "See also" block references. Editing them in a single plan keeps the references consistent.

## 3. Research skip

| Option | Description | Selected |
|--------|-------------|----------|
| Skip research (Recommended) | Planning state + docs-only + locked decisions are known. | ✓ |
| Run research | Investigate `.planning/STATE.md` / `PROJECT.md` existing format, gated-phase state. | |

**User's choice:** Skip research (recommended).
**Notes:** Planning-state work with locked decisions does not benefit from research. The plan-checker reviews the plan to confirm correctness.

## 4. Scoreboard 4/7 vs 5/7

| Option | Description | Selected |
|--------|-------------|----------|
| Patch 4/7 to 5/7 (Recommended) | In STATE.md, change "4/7 components at 9.0+" to "5/7", matching the scoreboard table. web/ still counts as 5/7. | ✓ |
| Patch web/ to 8/10 (not 9.0) | Demote web/ from 9.0 to 8/10 in the table; both prose and table say 4/7. | |

**User's choice:** Patch 4/7 to 5/7 (recommended).
**Notes:** The scoreboard table has 5 entries at 9.0: grid-cli, web/, grid-server, grid-eval, grid-platform. The prose is a clear typo — the table is the source of truth.

## 5. PROJECT Current Phase rewrite

| Option | Description | Selected |
|--------|-------------|----------|
| Post-Activation + next milestone TBD (Recommended) | Rewrite to "Post-Activation (next milestone definition pending)", v3.5/Activation marked shipped, EAASP platform gaps as future. | ✓ |
| Active: v3.6 docs sync | Rewrite to "Active: v3.6 Post-Activation Docs Sync", explicit current phase status. | |
| Add "HISTORICAL" marker, leave body | Insert a marker without rewriting the body text. | |

**User's choice:** Post-Activation + next milestone TBD (recommended).
**Notes:** The 3.6 milestone IS in progress, but PROJECT.md's "Current Phase" describes the project-level state, not a single GSD phase. Post-Activation is the right framing.

## 6. PROJECT Active list — v3.5 entry

| Option | Description | Selected |
|--------|-------------|----------|
| Move v3.5/Activation to Validated + add v3.6 + EAASP gaps (Recommended) | Move v3.5 from Active to Validated (shipped). Active now lists v3.6 docs sync + 4 EAASP platform-evolution gaps. | ✓ |
| Only clean Active, leave Validated | Just remove v3.5 from Active. Validated untouched. | |

**User's choice:** Move v3.5/Activation to Validated + add v3.6 + EAASP gaps (recommended).
**Notes:** v3.5 was already shipped. Leaving it in Active is a documentation drift. Moving it to Validated is consistent with the new Current Phase framing.

## 7. STATE Blockers — dormancy

| Option | Description | Selected |
|--------|-------------|----------|
| Rewrite to post-Activation reality (Recommended) | Remove "dormancy" entry. Replace with quality gaps (web-platform 7.5/10 + grid-desktop 6.5/10) + 4 EAASP platform gaps + 138 unpushed commits + local env. | ✓ |
| Only remove dormancy sentence, no additions | Delete the dormancy line; leave the rest of Blockers section unchanged. | |
| Leave Blockers unchanged | Don't touch Blockers. | |

**User's choice:** Rewrite to post-Activation reality (recommended).
**Notes:** The dormancy claim is false (Activation shipped 2026-06-17). The replacement is more accurate and aligned with the new Current Phase framing.

## 8. Scope of this discuss session

| Option | Description | Selected |
|--------|-------------|----------|
| Only write 3.6.3 CONTEXT.md (Recommended) | Record only Phase 3.6.3 decisions. Plan in a later session. | ✓ |
| Write CONTEXT.md + go to PLAN immediately | Continue from discuss into plan-phase in this session. | |

**User's choice:** Only write 3.6.3 CONTEXT.md (recommended).
**Notes:** Splitting discuss and plan into separate user turns keeps each phase's gray-area exploration focused. Phase 3.6.2's plan is being run in parallel by another agent and may produce findings that inform 3.6.3's plan (e.g. the exact path of the dated snapshot).

---

## the agent's Discretion

- Exact wording of the rewritten "Current Phase" block in PROJECT.md (D-10): the "Post-Activation scope" prose can be 2-4 paragraphs depending on how much detail is useful for a future reader.
- Whether the `last_activity` field in STATE.md frontmatter is `2026-07-19` (today) or `2026-07-18` (yesterday, the date the prior commits landed). Recommendation: `2026-07-19` since the 3.6.3 commits themselves happen today.

## Deferred Ideas

- Full `.planning/` re-init via `gsd-tools config-new-project` — out of scope; not requested.
- PHASE_3_DESIGN.md implementation — out of scope; Phase 3 OPA backend is a separate future milestone.
- `.planning/REQUIREMENTS.md` — referenced by gsd-tools but may not exist; not Phase 3.6.3's scope to create it.
- Phase 3.6.1 + 3.6.2 execution sequencing — sequencing decision, not 3.6.3 scope.
