# Phase 3.6.2: AGENTS.md canonical-facts block + CLAUDE.md symlink + README status sections - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-18
**Phase:** 3.6-post-activation-docs-sync / Phase 3.6.2
**Areas discussed:** (1) Plan split, (2) Research skip, (3) AGENTS.md block placement, (4) AGENTS.md table depth, (5) CLAUDE.md symlink mode, (6) README Product status position, (7) README existing-text update policy, (8) Chinese "genuine" criterion, (9) Scope of this discuss session

---

## 1. Plan split

| Option | Description | Selected |
|--------|-------------|----------|
| 1 PLAN with 4 steps | One plan: AGENTS canonical-facts → CLAUDE symlink → bilingual README Product status. Two commits (AGENTS+CLAUDE, README×2). | ✓ |
| 3 PLANS per surface | Three independent plans: AGENTS, CLAUDE, READMEs. Three commits in one go. | |

**User's choice:** 1 PLAN with 4 steps (recommended).
**Notes:** Two-commit split keeps each diff cleanly reviewable.

## 2. Research skip

| Option | Description | Selected |
|--------|-------------|----------|
| Skip research (Recommended) | docs-only task + locked decisions + known AGENTS/CLAUDE/README state. | ✓ |
| Run research | Investigate AGENTS.md / README state, GitHub workflows, README conventions. | |

**User's choice:** Skip research (recommended).
**Notes:** Research is opt-in for ambiguous domains. Documentation tasks with known surfaces do not benefit from research.

## 3. AGENTS.md canonical-facts block placement

| Option | Description | Selected |
|--------|-------------|----------|
| After §1, before "Product legs at a glance" table (Recommended) | Top-down reader flow: (1) what this repo is, (2) canonical facts, (3) dual-axis framing. | ✓ |
| At end of file (after Tech Stack) | Document-end placement. | |
| After Tech Stack, before Reference Linkage | After architecture, before references. | |

**User's choice:** After §1, before "Product legs at a glance" table (recommended).
**Notes:** Top-of-file placement is the standard pattern for canonical-facts blocks. The existing §1 (top-of-file project memory) is preserved.

## 4. AGENTS.md table depth

| Option | Description | Selected |
|--------|-------------|----------|
| 5 facts + 1 "See also" table (Recommended) | Use the same 5-fact table as the SSOT §3.0 + a "See also" table pointing to SSOT + snapshot + ADRs. | ✓ |
| 5 facts + "see SSOT" link only | Only the 5 facts pointing to SSOT; no in-AGENTS table copy. | |

**User's choice:** 5 facts + 1 "See also" table (recommended).
**Notes:** AGENTS.md is the primary agent memory; surfacing the 5 facts as a table lets Claude Code answer post-Activation status questions without round-tripping to SSOT. The "See also" table keeps the table small while preserving the link-out.

## 5. CLAUDE.md creation

| Option | Description | Selected |
|--------|-------------|----------|
| Standard symlink (Recommended) | Add `CLAUDE.md -> AGENTS.md` as a relative symlink (git mode 120000). | ✓ |
| Independent CLAUDE.md | Add a separate `CLAUDE.md` file with the same content, no symlink. | |

**User's choice:** Standard symlink (recommended).
**Notes:** The symlink is the same pattern used in commit `0461d97f` (docs: align Grid/EAASP narrative). This preserves the Claude Code/Codex dual entrypoint convention established by the user.

## 6. README Product status position

| Option | Description | Selected |
|--------|-------------|----------|
| Before Quick Start (Recommended) | Insert at the top of the user-facing content, so a new reader sees post-Activation status before any onboarding steps. | ✓ |
| After Architecture, before Quick Start | Reader sees architecture first, then status, then onboarding. | |
| At end of file (after Tech Stack) | Reader sees full design before status. | |

**User's choice:** Before Quick Start (recommended).
**Notes:** Onboarding-first: a new user cloning the repo and reading the README should see the post-Activation state immediately.

## 7. README existing-text update policy

| Option | Description | Selected |
|--------|-------------|----------|
| Don't touch existing, only insert new (Recommended) | Preserve existing "16 methods v1.1.0 test suite" as historical accuracy. New Product status section is authoritative for current-state claims. | ✓ |
| Patch existing + insert new | Change existing "v1.1.0 test suite" to v1.2.0 historical phrasing + insert new section. | |
| No new section, only rewrite README | Don't insert, just rewrite existing paragraphs to v1.2.0. | |

**User's choice:** Don't touch existing, only insert new (recommended).
**Notes:** The existing README text is a historical product-design claim (the 6 comparison runtimes passed `contract-v1.1.0`). Patching it would lose the historical layer. The new Product status section is the authoritative location for current-state claims.

## 8. Chinese "genuine" criterion

| Option | Description | Selected |
|--------|-------------|----------|
| Technical Chinese with retained technical terms (Recommended) | Chinese grammar and flow, with gRPC / L1 runtime / SSOT / activation / A2A / Cowork terms kept in English. Matches the existing README.zh.md / AGENTS.md bilingual style. | ✓ |
| Chinese-primary, English only in parentheticals | Avoid technical terms; English only in parentheticals. | |

**User's choice:** Technical Chinese with retained technical terms (recommended).
**Notes:** This is the same bilingual style already used in `README.zh.md` (e.g. "grid-runtime 实现了一套 16 方法的 gRPC 合同"). The plan checker must validate that no English-to-Chinese literal-translation patterns are used (e.g. "A.0–A.8 在 2026-06-17 装运" would fail — the correct form is "已于 2026-06-17 全部交付").

## 9. Scope of this discuss session

| Option | Description | Selected |
|--------|-------------|----------|
| Only write 3.6.2 CONTEXT.md (Recommended) | Record only Phase 3.6.2 decisions. 3.6.3 gets its own discuss session. | ✓ |
| Cross into 3.6.3 discussion now | Bundle 3.6.2 + 3.6.3 in one session, write both CONTEXT.md files. | |

**User's choice:** Only write 3.6.2 CONTEXT.md (recommended).
**Notes:** Splitting discussions keeps each phase's gray-area exploration focused. Phase 3.6.3 (planning state sync) has its own gray areas (e.g. should `STATE.md` frontmatter be normalized to GSD's standard `milestone: <version>` format, or kept as the project's existing non-standard `milestone: <name>` format?).

---

## the agent's Discretion

- Exact placement of the "See also" table within the AGENTS.md block (above the 5-fact table vs. below it). Recommendation: below the 5-fact table.
- Whether to commit the AGENTS+CLAUDE pair as one commit and README pair as a second commit (D-13) or batch all four file changes into a single commit. Recommendation: two commits (D-13) for clean reviewability.

## Deferred Ideas

- README screenshots / diagrams — Adding a visual diagram of the L0–L4 architecture in the Product status section is nice-to-have but not in scope. The existing EAASP architecture diagram in `AGENTS.md` (lines 43-58) is sufficient.
- Translated CLAUDE.md body — Some projects keep a Chinese mirror at `CLAUDE.zh.md` for Codex + Claude Code bilingual coverage. Out of scope here; the `README.zh.md` + `AGENTS.md` Chinese body is sufficient.
- i18n automation — Future workflow could generate `README.<lang>.md` from a single source. Out of scope.
- Phase 3.6.1 + 3.6.3 sequencing — Whether to execute 3.6.2 before or after 3.6.1 is a sequencing decision, not a 3.6.2 scope decision. The 3.6.2 plan's execution must be tolerant of either order (forward-references resolve to whichever file lands last).
