---
phase: 03.10.3
plan: 01
type: auto
autonomous: true
wave: 4
depends_on: [03.10.2]
requirements: [PIPE-01, VERIFY-02, VERIFY-03, COMPAT-01, COMPAT-02, COMPAT-03, TRACE-01, TRACE-02]
---

# Phase 03.10.3 Plan — VERIFY spec auditor and milestone closure

## Objective

Turn the point-in-time platform skeleton matrix into a deterministic local/CI audit, verify compatibility and the Phase 0.5 real-skill benchmark, and close v3.10 without widening contract-v1.2.0.

## Tasks

### Task 1: implement a deterministic spec-alignment auditor

Add a certifier subcommand and `make v3.10-spec-audit` target using existing dependencies. The auditor validates matrix structure and fails on undeclared drift/missing/not-certified states while allowing explicitly deferred rows with owner and rationale.

### Task 2: wire CI without disturbing v3.9 ordering

Preserve the required sequence `cargo check → rbac-audit → v3.10-spec-audit → tests`. Do not change route catalog or AuthMode semantics. Add positive and synthetic-negative tests for the auditor.

### Task 3: run compatibility and executable benchmark gates

Run targeted certifier and L1 substitutability checks, targeted L2-L4 tests, and actual service verification. Use `make dev-eaasp` plus `eaasp-cli` real-skill execution when credentials are available; if authentication is unavailable, stop as an auth gate rather than fabricating success.

### Task 4: finalize evidence and milestone state

Finalize alignment matrix, production-usability evidence, deferred ledger, requirements, roadmap, state, phase summaries, and milestone verification. Independently review security boundaries, audit vacuity, protocol compatibility, and CI ordering.

## Verification

- `make v3.10-spec-audit` passes production matrix and fails a synthetic mismatch.
- `cargo check --workspace` → `make rbac-audit` → `make v3.10-spec-audit` pass in order.
- Targeted certifier/L1/L2/L3/L4 tests pass; no workspace full suite runs.
- Real-skill benchmark result is recorded truthfully.

## Success criteria

- All 16 v3.10 requirements are closed with evidence or execution stops at a real authentication gate.
- Contract-v1.2.0, v3.9 RBAC, AuthMode, Owner-only, and shared-core constraints remain intact.
- Future OPA/A2A/Cowork/ecosystem work is registered, not prematurely implemented.
