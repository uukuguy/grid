---
phase: 07.2-l2-connection-pool-pipeline-verify-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/EAASP/DEFERRED_LEDGER.md
  - .planning/STATE.md
autonomous: true
requirements:
  - L2-01
  - L2-02
  - L2-03
  - L2-04
  - L2-05
  - L2-06
  - L2-07
  - L2-08

must_haves:
  truths:
    - "D12 (L2-01): LEDGER L115 shows ✅ CLOSED @ 50a6996+ea0bbfc — connection-pool via MemoryStore singleton + zero database-is-locked errors"
    - "D94 (L2-02): LEDGER L224 shows ✅ CLOSED @ 50a6996+ea0bbfc — MemoryStore singleton + shared connection (D12 closeout)"
    - "D91 (L2-03): LEDGER L221 shows ✅ CLOSED @ ca13d04+49f0cc4 — HNSW tombstone rebuild at TOMBSTONE_REBUILD_THRESHOLD=0.30"
    - "D93 (L2-04): LEDGER L223 shows ✅ CLOSED @ ec31ed4+26f2159 — embed_batch concurrent fan-out ~10x speedup"
    - "D98 (L2-05): LEDGER L228 shows ✅ CLOSED @ 7734a2b — HybridIndex.search() no longer per-call HNSW rebuild"
    - "D11 (L2-06): LEDGER L114 shows ✅ CLOSED @ 382d7864 — skill-registry scope filter before LIMIT"
    - "D13 (L2-07): LEDGER L116 shows ✅ CLOSED @ 079932cd — archive() hides rows from FTS"
    - "D30 (L2-08): LEDGER L142 shows ✅ CLOSED @ 341eec2 — L2/L3 busy_timeout unified constant BUSY_TIMEOUT_MS=5000"
  artifacts:
    - path: "docs/design/EAASP/DEFERRED_LEDGER.md"
      provides: "D11/D12/D13/D30/D91/D93/D94/D98 resolution entries"
      contains: "✅ CLOSED 2026-06-02 Phase 7.2"
    - path: "tools/eaasp-l2-memory-engine/tests/test_tombstone_rebuild.py"
      provides: "D91 tombstone rebuild threshold tests"
    - path: "tools/eaasp-l2-memory-engine/tests/test_embed_batch_concurrency.py"
      provides: "D93 embed_batch concurrency tests (10x speedup assertion)"
    - path: "tools/eaasp-l2-memory-engine/tests/test_hybrid_index_cache.py"
      provides: "D98 HybridIndex cache non-regression test"
    - path: "tools/eaasp-l2-memory-engine/tests/test_archive_isolation.py"
      provides: "D13 archive isolation tests (3/3 scenarios)"
    - path: "tools/eaasp-l2-memory-engine/tests/test_throughput.py"
      provides: "D12/D94 high-concurrency throughput test (10 coroutines × 10 ops)"
  key_links:
    - from: "tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/db.py"
      to: "BUSY_TIMEOUT_MS constant"
      via: "get_shared_connection + init_db pragma"
      pattern: "BUSY_TIMEOUT_MS = 5000"
    - from: "tools/eaasp-l2-memory-engine/tests/test_throughput.py"
      to: "tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/db.py"
      via: "get_shared_connection singleton"
      pattern: "database is locked"
    - from: "tools/eaasp-l2-memory-engine/tests/test_tombstone_rebuild.py"
      to: "tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/vector_index.py"
      via: "TOMBSTONE_REBUILD_THRESHOLD gate"
      pattern: "TOMBSTONE_REBUILD_THRESHOLD = 0.30"
    - from: "tools/eaasp-l2-memory-engine/tests/test_archive_isolation.py"
      to: "tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/files.py"
      via: "archive() → DELETE FROM memory_fts"
      pattern: "DELETE FROM memory_fts"
---

<objective>
Verify all 8 L2 carry-forward D-items (D11/D12/D13/D30/D91/D93/D94/D98) are ✅ CLOSED with correct commit hashes in DEFERRED_LEDGER.md and their L2 test suite still passes, then close out Phase 7.2 with a LEDGER 状態変更ログ entry.

Purpose: Phase 7.2 is a pure verify-and-close phase — all 8 items were fully implemented and closed on 2026-06-02 across Phase 7.2 Plans 01/02/03. This plan confirms they remain closed and tests still pass, closing out the carry-forward from v3.3.
Output: Updated DEFERRED_LEDGER.md with re-verification entry in 状態変更ログ; STATE.md updated to mark Phase 7.2 complete.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.2-l2-connection-pool-pipeline-verify-close/07.2-CONTEXT.md
@docs/design/EAASP/DEFERRED_LEDGER.md

<interfaces>
<!-- Key references the executor needs for verification. All items are already implemented. -->

From LEDGER — confirmed closed entries (read for commit hash cross-check):
- D11 (L2-06) @ L114: `✅ CLOSED 2026-06-02 Phase 7.2 Plan 03 @ 382d7864` — skill-registry scope filter
- D12 (L2-01) @ L115: `✅ CLOSED 2026-06-02 Phase 7.2 Plan 01 @ 50a6996+ea0bbfc` — connection-per-call → connection pool
- D13 (L2-07) @ L116: `✅ CLOSED 2026-06-02 Phase 7.2 Plan 03 @ 079932cd` — archive() hides rows from FTS
- D30 (L2-08) @ L142: `✅ CLOSED 2026-06-02 Phase 7.2 Plan 01 @ 341eec2` — L2/L3 busy_timeout unified (BUSY_TIMEOUT_MS=5000)
- D91 (L2-03) @ L221: `✅ CLOSED 2026-06-02 Phase 7.2 Plan 02 @ ca13d04+49f0cc4` — HNSW tombstone rebuild threshold (TOMBSTONE_REBUILD_THRESHOLD=0.30)
- D93 (L2-04) @ L223: `✅ CLOSED 2026-06-02 Phase 7.2 Plan 02 @ ec31ed4+26f2159` — embed_batch concurrent fan-out (asyncio.gather + Semaphore)
- D94 (L2-02) @ L224: `✅ CLOSED 2026-06-02 Phase 7.2 Plan 01 @ 50a6996+ea0bbfc` — MemoryStore singleton + shared connection (D12 closeout)
- D98 (L2-05) @ L228: `✅ CLOSED 2026-06-02 Phase 7.2 Plan 01 @ 7734a2b` — HybridIndex.search() HNSW cache (no per-call rebuild)

Test file locations (already exist, just verify they pass):
- `tools/eaasp-l2-memory-engine/tests/test_tombstone_rebuild.py` — D91: under-threshold (29%) no-rebuild + at-threshold (30%) rebuild + recall preservation
- `tools/eaasp-l2-memory-engine/tests/test_embed_batch_concurrency.py` — D93: batch=10 ~10x speedup (52ms vs 511ms), concurrency cap enforcement
- `tools/eaasp-l2-memory-engine/tests/test_hybrid_index_cache.py` — D98: cache pointer-identity across N searches
- `tools/eaasp-l2-memory-engine/tests/test_archive_isolation.py` — D13: post-archive search hides memory + restart persists + unrelated memory unaffected
- `tools/eaasp-l2-memory-engine/tests/test_throughput.py` — D12/D94: 10 coroutines × 10 ops, mixed read+write, 0 database-is-locked errors

Source files with D-item implementations (for context if test fails):
- `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/db.py` — D12 get_shared_connection, D30 BUSY_TIMEOUT_MS=5000
- `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/files.py` — D13 archive() DELETE FROM memory_fts + D12/D94 get_shared_connection
- `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/index.py` — D98 _hnsw_cache, D13 defense-in-depth AND mf.status != 'archived'
- `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/vector_index.py` — D91 TOMBSTONE_REBUILD_THRESHOLD=0.30 + rebuild on add()
- `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/embedding.py` — D93 embed_batch with asyncio.gather + Semaphore
- `tools/eaasp-skill-registry/src/store.rs` — D11 scope filter before LIMIT (Rust)

Skill-registry test (D11 — Rust side):
- `crates/eaasp-skill-registry/` — `cargo test -p eaasp-skill-registry` for D11 scope-filter test
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LEDGER cross-check — verify all 8 D-rows are ✅ CLOSED with correct commit hashes</name>
  <files>docs/design/EAASP/DEFERRED_LEDGER.md</files>
  <read_first>
    - docs/design/EAASP/DEFERRED_LEDGER.md (lines 110-145, 218-232 — verify exact D-row entries for D11/D12/D13/D30/D91/D93/D94/D98)
  </read_first>
  <action>
Verify that all 8 L2 D-items in DEFERRED_LEDGER.md have the ✅ CLOSED status with the expected commit hashes from Phase 7.2 execution (2026-06-02). This is a read-only verification — confirm the LEDGER has NOT drifted since CONTEXT.md was written.

Run the following checks:
1. `grep -n "D11\b" docs/design/EAASP/DEFERRED_LEDGER.md` — confirm line ~114 shows `✅ CLOSED 2026-06-02 Phase 7.2 Plan 03 @ 382d7864`
2. `grep -n "D12\b" docs/design/EAASP/DEFERRED_LEDGER.md` — confirm line ~115 shows `✅ CLOSED 2026-06-02 Phase 7.2 Plan 01 @ 50a6996+ea0bbfc`
3. `grep -n "D13\b" docs/design/EAASP/DEFERRED_LEDGER.md` — confirm line ~116 shows `✅ CLOSED 2026-06-02 Phase 7.2 Plan 03 @ 079932cd`
4. `grep -n "D30\b" docs/design/EAASP/DEFERRED_LEDGER.md` — confirm line ~142 shows `✅ CLOSED 2026-06-02 Phase 7.2 Plan 01 @ 341eec2`
5. `grep -n "D91\b" docs/design/EAASP/DEFERRED_LEDGER.md` — confirm line ~221 shows `✅ CLOSED 2026-06-02 Phase 7.2 Plan 02 @ ca13d04+49f0cc4`
6. `grep -n "D93\b" docs/design/EAASP/DEFERRED_LEDGER.md` — confirm line ~223 shows `✅ CLOSED 2026-06-02 Phase 7.2 Plan 02 @ ec31ed4+26f2159`
7. `grep -n "D94\b" docs/design/EAASP/DEFERRED_LEDGER.md` — confirm line ~224 shows `✅ CLOSED 2026-06-02 Phase 7.2 Plan 01 @ 50a6996+ea0bbfc`
8. `grep -n "D98\b" docs/design/EAASP/DEFERRED_LEDGER.md` — confirm line ~228 shows `✅ CLOSED 2026-06-02 Phase 7.2 Plan 01 @ 7734a2b`

For each D-row, verify the commit exists in git history:
```bash
# Short hashes (7-char): git log --oneline <hash> | head -1
# Long short hashes (8-char): git log --oneline | grep <hash>
git log --oneline --all | grep "382d7864"  # D11
git log --oneline --all | grep "50a6996"   # D12 + D94
git log --oneline --all | grep "079932cd"  # D13
git log --oneline --all | grep "341eec2"   # D30
git log --oneline --all | grep "ca13d04"   # D91
git log --oneline --all | grep "ec31ed4"   # D93
git log --oneline --all | grep "7734a2b"   # D98
```
  </action>
  <verify>
    <automated>bash -c '
      for d in D11 D12 D13 D30 D91 D93 D94 D98; do
        echo "Checking $d...";
        grep -n "$d\b" docs/design/EAASP/DEFERRED_LEDGER.md | grep "✅ CLOSED 2026-06-02";
      done
    '</automated>
  </verify>
  <acceptance_criteria>
    - All 8 D-rows contain "✅ CLOSED 2026-06-02" in DEFERRED_LEDGER.md
    - All 8 commit hashes (382d7864, 50a6996, 079932cd, 341eec2, ca13d04, ec31ed4, 7734a2b, ea0bbfc) exist in `git log --all`
    - No D-row has been changed/superseded since CONTEXT.md was written
    - D12 and D94 share the same commit hash (50a6996+ea0bbfc) — both closed in same Plan 01 T01/T02
  </acceptance_criteria>
  <done>All 8 LEDGER entries confirmed ✅ CLOSED with correct commit hashes from June 2, 2026 — no drift.</done>
</task>

<task type="auto">
  <name>Task 2: Run L2 verification test suite — confirm all L2 tests pass</name>
  <files>
    tools/eaasp-l2-memory-engine/tests/test_tombstone_rebuild.py
    tools/eaasp-l2-memory-engine/tests/test_embed_batch_concurrency.py
    tools/eaasp-l2-memory-engine/tests/test_hybrid_index_cache.py
    tools/eaasp-l2-memory-engine/tests/test_archive_isolation.py
    tools/eaasp-l2-memory-engine/tests/test_throughput.py
    tools/eaasp-l2-memory-engine/tests/
  </files>
  <read_first>
    - tools/eaasp-l2-memory-engine/tests/test_tombstone_rebuild.py (confirm D91 test still loads)
    - tools/eaasp-l2-memory-engine/tests/test_embed_batch_concurrency.py (confirm D93 test still loads)
    - tools/eaasp-l2-memory-engine/tests/test_hybrid_index_cache.py (confirm D98 test still loads)
    - tools/eaasp-l2-memory-engine/tests/test_archive_isolation.py (confirm D13 test still loads — 3 scenarios)
    - tools/eaasp-l2-memory-engine/tests/test_throughput.py (confirm D12/D94 throughput test still loads)
  </read_first>
  <action>
Run the full L2 Python test suite. These tests were written during the original Phase 7.2 Plans 01/02/03 execution and have been passing since June 2. Confirm they still pass — no code changes expected.

Execute the following test commands in order. ANY failure is a regression that needs investigation (do NOT skip):

1. **Full L2 test suite:**
   ```bash
   cd tools/eaasp-l2-memory-engine && uv run python -m pytest -x -q 2>&1
   ```
   Records all test results. `-x` stops on first failure for fast diagnosis.

2. **D91-specific (tombstone rebuild):**
   ```bash
   cd tools/eaasp-l2-memory-engine && uv run python -m pytest tests/test_tombstone_rebuild.py -v 2>&1
   ```
   Verifies: under-threshold (29%) no-rebuild + at-threshold (30%) rebuild + recall preservation (100% on 16-survivor, score delta ±0.05).

3. **D93-specific (embed_batch concurrency):**
   ```bash
   cd tools/eaasp-l2-memory-engine && uv run python -m pytest tests/test_embed_batch_concurrency.py -v 2>&1
   ```
   Verifies: batch=10 wall-clock ~10% of sequential (52ms vs 511ms), concurrency cap=5 enforcement.

4. **D98-specific (HybridIndex cache):**
   ```bash
   cd tools/eaasp-l2-memory-engine && uv run python -m pytest tests/test_hybrid_index_cache.py -v 2>&1
   ```
   Verifies: cache pointer-identity across N searches — no per-call rebuild.

5. **D13-specific (archive isolation):**
   ```bash
   cd tools/eaasp-l2-memory-engine && uv run python -m pytest tests/test_archive_isolation.py -v 2>&1
   ```
   Verifies 3/3 scenarios: (a) post-archive search hides memory, (b) fresh HybridIndex after restart hides it, (c) archiving A doesn't affect unrelated B.

6. **D12/D94-specific (throughput):**
   ```bash
   cd tools/eaasp-l2-memory-engine && uv run python -m pytest tests/test_throughput.py -v 2>&1
   ```
   Verifies: 10 coroutines × 10 ops, mixed read+write, 0 database-is-locked errors.

7. **D11-specific (skill-registry scope — Rust):**
   ```bash
   cargo test -p eaasp-skill-registry 2>&1 | tail -10
   ```
   Verifies: scope filter test (scope=X, limit=5 returns ≤5 all scope=X).

8. **D30 cross-check (busy_timeout constant):**
   ```bash
   grep -n "BUSY_TIMEOUT_MS = 5000" tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/db.py
   ```
   Verifies the constant is present in the L2 source file.
  </action>
  <verify>
    <automated>
# Run all L2 checks and collect results
echo "=== Full L2 test suite ==="
cd tools/eaasp-l2-memory-engine && uv run python -m pytest -x -q 2>&1 | tail -15
echo ""
echo "=== D91: tombstone rebuild ==="
cd tools/eaasp-l2-memory-engine && uv run python -m pytest tests/test_tombstone_rebuild.py -v 2>&1 | tail -10
echo ""
echo "=== D93: embed_batch concurrency ==="
cd tools/eaasp-l2-memory-engine && uv run python -m pytest tests/test_embed_batch_concurrency.py -v 2>&1 | tail -10
echo ""
echo "=== D98: HybridIndex cache ==="
cd tools/eaasp-l2-memory-engine && uv run python -m pytest tests/test_hybrid_index_cache.py -v 2>&1 | tail -10
echo ""
echo "=== D13: archive isolation ==="
cd tools/eaasp-l2-memory-engine && uv run python -m pytest tests/test_archive_isolation.py -v 2>&1 | tail -10
echo ""
echo "=== D12/D94: throughput ==="
cd tools/eaasp-l2-memory-engine && uv run python -m pytest tests/test_throughput.py -v 2>&1 | tail -10
echo ""
echo "=== D11: skill-registry scope (Rust) ==="
cargo test -p eaasp-skill-registry 2>&1 | tail -5
echo ""
echo "=== D30: BUSY_TIMEOUT_MS constant ==="
grep -n "BUSY_TIMEOUT_MS" tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/db.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest -x -q` exits 0 with all L2 tests PASS
    - `tests/test_tombstone_rebuild.py` exits 0 (D91 ✅ — threshold gating + recall preservation)
    - `tests/test_embed_batch_concurrency.py` exits 0 (D93 ✅ — ~10x speedup + concurrency cap)
    - `tests/test_hybrid_index_cache.py` exits 0 (D98 ✅ — cache pointer-identity)
    - `tests/test_archive_isolation.py` exits 0 with 3/3 PASS (D13 ✅ — FTS hide + restart persist + isolation)
    - `tests/test_throughput.py` exits 0 (D12/D94 ✅ — 0 database-is-locked errors)
    - `cargo test -p eaasp-skill-registry` exits 0 (D11 ✅ — scope filter Rust tests)
    - `grep "BUSY_TIMEOUT_MS = 5000" tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/db.py` returns a match (D30 ✅)
  </acceptance_criteria>
  <done>All 8 L2 items verified: D91 (tombstone rebuild tests PASS), D93 (embed_batch concurrency ~10x speedup confirmed), D98 (HybridIndex cache no per-call rebuild), D13 (archive isolation 3/3 PASS), D12/D94 (throughput 0 locked errors), D11 (skill-registry scope Rust tests PASS), D30 (BUSY_TIMEOUT_MS=5000 confirmed). Full L2 test suite PASS. No regressions since June 2 close.</done>
</task>

<task type="auto">
  <name>Task 3: LEDGER close-out — add re-verification entry in 状態変更ログ and update STATE.md</name>
  <files>
    docs/design/EAASP/DEFERRED_LEDGER.md
    .planning/STATE.md
  </files>
  <read_first>
    - docs/design/EAASP/DEFERRED_LEDGER.md (find 状態変更ログ section — typically near end of file — note the date format and row pattern from existing entries like Phase 7.0 and 7.1 close-outs)
    - .planning/STATE.md (current state to update — Phase 7.2 status + progress table)
  </read_first>
  <action>
After Tasks 1 and 2 confirm all 8 items are ✅ CLOSED and tests pass, add a single close-out entry to the DEFERRED_LEDGER.md 状態変更ログ section.

1. **Find the 状態変更ログ section** in `docs/design/EAASP/DEFERRED_LEDGER.md` — look for the table at the end of the file with columns like `| 日期 | 阶段 | 变更内容 |`. The existing entries show Phase 7.0 and 7.1 close-outs as the most recent rows.

2. **Add one row** matching this exact format (follow existing pattern):
```
| 2026-06-08 | Phase 7.2 v3.4 Plan 01 | D11 ✅ / D12 ✅ / D13 ✅ / D30 ✅ / D91 ✅ / D93 ✅ / D94 ✅ / D98 ✅ (all re-verified CLOSED + all L2 tests pass) |
```
Use today's actual date from `date +%Y-%m-%d`.

3. **Update `.planning/STATE.md`**:
   - Change Phase 7.2 status to completed.
   - Update the progress table entry for "7.2 L2 connection-pool + Pipeline (V&C)" to `0/1` → `1/1` and status to `✅ Complete`.
   - Update progress counters: completed_phases → 3, completed_plans → 3.

4. **Commit both files**:
```bash
git add docs/design/EAASP/DEFERRED_LEDGER.md .planning/STATE.md
git commit -m "$(cat <<'EOF'
docs(07.2): close Phase 7.2 — all 8 L2 items re-verified CLOSED

D11/D12/D13/D30/D91/D93/D94/D98 all confirmed ✅ CLOSED with correct
commit hashes. Full L2 test suite passes: tombstone rebuild, embed_batch
concurrency (~10x), HybridIndex cache, archive isolation (3/3), throughput
(0 db-locked errors), skill-registry scope, BUSY_TIMEOUT_MS=5000 constant.
All 8 commit hashes confirmed in git log.

Phase 7.2 v3.4 Plan 01 complete — pure verify-and-close, no implementation changes.

Generated-By: Claude (deepseek-v4-pro) via Claude Code CLI
Co-Authored-By: claude-flow <ruv@ruv.net>
EOF
)"
```
  </action>
  <verify>
    <automated>bash -c '
      echo "=== LEDGER close-out entry ===";
      grep "Phase 7.2 v3.4" docs/design/EAASP/DEFERRED_LEDGER.md;
      echo "=== STATE.md Phase 7.2 status ===";
      grep -A1 "7.2 L2 connection" .planning/STATE.md;
      echo "=== Git status ===";
      git log --oneline -1
    '</automated>
  </verify>
  <acceptance_criteria>
    - DEFERRED_LEDGER.md 状態変更ログ contains one new row with "Phase 7.2 v3.4 Plan 01" and all 8 D-IDs listed (D11/D12/D13/D30/D91/D93/D94/D98)
    - STATE.md shows Phase 7.2 as completed (1/1 plans, ✅ Complete)
    - STATE.md completed_phases counter incremented to 3
    - git commit created with both files staged
  </acceptance_criteria>
  <done>LEDGER close-out entry added, STATE.md updated to Phase 7.2 complete, commit created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test infrastructure trust | Existing tests are trusted — they were written and committed on June 2 by the same project. Verify tests still pass but do not rewrite them. |
| L2 database files | Test suite creates temporary SQLite databases. No production data is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-01 | Tampering | DEFERRED_LEDGER.md entries | mitigate | Task 1 cross-checks each D-row's ✅ CLOSED status + commit hash exists in git log. Any drift from CONTEXT.md is flagged before proceeding. |
| T-07.2-02 | Tampering | L2 test files (test_tombstone_rebuild.py, etc.) | mitigate | Task 2 runs the full L2 test suite. A compromised test would fail or produce different results than the June 2 baseline. Read-first step confirms test files exist before execution. |
| T-07.2-03 | Information Disclosure | Test output logs | accept | Test output may contain filenames/paths but these are already public in the repo. No PII or secrets in test assertions. |
| T-07.2-04 | Denial of Service | L2 test suite runtime | accept | Full L2 test suite runs in seconds (Python unit tests). No long-running processes. Production impact: none — this is a local verification only. |
</threat_model>

<verification>
## Overall Phase Verification

After all tasks complete, verify end-to-end:
1. `grep "Phase 7.2 v3.4" docs/design/EAASP/DEFERRED_LEDGER.md` — confirms close-out row exists
2. `grep "7.2.*Complete\|7.2 L2 connection" .planning/STATE.md` — confirms state updated
3. `git log --oneline -3` — confirms close-out commit is the most recent
4. All 8 L2 REQ-IDs are covered by this plan's verification
</verification>

<success_criteria>
## Phase 7.2 Success Criteria (from ROADMAP.md)

1. ✅ L2-01 (D12): LEDGER L115 ✅ CLOSED @ 50a6996+ea0bbfc — connection pool via MemoryStore singleton + zero database-is-locked errors
2. ✅ L2-02 (D94): LEDGER L224 ✅ CLOSED @ 50a6996+ea0bbfc — MemoryStore singleton + shared connection (D12 closeout)
3. ✅ L2-03 (D91): LEDGER L221 ✅ CLOSED @ ca13d04+49f0cc4 — HNSW tombstone rebuild threshold AUTO-REBUILD at TOMBSTONE_REBUILD_THRESHOLD=0.30
4. ✅ L2-04 (D93): LEDGER L223 ✅ CLOSED @ ec31ed4+26f2159 — embed_batch concurrent fan-out ~10x speedup (asyncio.gather + Semaphore)
5. ✅ L2-05 (D98): LEDGER L228 ✅ CLOSED @ 7734a2b — HybridIndex.search() no per-call HNSW rebuild (cache verified)
6. ✅ L2-06 (D11): LEDGER L114 ✅ CLOSED @ 382d7864 — skill-registry scope filter applied BEFORE LIMIT
7. ✅ L2-07 (D13): LEDGER L116 ✅ CLOSED @ 079932cd — archive() hides rows from FTS (3/3 isolation tests)
8. ✅ L2-08 (D30): LEDGER L142 ✅ CLOSED @ 341eec2 — L2/L3 busy_timeout unified constant BUSY_TIMEOUT_MS=5000

All items already implemented and closed — this plan verifies they remain closed and tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-l2-connection-pool-pipeline-verify-close/07.2-01-SUMMARY.md`
</output>
