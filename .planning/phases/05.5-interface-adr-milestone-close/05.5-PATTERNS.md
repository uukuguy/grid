# Phase 5.5: Interface ADR + Milestone Close — Pattern Map

**Mapped:** 2026-05-22
**Artifacts mapped:** 14 (2 new ADRs / 9 ADR frontmatter edits / 2 Rust source edits / 1 new Rust test / 1 Python source edit / 1 new Python test / 1 LEDGER edit / 3 planning state edits + 1 REQUIREMENTS verify)
**Analogs found:** 14 / 14 — every artifact has a direct Phase 5.3/5.4 precedent
**RESEARCH.md alignment:** §Architecture Patterns 1–4 already enumerate the four core code patterns; this file maps each Phase 5.5 artifact onto a concrete on-disk analog plus line ranges so `gsd-planner` can fill `<read_first>` blocks without re-searching.

> **Note:** Phase 5.5 is documentation-and-cleanup heavy. 12 of 14 artifacts are markdown / YAML frontmatter / planning state; only 3 are source code (2 Rust, 1 Python). The "no analog found" set is empty — Grid has ≥1 precedent for every artifact shape in this phase.

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `docs/design/EAASP/adrs/ADR-V2-029-engine-data-integration-boundary.md` (NEW) | strategy ADR (governance doc) | static-doc | `docs/design/EAASP/adrs/ADR-V2-025-l1-runtime-contract-tier-strategy.md` | **exact** (same type / same `enforcement.level: strategic` / same trace shape) |
| `docs/design/EAASP/adrs/ADR-V2-032-tui-log-path-convention.md` (NEW) | record ADR (governance doc) | static-doc | `docs/design/EAASP/adrs/ADR-V2-028-strict-config-validation.md` | role-match (similar scope: single tech decision; V2-028 is `contract`, V2-032 is `record` per RESEARCH §2.5 recommendation) |
| `docs/design/EAASP/adrs/ADR-V2-006-hook-envelope-contract.md` (MOD frontmatter) | contract ADR — trace fill | static-doc | `docs/design/EAASP/adrs/ADR-V2-019-l1-runtime-deployment-model.md` (Phase 5.4 Q9 trace fill @ `70b5e94`) | **exact** (same operation: targeted `Edit` of `enforcement.trace`) |
| `docs/design/EAASP/adrs/ADR-V2-018-precompact-hook-and-iterative-summary.md` (MOD) | contract ADR — trace fill | static-doc | same as V2-006 | exact |
| `docs/design/EAASP/adrs/ADR-V2-020-tool-namespace-contract.md` (MOD) | contract ADR — trace fill | static-doc | same as V2-006 | exact |
| `docs/design/EAASP/adrs/ADR-V2-027-openai-compat-quirks.md` (MOD) | contract ADR — trace fill | static-doc | same as V2-006 | exact |
| `docs/design/EAASP/adrs/ADR-V2-028-strict-config-validation.md` (MOD) | contract ADR — trace fill | static-doc | same as V2-006 | exact |
| `docs/design/EAASP/adrs/ADR-V2-001-emit-event-interface.md` (MOD) | strategy ADR — rationale comment | static-doc | **no precedent** in repo — new convention from CONTEXT §Specific Ideas L177-184 | new-convention |
| `docs/design/EAASP/adrs/ADR-V2-002-event-stream-backend.md` (MOD) | strategy ADR — rationale comment | static-doc | same template as V2-001 (above) | new-convention |
| `docs/design/EAASP/adrs/ADR-V2-003-event-clustering-interface.md` (MOD) | strategy ADR — rationale comment | static-doc | same template as V2-001 | new-convention |
| `docs/design/EAASP/adrs/ADR-V2-005-tool-sandbox-container.md` (MOD) | strategy ADR — rationale comment | static-doc | same template as V2-001 | new-convention |
| `crates/grid-cli/src/commands/session.rs` (MOD L117 + opt. L102+L157) | controller / CLI subcommand handler | request-response | same file L99-103 (`delete_session`) and L157 (`export_session`) — same `eprintln! + anyhow!` anti-pattern, identical fix shape | **exact (self-analog)** |
| `crates/grid-cli/src/main.rs` (MOD L92-96) | binary entrypoint / exit-code mapper | request-response | same file L83-90 (`AppState::with_grid_root` Err arm — uses the same `GridError::other(e.to_string()).into()` pattern that needs the same `downcast_ref` enhancement) | **exact (self-analog)** |
| `crates/grid-cli/tests/cli_integration.rs` (MOD — append new test) | integration test | smoke (subprocess invocation) | same file L38-55 (`ask_help_renders_and_exits_zero`) — same `env!("CARGO_BIN_EXE_grid")` + `Command::new` + `assert_eq!(out.status.code(), ...)` shape | **exact (self-analog)** |
| `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/vector_index.py` (MOD L189-192 + L225-235) | service / vector index manager | CRUD | same file L221-236 (`save()` — meta dict assembly) and L142-161 (`_try_load_sync` — meta dict consumption) | **exact (self-analog)** |
| `tools/eaasp-l2-memory-engine/tests/test_vector_index.py` (MOD — append 2 tests) | unit test | CRUD | same file L211-233 (`test_hnsw_growth_resize`) — same fixture `_rand_vec(rng)` + `HNSWVectorIndex(...)` + `await idx.add(...)` shape | **exact (self-analog)** |
| `docs/design/EAASP/DEFERRED_LEDGER.md` (4 row-add + 1 row-edit) | ledger / debt SSOT | static-doc | commit `279b639` (Phase 5.4 Task 11 — 5-row close `D142+D143+NEW-A2+NEW-E3+NEW-F3`) + existing rows L262-263 (D142/D143 format) | **exact** |
| `.planning/PROJECT.md` (§Active → §Validated flip) | planning state | static-doc | **no current-repo milestone-close precedent yet** (v3.0 close at Phase 4.2 used a different file shape pre-GSD-pivot). Closest: this very phase's `.planning/PROJECT.md` §Active section currently — flip operation is symmetric. | new-pattern (documented in RESEARCH §Recommended Task Ordering Plan 02 Task 02.01) |
| `.planning/ROADMAP.md` (Phase 5.5 → Status=Complete + milestone line flip) | planning state | static-doc | commit `dc24f2f` (Phase 5.4 close — STATE row updated; ROADMAP entry-status flip pattern is the same shape applied per-phase) | role-match |
| `.planning/STATE.md` (frontmatter `status: executing` → `milestone-complete`, 6/6 progress + Performance Metrics + Progress bar) | planning state | static-doc | commit `dc24f2f` (Phase 5.4 close — same STATE.md edit shape; this phase extends to milestone-level not just phase-level) | exact (per-edit shape) |
| `.planning/REQUIREMENTS.md` (Traceability table verify — minimal/no edit) | planning state | static-doc | same file's existing Traceability table — verify-only operation | self-analog |

---

## Pattern Assignments

### 1. NEW `docs/design/EAASP/adrs/ADR-V2-029-engine-data-integration-boundary.md` (strategy ADR)

**Analog:** `docs/design/EAASP/adrs/ADR-V2-025-l1-runtime-contract-tier-strategy.md` (lines 1-35)
**Why:** Same `type: strategy`, same `enforcement.level: strategic`, lives in the same directory under the same governance plugin.

**Frontmatter template** (extract V2-025 L1-35 — adapt id/title/date/related; V2-029 sets `trace: []` instead of V2-025's 2 anchors, per D-02):
```yaml
---
id: ADR-V2-025
title: "L1 Runtime 契约执行强度差异化策略（四档）"
type: strategy
status: Accepted
date: 2026-05-02
accepted_at: 2026-05-02
phase: "Phase 5.1 — Engine Hardening"
author: "Jiangwen Su"
supersedes: []
superseded_by: null
deprecated_at: null
deprecated_reason: null
enforcement:
  level: strategic
  trace:
    - "tests/contract/cases/test_chunk_type_contract.py"
    - ".github/workflows/phase3-contract.yml"
  review_checklist: "docs/design/EAASP/adrs/ADR-V2-025-l1-runtime-contract-tier-strategy.md"
affected_modules:
  - "crates/grid-runtime/"
  - "crates/eaasp-goose-runtime/"
  ...
related:
  - ADR-V2-017
  - ADR-V2-021
  - ADR-V2-024
---
```

**Body skeleton** (lift from `docs/design/EAASP/adrs/ADR-TEMPLATE.md`, 60 LOC).
**Reference doc for substance:** `docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md` §1 双轴模型 (table-driven decision section is the shape V2-029 should mirror; CONTEXT D-01 + RESEARCH §Code Examples Example 1 spell out the exact target).
**Creation mechanism:** `/adr:new --type strategy --id V2-029` then `/adr:accept V2-029` (per RESEARCH §Architecture Patterns Pattern 1 + global CLAUDE.md §8 ADR plugin discipline).
**Lint expectation:** `python3 .adr-plugin/scripts/adr_lint.py 2>&1 | grep -E "ADR-V2-029.*\[F[1-4]"` → all F1-F4 PASS, no F3 WARN (strategy ADR with `trace: []` is silent on F3 per R4 verified on V2-024).

---

### 2. NEW `docs/design/EAASP/adrs/ADR-V2-032-tui-log-path-convention.md` (record ADR — per RESEARCH §2.5 recommendation; counter-option `strategy`)

**Analog:** `docs/design/EAASP/adrs/ADR-V2-028-strict-config-validation.md` (lines 1-35) — newest single-tech-decision ADR with a populated `trace` list pointing at 2 anchors.
**Why:** V2-032 has 1 anchor to point at (`crates/grid-cli/src/studio_main.rs::resolve_tui_log_path`), so the `trace` list shape matches V2-028 closely. Note: V2-028 is `type: contract`, V2-032 is `type: record` (RESEARCH §2.5) — set `enforcement.level: strategic` since record-type ADRs don't enforce on CI. There is no existing `record`-type ADR in `docs/design/EAASP/adrs/` (the `decisions/` subdir holds a `2026-04-27-leg-decision-audit.md` doc, not a plugin-managed ADR). **Confirm with `.adr-plugin/scripts/adr_frontmatter.py` whether `record` is an accepted `type` value before authoring**.

**Frontmatter template** (extract V2-028 L1-25 — adapt fields):
```yaml
---
id: ADR-V2-028
title: "Strict-by-default Configuration Validation (no silent env fallbacks)"
type: contract           # V2-032 → record (or strategy fallback)
status: Accepted
date: 2026-05-21
accepted_at: 2026-05-21
phase: "Phase 5.4 — Server Hardening"
author: "Jiangwen Su"
...
enforcement:
  level: contract-test   # V2-032 → strategic
  trace:
    - "crates/grid-engine/src/providers/config.rs"
    - "crates/grid-runtime/src/config.rs"
  ...
```

**Body required elements** (RESEARCH §2.5): Default path `./logs/tui.log`, override `GRID_TUI_LOG` env, deprecated `dirs::data_local_dir()` path, sanity stderr print `[grid-studio] log: <path>`, cross-link to ADR-V2-028 §D3 (logging-filter strict include).
**Creation mechanism:** `/adr:new --type record --id V2-032` (fallback `--type strategy`).

---

### 3-7. MODIFY 5 contract ADRs — F3 enforcement.trace fill (V2-006, V2-018, V2-020, V2-027, V2-028)

**Shared analog:** `docs/design/EAASP/adrs/ADR-V2-019-l1-runtime-deployment-model.md` (lines 14-21) — the Phase 5.4 Q9 trace-fill applied 2026-05-21 @ `70b5e94`.

**Pattern (V2-019 L14-21 — what already exists in the repo):**
```yaml
enforcement:
  level: strategic
  trace:
    - "crates/grid-runtime/src/config.rs::RuntimeConfig::deployment_mode"   # D142 close (5.4-02-07 @ d12f6ec)
    - "crates/grid-runtime/src/service.rs::RuntimeServiceImpl::create_session"   # gate (5.4-02-07 @ d12f6ec)
    - "lang/claude-code-runtime-python/src/claude_code_runtime/service.py::RuntimeServiceImpl.__init__::_deployment_mode"   # D143 close (5.4-02-08 @ 2453447)
    - "lang/claude-code-runtime-python/src/claude_code_runtime/service.py::RuntimeServiceImpl.Initialize"   # gate (5.4-02-08 @ 2453447)
  review_checklist: "docs/design/EAASP/adrs/ADR-V2-019-l1-runtime-deployment-model.md"
```

**Edit operation:** Single `Edit` per ADR replacing only the `trace:` list contents. **Status / accepted_at MUST stay unchanged** (Phase 5.4 Q9 lesson — verified via `head -25 ADR-V2-019…`).
**Per-ADR CI workflow target** (from RESEARCH §2.4 mapping table):
- V2-006 → `.github/workflows/phase3-contract.yml`
- V2-018 → `.github/workflows/phase3-contract.yml` + `.github/workflows/phase2_5-contract.yml`
- V2-020 → `.github/workflows/phase3-contract.yml`
- V2-027 → `.github/workflows/eval-ci.yml`
- V2-028 → `.github/workflows/eval-ci.yml` (with 1-line `paths:` expansion to add `crates/grid-runtime/**`)

**OPEN QUESTION (RESEARCH Q1):** Before authoring the 5 Edit tasks, planner reads `.adr-plugin/scripts/adr_lint.py` to confirm F3 match semantics — is it substring-in-workflow-file-content vs `paths:` filter glob? Cheap fallback (if substring): append a reference-comment block at the bottom of each target workflow file listing the verbatim trace paths.

---

### 8-11. MODIFY 4 strategy ADRs — rationale-comment annotation (V2-001, V2-002, V2-003, V2-005)

**Analog:** No on-disk precedent for the `# trace=[] intentional: strategic ADR` rationale-comment convention. **This is a new convention introduced in this phase**, sourced from `.planning/phases/05.5-interface-adr-milestone-close/05.5-CONTEXT.md` §Specific Ideas L177-184.

**Current state (V2-001 L13-22 — what the lint sees today):**
```yaml
enforcement:
  level: contract-test
  trace:
    - "tools/eaasp-l4-orchestration/tests/test_event_interceptor.py"
    - "tools/eaasp-l4-orchestration/tests/test_event_api.py"
    ...
```

**Target shape (per CONTEXT D-04 + RESEARCH §Code Examples Example 2 — keep existing trace entries, downgrade `level`, add rationale):**
```yaml
enforcement:
  level: strategic
  # trace=[] intentional: strategic ADR, no concrete code-to-CI mapping yet.
  # Existing trace entries point to tools/eaasp-l4-orchestration tests that
  # are not (yet) in any .github/workflows/*.yml — kept as documentation
  # anchors but downgraded from contract-test to strategic per Phase 5.5
  # NEW-E2 sweep (D-04 strategy tier).
  trace:
    - "tools/eaasp-l4-orchestration/tests/test_event_interceptor.py"
    ...
```

**OPEN QUESTION (RESEARCH Q2):** Retier `level: contract-test → strategic` (suppresses F3 WARN entirely) vs keep `contract-test` with rationale-only (keeps WARN but explains it). RESEARCH §Open Questions Q2 recommends **keep `level: contract-test`, add rationale comment only** per "13 WARN remain, each explicitly annotated with rationale" in D-04. **Planner picks at task-authoring time.**

---

### 12. MODIFY `crates/grid-cli/src/commands/session.rs` L117 (NEW-A3 — `kill_session` site)

**Analog:** Same file lines 99-103 (`delete_session`) and 157 (`export_session`) — both carry the identical `eprintln! + anyhow!` anti-pattern. **Self-analog: fixing all three (kill_session + delete_session + export_session) in a single task is the consistent shape, per RESEARCH §2.1 recommendation.**

**Current state (session.rs L113-118 — the bug site):**
```rust
// Check if session exists
if session_store.get_session(&sid).await.is_none() {
    eprintln!("Session not found: {}", session_id);
    return Err(anyhow::anyhow!("Session not found: {}", session_id));
}
```

**Target shape (RESEARCH §Code Examples Example 3 + RESEARCH §2.1):**
```rust
// AFTER (the fix — preserves the same eprint! call at L116)
return Err(grid_cli::error::GridError::session_not_found(session_id).into());
```

**Mapper already exists:** `crates/grid-cli/src/error.rs` L103-115 has `impl From<GridError> for ExitCode` that maps `SessionNotFound(_) → ExitCode::SessionNotFound = 4`. **No new mapper code needed.** Constructor `session_not_found()` exists at error.rs L124-126.
**Authoritative constant:** `crates/grid-cli/src/exit_codes.rs` L28 — `ExitCode::SessionNotFound = 4`. Lock by referencing the enum variant, not a numeric literal (per RESEARCH §R1 — never invent `EXIT_SESSION = 71`).

---

### 13. MODIFY `crates/grid-cli/src/main.rs` L92-96 (NEW-A3 — preserve typed exit code on downcast)

**Analog:** Same file L83-90 — `AppState::with_grid_root` Err arm uses the identical `GridError::other(e.to_string()).into()` pattern that drops the typed `SessionNotFound` variant. **Self-analog.** (Note: the fix in L92-96 is the higher-priority one because user-facing errors propagate through `run_command`; the L83-90 site is bootstrap errors and may be left as-is unless reviewer wants symmetry.)

**Current state (main.rs L92-96):**
```rust
if let Err(e) = run_command(cli.command, &state).await {
    eprintln!("Error: {}", e);
    let exit_code: ExitCode = GridError::other(e.to_string()).into();
    std::process::exit(exit_code as i32);
}
```

**Target shape (RESEARCH §Architecture Patterns Pattern 3 wrinkle (a) + RESEARCH §Code Examples Example 3 — surgical, no signature churn, ≤10 LOC):**
```rust
let exit_code: ExitCode = e
    .downcast_ref::<GridError>()
    .cloned()
    .map(ExitCode::from)
    .unwrap_or_else(|| GridError::other(e.to_string()).into());
```

---

### 14. NEW test in `crates/grid-cli/tests/cli_integration.rs` (append `test_kill_nonexistent_session_exits_4`)

**Analog:** Same file L38-55 (`ask_help_renders_and_exits_zero`) — uses `env!("CARGO_BIN_EXE_grid")` + `Command::new` + assertion on `out.status.code()`. Same harness, same convention.

**Existing pattern (cli_integration.rs L38-55 — fixture + assertion shape):**
```rust
fn grid_bin() -> Command {
    let path = env!("CARGO_BIN_EXE_grid");
    let mut cmd = Command::new(path);
    cmd.env_remove("NO_COLOR");
    cmd.env_remove("CLICOLOR");
    cmd.env_remove("CLICOLOR_FORCE");
    cmd
}

#[test]
fn ask_help_renders_and_exits_zero() {
    let out = grid_bin()
        .args(["ask", "--help"])
        .output()
        .expect("failed to run grid ask --help");
    assert!(out.status.success(), "grid ask --help must exit 0, got {:?}", out.status);
    ...
}
```

**New test shape (RESEARCH §Code Examples Example 3 — reuse the `grid_bin()` helper):**
```rust
#[test]
fn test_kill_nonexistent_session_exits_4() {
    let out = grid_bin()
        .args(["session", "kill", "does-not-exist-xyz"])
        .output()
        .unwrap();
    assert_eq!(out.status.code(), Some(4),
               "kill_session on missing id should exit 4 (SessionNotFound), got {:?}",
               out.status.code());
}
```

**Gate note:** File-level `#![cfg(feature = "studio")]` at L22 means the test runs under `cargo test -p grid-cli --features studio`. Phase gate per D-10 calls `cargo test -p grid-cli` (default features = studio enabled in this crate's default config — verify in `crates/grid-cli/Cargo.toml`).

---

### 15. MODIFY `crates/grid-cli/src/studio_main.rs` L57-60 (NEW-F4 — delete dead `dirs::data_local_dir()` branch)

**Analog:** Surrounding code in the same function (`resolve_tui_log_path` at L43-61). L51-55 (`cwd_logs` happy path) stays; L57-60 are unreachable because L54 `is_ok()` shortcircuits the only failure mode.

**Current state (studio_main.rs L43-61):**
```rust
/// Resolve the TUI log file path. Honors `GRID_TUI_LOG` for explicit override,
/// otherwise writes to `./logs/tui.log` in the current working directory (close
/// to the repo for fast `tail -f` access during development). Falls back to    // <- update
/// `dirs::data_local_dir()/grid/tui.log` if cwd is unwritable.                  // <- update
fn resolve_tui_log_path() -> std::path::PathBuf {
    if let Ok(p) = std::env::var("GRID_TUI_LOG") {
        return std::path::PathBuf::from(p);
    }
    let cwd_logs = std::env::current_dir()
        .unwrap_or_else(|_| std::path::PathBuf::from("."))
        .join("logs");
    if std::fs::create_dir_all(&cwd_logs).is_ok() {
        return cwd_logs.join("tui.log");
    }
    dirs::data_local_dir()                          // <- DELETE L57-60
        .unwrap_or_else(|| std::path::PathBuf::from("."))
        .join("grid")
        .join("tui.log")
}
```

**Target shape (RESEARCH §Architecture Patterns Pattern 4 — keep `dirs` Cargo dep because `tui/formatters/path_shortener.rs:20,127` still uses `dirs::home_dir()`):**
```rust
fn resolve_tui_log_path() -> std::path::PathBuf {
    if let Ok(p) = std::env::var("GRID_TUI_LOG") {
        return std::path::PathBuf::from(p);
    }
    let cwd_logs = std::env::current_dir()
        .unwrap_or_else(|_| std::path::PathBuf::from("."))
        .join("logs");
    let _ = std::fs::create_dir_all(&cwd_logs);
    cwd_logs.join("tui.log")
}
```

Also update docstring L45-46 — drop the "Falls back to `dirs::data_local_dir()`" mention.
**Verification grep:** `grep -c "dirs::data_local_dir" crates/grid-cli/src/studio_main.rs` → expect `0` (was `1` + `1` in docstring).

---

### 16. MODIFY `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/vector_index.py` L189-192 + L225-235 (NEW-L1)

**Analog 1 (cap-fix site):** Same file L184-207 (the `add()` method body) — `current_count = self._index.get_current_count()` block. The fix replaces the doubling at L190-192 with a `min(..., HARD_CAP)` + actionable `RuntimeError`.
**Analog 2 (meta.json shape):** Same file L221-236 (`save()` method — meta dict assembly with 8 keys) and L142-161 (`_try_load_sync` — meta dict consumption with `meta.get(...)` calls). Add `max_elements` as a 9th key in `save()`; restore from meta on load in `_try_load_sync`.

**Current state (vector_index.py L184-192 — bug site):**
```python
if len(vec) != self.dim:
    raise DimensionMismatchError(f"vec len {len(vec)} != index dim {self.dim}")
async with self._write_lock:
    # Grow if we're about to hit capacity.
    current_count = self._index.get_current_count()
    if current_count >= self._max_elements - 1:
        new_max = self._max_elements * 2
        self._index.resize_index(new_max)
        self._max_elements = new_max
```

**Current state (vector_index.py L225-235 — meta dict):**
```python
meta = {
    "model_id": self.model_id,
    "dim": self.dim,
    "space": self.space,
    "M": self.M,
    "ef_construction": self.ef_construction,
    "next_label": self._next_label,
    "id_to_label": self._id_to_label,
    # Stringify keys for JSON; parsed back to int on load.
    "label_to_id": {str(k): v for k, v in self._label_to_id.items()},
}
self.meta_path.write_text(json.dumps(meta))
```

**Target shape (RESEARCH §Code Examples Example 4):** cap doubling with `HNSW_HARD_CAP = 1_000_000` (module-level const, dim=1024 fp32 ≈ 4GB); add `"max_elements": self._max_elements` key to meta dict; in `_try_load_sync`, set `self._max_elements = int(meta.get("max_elements", self._max_elements))` after load.
**Atomic-write preservation** (RESEARCH §Security Domain V14.1.5): The existing `meta_path.write_text(json.dumps(meta))` at L236 is atomic enough for this fix. **Do NOT introduce non-atomic write patterns.**

---

### 17. MODIFY `tools/eaasp-l2-memory-engine/tests/test_vector_index.py` — append `test_grow_hits_hard_cap` + `test_meta_json_max_elements_roundtrip`

**Analog:** Same file L211-233 (`test_hnsw_growth_resize`) — uses `tmp_path` fixture, `_rand_vec(rng)` helper, `HNSWVectorIndex(model_id=..., octo_root=tmp_path, max_elements=5)` constructor, then `await idx.add(...)` loop. Same fixtures, same assertion shape.

**Existing pattern (test_vector_index.py L211-227):**
```python
async def test_hnsw_growth_resize(tmp_path: Path) -> None:
    """Start with a tiny capacity and confirm that surpassing it triggers
    resize without crashing, and that every insert remains queryable."""
    idx = HNSWVectorIndex(
        model_id="bge-m3:fp16@ollama",
        octo_root=tmp_path,
        max_elements=5,
    )
    rng = np.random.default_rng(21)
    vecs = [_rand_vec(rng) for _ in range(6)]

    for i, v in enumerate(vecs):
        await idx.add(f"id-{i}", v)

    assert idx.count() == 6
    # Capacity should have grown (at least double of the original 5).
    assert idx._max_elements >= 10
```

**New test shape (RESEARCH §2.3):** `test_grow_hits_hard_cap` constructs `HNSWVectorIndex(max_elements=HNSW_HARD_CAP - 1)`, fills, asserts `RuntimeError` on next add. `test_meta_json_max_elements_roundtrip` round-trips: save → reload via `HNSWVectorIndex(model_id=..., octo_root=tmp_path)` constructor → assert `idx._max_elements` equals the persisted value (not the default 10_000).
**Existing assertion at L227 (`assert idx._max_elements >= 10`)** stays as-is — 5 → 10 doubling stays well below any cap.

---

### 18. MODIFY `docs/design/EAASP/DEFERRED_LEDGER.md` — 4 row-add + 1 row-edit + 14 verify

**Analog 1 (5-row close commit shape):** Commit `279b639` (Phase 5.4 Task 11) — "5-row close D142+D143+NEW-A2+NEW-E3+NEW-F3 (Task 11)". Same operation: row-edit-in-place + closing-commit hash, no row deletion (per Phase 4.0 CLEANUP-02 convention; RESEARCH §R6 reaffirms — `git diff --stat` shows additions only).

**Analog 2 (per-row format):** DEFERRED_LEDGER.md L262 (D142 row, ✅ CLOSED 2026-05-21 Phase 5.4):
```markdown
| **D142** | Phase 2.5 post-ADR-V2-019 — grid-runtime 不读 `EAASP_DEPLOYMENT_MODE` env | ✅ CLOSED 2026-05-21 Phase 5.4 @ `d12f6ec` (5.4-02-07) | **Resolution**: `crates/grid-runtime/src/config.rs::RuntimeConfig::deployment_mode` 新增字段从 `EAASP_DEPLOYMENT_MODE` env 读取... |
```

**Analog 3 (re-targeted row to flip to CLOSED):** DEFERRED_LEDGER.md L472 (NEW-F4 — `🟡 P2 | **Re-targeted to Phase 5.5** per Phase 5.4 CONTEXT D-13 ...`). Plan 02 edits this row's status cell `🟡 P2` → `✅ CLOSED YYYY-MM-DD Phase 5.5 @ <hash>` + Resolution text appended.

**Sweep operation per RESEARCH §2.6 + Q3:**
- **ADD 4 new rows:** NEW-E2, NEW-A3, NEW-L1, grid-cli mod-output (all `✅ CLOSED Phase 5.5`).
- **EDIT 1 row:** NEW-F4 (flip from "Re-targeted to Phase 5.5" → `✅ CLOSED`).
- **VERIFY 14 already-CLOSED rows:** D109, D120, D134, D136, D142, D143, NEW-A2, NEW-D2, NEW-E3, NEW-E4, NEW-F1, NEW-F2, NEW-F3 + (any others per closure target list).
- **Verification:** `grep -c "✅ CLOSED" docs/design/EAASP/DEFERRED_LEDGER.md` should grow by exactly 4 (4 new ✅ rows added); NEW-F4 row no longer prefixed `Re-targeted`.

---

### 19. MODIFY `.planning/PROJECT.md` (§Active → §Validated milestone-flip)

**Analog:** No prior-commit precedent for this exact flip in the current GSD-managed PROJECT.md (the v3.0 close at Phase 4.2 predates this workflow). **Closest reference is the file's own current shape** — Plan 02 reads PROJECT.md §Active section, identifies the `[ ] Phase 5 milestone (v3.1)` line (per RESEARCH §code_context Integration Points), and moves it to §Validated with V2-025 / V2-026 (ExecutionMode) / V2-029 commit hashes referenced.

**Operation:** Single targeted `Edit` per Plan 02 Task 02.01.
**Verification grep (RESEARCH §Phase Requirements → Test Map):** `grep -c "Phase 5 milestone (v3.1)" .planning/PROJECT.md` filtered to §Active → expect `0`.

---

### 20. MODIFY `.planning/ROADMAP.md` (Phase 5.5 → Status=Complete + milestone line flip)

**Analog:** Commit `dc24f2f` (Phase 5.4 close — "docs(05.4): close phase — VERIFICATION PASSED, STATE row updated") — diff stat shows `.planning/STATE.md | 16 +- ...`. While that commit is STATE-focused, the ROADMAP entry-status flip pattern is per-phase symmetric: change `Status: executing` → `Status: complete` with completion date.

**Operation:** Plan 02 Task 02.02 makes 2 edits:
1. Phase 5.5 entry — `Status: Complete YYYY-MM-DD` (plus all 6 phases 5.0..5.5 verified complete).
2. §Milestones top-of-file — `🚧 v3.1 Phase 5 — Engine Hardening` → `✅ v3.1 Phase 5 — SHIPPED YYYY-MM-DD`.
3. SC#3 wording revision per CONTEXT D-04 — "≤5 unjustified" → "0 unjustified + 13 explicit-strategic" semantic.

**Verification grep:** `grep -c "✅.*v3.1.*SHIPPED" .planning/ROADMAP.md` → expect `1`.

---

### 21. MODIFY `.planning/STATE.md` (frontmatter + Performance Metrics + Progress bar)

**Analog:** Commit `dc24f2f` (Phase 5.4 close) — `.planning/STATE.md | 16 +-` — same file, same edit shape. Plan 02 extends to **milestone-level** (status flip + 6/6 progress + Performance Metrics) not just phase-level row update.

**Edits per RESEARCH §R7:**
- Frontmatter: `status: executing` → `status: milestone-complete`.
- Progress: `6/6 = 100%`.
- Progress bar (L34 per RESEARCH §R7): `▓▓▓▓▓░░░░░ 83%` → `▓▓▓▓▓▓▓▓▓▓ 100%`.
- Performance Metrics velocity table — add Phase 5.5 row.
- Phase Position section — milestone-close timestamp.

**Verification grep:** `grep -c "^status: milestone-complete" .planning/STATE.md` → expect `1`.

---

### 22. MODIFY `.planning/REQUIREMENTS.md` (Traceability table verify — minimal/no edit)

**Analog:** The file's own existing Traceability table (end of file). Plan 02 Task 02.04 is a verify-only operation: confirm every REQ-ID has a Phase column and per-phase verification ✅.

**Operation:** Read the table; if all 23 REQ-IDs are ✅ no edit needed; otherwise fill missing checkmarks.
**Verification grep:** `grep -c "REQ-" .planning/REQUIREMENTS.md` and cross-check Traceability section for any `[ ]` or empty cells.

---

## Shared Patterns

### Pattern A — ADR creation via `/adr:new` + human-verify checkpoint (Phase 5.4 Task 9 precedent)

**Source:** `.planning/phases/05.4-server-hardening/05.4-02-PLAN.md` Task 9 (V2-028 Accepted) — verbatim shape lifted into RESEARCH §Architecture Patterns Pattern 1.
**Apply to:** Tasks creating ADR-V2-029 and ADR-V2-032 (artifacts 1 and 2 above).
**Anti-pattern to avoid (global CLAUDE.md §8 + MEMORY.md):** Hand-writing ADR frontmatter. Always `/adr:new --type <strategy|record|contract> --id V2-NNN` then `/adr:accept V2-NNN`.

### Pattern B — F3 trace-fill targeted Edit (Phase 5.4 Task 10 precedent)

**Source:** `.planning/phases/05.4-server-hardening/05.4-02-PLAN.md` Task 10 (V2-019 trace fill at commit `70b5e94`) — verbatim shape lifted into RESEARCH §Architecture Patterns Pattern 2.
**Apply to:** All 9 ADR frontmatter edit tasks (artifacts 3-11 above). Status / accepted_at MUST stay unchanged (Phase 5.4 Q9 lesson — re-verified via `head -25` before each Edit per MEMORY.md "ADR 状态二次校验" always-on rule).

### Pattern C — Self-analog reuse for code edits

**Source:** Existing function in the same file that already implements the target pattern.
**Apply to:** Artifacts 12 (session.rs — L99-103 and L157 are self-analogs for L117 fix), 13 (main.rs — L83-90 mirrors L92-96), 14 (cli_integration.rs — L38-55 `grid_bin()` helper + assertion shape), 15 (studio_main.rs — keep surrounding `cwd_logs` happy path), 16 (vector_index.py — L221-236 meta dict assembly is the self-analog for adding `max_elements` key), 17 (test_vector_index.py — L211-233 `test_hnsw_growth_resize` shape).
**Why this matters:** Phase 5.5 has zero novel patterns at the code level. Every code edit is a copy-and-adapt from a sibling already present in the same file or directory.

### Pattern D — LEDGER row-edit-on-close (Phase 4.0 CLEANUP-02 convention, locked by CONTEXT D-05)

**Source:** Commit `279b639` (Phase 5.4 Task 11 — 5-row close) + the in-file shape of any already-✅-CLOSED row (e.g., D142 at L262).
**Apply to:** Artifact 18 (DEFERRED_LEDGER 18-row sweep).
**Anti-pattern to avoid (RESEARCH §R6):** Bulk-delete-and-readd via script. Use per-row Edit only. Acceptance criteria: `git diff --stat docs/design/EAASP/DEFERRED_LEDGER.md` shows additions (4 new rows) with few/no deletions; `wc -l` count grows not shrinks.

### Pattern E — Commit footer (global, every commit in this phase)

**Source:** `~/.claude/CLAUDE.md` §12 Commit Message Guidelines + repo `CLAUDE.md` §Git Commit Guidelines (binding).
**Apply to:** Every commit in Phase 5.5 (estimated 8-12 commits across 2 plans).
**Footer:**
```
Generated-By: Claude (claude-opus-4-7) via Claude Code CLI
Co-Authored-By: claude-flow <ruv@ruv.net>
```

---

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| (none) | — | — | Every Phase 5.5 artifact has a Phase 5.3/5.4 or self-analog precedent. The 4 strategy-ADR rationale-comment edits (V2-001/002/003/005) follow a **new convention** but the convention is fully spelled out in CONTEXT §Specific Ideas L177-184 + RESEARCH §Code Examples Example 2 — planner has the template, no archaeological dig needed. |

---

## Metadata

**Analog search scope:**
- `docs/design/EAASP/adrs/` (29 files — read frontmatter of V2-001, V2-019, V2-025, V2-028 directly; rest verified via RESEARCH §Sources)
- `crates/grid-cli/src/` (commands/session.rs, main.rs, studio_main.rs, lib.rs, error.rs, exit_codes.rs — pre-read by RESEARCH §Sources)
- `crates/grid-cli/tests/cli_integration.rs` (Phase 5.2 closure baseline)
- `tools/eaasp-l2-memory-engine/src/eaasp_l2_memory_engine/vector_index.py` (294 LOC, pre-read by RESEARCH §2.3)
- `tools/eaasp-l2-memory-engine/tests/test_vector_index.py` (233 LOC, partial-read for `test_grow` shape)
- `docs/design/EAASP/DEFERRED_LEDGER.md` (read via grep on D-IDs + row L262/L472)
- `git log` for commits `f497eef` (Phase 4.2 ADR flip), `dc24f2f` (Phase 5.4 STATE close), `279b639` (Phase 5.4 LEDGER 5-row close)

**Files scanned:** 14 source/doc files + 6 commit-shape lookups.
**Pattern extraction date:** 2026-05-22.
**Stop criterion met:** All 14 artifacts have a documented analog with file path + line range; planner can author `read_first` blocks without re-searching.

---

*Phase: 05.5-interface-adr-milestone-close*
*Pattern mapping: 2026-05-22*
