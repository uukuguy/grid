---
phase: 05.5-interface-adr-milestone-close
plan: 02
type: execute
wave: 2
depends_on: [05.5-01]
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - docs/design/EAASP/DEFERRED_LEDGER.md
autonomous: true
requirements: [INTERFACE-01, WATCH-06]
requirements_addressed: [INTERFACE-01, WATCH-06]
tags: [milestone-close, ledger, state-update, governance]

must_haves:
  truths:
    - "PROJECT.md no longer lists Phase 5 milestone (v3.1) as in-flight in §Active section"
    - "ROADMAP.md top-of-file milestone line shows ✅ v3.1 Phase 5 SHIPPED with completion date"
    - "ROADMAP.md all 6 Phase 5 entries (5.0..5.5) marked Status=Complete or [x]"
    - "ROADMAP.md SC#3 wording revised per CONTEXT D-04: 33 baseline + 0 unjustified + 13 explicit-strategic (NOT the stale ≤5 unjustified target)"
    - "STATE.md frontmatter has status: milestone-complete + progress: 6/6 = 100%"
    - "STATE.md Progress bar updated from ▓▓▓▓▓░░░░░ 83% to ▓▓▓▓▓▓▓▓▓▓ 100%"
    - "REQUIREMENTS.md Traceability table — all 23 REQ-IDs have phase column filled"
    - "DEFERRED_LEDGER row count of ✅ CLOSED grows from 42 baseline by exactly 4 (NEW-E2, NEW-A3, NEW-L1, grid-cli mod-output)"
    - "DEFERRED_LEDGER NEW-F4 row no longer prefixed with 'Re-targeted to Phase 5.5'"
    - "Phase gate per D-10 passes: ADR lint 0 FAIL + targeted Rust tests + Python tests"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "milestone-complete frontmatter + 6/6 progress + closure prose"
      contains: "status: milestone-complete"
    - path: ".planning/ROADMAP.md"
      provides: "All Phase 5 entries marked complete + ✅ v3.1 SHIPPED milestone line"
      pattern: "✅.*v3.1.*SHIPPED"
    - path: ".planning/PROJECT.md"
      provides: "§Active no longer lists Phase 5 milestone as in-flight"
      pattern: "v3.1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table all 23 REQ-IDs ✅"
      pattern: "23"
    - path: "docs/design/EAASP/DEFERRED_LEDGER.md"
      provides: "4 new ✅ CLOSED rows + 1 NEW-F4 row edit + 14 existing rows verified"
      pattern: "✅ CLOSED.*Phase 5.5"
  key_links:
    - from: "STATE.md frontmatter status"
      to: "milestone v3.1 complete"
      via: "status: milestone-complete + progress.percent: 100"
      pattern: "milestone-complete"
    - from: "ROADMAP.md §Milestones top"
      to: "✅ v3.1 Phase 5 SHIPPED"
      via: "🚧 → ✅ flip"
      pattern: "✅.*v3.1.*SHIPPED"
    - from: "DEFERRED_LEDGER 4 new rows"
      to: "Plan 01 commit hashes from Task 01.A1/A3/B1/B4"
      via: "Each row Resolution cites the commit hash from Plan 01 SUMMARY"
      pattern: "Phase 5.5 @"
---

<objective>
Phase 5.5 Plan 02 — Milestone v3.1 Close Cascade.

Cascade the milestone-close edits across PROJECT.md / ROADMAP.md / STATE.md / REQUIREMENTS.md plus DEFERRED_LEDGER row-edit sweep (4 row-ADDs + 1 row-EDIT + 14 row-VERIFY) per CONTEXT D-09. Run the phase gate per CONTEXT D-10 (ADR lint + targeted Rust tests + Python tests + `cargo check --workspace` — NO `cargo test --workspace`).

Purpose: This plan is the v3.1 milestone close gesture. It does not produce new functional artifacts — instead it records the milestone outcome across the 4 SSOT planning docs and the deferred ledger so future sessions can read "what shipped + what's outstanding" without re-deriving from git history.

Precondition: Plan 01 verifier must have PASSED (both ADRs Accepted, 9-ADR F3 sweep done, 4 OOS items closed). This plan reads Plan 01 commit hashes (from `.planning/phases/05.5-interface-adr-milestone-close/05.5-01-SUMMARY.md`) and embeds them in LEDGER row Resolutions.

Output: 4 doc file edits + 5 LEDGER row actions (4 ADD + 1 EDIT) + 1 phase gate verification commit. Estimated 5-7 commits total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.5-interface-adr-milestone-close/05.5-CONTEXT.md
@.planning/phases/05.5-interface-adr-milestone-close/05.5-RESEARCH.md
@.planning/phases/05.5-interface-adr-milestone-close/05.5-PATTERNS.md
@.planning/phases/05.5-interface-adr-milestone-close/05.5-VALIDATION.md
@.planning/phases/05.5-interface-adr-milestone-close/05.5-01-SUMMARY.md
@.planning/phases/05.4-server-hardening/05.4-VERIFICATION.md
@docs/design/EAASP/DEFERRED_LEDGER.md
@CLAUDE.md

<interfaces>
<!-- Key state to embed in the close cascade — sourced from RESEARCH + live verification 2026-05-22 -->

LEDGER baseline verified 2026-05-22:
- Current ✅ CLOSED row count: 42 (target post-5.5: 46 — add 4)
- 14 already-closed rows to VERIFY (no edit): D109, D120, D134, D136, D142, D143, NEW-A2, NEW-D2, NEW-E3, NEW-E4, NEW-F1, NEW-F2, NEW-F3
- 4 rows to ADD as ✅ CLOSED Phase 5.5:
  - NEW-E2 (commit hash from Plan 01 Task 01.A3)
  - NEW-A3 (commit hash from Plan 01 Task 01.B1)
  - NEW-L1 (commit hash from Plan 01 Task 01.B4)
  - grid-cli mod output E0583 (no Plan 01 commit; note "resolved by prior commit, verified clean 2026-05-22")
- 1 row to EDIT: NEW-F4 (currently "Re-targeted to Phase 5.5" → ✅ CLOSED with Task 01.A2 + 01.B2 commit hashes)

F3 baseline verified 2026-05-22 via `python3 .adr-plugin/scripts/adr_lint.py`:
- Total: 164 PASS / 0 FAIL / 105 WARN
- F3 subset: 33 WARN across 9 ADRs

ROADMAP SC#3 stale wording (Phase 5.5 §Phase Details L120) needs revision per D-04 — see Task 02.02.

STATE.md current key state (verified at session start 2026-05-22):
- frontmatter status: verifying (must flip to milestone-complete)
- progress.completed_phases: 5 (must flip to 6)
- Progress bar L34: `[▓▓▓▓▓░░░░░] 83% (5/6 milestone phases complete)` → must flip to 100% 6/6
- Performance Metrics velocity table: needs Phase 5.5 row
- Deferred Items table at L115-131: NEW-A3 + NEW-E2 rows need status flip to ✅ CLOSED

PROJECT.md §Active section to find + flip (per RESEARCH code_context):
- Search for `Phase 5 milestone (v3.1)` line
- Flip [ ] → [x] OR move from §Active to §Validated

REQUIREMENTS.md Traceability table (L82-106) — verify-only:
- 23 rows; confirm all REQ-IDs have phase column filled (currently all do per L84-106)
- If any [ ] checkbox remains in §A-E lists at L13-47, flip to [x]

Phase gate command sequence per D-10 (Task 02.06):
```bash
python3 .adr-plugin/scripts/adr_lint.py            # 0 FAIL, F3 WARN ≤ 13
cargo test -p grid-cli                              # 147+ tests PASS
cargo build -p grid-cli --features studio           # exit 0
cargo check --workspace                             # exit 0
uv run pytest tools/eaasp-l2-memory-engine/tests/test_vector_index.py   # PASS
```

NO `cargo test --workspace` — explicitly forbidden by D-10 + CLAUDE.md + MEMORY.md feedback_no_full_tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 02.01: PROJECT.md §Active → §Validated milestone flip</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (full file — locate §Active section; find line referencing "Phase 5 milestone (v3.1)")
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-01-SUMMARY.md (read commit hashes for V2-029 from Task 01.A1, V2-032 from Task 01.A2)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-PATTERNS.md §19
  </read_first>
  <action>
Read `.planning/PROJECT.md` end-to-end. Locate the §Active section. Find line(s) referencing `Phase 5 milestone (v3.1)`.

Single targeted `Edit`: either (a) move the `[ ] Phase 5 milestone (v3.1)` line from §Active to §Validated, OR (b) mark in-place with `[x]` and add a §Validated reference. Pick whichever matches PROJECT.md's existing schema.

The §Validated line (or in-place note) should reference these ADRs (in chronological order):
- V2-025 (Phase 5.1 — Runtime Tier Strategy)
- V2-026 (Phase 5.3 — ExecutionMode supersede V2-016)
- V2-027 (Phase 5.3 — OpenAI-compat Quirks)
- V2-028 (Phase 5.4 — Strict Config Validation)
- V2-029 (Phase 5.5 — Engine vs Data/Integration Boundary; commit hash from Plan 01 SUMMARY)
- V2-032 (Phase 5.5 — TUI Log Path Convention; commit hash from Plan 01 SUMMARY)

If PROJECT.md has no §Validated section, append a brief note: "Milestone v3.1 — Phase 5 Engine Hardening (closed 2026-05-22 via V2-025/V2-026/V2-027/V2-028/V2-029/V2-032)".

Commit: `docs(project): mark Phase 5 milestone v3.1 complete in PROJECT.md` with mandatory footer per CLAUDE.md §Git Commit Guidelines.

Stay in project root — Edit tool uses absolute path.
  </action>
  <verify>
    <automated>grep -c "V2-029" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "V2-029" .planning/PROJECT.md` returns ≥ 1 (ADR-V2-029 referenced)
    - `grep -c "V2-032" .planning/PROJECT.md` returns ≥ 1 (ADR-V2-032 referenced)
    - PROJECT.md no longer carries `[ ] Phase 5 milestone (v3.1)` line (either moved to §Validated or flipped to `[x]`); verifiable by `grep -c "^- \[ \].*Phase 5 milestone" .planning/PROJECT.md` returning 0
    - Commit landed with footer
  </acceptance_criteria>
  <done>PROJECT.md reflects Phase 5 milestone v3.1 complete; commit landed.</done>
</task>

<task type="auto">
  <name>Task 02.02: ROADMAP.md milestone line flip + 6 phase entries Complete + SC#3 wording revision</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file — focus on §Milestones top L10-11, Phase 5.0/5.1/5.2/5.3/5.4/5.5 entries L30-35, Phase 5.5 §Phase Details L112-124 incl. SC#3 at L120, §Progress table L146-153)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-CONTEXT.md §D-04 (SC#3 revised wording target)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-RESEARCH.md §Pitfall 4 (33 baseline + 13 explicit-strategic)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-01-SUMMARY.md (Plan 01 outcome — commit count for Progress row)
  </read_first>
  <action>
Three edit groups in ROADMAP.md:

**Edit Group A — Milestone line flip (L11)**:
- BEFORE: `🚧 **v3.1 Phase 5 — Engine Hardening (grid-cli + grid-server)** — Phases 5.0/5.1/5.2/5.3/5.4 COMPLETE; 5.5 remaining`
- AFTER: `✅ **v3.1 Phase 5 — Engine Hardening (grid-cli + grid-server)** — SHIPPED 2026-05-22 (Phases 5.0/5.1/5.2/5.3/5.4/5.5 all complete)`

**Edit Group B — Phase 5 entry checkboxes (L30-35)**:
- L30 `[ ] **Phase 5.0:` → `[x] **Phase 5.0:` (Phase 5.0 is complete per STATE table L54)
- L32 `[ ] **Phase 5.2:` → `[x] **Phase 5.2:` (Phase 5.2 closed 2026-05-17)
- L34 `[ ] **Phase 5.4:` → `[x] **Phase 5.4:`
- L35 `[ ] **Phase 5.5:` → `[x] **Phase 5.5:`
- Phases 5.1 + 5.3 already `[x]` — leave as-is

**Edit Group C — Phase 5.5 §Phase Details SC#3 wording revision (L120)**:
- BEFORE: `3. NEW-E2 sweep 完成 — 29 missing 'enforcement.trace' items 在相关 ADR 中按 advisory level 补全或显式标记 "trace=[] is intentional for strategic ADRs"; F3 lint 输出从 29 missing 降至 ≤ 5 (剩余项明示 strategic / strategic-with-rationale); NEW-E2 在 DEFERRED_LEDGER 标 ✅ CLOSED 并附 commit hash`
- AFTER: `3. NEW-E2 sweep 完成 — 33 F3 WARN baseline (verified 2026-05-22; correction per Phase 5.5 RESEARCH §Pitfall 4 — ROADMAP 原文 29 已 drift) 经 9-ADR sweep (5 contract trace fix + 4 strategy rationale annotation) 降至 13 explicit-strategic + 0 unjustified WARN; 剩余 13 项均在 ADR frontmatter 内带 "F3-WARN expected" rationale 注释 (per CONTEXT D-04 strategy tier); NEW-E2 在 DEFERRED_LEDGER 标 ✅ CLOSED 并附 commit hash`

**Edit Group D — §Progress table (L146-153)**:
Update or replace the Phase 5.5 row to:
```
| 5.5 Interface ADR + Milestone Close | **2/2 ✅** | COMPLETE 2026-05-22 | Plan 01: ADR-V2-029 + V2-032 Accepted + F3 sweep (33 WARN → 13 explicit-strategic) + 4 OOS code fixes (NEW-A3 exit 4 / NEW-F4 dead-path / grid-cli mod output verify / NEW-L1 HNSW cap+schema); Plan 02: milestone close cascade + 5-row LEDGER sweep |
```

Stay in project root. Commit: `docs(roadmap): close Phase 5 milestone v3.1 — 6/6 phases complete + SC#3 wording correction` with mandatory footer.
  </action>
  <verify>
    <automated>grep -c "✅.*v3.1.*SHIPPED" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "✅.*v3.1.*SHIPPED" .planning/ROADMAP.md` returns ≥ 1
    - `grep -c "🚧.*v3.1" .planning/ROADMAP.md` returns 0 (no remaining in-progress marker)
    - `grep -cE '^- \[ \] \*\*Phase 5\.' .planning/ROADMAP.md` returns 0 (no Phase 5 entries still unchecked)
    - `grep -c "33 F3 WARN baseline\|33 WARN baseline\|33 → 13" .planning/ROADMAP.md` returns ≥ 1 (SC#3 wording revised)
    - `grep -c "5.5 Interface ADR.*COMPLETE 2026-05-22\|5.5 Interface ADR.*COMPLETE" .planning/ROADMAP.md` returns ≥ 1 (Progress table updated)
  </acceptance_criteria>
  <done>Milestone v3.1 SHIPPED line at top; all 6 Phase 5 entries [x]; SC#3 wording reflects 33 baseline + 13 explicit-strategic; Progress table has Phase 5.5 row.</done>
</task>

<task type="auto">
  <name>Task 02.03: STATE.md full pass — frontmatter + progress bar + Performance Metrics + Deferred row updates</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (full file — focus on frontmatter L1-15, "Current Position" L26-32, Progress bar L34, Performance Metrics By-Phase table L48-60, Deferred Items table L115-131, Session Continuity L135-152)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-CONTEXT.md (Phase 5.5 outcome summary)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-01-SUMMARY.md (Plan 01 commit hashes for Deferred row Resolutions)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-RESEARCH.md §R7 (missing-doc risk — ensure STATE.md full pass, not just frontmatter)
  </read_first>
  <action>
Seven edit groups in STATE.md:

**Edit Group A — Frontmatter (L1-15)**:

Replace:
```yaml
status: verifying
stopped_at: "Phase 5.5 context gathered — ..."
last_updated: "2026-05-21T..."
last_activity: 2026-05-21 -- Phase 5.4 verification PASSED ...
progress:
  total_phases: 6
  completed_phases: 5
  total_plans: 7
  completed_plans: 7
  percent: 100
```

With:
```yaml
status: milestone-complete
stopped_at: "Milestone v3.1 closed 2026-05-22 — 6/6 phases complete (5.0/5.1/5.2/5.3/5.4/5.5), 23/23 REQ-IDs traced, ADR-V2-029 + V2-032 Accepted, 4-row LEDGER ADD + 1 EDIT, phase gate PASS"
last_updated: "2026-05-22T<ISO 8601 timestamp at commit time>"
last_activity: 2026-05-22 -- Milestone v3.1 closed — Phase 5.5 SUMMARY + close cascade landed
progress:
  total_phases: 6
  completed_phases: 6
  total_plans: 9
  completed_plans: 9
  percent: 100
```

**Edit Group B — Progress bar (L34)**:
- BEFORE: `Progress: [▓▓▓▓▓░░░░░] 83% (5/6 milestone phases complete — 5.0 + 5.1 + 5.2 + 5.3 + 5.4)`
- AFTER: `Progress: [▓▓▓▓▓▓▓▓▓▓] 100% (6/6 milestone phases complete — 5.0 + 5.1 + 5.2 + 5.3 + 5.4 + 5.5) — MILESTONE v3.1 CLOSED 2026-05-22`

**Edit Group C — Current Position prose (L26-32)**:
- Update "Phase: 5.4 (Server Hardening) — ✅ COMPLETE + VERIFIED" → "Phase: 5.5 (Interface ADR + Milestone Close) — ✅ COMPLETE + MILESTONE v3.1 CLOSED 2026-05-22"
- Update "Next phase" line to: `Next milestone: v3.2 (TBD) — ready for /gsd-new-milestone (or pause)`

**Edit Group D — Performance Metrics By-Phase table (L48-60)**:
Add Phase 5.5 row at bottom of table (matching format of existing rows):
```
| 5.5 Interface ADR + Milestone Close | **2/2 ✅** | COMPLETE 2026-05-22 | Plan 01: 2 ADRs Accepted (V2-029 + V2-032) + F3 sweep (33 WARN → 13 explicit-strategic) + 4 OOS code fixes (NEW-A3 + NEW-F4 + grid-cli mod output verify + NEW-L1); Plan 02: 4 doc files + 5-row LEDGER sweep + phase gate PASS |
```

**Edit Group E — Recent Trend (L62-66)**:
Add Phase 5.5 plans to the Last-5-plans rolling list, e.g.:
```
- Last 5 plans: [05.5-02 ✅ 2026-05-22, 05.5-01 ✅ 2026-05-22, 05.4-02 ✅ 2026-05-21, 05.4-01 ✅ 2026-05-21, 05.3-02 ✅ 2026-05-20]
```

**Edit Group F — Deferred Items table (L115-131)**:
- Update NEW-A3 row (L131) status: `🟢 P2 (CLI ergonomics), mapped to Phase 5.4` → `✅ CLOSED 2026-05-22 Phase 5.5 Plan 01 @ <commit hash from Plan 01 Task 01.B1>`
- Update NEW-E2 row (L124) status: `🟡 advisory, mapped to Phase 5.5 (WATCH-06)` → `✅ CLOSED 2026-05-22 Phase 5.5 Plan 01 @ <commit hash from Plan 01 Task 01.A3>`
- (Optional) Add a 1-line note that detail SSOT remains DEFERRED_LEDGER

**Edit Group G — Session Continuity / Resume path (L135 onward)**:
- Update "Last session" timestamp
- Update "Stopped at" to milestone-closed
- Update "Resume path" to: `Milestone v3.1 closed 2026-05-22. Next action: `/gsd-new-milestone` (or pause).`

Stay in project root. Commit: `docs(state): close milestone v3.1 — STATE.md milestone-complete + Phase 5.5 metrics + Deferred row updates` with mandatory footer.
  </action>
  <verify>
    <automated>grep -c "^status: milestone-complete" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: milestone-complete" .planning/STATE.md` returns 1
    - `grep -c "100% (6/6 milestone phases complete" .planning/STATE.md` returns ≥ 1
    - `grep -c "MILESTONE v3.1 CLOSED" .planning/STATE.md` returns ≥ 1
    - `grep -c "5.5 Interface ADR.*COMPLETE 2026-05-22\|5.5 Interface ADR.*COMPLETE" .planning/STATE.md` returns ≥ 1 (Performance Metrics row added)
    - `grep -c "NEW-A3.*✅ CLOSED 2026-05-22 Phase 5.5" .planning/STATE.md` returns ≥ 1 (Deferred row updated)
    - `grep -c "NEW-E2.*✅ CLOSED 2026-05-22 Phase 5.5" .planning/STATE.md` returns ≥ 1 (Deferred row updated)
  </acceptance_criteria>
  <done>STATE.md frontmatter milestone-complete, 6/6 progress, Progress bar 100%, Performance Metrics has Phase 5.5 row, Deferred Items table updated; commit landed.</done>
</task>

<task type="auto">
  <name>Task 02.04: REQUIREMENTS.md Traceability table verify (mostly no edits)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — focus on §A-E checkbox lists L13-47 + Traceability table L82-106)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-PATTERNS.md §22
  </read_first>
  <action>
Read REQUIREMENTS.md end-to-end.

**Verify-first pass**: Confirm the Traceability table at L82-106 has all 23 REQ-IDs with a phase column filled. Per RESEARCH and direct file inspection, all 23 rows are already populated. No edit expected here.

**Checkbox edit pass**: Walk through §A (CLI 6 items L13-18), §B (SERVER 5 items L22-26), §C (CONTRACT 3 items L30-32), §D (WATCHLIST 8 items L36-43), §E (INTERFACE 1 item L47). For each REQ-ID:
- If the checkbox is `[ ]` and the REQ-ID's mapped phase is COMPLETE per ROADMAP §Progress (Task 02.02), flip to `[x]`.
- If already `[x]`, leave as-is.

Status snapshot (post-Plan 01 + ongoing Plan 02):
- CLI-01..06 → Phase 5.2 ✅ COMPLETE (flip if [ ])
- SERVER-01..05 → Phase 5.4 ✅ COMPLETE (flip if [ ])
- CONTRACT-00 → Phase 5.1 already [x]
- CONTRACT-01..02 → Phase 5.3 ✅ COMPLETE (flip if [ ])
- WATCH-00 → Phase 5.0 ✅ COMPLETE (flip if [ ])
- WATCH-01 → Phase 5.3 ✅ COMPLETE (flip if [ ])
- WATCH-02 → Phase 5.0 ✅ COMPLETE (flip if [ ])
- WATCH-03 → Phase 5.3 ✅ COMPLETE (flip if [ ])
- WATCH-04 → Phase 5.4 ✅ COMPLETE (flip if [ ])
- WATCH-05 → Phase 5.1 already [x]
- WATCH-06 → Phase 5.5 ✅ COMPLETE (flip after Plan 01 Task 01.A3 commit lands)
- WATCH-07 → Phase 5.4 ✅ COMPLETE (flip if [ ])
- INTERFACE-01 → Phase 5.5 ✅ COMPLETE (flip after Plan 01 Task 01.A1 commit lands)

Optional: Append a 1-line note at end-of-file: `*Milestone v3.1 closed 2026-05-22; all 23 REQ-IDs traced to completed phases.*`

Stay in project root. Commit: `docs(requirements): mark all 23 v3.1 REQ-IDs complete (milestone close)` with mandatory footer.
  </action>
  <verify>
    <automated>! grep -qE '^- \[ \] \*\*(CLI|SERVER|CONTRACT|WATCH|INTERFACE)-' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^- \[ \] \*\*(CLI|SERVER|CONTRACT|WATCH|INTERFACE)-' .planning/REQUIREMENTS.md` returns 0 (no unchecked REQ-IDs remain in §A-E)
    - `grep -cE '^- \[x\] \*\*(CLI|SERVER|CONTRACT|WATCH|INTERFACE)-' .planning/REQUIREMENTS.md` returns 23 (all 23 REQ-IDs flipped)
    - `grep -c "Total v3.1 requirements:.*23" .planning/REQUIREMENTS.md` returns ≥ 1 (Traceability footer matches)
    - Commit landed
  </acceptance_criteria>
  <done>All 23 REQ-IDs marked [x]; Traceability table verified intact; commit landed.</done>
</task>

<task type="auto">
  <name>Task 02.05: DEFERRED_LEDGER 18-row sweep — 14 VERIFY + 4 ADD + 1 EDIT</name>
  <files>docs/design/EAASP/DEFERRED_LEDGER.md</files>
  <read_first>
    - docs/design/EAASP/DEFERRED_LEDGER.md (large file — read full to locate all 18 target rows; specifically rows for D109, D120, D134, D136, D142 (L262), D143 (L263), NEW-A2 (L487), NEW-D2 (L377/450), NEW-E2 (NEW row), NEW-E3 (L488), NEW-E4 (L468), NEW-F1 (L469), NEW-F2 (L470), NEW-F3 (L471), NEW-F4 (L472 — EDIT row), NEW-A3 (NEW row), NEW-L1 (NEW row), grid-cli mod output (NEW row))
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-CONTEXT.md §D-09 (sweep scope)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-RESEARCH.md §2.6 (18-row state table)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-PATTERNS.md §18 (row-edit-on-close convention)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-01-SUMMARY.md (commit hashes for 4 new rows)
  </read_first>
  <action>
**Pre-step**: Baseline grep:
```bash
grep -c "✅ CLOSED" docs/design/EAASP/DEFERRED_LEDGER.md
# expect: 42 (verified 2026-05-22)
wc -l docs/design/EAASP/DEFERRED_LEDGER.md
# record baseline line count
```

**Block A — VERIFY 14 already-closed rows (no edit)**:
For each of the 14 IDs below, grep that `✅ CLOSED` text appears in the row. If missing, the row is stale — note for follow-up but do NOT fabricate a commit hash.
- D109, D120, D134, D136, D142, D143, NEW-A2, NEW-D2, NEW-E3, NEW-E4, NEW-F1, NEW-F2, NEW-F3
(plus any sibling rows discovered during read; minimum 13-14 ID grep)

Verification command:
```bash
for id in D109 D120 D134 D136 D142 D143 NEW-A2 NEW-D2 NEW-E3 NEW-E4 NEW-F1 NEW-F2 NEW-F3; do
  echo "$id: $(grep -c "\\*\\*$id\\*\\*.*✅ CLOSED" docs/design/EAASP/DEFERRED_LEDGER.md)"
done
```
Each ID should print `1` (one row matching). If any prints `0`, investigate but do not fabricate.

**Block B — ADD 4 new ✅ CLOSED rows**:
Append (in the natural section for each row) using exact format from existing rows (e.g., D142 at L262 as the template — pipe-delimited 4-column markdown table row):

Row 1 — NEW-E2:
```
| **NEW-E2** | F3 ADR enforcement.trace 29 (corrected to 33) missing items advisory | ✅ CLOSED 2026-05-22 Phase 5.5 Plan 01 @ `<TASK_01.A3_COMMIT_HASH>` | **Resolution**: F3 sweep across 9 ADRs — 5 contract ADRs (V2-006/018/020/027/028) trace anchors appended to .github/workflows/{phase3-contract,eval-ci,phase2_5-contract}.yml; 4 strategy ADRs (V2-001/002/003/005) annotated with `# F3-WARN expected: strategic ADR documentation anchor` rationale comment in frontmatter `enforcement:` block. Live F3 baseline 33 WARN → 13 explicit-strategic + 0 unjustified. ADR status / accepted_at fields UNCHANGED per MEMORY.md "ADR 状态二次校验" rule. |
```

Row 2 — NEW-A3:
```
| **NEW-A3** | `kill_session` in `commands/session.rs:117` returns `anyhow!` mapping to exit 1; should be GridError::SessionNotFound → exit 4 | ✅ CLOSED 2026-05-22 Phase 5.5 Plan 01 @ `<TASK_01.B1_COMMIT_HASH>` | **Resolution**: replaced `anyhow!("Session not found: ...")` with `GridError::session_not_found(session_id).into()` at session.rs:117; enhanced main.rs:92-96 to `downcast_ref::<GridError>` before falling back to `GridError::other(...)`, preserving the typed `SessionNotFound` variant through `?` propagation. Regression test `test_kill_nonexistent_session_exits_4` added to `crates/grid-cli/tests/cli_integration.rs`. `cargo test -p grid-cli` PASS. |
```

Row 3 — NEW-L1:
```
| **NEW-L1** | `tools/eaasp-l2-memory-engine/.../vector_index.py:189-192` unbounded `max_elements` doubling + meta.json schema gap (94GB disk leak observed 2026-05-20) | ✅ CLOSED 2026-05-22 Phase 5.5 Plan 01 @ `<TASK_01.B4_COMMIT_HASH>` | **Resolution**: introduced module-level `HNSW_HARD_CAP = 1_000_000` constant; capped growth to `min(self._max_elements * 2, HNSW_HARD_CAP)` with actionable `RuntimeError` when cap exceeded; added `"max_elements": self._max_elements` key to meta dict in `save()`; restored on reload via `meta.get("max_elements", self._max_elements)` in `_try_load_sync`. 2 regression tests added (`test_grow_hits_hard_cap` + `test_meta_json_max_elements_roundtrip`). `uv run pytest tools/eaasp-l2-memory-engine/tests/test_vector_index.py` PASS. Atomic write at L236 preserved (V14.1.5). |
```

Row 4 — grid-cli mod output E0583:
```
| **grid-cli mod output E0583** | Phase 5.3 reported compile error on `pub mod output;` (lib.rs:20); deferred to Phase 5.4 → 5.5 | ✅ CLOSED 2026-05-22 Phase 5.5 (verification-only) | **Resolution**: Both `cargo build -p grid-cli` and `cargo build -p grid-cli --features studio` verified clean 2026-05-22 (exit 0, no E0583). Root cause unknown — resolved by an earlier commit between Phase 5.3 report and Phase 5.5 verification. Note: `cargo build -p grid-cli --all-features` still fails with 12 unrelated grid-engine errors (E0596/E0412/E0425/E0433); filed as deferred row NEW-X3 below. |
```

**Block C — EDIT 1 row (NEW-F4 at L472)**:
Find the NEW-F4 row (currently prefixed "Re-targeted to Phase 5.5 ..."). Replace its status cell content from `🟡 P2 | Re-targeted to Phase 5.5 ...` (or similar) to:
```
✅ CLOSED 2026-05-22 Phase 5.5 Plan 01 @ `<TASK_01.A2_COMMIT_HASH>` + `<TASK_01.B2_COMMIT_HASH>`
```
And append to its Resolution column: `ADR-V2-032 (TUI Log Path Convention) Accepted Phase 5.5 Task 01.A2; dead `dirs::data_local_dir()` branch removed from `crates/grid-cli/src/studio_main.rs::resolve_tui_log_path` L57-60 Phase 5.5 Task 01.B2; `cargo build -p grid-cli --features studio` clean. `dirs` Cargo dep retained (still used by `path_shortener.rs:20,127`).`

**Optional Block D — File new deferred rows for follow-up** (per RESEARCH §Anti-Patterns + R2):
- NEW-X2: `commands/session.rs:99-103 delete_session + L157 export_session` use the same `anyhow!("Session not found: ...")` anti-pattern as NEW-A3. P3, defer to v3.2+.
- NEW-X3: `cargo build -p grid-cli --all-features` fails with 12 errors in grid-engine (E0596 borrow-checker + E0412/E0425/E0433 in hooks module). Pre-existing as of 2026-05-22; out of Phase 5.5 scope. P3, defer to v3.2+.

**Anti-pattern**: Do NOT bulk-delete-and-readd rows. Use per-row Edit / per-row Append only. Per RESEARCH §R6, `git diff --stat docs/design/EAASP/DEFERRED_LEDGER.md` after this task should show additions only, not deletions of existing rows.

Verification:
```bash
grep -c "✅ CLOSED" docs/design/EAASP/DEFERRED_LEDGER.md
# expect: 46 (baseline 42 + 4 new)
grep -c "Re-targeted to Phase 5.5" docs/design/EAASP/DEFERRED_LEDGER.md
# expect: 0 (NEW-F4 row flipped)
```

Stay in project root. Commit: `docs(ledger): close NEW-E2 + NEW-A3 + NEW-L1 + grid-cli mod-output + flip NEW-F4 (Phase 5.5 milestone close sweep)` with mandatory footer.
  </action>
  <verify>
    <automated>grep -c "✅ CLOSED" docs/design/EAASP/DEFERRED_LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "✅ CLOSED" docs/design/EAASP/DEFERRED_LEDGER.md` returns 46 (baseline 42 + 4 new closures)
    - `grep -c "Re-targeted to Phase 5.5" docs/design/EAASP/DEFERRED_LEDGER.md` returns 0 (NEW-F4 row flipped)
    - `grep -c "NEW-E2.*✅ CLOSED 2026-05-22 Phase 5.5" docs/design/EAASP/DEFERRED_LEDGER.md` returns ≥ 1
    - `grep -c "NEW-A3.*✅ CLOSED 2026-05-22 Phase 5.5" docs/design/EAASP/DEFERRED_LEDGER.md` returns ≥ 1
    - `grep -c "NEW-L1.*✅ CLOSED 2026-05-22 Phase 5.5" docs/design/EAASP/DEFERRED_LEDGER.md` returns ≥ 1
    - `grep -c "grid-cli mod output\|mod output E0583.*✅ CLOSED" docs/design/EAASP/DEFERRED_LEDGER.md` returns ≥ 1
    - `grep -c "NEW-F4.*✅ CLOSED 2026-05-22 Phase 5.5" docs/design/EAASP/DEFERRED_LEDGER.md` returns ≥ 1
    - `git diff --stat docs/design/EAASP/DEFERRED_LEDGER.md` shows mostly additions; no large deletions (Pattern D — no bulk delete-and-readd)
  </acceptance_criteria>
  <done>4 new ✅ CLOSED rows + 1 NEW-F4 row flip landed; 14 already-closed rows verified intact; LEDGER ✅ count = 46; commit landed.</done>
</task>

<task type="auto">
  <name>Task 02.06: Phase gate — ADR lint + targeted Rust + Python tests per CONTEXT D-10 (NO --workspace tests)</name>
  <files></files>
  <read_first>
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-CONTEXT.md §D-10 (phase gate spec)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-VALIDATION.md §Sampling Rate (phase gate commands)
    - .planning/phases/05.5-interface-adr-milestone-close/05.5-RESEARCH.md §Validation Architecture §Phase Requirements → Test Map
    - CLAUDE.md §Test Execution + MEMORY.md `feedback_no_full_tests`
  </read_first>
  <action>
Run the phase gate sequence per CONTEXT D-10. All commands target-scoped — **NEVER `cargo test --workspace`**, **NEVER `make test`**.

Execute from project root (use absolute paths, never `cd subdir`):

```bash
# 1. ADR lint — expect 0 FAIL, F3 WARN ≤ 13
python3 .adr-plugin/scripts/adr_lint.py 2>&1 | tee /tmp/phase55-gate-adr-lint.log | tail -3
ADR_F3_WARN=$(grep -c "^\[F3 WARN\]" /tmp/phase55-gate-adr-lint.log)
ADR_FAIL=$(grep -c "^\[F[12345] FAIL\]" /tmp/phase55-gate-adr-lint.log)
echo "ADR lint: ${ADR_FAIL} FAIL / ${ADR_F3_WARN} F3 WARN"

# 2. Targeted Rust tests (default features) — expect 147+ PASS
cargo test -p grid-cli 2>&1 | tee /tmp/phase55-gate-grid-cli.log | tail -3

# 3. Studio feature build — expect exit 0
cargo build -p grid-cli --features studio 2>&1 | tail -2

# 4. Workspace sanity check — expect exit 0
cargo check --workspace 2>&1 | tail -2

# 5. Python L2 vector_index regression — expect all PASS
uv run pytest tools/eaasp-l2-memory-engine/tests/test_vector_index.py -x 2>&1 | tee /tmp/phase55-gate-vector-index.log | tail -3
```

If any command fails (non-zero exit), STOP and investigate. Do not paper over failures.

If all PASS, record the gate output (counts + ADR_F3_WARN final value + test counts) in the Plan 02 SUMMARY (Task 02.07 implicit — SUMMARY authored via /gsd-end-phase or similar workflow).

Verification grep against all the close-cascade docs (cross-check Task 02.01-05 work landed):
```bash
# Cross-doc grep gates per RESEARCH §Validation Architecture
# Each line MUST exit 0 to be a true gate. Use ! grep -q inversion for expect-0 cases;
# wrap count-comparison cases in a [ "$count" = N ] check.
[ "$(grep -c '^status: milestone-complete' .planning/STATE.md)" = "1" ] || { echo "FAIL: STATE.md milestone-complete count != 1"; exit 1; }
grep -q "✅.*v3.1.*SHIPPED" .planning/ROADMAP.md || { echo "FAIL: ROADMAP missing ✅ v3.1 SHIPPED"; exit 1; }
! grep -qE '^- \[ \] \*\*(CLI|SERVER|CONTRACT|WATCH|INTERFACE)-' .planning/REQUIREMENTS.md || { echo "FAIL: REQUIREMENTS still has unchecked REQ-IDs"; exit 1; }
grep -q "V2-029" .planning/PROJECT.md || { echo "FAIL: PROJECT.md missing V2-029"; exit 1; }
[ "$(grep -c '✅ CLOSED' docs/design/EAASP/DEFERRED_LEDGER.md)" = "46" ] || { echo "FAIL: LEDGER ✅ CLOSED count != 46"; exit 1; }
```

If ALL of the above gates PASS (each command returns the expected count), the Phase 5.5 phase gate is GREEN.

If any cross-doc gate fails, the corresponding Task 02.0N has incomplete work — return to that task before declaring gate PASS.

**No commit for this task** (verification-only — no source change). Phase 5.5 verification artifact will be written as `.planning/phases/05.5-interface-adr-milestone-close/05.5-VERIFICATION.md` by the verifier subagent (or end-phase workflow), not by this task.

Stay in project root throughout.
  </action>
  <verify>
    <automated>! ( python3 .adr-plugin/scripts/adr_lint.py 2>&1 | grep -qE "^\[F[12345] FAIL\]" )</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .adr-plugin/scripts/adr_lint.py 2>&1 | grep -cE "^\[F[12345] FAIL\]"` returns 0
    - `python3 .adr-plugin/scripts/adr_lint.py 2>&1 | grep -c "^\[F3 WARN\]"` returns ≤ 13
    - `cargo test -p grid-cli` exits 0
    - `cargo build -p grid-cli --features studio` exits 0
    - `cargo check --workspace` exits 0
    - `uv run pytest tools/eaasp-l2-memory-engine/tests/test_vector_index.py -x` exits 0
    - Cross-doc gates ALL pass: `grep -c "^status: milestone-complete" .planning/STATE.md` returns 1; `grep -c "✅.*v3.1.*SHIPPED" .planning/ROADMAP.md` returns ≥ 1; `grep -cE '^- \[ \] \*\*(CLI|SERVER|CONTRACT|WATCH|INTERFACE)-' .planning/REQUIREMENTS.md` returns 0; `grep -c "✅ CLOSED" docs/design/EAASP/DEFERRED_LEDGER.md` returns 46
    - `cargo test --workspace` is NEVER invoked (verifiable by absence in commit history / shell history)
  </acceptance_criteria>
  <done>Phase gate GREEN — ADR lint clean, targeted tests PASS, cross-doc grep gates PASS; milestone v3.1 ready for /gsd-verify-work and SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LEDGER row Resolution → user reader | LEDGER text becomes future authoritative record of what shipped |
| STATE.md frontmatter → automation | gsd-sdk + future orchestrators read `status: milestone-complete` to determine entry point |
| ROADMAP.md milestone line → next-milestone planner | Stale 🚧 marker would cause /gsd-new-milestone to misroute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-07 | Tampering | LEDGER bulk delete-and-readd corrupts audit trail | mitigate | Per-row Edit only (Task 02.05); acceptance criteria includes `git diff --stat` shows mostly additions |
| T-05.5-08 | Repudiation | Missing commit hash in LEDGER Resolution makes "what shipped" un-traceable | mitigate | Task 02.05 mandates commit hash inline in each new row (from Plan 01 SUMMARY) |
| T-05.5-09 | Information Disclosure | LEDGER Resolution text includes file paths | accept | All paths are already public repo paths; no secrets |
| T-05.5-10 | DoS (orchestration) | STATE.md frontmatter mismatch with progress numbers causes /gsd-new-milestone to fail | mitigate | Task 02.03 acceptance criteria validates frontmatter via grep before commit |
</threat_model>

<verification>
After all 6 tasks complete, the phase gate (Task 02.06) is the ultimate verification.

Cross-doc grep gates (re-checkable post-completion):
```bash
grep -c "^status: milestone-complete" .planning/STATE.md                                  # 1
grep -c "✅.*v3.1.*SHIPPED" .planning/ROADMAP.md                                          # ≥ 1
grep -c "🚧.*v3.1" .planning/ROADMAP.md                                                   # 0
grep -cE '^- \[ \] \*\*Phase 5\.' .planning/ROADMAP.md                                    # 0
grep -cE '^- \[ \] \*\*(CLI|SERVER|CONTRACT|WATCH|INTERFACE)-' .planning/REQUIREMENTS.md  # 0
grep -c "V2-029" .planning/PROJECT.md                                                     # ≥ 1
grep -c "V2-032" .planning/PROJECT.md                                                     # ≥ 1
grep -c "✅ CLOSED" docs/design/EAASP/DEFERRED_LEDGER.md                                  # 46
grep -c "Re-targeted to Phase 5.5" docs/design/EAASP/DEFERRED_LEDGER.md                   # 0
python3 .adr-plugin/scripts/adr_lint.py 2>&1 | grep -cE "^\[F[12345] FAIL\]"              # 0
python3 .adr-plugin/scripts/adr_lint.py 2>&1 | grep -c "^\[F3 WARN\]"                     # ≤ 13
```

DO NOT run `cargo test --workspace` (D-10 + CLAUDE.md + MEMORY.md feedback_no_full_tests).
</verification>

<success_criteria>
- PROJECT.md no longer carries Phase 5 milestone (v3.1) as in-flight; ADR-V2-029 + V2-032 referenced
- ROADMAP.md milestone line `✅ v3.1 Phase 5 SHIPPED 2026-05-22`; all 6 Phase 5 entries `[x]`; SC#3 wording revised (33 baseline + 13 explicit-strategic)
- STATE.md frontmatter `status: milestone-complete`, 6/6 = 100%; Progress bar 100%; Performance Metrics has Phase 5.5 row; Deferred NEW-A3 + NEW-E2 rows flipped to ✅
- REQUIREMENTS.md all 23 REQ-IDs `[x]` in §A-E
- DEFERRED_LEDGER ✅ CLOSED count grows from 42 → 46; NEW-F4 row flipped from "Re-targeted" → ✅; row-edit-on-close convention preserved (per-row Edit, no bulk delete-and-readd)
- Phase gate per D-10 PASSes: ADR lint 0 FAIL + F3 WARN ≤ 13 + cargo test -p grid-cli + studio build + workspace check + pytest test_vector_index
- All commits include mandatory footer
- `cargo test --workspace` is NEVER invoked
- Local commits ahead of origin grows by 5-7 (Plan 02 commits); user pushes at their own discretion (per project rule)
</success_criteria>

<output>
After completion, create `.planning/phases/05.5-interface-adr-milestone-close/05.5-02-SUMMARY.md` capturing:
- Each task's outcome (commit hash + brief result line)
- Final F3 WARN count (vs. 33 baseline)
- Final LEDGER ✅ count (vs. 42 baseline)
- Cross-doc grep gate results
- Phase gate command outputs (truncated tails)
- Total Plan 02 commits landed
- Confirmation that milestone v3.1 is fully closed and `/gsd-verify-work` can run
- Any deviations from this plan with rationale
- Resume guidance for next session: `Milestone v3.1 closed 2026-05-22; ready for /gsd-new-milestone (v3.2 TBD) — or pause.`
</output>
