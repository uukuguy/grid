---
phase: 03.7.2-web-production
plan: 02
type: execute
wave: 2
depends_on:
  - 3.7.2-01
files_modified:
  - web/src/components/SessionBar.tsx
  - web/src/components/SessionControls.tsx
  - web/src/components/Toast.tsx
  - web/src/components/layout/AppLayout.tsx
  - web/src/atoms/session.ts
  - web/src/atoms/ui.ts
  - web/src/ws/types.ts
  - web/src/ws/manager.ts
  - web/src/ws/events.ts
  - web/src/pages/Tasks.tsx
  - web/src/pages/Memory.tsx
  - web/src/pages/Debug.tsx
  - web/src/globals.css
  - web/src/test/atoms.test.ts
  - web/src/test/session-bar.test.tsx
  - web/src/test/ws-reconnect.test.ts
  - web/src/test/memory-toast.test.tsx
  - web/playwright.config.ts
  - web/e2e/S7-stop-resume.spec.ts
  - web/e2e/S7-memory-toast.spec.ts
  - web/e2e/S7-ws-reconnect.spec.ts
  - docs/design/web-ui-tokens.md
  - docs/cli/scenarios/S7-web-dashboard.md
  - docs/cli/QUICKSTART.md
  - docs/status/WEB_PRODUCTION_USABILITY_<date>.md
autonomous: true
requirements:
  - REQ-WEB-01
  - REQ-WEB-02
  - REQ-WEB-03
  - REQ-WEB-04
  - REQ-WEB-05
  - REQ-WEB-06
  - REQ-WEB-07
  - REQ-WEB-08
  - REQ-WEB-09
user_setup: []

must_haves:
  truths:
    - "A non-developer can open any dashboard tab, see the active agent's running state and tool-call activity through visible UI, and does not need browser devtools."
    - "When an agent writes memory, a dismissible cyan `Memory written` toast appears within 1 second and the Memory page shows the new item with a temporary highlight and `Live` badge."
    - "When an agent is stopped, the Stop control immediately disables, the UI reaches stopped state within 2 seconds, and Resume calls the exact resume endpoint and returns the UI to running."
    - "When the server is killed during streaming, the WebSocket retries at most five times with documented backoff, includes the same `session_id`, and resumes without a page reload when the server returns."
    - "Every ServerMessage carries optional `seq`; debug mode enabled by `?debug=1` reports sequence gaps without changing normal event ordering."
    - "Vitest and Playwright tests are hermetic, use no live LLM key, and cover Stop/Resume, memory toast, reconnect attempts, and the three S7 UAT flows."
    - "The UI uses the approved design contract: existing tokens, `cn()`, Lucide icons, component padding at least `px-2 py-1`, buttons `font-normal`, action-specific labels, visible focus, reduced-motion support, and 44px mobile touch targets."
    - "D-09 quality bar: `gsd-ui-auditor` reports at least 8.5/10 across the six pillars (consistency, hierarchy, readability, accessibility, responsiveness, delight), and the dated status record includes the actual auditor score with command evidence."
  artifacts:
    - path: "web/src/components/SessionControls.tsx"
      provides: "Always-mounted Stop/Resume controls and three-state live indicator"
      contains: "api/v1/sessions"
    - path: "web/src/atoms/session.ts"
      provides: "sessionStatusAtom, stoppedByUserAtom, and recentlyAddedMemoryIdsAtom"
      contains: "sessionStatusAtom"
    - path: "web/src/ws/types.ts"
      provides: "Backward-compatible optional seq field on ServerMessage"
      contains: "seq"
    - path: "web/src/ws/events.ts"
      provides: "memory_added dispatch to toast and Memory live-update state"
      contains: "memory_added"
    - path: "web/src/test/session-bar.test.tsx"
      provides: "Stop/Resume state transition tests"
      min_lines: 40
    - path: "web/src/test/ws-reconnect.test.ts"
      provides: "Five-attempt reconnect/backoff tests"
      min_lines: 35
    - path: "web/src/test/memory-toast.test.tsx"
      provides: "Memory toast emission/render test"
      min_lines: 30
    - path: "web/playwright.config.ts"
      provides: "Hermetic Playwright project configuration when no existing config is present"
      contains: "defineConfig"
    - path: "web/e2e/S7-stop-resume.spec.ts"
      provides: "E1 dashboard Stop flow"
      contains: "Stop"
    - path: "web/e2e/S7-memory-toast.spec.ts"
      provides: "E2 memory toast flow"
      contains: "Memory written"
    - path: "web/e2e/S7-ws-reconnect.spec.ts"
      provides: "E3 WebSocket crash/restart flow"
      contains: "session_id"
    - path: "docs/design/web-ui-tokens.md"
      provides: "Approved UI-SPEC color, typography, and spacing token reference"
      contains: "#3b82f6"
    - path: "docs/cli/scenarios/S7-web-dashboard.md"
      provides: "Non-developer S7 dashboard walkthrough"
      contains: "Stop"
    - path: "docs/status/WEB_PRODUCTION_USABILITY_<date>.md"
      provides: "Dated self-recorded S3 walkthrough record and acceptance evidence"
      contains: "S3"
  key_links:
    - from: "web/src/components/layout/AppLayout.tsx"
      to: "web/src/components/SessionControls.tsx"
      via: "always-mounted sibling outside activeTab === chat branch"
      pattern: "SessionControls"
    - from: "web/src/components/SessionControls.tsx"
      to: "web/src/atoms/session.ts"
      via: "sessionStatusAtom/isStreamingAtom and stoppedByUserAtom"
      pattern: "sessionStatusAtom|isStreamingAtom"
    - from: "web/src/ws/events.ts"
      to: "web/src/atoms/ui.ts"
      via: "memory_added -> pushMemoryEventAtom/addToastAtom"
      pattern: "memory_added.*addToastAtom|pushMemoryEventAtom"
    - from: "web/src/ws/manager.ts"
      to: "web/src/ws/types.ts"
      via: "bounded reconnect retains session_id and sequence state"
      pattern: "session_id|reconnect"
    - from: "web/e2e/S7-stop-resume.spec.ts"
      to: "/api/v1/sessions/:id/kill"
      via: "route interception/asserted POST"
      pattern: "sessions/.*/kill"
---

<objective>
Close every REQ-WEB item from `docs/audit/3.7.2-GAP-AUDIT.md`, ship the approved SessionControls/memory visibility/reconnect UX, add hermetic Vitest and Playwright coverage, and produce the S7 walkthrough plus dated usability record. Implement locked decisions D-02 through D-09 exactly; do not create a new monitor page or modify server/shared-core code.

Purpose: turn `web/` from Activation-quality code into a dashboard a non-developer can use to observe and control a real agent without devtools.
Output: production web changes, targeted tests, Playwright E1-E3, design-token documentation, S7 scenario documentation, and dated walkthrough evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.7.2-web-production/3.7.2-CONTEXT.md
@.planning/phases/03.7.2-web-production/03.7.2-UI-SPEC.md
@.planning/phases/03.7.2-web-production/3.7.2-01-SUMMARY.md
@docs/audit/3.7.2-GAP-AUDIT.md
@docs/cli/QUICKSTART.md
@docs/cli/scenarios/S1-multi-step-tool-use.md
@docs/status/PRODUCTION_USABILITY_2026-07-19.md
@web/src/components/SessionBar.tsx
@web/src/components/Toast.tsx
@web/src/components/layout/AppLayout.tsx
@web/src/atoms/session.ts
@web/src/atoms/ui.ts
@web/src/ws/types.ts
@web/src/ws/manager.ts
@web/src/ws/events.ts
@web/src/pages/Tasks.tsx
@web/src/pages/Memory.tsx
@web/src/pages/Debug.tsx
@web/src/test/atoms.test.ts
@web/package.json
@web/vite.config.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define state and WebSocket contracts with failing unit tests</name>
  <read_first>
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/audit/3.7.2-GAP-AUDIT.md (REQ-WEB backlog and evidence; implement only listed gaps)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.2-web-production/3.7.2-CONTEXT.md (D-02, D-03, D-04, D-05, D-08)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.2-web-production/03.7.2-UI-SPEC.md (Sections 9.1, 9.2, 10.3, 12, 13)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/atoms/session.ts
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/atoms/ui.ts
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/ws/types.ts
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/ws/manager.ts
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/ws/events.ts
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/test/atoms.test.ts
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/package.json
  </read_first>
  <files>
    - web/src/atoms/session.ts
    - web/src/atoms/ui.ts
    - web/src/ws/types.ts
    - web/src/ws/manager.ts
    - web/src/ws/events.ts
    - web/src/test/atoms.test.ts
    - web/src/test/ws-reconnect.test.ts
    - web/src/test/memory-toast.test.tsx
  </files>
  <behavior>
    - Test 1: `sessionStatusAtom` is `running` when `isStreamingAtom` is true or a tool execution has status `running`.
    - Test 2: Stop writes `stoppedByUserAtom=true`; Resume/new session clears it; stopped status exposes Resume rather than Stop.
    - Test 3: `memory_added` creates a memory toast with type `memory`, title `Memory written`, and content truncated to 60 characters.
    - Test 4: reconnect attempts are numbered 1 through 5, use the same `session_id`, and attempt 6 does not schedule another socket.
    - Test 5: optional `seq` is accepted without changing existing discriminated-union event handling.
  </behavior>
  <action>
    Add the state/event contracts before component wiring. In `web/src/atoms/session.ts`, export `stoppedByUserAtom = atom<boolean>(false)`, `sessionStatusAtom = atom<'running' | 'paused' | 'stopped' | 'idle'>((get) => { ... })` using `isStreamingAtom` and `toolExecutionsAtom`, and `recentlyAddedMemoryIdsAtom` with a write path that adds an ID and removes it after 4500ms. In `web/src/atoms/ui.ts`, export write-only `pushMemoryEventAtom` that calls the existing `addToastAtom` with `{ type: 'memory', title: 'Memory written', message: 'Stored: ' + truncate(content, 60) + '…' }`. In `web/src/ws/types.ts`, add `seq?: number` to the ServerMessage base/common shape (TypeScript number represents the JSON u64) without removing or renaming existing fields.

    In `web/src/ws/events.ts`, add a `memory_added` case that passes the event content/id to `pushMemoryEventAtom` and the recent-memory atom while preserving current ordering and all existing cases. In `web/src/ws/manager.ts`, expose or keep a testable reconnect scheduling function with exact behavior `maxReconnectAttempts = 5`, exponential delays `1000, 2000, 4000, 8000, 16000` milliseconds (cap at 30000ms), and URL query `session_id=<same id>` on each attempt; do not buffer or reorder messages. Create the three tests with deterministic fake timers and mocked WebSocket/fetch. Run the new tests once and confirm RED before implementation, then implement the contracts and run them GREEN. Do not add packages or live LLM calls.
  </action>
  <verify>
    <automated>cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web && npm run test -- --run src/test/atoms.test.ts src/test/ws-reconnect.test.ts src/test/memory-toast.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'sessionStatusAtom' web/src/atoms/session.ts`, `grep -q 'pushMemoryEventAtom' web/src/atoms/ui.ts`, and `grep -q 'seq' web/src/ws/types.ts` all succeed.
    - The targeted Vitest command passes with the pre-existing atom tests plus the new reconnect and memory tests.
    - Reconnect tests prove exactly five scheduled attempts and no sixth attempt, not merely that a reconnect function exists.
    - The event handler preserves existing event order and has a concrete `memory_added` branch.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement global SessionControls, task actions, memory UX, and UI tokens</name>
  <read_first>
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.2-web-production/3.7.2-01-SUMMARY.md
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/audit/3.7.2-GAP-AUDIT.md (all REQ-WEB items assigned to component/UI implementation)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.2-web-production/03.7.2-UI-SPEC.md (Sections 3, 5-15, and 18; this is the binding visual contract)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/components/SessionBar.tsx
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/components/Toast.tsx
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/components/layout/AppLayout.tsx
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/atoms/session.ts
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/atoms/ui.ts
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/pages/Tasks.tsx
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/pages/Memory.tsx
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/pages/Debug.tsx
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/globals.css
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/src/lib/utils.ts
  </read_first>
  <files>
    - web/src/components/SessionBar.tsx
    - web/src/components/SessionControls.tsx
    - web/src/components/Toast.tsx
    - web/src/components/layout/AppLayout.tsx
    - web/src/pages/Tasks.tsx
    - web/src/pages/Memory.tsx
    - web/src/pages/Debug.tsx
    - web/src/globals.css
    - docs/design/web-ui-tokens.md
    - web/src/test/session-bar.test.tsx
  </files>
  <behavior>
    - Test 1: running state renders a visible Stop button, an accessible live indicator with `bg-primary animate-pulse`, and no Resume button.
    - Test 2: stopped/paused state renders Resume and no Stop; clicking Resume POSTs `/api/v1/sessions/:id/resume`; clicking Stop POSTs `/api/v1/sessions/:id/kill` and immediately disables the clicked control.
    - Test 3: failed stop/resume restores the prior state and emits the exact failure toast text from UI-SPEC §11.2.
    - Test 4: controls remain mounted while active tab is Tasks, Memory, Debug, or another non-Chat page.
  </behavior>
  <action>
    Create `web/src/components/SessionControls.tsx` and mount it unconditionally in `web/src/components/layout/AppLayout.tsx`, outside any `activeTab === 'chat'` conditional. Keep SessionBar's existing session pills and ConnectionStatus; do not create a second global monitor page. Use the active session ID from existing atoms and exact fetch targets `POST /api/v1/sessions/${sessionId}/kill` and `POST /api/v1/sessions/${sessionId}/resume`. Implement `handleStop()` and `handleResume()` with immediate in-flight disabled state, optimistic atom transitions, rollback on non-2xx/fetch failure, and the exact error messages `Failed to stop session. Try again.` and `Failed to resume session. Try again.`. Register `Cmd+.` on macOS / `Ctrl+.` elsewhere using a window keydown listener that calls the same stop handler only while status is `running`.

    Apply UI-SPEC exact classes: Stop default `bg-secondary text-foreground border border-border rounded-md px-2 py-1 text-xs font-normal`; Stop hover `bg-destructive/10 text-destructive border-destructive/30`; Stop active `scale-95`; Resume `bg-primary text-primary-foreground rounded-md px-2 py-1 text-xs font-normal`; all focus rings `ring-2 ring-primary ring-offset-2 ring-offset-background`; in-flight `opacity-50 cursor-wait`; icons `Square` and `Play` from `lucide-react` at `h-3.5 w-3.5 fill-current`; labels exactly `Stop` and `Resume`; mobile wrappers `min-h-11 min-w-11`. Use `cn()` for conditional classes, `aria-label` with truncated session ID, `aria-busy`, and `aria-disabled`. Render live dot states exactly `bg-primary animate-pulse`, `bg-emerald-500`, and `bg-red-500`, each paired with visible text/accessible label.

    Extend `Toast.tsx` with only the `memory` type: Database icon, `text-cyan-400`, `border-cyan-600`, `bg-cyan-950/80`, `text-cyan-200`, `w-80`, `right-4 top-4`, 4000ms duration, `role='alert'`, and `aria-live='polite'`; preserve all existing variants. In Tasks, add icon-only Stop/Resume actions in row/detail action areas with exact `h-6 w-6`, `Square`/`Play`, `aria-label`, `font-normal`, `px-2 py-1` wrappers where used, and replace any generic Submit label with `Send task`; verify endpoint availability from the audit before selecting cancel/resume. In Memory, add the exact `Live` badge markup from UI-SPEC, highlight recently-added IDs with `bg-cyan-950/30`, and the specified Database empty state. In Debug, only when `new URLSearchParams(location.search).get('debug') === '1'`, log a concrete `seq` gap such as `WS sequence gap: expected ${previous + 1}, received ${current}`; normal mode must not log it. Add the UI-SPEC reduced-motion `@layer base` media block to `web/src/globals.css`. Create `docs/design/web-ui-tokens.md` documenting the exact existing palette (`#09090b`, `#27272a`, `#3b82f6`, semantic colors), typography (`text-sm/text-xs/text-lg/text-2xl`, weights 400/600), and spacing (`p-1/p-2/p-4/p-6/p-8`, component minimum `px-2 py-1`) without adding new CSS tokens or dependencies.
  </action>
  <verify>
    <automated>cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web && npm run test -- --run src/test/session-bar.test.tsx src/test/atoms.test.ts && npm run build && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - SessionControls is imported and mounted outside the Chat-only branch, and component tests pass for running, stopped/paused, click, rollback, and non-Chat visibility.
    - Exact kill/resume endpoint strings occur in the component and are asserted by tests.
    - `Toast.tsx` contains a `memory` variant with `Database`, `bg-cyan-950/80`, `border-cyan-600`, and 4000ms behavior.
    - Changed interactive controls use `font-normal`, minimum `px-2 py-1`, exact action labels, Lucide icons, accessible names, and focus/reduced-motion classes from UI-SPEC.
    - `npm run build` and `npx tsc -b` pass without modifying `web/package.json` dependencies.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Initialize hermetic Playwright E1-E3 and write S7 scenario documentation</name>
  <read_first>
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/audit/3.7.2-GAP-AUDIT.md (REQ-WEB backlog and Playwright finding)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.2-web-production/3.7.2-CONTEXT.md (D-03, D-05, D-07, D-09)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.2-web-production/03.7.2-UI-SPEC.md (Sections 12-15)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/package.json
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/vite.config.ts
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web/.playwright-cli/
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/cli/QUICKSTART.md
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/cli/scenarios/S1-multi-step-tool-use.md
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/status/PRODUCTION_USABILITY_2026-07-19.md
  </read_first>
  <files>
    - web/playwright.config.ts
    - web/e2e/S7-stop-resume.spec.ts
    - web/e2e/S7-memory-toast.spec.ts
    - web/e2e/S7-ws-reconnect.spec.ts
    - docs/cli/scenarios/S7-web-dashboard.md
    - docs/cli/QUICKSTART.md
    - docs/status/WEB_PRODUCTION_USABILITY_<date>.md
  </files>
  <action>
    If no `web/playwright.config.ts` exists, create it with `defineConfig`, `testDir: './e2e'`, baseURL from `WEB_BASE_URL` defaulting to `http://127.0.0.1:5180`, `use: { trace: 'retain-on-failure', video: 'retain-on-failure' }`, and no live LLM dependency. Do not add packages; use the installed Playwright package if present. Use route interception and deterministic WebSocket/server test doubles where possible.

    Create E1 `web/e2e/S7-stop-resume.spec.ts`: route a running session/task fixture, open the dashboard, navigate Tasks, assert visible `Stop`, click it, assert a POST to `/api/v1/sessions/<id>/kill`, assert stopped state and visible `Resume`, click Resume, assert POST `/api/v1/sessions/<id>/resume` and running state. Create E2 `web/e2e/S7-memory-toast.spec.ts`: inject/broadcast a `memory_added` fixture, assert `Memory written`, `Stored:` content, dismissibility, and Memory page live/highlight affordance. Create E3 `web/e2e/S7-ws-reconnect.spec.ts`: close the mocked socket during active streaming, restart the mocked endpoint, assert reconnect within 30 seconds, exact retained `session_id`, and sequence-gap debug visibility only with `?debug=1`. Tests must fail with a clear prerequisite message rather than silently skip if the dashboard server is unavailable.

    Write `docs/cli/scenarios/S7-web-dashboard.md` in the S1-S6 scenario style with prerequisites, exact commands, expected visible behavior, stop/resume steps, memory-write steps, reconnect steps, and no-devtools acceptance. Add an S7 row linking this file to `docs/cli/QUICKSTART.md` without rewriting unrelated scenario rows. Run `date +%Y-%m-%d`, then create `docs/status/WEB_PRODUCTION_USABILITY_<date>.md` using the real date. Record the three Playwright results, the non-developer S3 hook-governance walkthrough steps, timestamps/placeholders for the actual screen recording under `.planning/phases/03.7.2-web-production/walkthrough/`, and all 14 acceptance criteria as PASS/FAIL with command evidence. Do not fabricate a PASS for a manual recording that was not actually performed; if runtime credentials/server are unavailable, mark that item BLOCKED with the exact prerequisite and retain the test evidence.
  </action>
  <verify>
    <automated>cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web && npx playwright test --config=playwright.config.ts e2e/S7-stop-resume.spec.ts e2e/S7-memory-toast.spec.ts e2e/S7-ws-reconnect.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `web/playwright.config.ts` exists if and only if the audit found no existing config, and Playwright discovers exactly the three S7 specs.
    - E1 asserts both exact session POST endpoints and visible state transitions; E2 asserts memory toast plus Memory page update; E3 asserts bounded reconnect, same `session_id`, and 30-second recovery criterion.
    - `docs/cli/QUICKSTART.md` contains an S7 link and `S7-web-dashboard.md` contains prerequisites, exact commands, expected output, and no-devtools acceptance.
    - The dated status filename uses the actual `date +%Y-%m-%d` value; its evidence distinguishes automated PASS from manual BLOCKED/PASS and never claims an unperformed recording.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Run phase verification, UI quality gates, and commit web closure</name>
  <read_first>
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/audit/3.7.2-GAP-AUDIT.md
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.2-web-production/3.7.2-CONTEXT.md (Acceptance Standard 1-14)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/.planning/phases/03.7.2-web-production/03.7.2-UI-SPEC.md (Sections 15 and 18)
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/design/web-ui-tokens.md
    - /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/docs/status/WEB_PRODUCTION_USABILITY_<date>.md
  </read_first>
  <files>all files listed in this plan's `files_modified` frontmatter that were actually changed</files>
  <action>
    Run only targeted verification: `npm run test`, `npm run build`, `npx tsc -b`, the three-spec Playwright command, and `cargo check --workspace` as the required cross-language regression gate. Run `gsd-ui-checker` against the changed surfaces and then `gsd-ui-auditor` retroactively against all six pillars; record the auditor score and require at least 8.5/10. Before committing, verify no new dependencies were added to `web/package.json`, no new `@theme` color tokens were added, no changed component uses padding below `px-2 py-1`, every changed button has `font-normal`, and `grep -v '^#'` checks do not count comments as evidence. Verify no files under `crates/`, `lang/`, `tools/`, `proto/`, `web-platform/`, `grid-server/`, or `grid-desktop/` changed. Update the dated status record with actual command outputs and UI checker/auditor results. Stage only the Phase 3.7.2 files and commit with subject `feat(web): Phase 3.7.2 production usability closure`, ending with `Generated-By: Claude (claude-opus-4-8) via Claude Code CLI` and `Co-Authored-By: claude-flow <ruv@ruv.net>`.
  </action>
  <verify>
    <automated>cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox/web && npm run test && npm run build && npx tsc -b && cd /Users/sujiangwen/sandbox/LLM/speechless.ai/SGAI/grid-sandbox && cargo check --workspace && grep -v '^#' -R -nE 'TODO|FIXME|mock|stub' web/src | head -20 && ! ls docs/status/WEB_PRODUCTION_USABILITY_<date>.md 2>/dev/null && test -f docs/status/WEB_PRODUCTION_USABILITY_$(date +%Y-%m-%d).md</automated>
  </verify>
  <acceptance_criteria>
    - Targeted Vitest, build, TypeScript, Playwright, and workspace cargo-check commands all have recorded results; no full test suite is invoked.
    - `gsd-ui-auditor` reports at least 8.5/10 across consistency, hierarchy, readability, accessibility, responsiveness, and delight, with any non-blocking flags documented.
    - No shared-core, server, platform, desktop, or web-platform files are changed.
    - The final commit contains only Phase 3.7.2 implementation/tests/docs and has the required subject and footers.
    - No literal `<date>` placeholder remains in any committed filename; `docs/status/WEB_PRODUCTION_USABILITY_$(date +%Y-%m-%d).md` exists with actual date value.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to grid-server HTTP | Stop/resume requests cross from browser-controlled UI into session-control endpoints; session ID and response status must be validated. |
| Browser to WebSocket | Server-originated event JSON controls UI state, memory toasts, and sequence diagnostics; payloads are untrusted input. |
| Browser to DOM/accessibility surface | Memory content and tool output are rendered into the dashboard and must not become executable markup. |
| Playwright test harness to local services | E2E tests may start or mock local services; no live LLM credentials or external network calls are permitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-3.7.2-04 | Tampering | `SessionControls.tsx` stop/resume requests | mitigate | Use existing API conventions, interpolate only the validated active session ID, require `response.ok`, and rollback optimistic state on failure. |
| T-3.7.2-05 | Spoofing | WebSocket `memory_added` event | mitigate | Accept only the existing discriminated `ServerMessage` event type, truncate content to 60 characters, and render through React text nodes rather than HTML injection. |
| T-3.7.2-06 | Denial of Service | `ws/manager.ts` reconnect loop | mitigate | Preserve `maxReconnectAttempts = 5`, use bounded exponential backoff capped at 30000ms, and emit one terminal warning rather than an unbounded retry loop. |
| T-3.7.2-07 | Information Disclosure | `?debug=1` sequence logging | mitigate | Make sequence-gap logs opt-in only, never include credentials or full memory payloads, and keep normal mode silent. |
| T-3.7.2-08 | Information Disclosure | Playwright/status documentation | mitigate | Use fixture values and redacted placeholders; never write API keys, cookies, tokens, or private user data to specs, recordings, or docs. |
| T-3.7.2-09 | Repudiation | Stop/resume audit trail | accept | UI tests assert exact request endpoints and status transitions; server-side audit semantics remain outside this web-only phase. |
| T-3.7.2-10 | Elevation of Privilege | Web UI control scope | mitigate | Keep implementation single-user and web-only per D-02 and ADR-V2-024; do not add tenant/admin bypasses or alter grid-server authorization. |
</threat_model>

<verification>
1. Audit-derived REQ-WEB items are all closed or explicitly documented with reproducible blocked prerequisites.
2. `npm run test`, `npm run build`, `npx tsc -b`, three S7 Playwright specs, and `cargo check --workspace` are recorded.
3. SessionControls works on every tab, Stop/Resume state transitions meet the 2-second acceptance, and Cmd/Ctrl+. invokes Stop.
4. Memory toast and Memory live highlight are driven by `memory_added`; no mock fallback is introduced.
5. `seq` is additive, ordering is unchanged, and debug sequence gaps are opt-in.
6. UI-SPEC tokens and accessibility/responsive/reduced-motion criteria are checked; gsd-ui-auditor score is at least 8.5/10.
7. The dated walkthrough record and S7 Quickstart link exist; the screen recording is either present at the required path or explicitly marked blocked rather than fabricated.
8. No deferred component or shared core is modified.
</verification>

<success_criteria>
A non-developer can use `web/` to open the dashboard, observe a running agent and tool calls, see memory writes in toast and Memory page, stop/resume the session without devtools, and recover from a server restart; the behavior is covered by hermetic unit/E2E tests and documented by S7 walkthrough evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/03.7.2-web-production/3.7.2-02-SUMMARY.md` following the summary template. Include REQ-WEB closure, test commands/results, Playwright setup decision, UI auditor score, walkthrough date, recording path/status, and any explicitly blocked manual prerequisites.
</output>
