---
phase: 03.7.2-web-production
plan: 01
subsystem: ui
tags: [web, react, vite, jotai, websocket, session-control, audit, gap-analysis]

# Dependency graph
requires:
  - phase: 03.7.2-web-production
    provides: "Locked decisions D-01..D-10 (web/ only, 8-page × 5-event matrix, ≤9 REQ-WEB items) + APPROVED UI-SPEC.md (3.7.2-UI-SPEC.md, 6/6 dimensions PASS per gsd-ui-checker round 2)"
  - phase: 03.7.1-grid-cli
    provides: "Sibling audit pattern (3.7.1-01-PLAN.md + 3.7.1-01-SUMMARY.md + docs/audit/3.7.1-GAP-AUDIT.md) and 9-item REQ-AUDIT cap convention"
provides:
  - "8-page × 5-event matrix (40 cells: 11 PASS / 9 MANUAL TUNING / 20 FAIL) with file:line evidence per cell"
  - "9 REQ-WEB items (REQ-WEB-01..09) bounded by D-01 cap, each with concrete file:line touches"
  - "Dominant gap theme: 3 root causes (no `memory_added` WS event, no global Stop/Resume, 5 of 8 pages not WS-subscribed)"
  - "D-06 mock fallback audit evidence (zero TODO/FIXME/mock/stub matches in web/src — confirmed N/A)"
  - "D-03 Playwright setup finding (no playwright.config.ts, no e2e/, no @playwright/test in web/package.json)"
  - "Overflow verdict: fits in Plan 02 (no L + new top-level-route collision per D-01)"
  - "Audit consistency verification record (19 checks, 135 file:line citations all resolved)"
affects:
  - 03.7.2-web-production-plan-02 (consumes REQ-WEB backlog + UI-SPEC + D-01 cap)
  - 03.7.2-web-production-plan-03 (only if REQ-WEB-03 reveals hidden architectural work — currently no signal)

# Tech tracking
tech-stack: {}  # No new tech added — read-only audit
patterns:
  - "Audit-only commit at end of plan (per sibling 3.7.1 pattern), not per-task"
  - "VERIFICATION-CITATION-RESOLVE: every `web/...:line` cited via grep + Python regex validator before committing"
  - "TYPED-FAIL-MATRIX: 8 pages × 5 events = 40 cells, each with one of 3 verdict tokens (PASS/MANUAL TUNING/FAIL) + one file:line evidence anchor"
  - "OVERFLOW-RULE: D-01 caps audit at 9 REQ-WEB items; larger goes to Plan 03 (no collision in current audit)"

key-files:
  created:
    - docs/audit/3.7.2-GAP-AUDIT.md (480 lines, single audit artifact — Sections 1-6)
  modified: []  # No application files modified per D-01 read-only scope

key-decisions:
  - "Section 1 cell count: 8 pages × 5 events = 40 matrix rows; unique REQ-WEB IDs capped at 9 (D-01)"
  - "Section 2 verdict distribution: 11 PASS / 9 MANUAL TUNING / 20 FAIL (matches 40-cell math)"
  - "Section 4 REQ-WEB-01..09 sizes: S×3 (01/05/09), M×5 (02/04/06/07/08), L×1 (03)"
  - "Section 5 overflow verdict: Fits in Plan 02 — REQ-WEB-03 (the one L item) is web/-bounded, no top-level route change"
  - "Section 6 documents 19 verification commands with results; guarantees 100% read-only + all citations resolve"

patterns-established:
  - "Audit scope disclaimer pattern: when REQ-WEB item involves a deferred surface (e.g. grid-server cancel semantic), explicitly scope to web/ only and note backend owner"
  - "Three-tier evidence depth: matrix row (1-line), Section 3 fenced snippets (3-line), Section 4 touches (path list)"

requirements-completed: [REQ-WEB-AUDIT]

# Metrics
duration: 24min
completed: 2026-07-20
---

# Phase 3.7.2 Plan 01 — Summary

**Evidence-backed 8-page × 5-event gap audit for web/ dashboard producing 9-item REQ-WEB backlog (Fits in Plan 02)**

## Performance

- **Duration:** ~24 min (1418s, 12:51:58Z → 13:15:36Z UTC)
- **Started:** 2026-07-20T12:51:58Z
- **Completed:** 2026-07-20T13:15:36Z
- **Tasks:** 3 (all auto)
- **Files modified:** 0 (audit-only plan per D-01)
- **Files created:** 1 (`docs/audit/3.7.2-GAP-AUDIT.md` — 480 lines, 19 verification checks)

## Accomplishments

- **40-cell matrix (8 pages × 5 events):** 11 PASS / 9 MANUAL TUNING / 20 FAIL with file:line evidence per cell, all 135 cited paths resolved within their file's line count (verified via inline Python regex validator).
- **3-root-cause gap theme identified:** (1) missing `memory_added` WS variant in `ws/types.ts` (drives 6 of 20 FAILs); (2) no global Stop/Resume affordance in `SessionBar.tsx` (drives 8 of 20); (3) 5 of 8 pages (Collaboration, Memory, Schedule, Tasks, McpWorkbench) don't subscribe to WS at all (drives the remaining 6).
- **9-item REQ-WEB backlog (REQ-WEB-01..09)** sized S×3 / M×5 / L×1, each with `Touches` column listing web-only paths. **Overflow verdict: Fits in Plan 02** (D-01 cap met; the single L item REQ-WEB-03 stays within web/ component tree, no top-level route change).
- **D-06 mock fallback audit evidence** — `grep -RnE "TODO|FIXME|mock|stub" web/src` returns ZERO matches after filtering imports/comments; documented as N/A in Section 3 E-12.
- **D-03 Playwright setup finding** — `web/playwright.config.ts` and `web/e2e/` do NOT exist; `web/package.json` has no `@playwright/test` dep; only `web/.playwright-cli/` exists (browser-state snapshots from `gsd-frontend-test`, not test specs). REQ-WEB-06 covers the setup + 3 spec files.

## Task Commits

The 3 tasks were committed atomically per plan (single audit-only commit at end, matching sibling 3.7.1 Plan 01 pattern):

1. **Task 1: Inventory surface + populate matrix** — committed in single audit commit (per sibling pattern)
2. **Task 2: Append Evidence Detail + Remediation Backlog + Overflow Assessment** — same
3. **Task 3: Consistency-check + commit artifact** — **`0fdf119d`** (`docs(audit): Phase 3.7.2 web production gap audit`)

**Plan metadata:** `0fdf119d` (audit artifact commit; PLAN.md committed separately by orchestrator).

## Files Created/Modified

- `docs/audit/3.7.2-GAP-AUDIT.md` — 480 lines, single audit artifact
  - **Section 1 — Surface Inventory**: 8-page table with `web/src/pages/<Page>.tsx` mounts + WebSocket/atom plumbing + Playwright/test infra findings + D-01..D-10 decision refs
  - **Section 2 — Eight-page × Five-event Matrix**: 40-cell table with verdict + file:line evidence; verdict distribution 11 PASS / 9 MANUAL TUNING / 20 FAIL
  - **Section 3 — Evidence Detail**: 12 fenced evidence snippets (E-01..E-12) + D-06 mock audit result + cross-cutting `AppLayout.tsx` finding + pass justifications
  - **Section 4 — Remediation Backlog**: 9 REQ-WEB items (REQ-WEB-01..09) with complexity (S/M/L) and touches
  - **Section 5 — Overflow Assessment**: per-REQ size table, D-08 trigger check, recommended Plan 02 task ordering, mitigation if Plan 02 reveals more work
  - **Section 6 — Audit Consistency**: 19 verification commands with results, read-only guarantee, threat-model mitigation confirmations, Plan 02 hand-off checklist

## Decisions Made

| Decision | Rationale |
|----------|-----------|
| **9 REQ-WEB items (no more, no fewer)** | D-01 caps the backlog at "≤9 items, similar to Phase 3.7.1's 9 REQ-AUDIT cap" — 20 FAILs collapse to 3 root causes, naturally fitting in 9 items |
| **Verdict tokens are 3-valued (PASS / MANUAL TUNING / FAIL)** | Phase 3.7.1 sibling used the same; "MANUAL TUNING" reserved for cases where the affordance partially works but needs polish (vs fully missing) |
| **REQ-WEB-03 is the marquee L item** | L only because new `SessionControls.tsx` component + new `AppLayout.tsx` mount + 2 new atoms + Toast extension = 6 files; Plan 02 should sequence it as task 3 (after additive WS changes) |
| **Section 4 REQ-WEB-02 explicitly scopes backend out** | Backend `grid-server` `memory_added` broadcast is owned by another team; web/ adds the client-side handler as a no-op-safe forward-compat |
| **Section 4 REQ-WEB-07 uses existing DELETE endpoint** | `grid-server` may not have `POST /api/v1/tasks/:id/cancel` — using existing `DELETE /api/v1/tasks/:id` keeps Plan 02 web-only |
| **Overflow verdict: Fits in Plan 02** | D-01 cap met (9 items); no single L item requires new top-level route; UI-SPEC.md (APPROVED) pre-specifies the design — Plan 02 can execute without further discovery |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Documentation] Section 4 REQ-WEB-02 / REQ-WEB-07 contained explicit `grid-server` references**

- **Found during:** Task 3 (consistency check)
- **Issue:** Plan's "no deferred surface assigned remediation" rule requires Section 4 `Touches` columns to be web-only. The original REQ-WEB-02 phrasing "(verify grid-server sends it; otherwise send a separate zero-feature REQ)" and REQ-WEB-07 "(verify endpoints in grid-server)" referenced deferred surfaces in the description body, even though the `Touches` column was already web-only.
- **Fix:** Rephrased both descriptions as explicit scope disclaimers: REQ-WEB-02 now says "**Scope:** `web/` only — backend broadcast is owned by grid-server (separate team, deferred)"; REQ-WEB-07 now says "Targets existing `DELETE /api/v1/tasks/:id` endpoint (grid-server owns the cancel semantic); client treats DELETE as graceful cancel without `confirm()` modal". Evidence Detail E-03 also rephrased to match.
- **Files modified:** `docs/audit/3.7.2-GAP-AUDIT.md`
- **Verification:** Re-ran deferred-surface grep on Section 4 — only scope disclaimers remain (acceptable; remediation is web-only).
- **Committed in:** `0fdf119d`

**2. [Rule 2 - Tooling] zsh shell expanded `grep -v '^#' -R -nE 'TODO|FIXME|mock|stub' web/src` differently than expected**

- **Found during:** Task 1 (initial D-06 mock audit)
- **Issue:** zsh process substitution / glob expansion broke the original command; first runs returned spurious import-line noise.
- **Fix:** Switched to `grep -RnE "TODO|FIXME|mock|stub" web/src | grep -vE "^[ \t]*//" | grep -vE "^[^:]+:[0-9]+:import"` — produces zero matches, confirming D-06 N/A. Documented in Section 3 E-12 with the exact command.
- **Files modified:** None (only affected how the executor ran the verification).
- **Verification:** Two independent grep runs both returned 0 matches after filtering.

---

**Total deviations:** 2 auto-fixed (both Rule 2 — documentation/tooling correctness; no code or scope changes)
**Impact on plan:** Zero. Both fixes improved clarity (Section 4 deferred-surface disclaimers) and verifier clarity (D-06 E-12 explicit zero-match command). No scope creep.

## Issues Encountered

- **Sibling 3.7.1 audit pattern reuse** — the 3.7.1 sibling used per-section evidence snippets + audit-only commit; this plan follows the same pattern verbatim. No issues.
- **`os.path` glob gotcha in Python validator** — first regex pass picked up `Tools:` from middle of column header; fixed by restricting patterns to `web/...` prefixes only. Caught via inline shell error.
- **`python` not directly callable** — used `python3 - <<'PY' … PY` heredoc instead; worked.

## User Setup Required

None — read-only audit plan, no external service configuration required.

## Next Phase Readiness

- **Plan 02 inputs available:** 9-item REQ-WEB backlog with verified file:line evidence; overflow verdict "Fits in Plan 02"; recommended task ordering (additive WS first → Playwright infra → global SessionControls → page-level → polish → tests → docs).
- **UI-SPEC.md ready:** APPROVED by `gsd-ui-checker` round 2; drives component-level design decisions (D-09 ≥ 8.5/10 quality bar).
- **Existing infrastructure reusable:** `web/src/atoms/ui.ts` `addToastAtom` (extend with `memory` variant); `web/src/components/Toast.tsx` (add `Database` icon entry); `web/src/ws/manager.ts` reconnect logic (verified OK per E-08).
- **No blockers:** all REQ-WEB items are web-bounded; no shared-core, no leg-A, no proto changes required.
- **Contingency:** if Plan 02 reveals hidden work on REQ-WEB-03 (e.g. new `ws/SessionControlsEvent` family needed vs additive `seq`/`memory_added`), open Plan 03 per D-08 — audit documents this mitigation path in Section 5.

---

*Phase: 3.7.2-web-production*
*Plan: 01 (audit)*
*Completed: 2026-07-20*
*Commit: 0fdf119d*

Generated-By: Claude (claude-opus-4-8) via Claude Code CLI
Co-Authored-By: claude-flow <ruv@ruv.net>
