# Phase 3.7.2 — web/ dashboard 实战可用性 — Discussion Log

> **Audit trail only.** Decisions are captured in `3.7.2-CONTEXT.md`.

**Date:** 2026-07-20
**Phase:** 3.7.2-web-production
**Areas discussed:** Plan 01 scope, UX surface, Validation strategy

---

## Plan 01 scope

| Option | Description | Selected |
|--------|-------------|----------|
| Audit + spec scenarios (Recommended) | 8 pages × 5 event-types matrix; produce `docs/audit/3.7.2-GAP-AUDIT.md`; derive REQ-WEB-NN. Mirrors Phase 3.7.1 Plan 01. | ✓ |
| Skip audit, fix directly | Go directly to fixing 5 ROADMAP-listed gaps. Risks blind spots. | |
| Pick 3 high-priority only | Just stop/resume + memory + tool-call order. Scope too narrow. | |

**User's choice:** Audit + spec scenarios (Recommended)
**Notes:** Mirrors Phase 3.7.1 sibling pattern (which achieved 9/9 REQ-AUDIT closure). Audit-first gives evidence-backed backlog.

## UX surface

| Option | Description | Selected |
|--------|-------------|----------|
| SessionBar as global controller (Recommended) | Add Stop/Resume buttons + live indicator + memory toast to existing SessionBar; cross-page fixed position. | ✓ |
| Chat page only | Stop/Resume + memory toast only in Chat. Scope too small. | |
| Independent SessionModal | Separate component. More invasive. | |

**User's choice:** SessionBar as global controller (Recommended)
**Notes:** SessionBar already exists in root layout; promoting it avoids layout churn.

## Validation strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Vitest + Playwright + self-recorded walkthrough (Recommended) | Three-layer: unit tests extend existing 9 vitest, NEW Playwright e2e (E1/E2/E3), user self-records S3 walkthrough. | ✓ |
| Vitest + Playwright only | Skip walkthrough. Missing observability per ROADMAP. | |
| YouTube walkthrough | Production-quality video. Out of scope budget. | |

**User's choice:** Vitest + Playwright + self-recorded walkthrough (Recommended)
**Notes:** User acts as non-developer observer — matches ROADMAP "or self-recorded walkthrough if external observers not available" clause.

## Other decisions (agent's discretion, not asked)

- **D-04 Tool-call ordering**: verify existing + add `seq: u64` debug field (no Lamport clock redesign).
- **D-05 WS reconnect**: stress-test existing 5-attempt reconnect with vitest + Playwright.
- **D-06 Mock fallback audit**: code grep found 0 TODO/FIXME/mock markers — mark ROADMAP item N/A unless Plan 01 audit finds mocks.
- **D-07 Walkthrough format**: 1 markdown + 1 screen recording + S7 walkthrough doc (mirrors S1-S6).
- **D-08 Inherits parent constraints**: D-04 + D-07 from 3.7.1 parent.

## Deferred Ideas

- `web-platform/` multi-tenant UI 实战化
- `grid-desktop` Tauri app feature completion
- WebSocket compression / binary protocol
- New dedicated "Agent Live Monitor" page (SessionBar promotion is sufficient)
- Server-side WS event format changes beyond `seq` field

---

*Phase: 3.7.2-web-production*
*Discussion: 2026-07-20*
*Author: Claude (claude-opus-4-8) via Claude Code CLI*
