---
phase: 08.1-contract-proto-cross-cutting
plan: 03
subsystem: cross-cutting
tags: [ws, websocket, deferred-ledger, tool_name, schema, verify-and-close]

# Dependency graph
requires:
  - phase: 08.1-contract-proto-cross-cutting
    provides: D83 closed at bdc5b8b (tool_name field added to AgentEvent::ToolResult)
provides:
  - D90 LEDGER close-out — confirmed tool_name already present on WS wire
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "verify-and-close: Debt sweep pattern — verify existing implementation, update LEDGER, zero code changes"

key-files:
  modified:
    - "docs/design/EAASP/DEFERRED_LEDGER.md — D90 row status updated, statistics recalculated"
  verified:
    - "crates/grid-server/src/ws_chunk.rs:173-182 — tool_name already in destructure + JSON payload"
    - "crates/grid-engine/src/agent/events.rs:26-35 — ToolResult variant has tool_name: String field"

key-decisions:
  - "D90 verified already resolved at D83/S1.T4 (bdc5b8b) — tool_name serialized to WS wire with zero additional changes"
  - "Grid-platform WS tool_name omission accepted per ADR-V2-024 dormancy — no fix needed for dormant crate"

patterns-established:
  - "Verify-and-close: Research-driven debt sweep — confirm implementation exists via grep evidence, update LEDGER, skip code changes"

requirements-completed: [SERVER-06]

# Metrics
duration: 1 min
completed: 2026-06-09
---

# Phase 08.1 Plan 03: D90 WS ToolName Verify-and-Close Summary

**Confirmed D90 already resolved — tool_name present in AgentEvent::ToolResult (D83/S1.T4) and serialized to WS wire at ws_chunk.rs:173-182; LEDGER closed with zero code changes**

## Performance

- **Duration:** 1 min
- **Started:** 2026-06-09T01:51:58Z
- **Completed:** 2026-06-09T01:53:13Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Verified `tool_name` field present in `AgentEvent::ToolResult` variant (events.rs:32, with D83/S1.T4 doc comment)
- Confirmed `tool_name` serialized in `ws_chunk.rs` map_event() — both destructure pattern (line 175) and JSON payload (line 181)
- Confirmed `tool_name` also present in ws_chunk.rs test code (line 336) for completeness
- Audited grid-platform WS — intentionally omits tool_name via `..` rest pattern per ADR-V2-024 dormancy (acceptable)
- D90 LEDGER row updated: 🟡 P1-defer → ✅ CLOSED 2026-06-09 @ e3fa59ce
- LEDGER statistics: closed count 38→39, P1-defer count 6→5

## Task Commits

Each task was committed atomically:

1. **Task 1: Verify D90 tool_name is present on WS wire** - `07b5cba6` (feat)

**Verification evidence:**
- `grep -c "tool_name" crates/grid-server/src/ws_chunk.rs` → 12 matches (≥2 required) ✅
- `grep -A 5 "ToolResult {" crates/grid-engine/src/agent/events.rs | grep tool_name` → match found ✅
- `grep "✅ CLOSED" docs/design/EAASP/DEFERRED_LEDGER.md | grep D90` → D90 present in closed list ✅
- `cargo check -p grid-server` → passes (warnings only, ControlMessage dead code) ✅

## Files Created/Modified
- `docs/design/EAASP/DEFERRED_LEDGER.md` — D90 row (L220): status + resolution note; closed count + P1-defer count updated

## Decisions Made
- D90 verified already resolved — zero code changes needed. The original D83/S1.T4 fix at commit `bdc5b8b` added `tool_name` to `AgentEvent::ToolResult`, and `ws_chunk.rs:173-182` has always serialized all fields in the JSON payload. D90 was a tracking-only follow-up item.
- Grid-platform WS `tool_name` omission accepted per ADR-V2-024 dormancy — the platform crate uses `..` rest pattern to ignore `tool_name`, which is intentional for a dormant crate.

## Deviations from Plan

### Auto-fixed Issues

**1. [Plan note — Extra ws_chunk.rs match] Two AgentEvent::ToolResult matches found instead of expected one**
- **Found during:** Task 1 verification (grep step 3)
- **Issue:** Plan expected exactly one `AgentEvent::ToolResult` match at line ~173 in ws_chunk.rs. Two matches found: line 173 (production map_event) and line 334 (test code). Both include `tool_name`.
- **Fix:** Not a deviation — second match is test code that also correctly serializes `tool_name`. Plan acceptance criteria already covers this case (≥2 matches required). No action needed.
- **Verification:** Second match at line 334-341 confirmed to be test-only, properly passing `tool_name: "bash".into()`.

---

**Total deviations:** 0 auto-fixes needed (plan note only — extra ws_chunk.rs match is benign)
**Impact on plan:** Zero code changes as planned. Verify-and-close pattern executed exactly as specified.

## Issues Encountered
None — all verifications passed on first attempt.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- D90 (SERVER-06) is now closed. Ready for remaining Phase 08.1 plans (08.1-01, 08.1-02).
- All three Phase 08.1 plans are independent — no execution ordering dependency.

---
*Phase: 08.1-contract-proto-cross-cutting*
*Completed: 2026-06-09*
