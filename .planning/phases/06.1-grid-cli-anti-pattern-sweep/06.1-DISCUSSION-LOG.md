# Phase 6.1: grid-cli Anti-pattern Sweep — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-25
**Phase:** 06.1-grid-cli-anti-pattern-sweep
**Areas discussed:** CLI-X3 strategy, CLI-X2 fix granularity, Test scope for CLI-X2, NEW-X3 LEDGER closure narrative

---

## CLI-X3 strategy — fix vs filter

**Context surfaced during discussion**:
- `cargo build -p grid-cli --all-features` 实测出 12 errors,分 3 buckets:
  - Bucket A (5): Phase BA Grid rename leftover (`octo:hook` WIT package + `OctoHookPlugin` 类型 + bindgen world name)
  - Bucket B (6): 1 missing `use HashMap` + 1 `_config` 函数签名 typo cascading 5x
  - Bucket C (1): borrow-checker `let mut`
- wasm hook code 实际被 `agent/runtime.rs:686-693` 使用,不是 dead code

| Option | Description | Selected |
|--------|-------------|----------|
| Fix all 12 errors (Option a) | Rename octo→grid in wit/ + bindings (Bucket A), add `use HashMap` + fix `_config` typo (Bucket B), add `let mut` (Bucket C). Total ~30 LOC. Keeps wasm hooks functional. Honest closure of Phase BA cleanup miss. Risk: low — these are mechanical fixes, no logic change. Sets up sandbox-wasm feature for future use without rework. | ✓ |
| Narrow --all-features matrix (Option b) | Add comment in grid-cli/Cargo.toml + Makefile excluding `grid-engine/full` from --all-features. Quicker (~5 LOC config). Leaves wasm hooks broken until next touch — punts to v3.3+. Risk: another contributor will trip on this. Violates 'No Hack for Validation' spirit if framed as defeat. | |
| Hybrid: fix Bucket B+C, gate Bucket A | Fix the obvious typos (~10 LOC) but leave the wit/ rename for a dedicated wasm-hooks-revive phase. Keeps the rename surgically scoped (touches more files). Documents intent in LEDGER. Risk: NEW-X3 stays half-closed in LEDGER. | |

**User's choice:** Fix all 12 errors (Option a) (Recommended)
**Notes:** Aligns with MEMORY.md `feedback_no_hack_for_validation` + `feedback_no_fallback` rules. Fix is small (~30 LOC) and root cause is now identified (Phase BA cleanup miss + small independent bugs). Wasm hook code is genuinely used.

---

## CLI-X2 fix granularity

**Context surfaced during discussion**:
- `delete_session` at L99-103 has 两个 bug (silent exit 0 via eprintln+Ok + 缺 typed error)
- `export_session` at L157 has anyhow! → exit 1
- `kill_session` (Phase 5.5 B1) 的 pattern 已落地为 NEW-A3 close: `get_session→None→return Err(GridError::session_not_found(id).into())`
- 三个 callsite 同形

| Option | Description | Selected |
|--------|-------------|----------|
| Copy kill_session pattern verbatim | Direct copy of the L110-120 NEW-A3 pattern into delete_session + export_session. ~5 LOC each. 3 sites is borderline for DRY but the bodies are tiny and read clearer flat. Stays consistent with Phase 5.5 precedent. Avoids speculative abstraction (rule from karpathy-guidelines). | ✓ |
| Extract `require_session_exists(sid)` helper | Small helper in session.rs: `fn require_session_exists(store, sid) -> Result<()>`. Each callsite becomes 1 line. Reduces duplication. Risk: premature abstraction — with only 3 callers and identical bodies, the helper saves few LOC but adds an indirection layer. v3.2 is intentional light triage. | |
| Defer DRY to v3.3 | Copy pattern now (same as option 1) and file a P3 row in LEDGER: 'consider session-exists helper when 5+ callsites accumulate'. Punts the abstraction decision until usage justifies it. Most conservative. | |

**User's choice:** Copy kill_session pattern verbatim (Recommended)
**Notes:** karpathy-guidelines + v3.2 intentional light triage 精神都指向这选项。3 site 不够 DRY 触发线。Phase 5.5 precedent 已建立读者预期。

---

## Test scope for CLI-X2

**Context surfaced during discussion**:
- `test_kill_nonexistent_session_exits_4` already exists in `tests/cli_integration.rs` 使用 `grid_bin()` 进程级 invocation
- 模式: 子进程 + `assert_eq!(out.status.code(), Some(4), ...)` + stderr 仅 log 不 assert

| Option | Description | Selected |
|--------|-------------|----------|
| Copy kill_nonexistent test pattern | Add `test_delete_nonexistent_session_exits_4` + `test_export_nonexistent_session_exits_4` to cli_integration.rs as direct copies of test_kill_nonexistent (just swap subcommand). Process-level E2E coverage. ~15 LOC each. Matches ROADMAP SC#2 verbatim. | ✓ |
| Copy pattern + add stderr message assertion | Same as option 1 but ALSO assert stderr contains 'not found'. Catches accidental message regression. ~20 LOC each. Slightly tighter contract — Phase 5.5 test only checked exit code. | |
| Single parametrized test for both verbs | One #[test_case(verb)] test using test-case crate. Less duplication. Requires adding test-case dep + matches less the established pattern. | |

**User's choice:** Copy kill_nonexistent test pattern (Recommended)
**Notes:** 与 Phase 5.5 既有测试风格一致。不加 stderr 断言保留 GridError Display 重构空间。不引 test-case dep。

---

## NEW-X3 LEDGER closure narrative

**Context surfaced during discussion**:
- LEDGER 现有 NEW-X3 deferred-note 写 "likely pre-existing tech debt from interim refactor",但实际根因是 Phase BA Grid rename (octo→grid 2026-04-04) 遗漏 — 比 deferred-note 猜测的更精确,有 archaeology 价值
- Phase 5.5 既有 closed row 行格式精炼,但 X3 比一般 row 多一层背景

| Option | Description | Selected |
|--------|-------------|----------|
| Full archaeology + cross-ref to Phase BA | Close-out: 'Root cause: Phase BA Grid rename (octo→grid 2026-04-04) missed wasm hook plugin module — wit/octo-hook.wit + OctoHookPlugin bindings + bindgen world name not renamed; revealed when --all-features path activated sandbox-wasm. Bucket B/C errors were independent typo (`_config` shadow) + missing `use HashMap` likely introduced same era. Decision: Option (a) fix all 12. Renamed wit/grid-hook.wit + package grid:hook + GridHookPlugin + world grid-hook. Added HashMap import, fixed _config, added let mut.' Useful audit context for future Phase BA-era surfacing. | ✓ |
| Minimal one-liner + commit hash | Close-out: 'Resolved Option (a): renamed octo→grid in wit/ + bindings, added missing imports + mut + typo fix. ~30 LOC. cargo build -p grid-cli --all-features now clean. @commit-hash.' Matches Phase 5.5 brevity for some rows. Less audit value. | |
| Archaeology + retroactively file Phase BA wasm-cleanup-miss row | Same as option 1 PLUS file a new D-row (e.g., NEW-X5) tagged DEAD/closed-by-X3 for v3.3 TRIAGE-01 audit trail. Increases LEDGER row count but improves traceability. | |

**User's choice:** Full archaeology + cross-ref to Phase BA (Recommended)
**Notes:** 不另开 D-row;archaeology 写在 X3 close-out 内已足够,v3.3 TRIAGE 不需要二次 surface。

---

## the agent's Discretion

- Commit boundary granularity (1 task = 1 commit vs 1 plan = 1 commit) — planner decides
- 但 **wit/ rename 必须单独 commit** (强建议) for revert-safety
- `cargo test -p grid-cli` 是否在 task 间增量跑 vs 仅最终 verify — planner 自决
- `cargo clippy -p grid-engine --features sandbox-wasm` warning 扫描 — planner spike,可做可不做

## Deferred Ideas

- `require_session_exists(sid)` helper — wait 5+ callsites
- Phase BA wasm-cleanup-miss 反向追责 D-row — not needed; archaeology in X3 close-out 已够
- `test-case` parametrize crate — 风格不一致,3 个 site 不值得引 dep
- `cargo clippy` 在本 phase 一并扫 — not blocking

---

*Discussion log written 2026-05-25*
