---
phase: 05.2
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05, CLI-06]
files_modified:
  - crates/grid-cli/src/main.rs
  - crates/grid-cli/src/commands/
  - crates/grid-cli/src/commands/ask.rs
  - crates/grid-cli/src/commands/session.rs
  - crates/grid-cli/src/commands/doctor.rs
  - crates/grid-cli/src/commands/doctor/checks.rs
  - crates/grid-cli/src/tui/key_handler.rs
  - crates/grid-cli/src/tui/key_handler/mod.rs
  - crates/grid-cli/src/tui/key_handler/normal.rs
  - crates/grid-cli/src/tui/key_handler/vim.rs
  - crates/grid-cli/src/tui/key_handler/search.rs
  - crates/grid-cli/src/tui/key_handler/selector.rs
  - crates/grid-cli/src/tui/key_handler/overlay.rs
  - crates/grid-cli/src/tui/key_handler/approval.rs
  - crates/grid-cli/src/tui/key_handler/common.rs
  - crates/grid-cli/src/tui/key_handler/INVARIANTS.md
  - crates/grid-cli/src/error.rs
  - crates/grid-cli/tests/key_handler_integration.rs
  - crates/grid-cli/tests/cli_integration.rs
  - scripts/proto-cli-sync-check.sh
must_haves:
  - exit code constants in error.rs (CLI-03)
  - streaming JSON output from `grid ask` (CLI-02)
  - `session kill --purge` hard-delete flag (CLI-05)
  - `grid doctor` expanded checks (CLI-06)
  - tui/key_handler.rs split into 7 mode-specific files (CLI-04)
  - INVARIANTS.md as source-of-truth before split (CLI-04)
  - ≥21 unit tests + ≥2 integration tests for key handlers (CLI-04)
test_coverage:
  - unit: exit codes for all error variants
  - unit: `grid ask --stream` produces streaming JSON
  - unit: `grid ask --no-stream` produces single JSON object
  - unit: `grid ask invalid-sub` exits with code 2
  - unit: `NO_COLOR=1 grid ask` produces zero ANSI codes
  - unit: `session kill` soft-deletes by default
  - unit: `session kill --purge` hard-deletes
  - unit: `grid doctor` runs all checks
  - integration: key_handler refactor preserves all overlay modes
---

# Plan 05.2-01: `grid` CLI Hardening — Full Implementation

**Covers:** CLI-01 (command tree), CLI-02 (streaming), CLI-03 (exit codes), CLI-04 (TUI refactor), CLI-05 (session lifecycle), CLI-06 (doctor expansion)

## Tasks

### H2-1: `grid ask` Command Tree + Streaming + Error Codes (CLI-01, CLI-02, CLI-03)

#### T-01.1: Audit and remove dead `grid` subcommands
- **File:** `crates/grid-cli/src/main.rs`
- **Action:** Audit all `Cmd::X` variants in the clap `Command` enum. Remove any subcommands not referenced in `run()` match arms. No-op if already clean.

#### T-01.2: Add `grid ask` command stub
- **File:** `crates/grid-cli/src/commands/ask.rs` (new)
- **Action:** Create new file with `AskArgs` struct and `run()` function:
```rust
use clap::Parser;

#[derive(Parser, Debug)]
pub struct AskArgs {
    #[arg(long)]
    pub model: Option<String>,
    #[arg(long)]
    pub no_stream: bool,
    #[arg(trailing_var_arg = true)]
    pub args: Vec<String>,
}

pub fn run(args: &AskArgs) -> Result<(), GridError> {
    todo!("CLI-01: wire up to LLM engine")
}
```

#### T-01.3: Register `grid ask` in main.rs
- **File:** `crates/grid-cli/src/main.rs`
- **Action:** Add `Cmd::Ask(ask::AskArgs)` to the `Cmd` enum and match arm. Call `ask::run(&args)` in the match arm.

#### T-01.4: Define exit code constants
- **File:** `crates/grid-cli/src/error.rs`
- **Action:** Add exit code constants:
```rust
pub const EXIT_OK: i32 = 0;
pub const EXIT_USAGE: i32 = 2;
pub const EXIT_EXECUTION: i32 = 70;
pub const EXIT_SESSION: i32 = 71;
pub const EXIT_SYNC: i32 = 73;
```

#### T-01.5: Wire exit codes in main.rs
- **File:** `crates/grid-cli/src/main.rs`
- **Action:** Map `GridError` variants to exit codes and call `std::process::exit()`.

#### T-01.6: Implement streaming JSON output
- **File:** `crates/grid-cli/src/commands/ask.rs`
- **Action:**
  - Default: stream mode via `.stream()` on LLM call
  - `--no-stream`: non-streaming via `.await` full response
  - Stream JSON objects to stdout: `{"type":"content_block_delta","text":"..."}`
  - Non-stream: `{"type":"content_block","text":"..."}`
  - Respect `NO_COLOR` env var

### H2-2: TUI Key Handler Refactor (CLI-04)

#### T-01.7: Capture Invariant Table (source-of-truth before refactor)
- **File:** `crates/grid-cli/src/tui/key_handler/INVARIANTS.md` (new)
- **Action:** Document every key→action mapping in the current 1556 LOC file as a markdown table:
  - Columns: Mode/SubState | Key | Action | Notes
  - One row per key binding per mode
  - Commit this file BEFORE any code changes

#### T-01.8: Create split module structure
- **Files:** `crates/grid-cli/src/tui/key_handler/mod.rs`, `normal.rs`, `vim.rs`, `search.rs`, `selector.rs`, `overlay.rs`, `approval.rs`, `common.rs`
- **Action:** Create `mod.rs` as dispatcher, one `.rs` per mode:
  - `normal.rs`: normal mode key bindings
  - `vim.rs`: vim-style `hjkl` navigation (future)
  - `search.rs`: search/find overlay
  - `selector.rs`: model/session picker
  - `overlay.rs`: agent debug overlay
  - `approval.rs`: approval mode
  - `common.rs`: shared utilities (dispatcher helpers)
- **Module structure:** `pub mod key_handler { pub mod normal; pub mod vim; ... }`

#### T-01.9: Create key handler dispatcher
- **File:** `crates/grid-cli/src/tui/key_handler/mod.rs`
- **Action:** Implement dispatcher using `match (overlay_mode, sub_state, key)` tuple pattern. Each arm delegates to the appropriate mode-specific module.

#### T-01.10: Migrate normal mode key bindings
- **File:** `crates/grid-cli/src/tui/key_handler/normal.rs`
- **Action:** Move normal mode key handling from old `key_handler.rs` to this file. Keep all behavior identical.

#### T-01.11: Migrate selector key bindings
- **File:** `crates/grid-cli/src/tui/key_handler/selector.rs`
- **Action:** Move selector/model-picker key handling. `j/k` or `↑/↓` navigate, `Enter` confirm, `Esc` cancel.

#### T-01.12: Migrate remaining overlay modes
- **Files:** `search.rs`, `overlay.rs`, `approval.rs`, `vim.rs`
- **Action:** Move each overlay mode's key handling. All must remain functionally identical.

#### T-01.13: Write unit tests for each mode file
- **Files:** Each mode file gets a `#[cfg(test)]` module
- **Action:** ≥21 unit tests total. Test each key binding per mode. Mock `AppState` with `MockStore`.
- **Coverage:** Every key in INVARIANTS.md has a corresponding test.

#### T-01.14: Create integration tests
- **File:** `crates/grid-cli/tests/key_handler_integration.rs`
- **Action:** ≥2 integration tests verifying full key sequences across mode transitions.

#### T-01.15: Verify INVARIANTS.md completeness
- **Action:** After all migrations, verify every row in INVARIANTS.md has a corresponding test. Update INVARIANTS.md if any gaps found.

### H2-3: Session Lifecycle Polish (CLI-05)

#### T-01.16: Add `session kill --purge` flag
- **File:** `crates/grid-cli/src/commands/session.rs`
- **Action:**
  - Add `--purge` flag to `session kill` subcommand
  - Without `--purge`: soft-delete (mark session state as deleted, keep proto sync markers)
  - With `--purge`: hard-delete (remove session state AND proto sync markers from disk)
  - Exits with `EXIT_SESSION` (71) if session not found
  - Exits with `EXIT_OK` (0) on success
  - Enables clean proto file sync for `grid doctor` check (CLI-06)

### H2-4: `grid doctor` Expansion (CLI-06)

#### T-01.17: Expand `grid doctor` checks
- **Files:** `crates/grid-cli/src/commands/doctor.rs`, `crates/grid-cli/src/commands/doctor/checks.rs`
- **Action:**
  - Add proto file sync check: verify `~/.config/grid/` proto files match embedded versions
  - Add session state integrity check
  - Add shell completion availability check
  - Each check produces structured output: `{check: "...", status: "pass|fail|warn", details: "..."}`
  - Exit code: 0 if all pass, `EXIT_SYNC` (73) if any proto sync check fails

#### T-01.18: Add proto-cli-sync-check.sh script
- **File:** `scripts/proto-cli-sync-check.sh` (new)
- **Action:** POSIX sh script that compares CLI proto file versions against disk. Used by `grid doctor` and by CI.

### H2-5: Integration Tests

#### T-01.19: Write CLI integration tests
- **File:** `crates/grid-cli/tests/cli_integration.rs`
- **Action:** Integration tests for:
  - `grid ask --help` renders
  - `grid ask invalid-sub` exits with code 2
  - `session kill` soft-deletes; `session kill --purge` hard-deletes
  - `grid doctor` runs all checks and exits with correct codes
  - `NO_COLOR=1 grid ask` produces zero ANSI codes

## D-Items

| ID | Description | Owner | Status |
|----|-------------|-------|--------|
| D-01 | Command tree audit notes (before/after) | impl | |
| D-02 | `ask.rs` initial stub | impl | |
| D-03 | `grid ask --help` output sample | impl | |
| D-04 | `grid ask --stream` output sample (JSON chunks) | impl | |
| D-05 | `grid ask --no-stream` output sample (single JSON) | impl | |
| D-06 | `grid ask invalid-sub` → exit 2 verification | impl | |
| D-07 | Error code constants in error.rs | impl | |
| D-08 | `NO_COLOR=1 grid ask` → no ANSI codes | impl | |
| D-09 | Exit code test matrix (all error types) | impl | |
| D-10 | INVARIANTS.md (before state, source-of-truth) | impl | |
| D-11 | Module structure for 7 key_handler files | impl | |
| D-12 | Unit test count ≥21 (all modes) | impl | |
| D-13 | Integration tests ≥2 | impl | |
| D-14 | `session kill --purge` verification | impl | |
| D-15 | `grid doctor` output sample (all checks pass) | impl | |
| D-16 | `grid doctor` output sample (proto check fails) | impl | |
| D-17 | `proto-cli-sync-check.sh` exit codes | impl | |

## Verification

1. `grid --help` shows only active subcommands
2. `grid ask --help` renders correctly
3. `grid ask "hello"` streams JSON chunks to stdout
4. `grid ask --no-stream` produces single JSON object
5. `grid ask invalid-sub` exits with code 2
6. All error types map to documented exit codes
7. `NO_COLOR=1 grid ask` produces zero ANSI codes
8. `session kill` soft-deletes by default; `--purge` hard-deletes
9. `grid doctor` runs all checks with correct exit codes
10. `key_handler.rs` refactored into 7 files; all tests pass
11. INVARIANTS.md verified against implemented behavior

## Waves

- **Wave 1 (this plan):** T-01.1 → T-01.19 — sequential, one developer
- **Note:** No wave 2 needed. All CLI-01 through CLI-06 delivered in wave 1.
