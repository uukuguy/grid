# Phase 5.3: Contract Evolution - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-19
**Phase:** 05.3-contract-evolution
**Areas discussed:** Scope inclusion, CONTRACT-01 ChunkType, CONTRACT-02 Hook event, ADR-V2-026 ExecutionMode default, ADR-V2-027 quirks ADR timing, D136 fix direction, D109 doc destination, Plan split, THINKING semantics, ATTACHMENT_REF backend

---

## Scope Inclusion (5.3 bucket boundary)

| Option | Description | Selected |
|--------|-------------|----------|
| (a) ROADMAP 原定 contract evolution | CONTRACT-01/02 + WATCH-01/03 + contract-v1.2.0 升级 + ADR-V2-021/006 增量 | ✓ |
| (b) NEW-E4 ADR-V2-026 ExecutionMode | RFC + impl ready, 5.3 promotes to Accepted ADR | ✓ |
| (c) NEW-F1/F2 LLM provider quirks 抽象 + 拆分判定 | ADR + 原型重构, contract-adjacent | ✓ |
| (d) NEW-F3/F4 silent fallback 移除 + log path ADR | infra cleanup, 推到 5.4 | — |

**User's choice:** "按你的规划来做" (delegated). the agent decision: (a)+(b)+(c) in 5.3; (d) pushed to 5.4.
**Notes:** Rationale captured in CONTEXT.md decisions section. (d) clusters with 5.4 auth/audit/config strictness.

---

## CONTRACT-01: ChunkType wire values

| Option | Description | Selected |
|--------|-------------|----------|
| THINKING_TRACE 单个 | Only THINKING_TRACE; skip ATTACHMENT_REF for scope | |
| ATTACHMENT_REF 单个 | Only attachment ref; needs URI scheme work | |
| 两个都加 | Both, matches ROADMAP SC#1 "1-2 new wire values" | ✓ |
| 两个都不加, 先设计框架 | Just proto extension point, no wire values | |

**User's choice:** "两个都加"
**Notes:** This makes 5.3 a larger phase. Wire 8 = THINKING_TRACE, wire 9 = ATTACHMENT_REF (in addition to existing 0-7). ATTACHMENT_REF backend storage explicitly deferred to next milestone — wire carries only opaque URI string.

---

## CONTRACT-02: Hook event additions

| Option | Description | Selected |
|--------|-------------|----------|
| SubagentStart 单个 | grid-engine has subagent_runtime.rs, semantic clear, L3 governance value | |
| TaskCheckpoint 单个 | Long-task observability, but semantic fuzzy | |
| 两个都加 | Both, matches ROADMAP SC#1 | ✓ |
| 两个都不加 | Proto extension point only | |

**User's choice:** "两个都加"
**Notes:** TaskCheckpoint semantic clarification (what is a task boundary? automatic vs manual checkpoint?) goes to plan-phase researcher.

---

## ADR-V2-026 ExecutionMode default value

| Option | Description | Selected |
|--------|-------------|----------|
| 锁定现状 | Default Conversational + grid-runtime opt-in LongWorkflow | ✓ |
| 反转: 默认 LongWorkflow + REPL opt-in | Preserves V2-016 intent but easy to forget opt-in | |
| 第三状态 Auto (推断) | Over-engineering, violates anti-silent-fallback principle | |

**User's choice:** "锁定现状 (Recommended)"
**Notes:** Implementation already in commit `f1999fb`. 1569/1569 lib test PASS. ADR only formalizes; no code change.

---

## ADR-V2-027 quirks ADR timing

| Option | Description | Selected |
|--------|-------------|----------|
| Phase 5.3 ADR + 原型实现 | ADR + OpenAIProvider quirks refactor + maybe LingProvider split | ✓ |
| Phase 5.3 只出 ADR, 实现推 5.4 | Risk "principle ships, impl doesn't" | |
| 推迟到 Phase 6 / next milestone | Hot fixes stay in OpenAIProvider unconditional | |

**User's choice:** "Phase 5.3 ADR + 原型实现 (Recommended)"
**Notes:** User direction 2026-05-19 explicit: "shared non-standard behaviors live in OpenAIProvider; provider-unique quirks split". Doing ADR+impl same phase avoids the fail mode user flagged in same session.

---

## D136 probe-turn hook fire fix direction

| Option | Description | Selected |
|--------|-------------|----------|
| Probe turn 现在 fire hook | observability 完整, 3 xfails 转 PASS, metadata.is_probe=true 软标记 | ✓ |
| Probe turn 不 fire | 收紧 ADR-V2-016, contract test 改为期望不 fire — 但丢 trajectory | |
| 引入 probe-specific hook event | 过度设计 | |

**User's choice:** "Probe turn 现在 fire hook (Recommended)"
**Notes:** Rationale: probe is real LLM/tool call; making it invisible to L3/L2 is bug-not-feature. Silly scenario "L3 denies probe" → fix L3 (add is_probe-aware policy), don't hide the event.

---

## D109 workflow.required_tools doc destination

| Option | Description | Selected |
|--------|-------------|----------|
| L1_RUNTIME_ADAPTATION_GUIDE.md | Per ROADMAP SC#3; sample-doc level, not ADR-worthy | ✓ |
| Independent ADR-V2-XXX | Over-weight; invariant is sample-doc class | |
| Increment ADR-V2-020 | Different layer (L1 vs L2 skill filter) | |

**User's choice:** "L1_RUNTIME_ADAPTATION_GUIDE.md (Recommended)"

---

## Plan-phase split count

| Option | Description | Selected |
|--------|-------------|----------|
| 2 plans | Plan A: CONTRACT-01/02 + WATCH-01/03 顺手. Plan B: ADR-V2-026 + ADR-V2-027 + Quirks 重构. B depends on A's contract-v1.2.0 done. | ✓ |
| 3 plans | CONTRACT-01 / CONTRACT-02 / ADR each its own plan — too granular | |
| 1 plan | Everything in one — too big, ~12+ tasks | |

**User's choice:** "2 plans (Recommended)"

---

## THINKING_TRACE vs CHUNK_TYPE_THINKING semantic separation

| Option | Description | Selected |
|--------|-------------|----------|
| THINKING = 增量流; THINKING_TRACE = 本轮汇总 | Coexist; downstream picks one/both/neither | ✓ |
| THINKING_TRACE deprecates THINKING | Violates ADR-V2-021 closed-enum "additive only" | |
| 两者同语义 (重复) | Violates closed-enum principle, semantic duplication | |

**User's choice:** "THINKING = 增量流; THINKING_TRACE = 本轮汇总 (Recommended)"
**Notes:** THINKING_TRACE content schema: `{effort_level, total_reasoning_tokens, reasoning_summary?}`. Emitted at end of turn typically right before MessageStop.

---

## ATTACHMENT_REF backend storage timing

| Option | Description | Selected |
|--------|-------------|----------|
| Wire opaque URI only, backend 推迟 | `{uri, mime_type, size}`, downstream interprets | ✓ |
| 5.3 同时定 URI scheme whitelist | file:// + l2-memory://, but expands scope | |
| 5.3 不加 ATTACHMENT_REF | Conflicts with earlier 两个都加 choice | |

**User's choice:** "ATTACHMENT_REF 的 wire 里只携 opaque URI, backend 推迟 (Recommended)"

---

## the agent's Discretion (delegated to planner / implementer)

- TaskCheckpoint semantic clarification location (RESEARCH.md vs mini-RFC vs inline V2-006 amendment)
- Proto package versioning approach (`v2/v1.2.0/` subdirectory vs logical doc-only version)
- Quirks struct shape (enum vs config struct vs trait objects)

## Deferred Ideas (noted for future phases)

- NEW-F3 silent-fallback removal → Phase 5.4
- NEW-F4 log path ADR → Phase 5.4
- ATTACHMENT_REF backend storage spec (URI scheme + adapters) → next milestone
- TaskCheckpoint full design (boundary detection + resumption) → potentially own phase
- F2 cascade for deepseek-reasoner: already split, document in ADR-V2-027 as precedent

