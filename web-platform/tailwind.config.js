// Grid design tokens are defined via @theme block in src/globals.css (Tailwind v4 CSS-first config).
// This file retained as anchor for tools that expect a config entry point.
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: { extend: {} },
  plugins: [],
}
